<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Kaely&#39;s Blog</title>
  
  <subtitle>生活就是迷阵</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://kael.top/"/>
  <updated>2020-05-17T09:04:57.340Z</updated>
  <id>http://kael.top/</id>
  
  <author>
    <name>Kaely</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>在web中渲染格点数据探讨（1）</title>
    <link href="http://kael.top/2020/05/17/web-grid-data-render-1/"/>
    <id>http://kael.top/2020/05/17/web-grid-data-render-1/</id>
    <published>2020-05-17T08:18:51.000Z</published>
    <updated>2020-05-17T09:04:57.340Z</updated>
    
    <content type="html"><![CDATA[<p>在地理信息行业中，我们常遇见的数据中有这么两类：</p><ul><li>一类是站点（散点）数据。地图上分布着位置不均匀的各种站点，比方说气象站，水文站，如果我们需要查看站点数值趋势，就可以通过等值线图等方式，比如<a href="https://github.com/d3/d3-contour" target="_blank" rel="noopener">d3 的 contour</a>，<a href="https://turfjs.org/docs/#isolines" target="_blank" rel="noopener">turf 的 isolines</a>，还可以通过插值方法将站点插值到网格上，转化成格点数据，后面我们也会讨论。</li><li>也有一类是格点数据。这个在气象上就非常常见，气象预报数据通常是模式跑出来的，如同渔网一样的格点，这种数据提供了查询任意位置数值的作用，它的渲染也同样常见，传统的方法是在服务器端生成好对应图片，在浏览器上贴图，这里追求实现的是<code>前端渲染</code>。</li></ul><a id="more"></a><p>太长不看可以直接<a href="https://github.com/zzcyrus/Leaflet.Tile.PixelLayer" target="_blank" rel="noopener">项目仓库</a></p><h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>格点数据的特点往往是数据量大，蕴含的信息量大，不仅包含着某个点的数值，同时还有这个点的经纬度，时间维度信息，空间高度信息，所以我们通常会听见学气象的同学说某个数据有四个维度。随着计算机的发展，图形学的发展，想要全部展示这些时空数据变得越来越可能，我会在这个系列中逐步的提出一些我的探索，希望能给大家带来帮助。</p><p>格点数据有很多格式，我们可能常接触到的一些格点数据文件比方说<a href="https://en.wikipedia.org/wiki/GeoTIFF" target="_blank" rel="noopener">GeoTIFF</a>，气象里面有<a href="https://en.wikipedia.org/wiki/GRIB" target="_blank" rel="noopener">GRIB</a>，nc 等等，甚至一张简单的 png 也可以是由格点数据转换成的，它会包含基本的<strong>格点数量和值</strong>，比方说在经度方向有 300 个点，纬度方向有 200 个点，那么整个文件包含了 6w 个格点的数值，还通常包含一些<strong>经纬度信息</strong>，比方说每个点的经纬度值，也有可能只是四个顶点的经纬度信息，所以 raster（栅格）图层也在我的讨论范围之内。</p><h1 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h1><p>我会以 Leaflet 为主要的实现手段，个人觉得它的灵活性比较强，功能虽然基础，但是利于拓展，所以各类图层插件也层出不穷。<br>假设我想要在地图上渲染一组格点数据，数据长 300，宽 200，一共 60000 个点，从原理上分析无非就是把一个个的点钉在了地图上，我们可以清楚的<strong>读取或者推算</strong>出每个点的经纬度，要解决的就是点与点之间的空白怎么办，毕竟每个点从经纬度上来看即使只差了 1 度，算上比例尺，在地图上也是不等的像素点，所以我们需要通过<strong>插值计算</strong>来补齐点与点之间的空白。于是我就有了大致如下的想法：</p><blockquote><p>准备基础数据和图例等信息 =&gt; 准备 canvas 图层 =&gt; 找到初始点 =&gt; 根据数值和图例找到对应颜色 =&gt; 渲染颜色 =&gt; 循环直至画完所有的点</p></blockquote><h1 id="开发"><a href="#开发" class="headerlink" title="开发"></a>开发</h1><p>按照上面的思路一步步开始，我已经准备好了一个<a href="https://github.com/zzcyrus/Leaflet.Tile.PixelLayer/blob/master/demo/data.json" target="_blank" rel="noopener">温度相关的数据</a>和图例信息，后面也会再讨论数据的生成和优化，所以直接从第二步开始，这里只说大致的流程，具体的算法可以去看<a href="https://github.com/zzcyrus/Leaflet.Tile.PixelLayer/blob/master/src/index.js" target="_blank" rel="noopener">源代码</a></p><blockquote><p>创建 TilePixelLayer 图层并继承基础的<code>TileLayer</code>，需要重写<code>createTile</code>方法，每一个 tile 最终都返回一个我们自己绘制出来的 canvas</p></blockquote><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">L.TilePixelLayer = L.TileLayer.extend(&#123;</span><br><span class="line">    createTile: <span class="function"><span class="keyword">function</span>(<span class="params">coords</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">var</span> dom = L.DomUtil.create(<span class="string">'canvas'</span>, <span class="string">'leafvar-pixel-tile'</span>)</span><br><span class="line">      <span class="keyword">var</span> ctx = dom.getContext(<span class="string">'2d'</span>)</span><br><span class="line">      <span class="comment">//  bounds为当前tile的一些边界信息，绘制时候要用到</span></span><br><span class="line">      <span class="keyword">this</span>.interpolateTile(ctx,  <span class="string">'bounds'</span>)</span><br><span class="line">      <span class="keyword">return</span> dom</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>我们在每一个 tile 对应的 canvas 中，从 0 点开始，分别循环 256 像素的行和列，然后通过插值得到像素对应的数值，再找到数值对应的颜色，直至完成整个 canvas 的上色过程。</p></blockquote><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">interpolateTile: <span class="function"><span class="keyword">function</span>(<span class="params">ctx, bounds</span>) </span>&#123;  <span class="comment">// 逐像素绘制每一个tile</span></span><br><span class="line">  <span class="keyword">var</span> x = bounds.x                 <span class="comment">// x坐标为当前瓦片在地图上的初始像素x</span></span><br><span class="line">  <span class="keyword">var</span> tileX = <span class="number">0</span>                       <span class="comment">// tileX为当前瓦片canvas的坐标，在0~256之间循环</span></span><br><span class="line">  <span class="keyword">var</span> gap = <span class="number">4</span>                        <span class="comment">// 每隔四个点计算一次加快渲染速度，这个数值可以自己设置</span></span><br><span class="line">  <span class="keyword">var</span> colorImageData = <span class="keyword">new</span> ImageData(<span class="number">256</span>, <span class="number">256</span>)  <span class="comment">// 创建一个imageData接收颜色</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 循环行</span></span><br><span class="line">  <span class="keyword">var</span> batchInterpolate = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">while</span> (tileX &lt; bounds.w) &#123;</span><br><span class="line">      interpolateColumn(x, tileX)</span><br><span class="line">      x += gap</span><br><span class="line">      tileX += gap</span><br><span class="line">    &#125;</span><br><span class="line">    ctx.putImageData(colorImageData, <span class="number">0</span>, <span class="number">0</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 循环列</span></span><br><span class="line">  <span class="keyword">var</span> interpolateColumn = <span class="function">(<span class="params">x, tileX</span>) =&gt;</span> &#123;</span><br><span class="line">   <span class="keyword">for</span> (                                   <span class="comment">// y坐标为当前瓦片在地图上的初始像素y</span></span><br><span class="line">      <span class="keyword">var</span> defaultColor = [<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>], y = bounds.y, height = <span class="number">0</span>;</span><br><span class="line">      height &lt;= bounds.h;</span><br><span class="line">      y += gap, height += gap</span><br><span class="line">    ) &#123;</span><br><span class="line">      <span class="comment">// 把循环到的某个点的像素坐标转化为经纬度</span></span><br><span class="line">      <span class="keyword">var</span> location = <span class="keyword">this</span>.map.unproject(L.point(x, y), zoom)</span><br><span class="line">      <span class="keyword">var</span> coord = [location.lng, location.lat]</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 找到经纬度对应的周围四个点的坐标，通过双线性插值算出一个近似值作为当前像素点的数值</span></span><br><span class="line">      <span class="keyword">var</span> gridValue = <span class="keyword">this</span>.gridInterpolate(coord)</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 用简单的线性插值通过图例得到数值对应的颜色</span></span><br><span class="line">      <span class="keyword">var</span> color = <span class="keyword">this</span>.gradient(gridValue)</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 因为设置了gap，gap范围内的每一个像素点都用这个颜色，如果gap设置为1，那么每个像素都独立参与了计算，性能有所慢，但是精度高</span></span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; gap; i++) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> j = <span class="number">0</span>; j &lt; gap; j++) &#123;</span><br><span class="line">          <span class="keyword">this</span>.setColor(colorImageData, i, j, color)  <span class="comment">// 上色</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 开始绘制</span></span><br><span class="line">  batchInterpolate()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>总结一下，每一个瓦片都对应了一个 canvas，每个 canvas 我们都逐像素处理，先把像素转化为经纬度，通过插值得到一个比较精确的对应格点数值，再通过图例进行了上色。最终就把所有的格点数据都渲染了出来。</p></blockquote><h1 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h1><p>完整的代码可以在<a href="https://github.com/zzcyrus/Leaflet.Tile.PixelLayer" target="_blank" rel="noopener">github</a>上看到。<code>dist</code>目录下有编译好的 js 文件，在项目中引入就好。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilePixelLayer = L.tilePixelLayer(&#123;</span><br><span class="line">  data: data,</span><br><span class="line">  overlayAlpha: <span class="number">230</span>, <span class="comment">// 透明度</span></span><br><span class="line">  gradient: [</span><br><span class="line">    [<span class="number">233.15</span>, [<span class="number">56</span>, <span class="number">4</span>, <span class="number">45</span>]],</span><br><span class="line">    [<span class="number">243.15</span>, [<span class="number">48</span>, <span class="number">0</span>, <span class="number">106</span>]],</span><br><span class="line">    [<span class="number">253.15</span>, [<span class="number">0</span>, <span class="number">14</span>, <span class="number">134</span>]],</span><br><span class="line">    [<span class="number">256.15</span>, [<span class="number">3</span>, <span class="number">44</span>, <span class="number">144</span>]],</span><br><span class="line">    <span class="comment">// ......</span></span><br><span class="line">  ], <span class="comment">// 准备一组图例</span></span><br><span class="line">  clickEvt: <span class="function"><span class="keyword">function</span> (<span class="params">e, gridValue</span>) </span>&#123;</span><br><span class="line">    alert(<span class="string">`该地点温度:<span class="subst">$&#123;<span class="built_in">Math</span>.round(gridValue - <span class="number">273.15</span>)&#125;</span>°`</span>);</span><br><span class="line">  &#125;, <span class="comment">// 事件</span></span><br><span class="line">&#125;);</span><br><span class="line">map.addLayer(tilePixelLayer); <span class="comment">// 添加到地图上</span></span><br></pre></td></tr></table></figure><p>效果：</p> <iframe height="400" width="100%" src="https://kael.top/Leaflet.Tile.PixelLayer/demo/" frameborder="0" allowfullscreen><br> </iframe><p><a href="https://kael.top/Leaflet.Tile.PixelLayer/demo/">在线 demo</a></p><h1 id="后续"><a href="#后续" class="headerlink" title="后续"></a>后续</h1><p>这样我们已经初步在浏览器上完成了一个前端完全可控的格点数据渲染，当然对整个过程是否有更好的优化方法，我们后面会继续展开讨论。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在地理信息行业中，我们常遇见的数据中有这么两类：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;一类是站点（散点）数据。地图上分布着位置不均匀的各种站点，比方说气象站，水文站，如果我们需要查看站点数值趋势，就可以通过等值线图等方式，比如&lt;a href=&quot;https://github.com/d3/d3-contour&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;d3 的 contour&lt;/a&gt;，&lt;a href=&quot;https://turfjs.org/docs/#isolines&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;turf 的 isolines&lt;/a&gt;，还可以通过插值方法将站点插值到网格上，转化成格点数据，后面我们也会讨论。&lt;/li&gt;
&lt;li&gt;也有一类是格点数据。这个在气象上就非常常见，气象预报数据通常是模式跑出来的，如同渔网一样的格点，这种数据提供了查询任意位置数值的作用，它的渲染也同样常见，传统的方法是在服务器端生成好对应图片，在浏览器上贴图，这里追求实现的是&lt;code&gt;前端渲染&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="WebGIS" scheme="http://kael.top/categories/WebGIS/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
      <category term="Leaflet" scheme="http://kael.top/tags/Leaflet/"/>
    
  </entry>
  
  <entry>
    <title>Leaflet.Canvas-Marker-Layer 图层的诞生</title>
    <link href="http://kael.top/2019/11/18/leaflet-canvas-marker/"/>
    <id>http://kael.top/2019/11/18/leaflet-canvas-marker/</id>
    <published>2019-11-18T08:49:18.000Z</published>
    <updated>2020-05-17T09:04:57.332Z</updated>
    
    <content type="html"><![CDATA[<p>之前写过一个基于 Leaflet 的标注点图层插件，<a href="https://github.com/zzcyrus/Leaflet.Canvas-Marker-Layer" target="_blank" rel="noopener">Leaflet.Canvas-Marker-Layer</a>，今天简单的说一下为什么需要用到它。在 Leaflet 中如果你需要渲染 marker，有两种方式：</p><ul><li>一种是基于 dom 的<a href="https://leafletjs.com/reference-1.6.0.html#marker" target="_blank" rel="noopener">UI-Marker</a></li><li>另一种是基于 canvas 的<a href="https://leafletjs.com/reference-1.6.0.html#circlemarker" target="_blank" rel="noopener">CircleMarker</a>。</li></ul><p>可想而知：</p><ul><li>dom 提供了强大的自定义能力，我们可以通过 css 绘制出各种想要的效果，填充各种想要的数据内容，但问题在于 marker 过多就会导致页面特别的卡顿，当然官方也悉心的给出了许多<a href="https://leafletjs.com/plugins.html#clusteringdecluttering" target="_blank" rel="noopener">cluster 解决方案</a>，通过聚类或者碰撞检测，只展示一部分内容，根据缩放等级来调整展示内容的数量。</li><li>canvas 则提供了非常高的渲染性能，但不足的是<code>CircleMarker</code>仅仅是在地图上绘制了一个矢量图形，比如圆，矩形等，灵活性比较差，通常是通过点击事件或 popup 来展示具体的内容。</li></ul><h1 id="起源"><a href="#起源" class="headerlink" title="起源"></a>起源</h1><p>那么如果我想两者兼得呢？实际上 canvas 本身的功能是很强大的，可以绘制各种图案，甚至各种颜色的文字，所以我们要做的其实就是拓展官方<code>CircleMarker</code>的功能，让它自定义程度更高，有点类似于<a href="https://leafletjs.com/reference-1.6.0.html#divicon" target="_blank" rel="noopener">DivIcon</a>的概念。</p><p>在 github 上搜寻类似功能时候，我找到了<a href="https://github.com/eJuke/Leaflet.Canvas-Markers" target="_blank" rel="noopener">eJuke 的 Leaflet.Canvas-Markers</a>这个库，和我想要的功能基本类似，采用 canvas 来绘制大量的 maker，但是他只提供了图片类型的 marker，所以我们稍加改动就好。</p><h1 id="源码说明"><a href="#源码说明" class="headerlink" title="源码说明"></a>源码说明</h1><p><code>ejuke</code>的库对于图层创建、事件处理已经比较完善，我们只要修改下<code>_drawMarker</code>方法就好：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">_drawMarker: <span class="function"><span class="keyword">function</span> (<span class="params">marker, pointPos</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> self = <span class="keyword">this</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">this</span>._imageLookup) <span class="keyword">this</span>._imageLookup = &#123;&#125;</span><br><span class="line">    <span class="keyword">if</span> (!pointPos) &#123;</span><br><span class="line">      pointPos = self._map.latLngToContainerPoint(marker.getLatLng())</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 支持通过userDrawFunc返回layer（this）对象给用户</span></span><br><span class="line">    <span class="comment">// 通过layer._context就可以画出任何自定义的marker</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.options.userDrawFunc &amp;&amp; <span class="keyword">typeof</span> (<span class="keyword">this</span>.options.userDrawFunc) === <span class="string">'function'</span>) &#123;</span><br><span class="line">      <span class="keyword">const</span> size = marker.options.icon.options.iconSize</span><br><span class="line">      <span class="keyword">this</span>.options.userDrawFunc(<span class="keyword">this</span>, marker, pointPos, size)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      self._drawIcon(marker, pointPos)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="使用说明"><a href="#使用说明" class="headerlink" title="使用说明"></a>使用说明</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** userDrawFunc提供的参数说明</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param </span>layer      图层对象，layer._context即canvas的context</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param </span>marker     当前marker上存储的信息</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param </span>pointPos   当前marker的像素坐标</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param </span>size       当前marker的大小</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 你可以简单的画一个圆</span></span><br><span class="line"><span class="keyword">var</span> layer = L.canvasMarkerLayer(&#123;</span><br><span class="line">  userDrawFunc: <span class="function"><span class="keyword">function</span> (<span class="params">layer, marker, pointPos, size</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> ctx = layer._context;</span><br><span class="line">    ctx.beginPath();</span><br><span class="line">    ctx.arc(pointPos.x, pointPos.y, size[<span class="number">0</span>] / <span class="number">2</span>, <span class="number">0</span>, <span class="number">2</span> * <span class="built_in">Math</span>.PI);</span><br><span class="line">    ctx.fillStyle = <span class="string">"rgba(255,12,0,0.4)"</span>;</span><br><span class="line">    ctx.fill();</span><br><span class="line">    ctx.closePath();</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;).addTo(map);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 你也可以画一个矩形里面再配上文字</span></span><br><span class="line"><span class="keyword">const</span> ciLayer = L.canvasMarkerLayer(&#123;</span><br><span class="line">  userDrawFunc: <span class="function"><span class="keyword">function</span> (<span class="params">layer, marker, pointPos, size</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> ctx = layer._context;</span><br><span class="line">    <span class="keyword">const</span> number = marker.properties.number;</span><br><span class="line">    ctx.beginPath();</span><br><span class="line">    ctx.fillStyle = <span class="string">"rgba(255,0,0,0.8)"</span>;</span><br><span class="line">    ctx.fillRect(</span><br><span class="line">      pointPos.x - size[<span class="number">0</span>] / <span class="number">2</span>,</span><br><span class="line">      pointPos.y - size[<span class="number">1</span>] / <span class="number">2</span>,</span><br><span class="line">      size[<span class="number">0</span>],</span><br><span class="line">      size[<span class="number">1</span>]</span><br><span class="line">    );</span><br><span class="line">    ctx.font = <span class="string">"12px Helvetica Neue"</span>;</span><br><span class="line">    ctx.fillStyle = <span class="string">"#000"</span>;</span><br><span class="line">    ctx.fillText(number, pointPos.x, pointPos.y + size[<span class="number">1</span>] / <span class="number">4</span>);</span><br><span class="line">    ctx.textAlign = <span class="string">"center"</span>;</span><br><span class="line">    ctx.closePath();</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;).addTo(map);</span><br></pre></td></tr></table></figure><p><a href="https://kael.top/Leaflet.Canvas-Marker-Layer/example/text.html">demo</a></p><p>通过这样在 canvas 上绘制大量 marker 的效果就大大改善了。</p><p><a href="https://github.com/zzcyrus/Leaflet.Canvas-Marker-Layer" target="_blank" rel="noopener">仓库</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;之前写过一个基于 Leaflet 的标注点图层插件，&lt;a href=&quot;https://github.com/zzcyrus/Leaflet.Canvas-Marker-Layer&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Leaflet.Canvas-
      
    
    </summary>
    
      <category term="WebGIS" scheme="http://kael.top/categories/WebGIS/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
      <category term="Leaflet" scheme="http://kael.top/tags/Leaflet/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata 插件发开指南</title>
    <link href="http://kael.top/2019/07/22/tilestrata-plugin/"/>
    <id>http://kael.top/2019/07/22/tilestrata-plugin/</id>
    <published>2019-07-22T10:16:33.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata" target="_blank" rel="noopener">Tilestrata</a>的插件开发基本围绕着生命周期各个阶段展开，如果要开发一个自己的插件，首先想清楚插件的功能属于生命周期的哪个阶段，依据官方规定好的该阶段代码结构，写好插件内容，在服务端利用<code>use</code>进行注册就好。个人觉得<code>Tilestrata</code>吸引人的一个地方就是在于插件机制非常的灵活和便捷，十分好上手。</p><a id="more"></a><p>这篇文章主要基于官方文档说明。</p><p>生命周期的各个阶段如下：</p><ul><li><code>request hook</code> – 请求预处理</li><li><code>cache</code> – 缓存控制</li><li><code>provider</code> – 数据源，比如 mapnik</li><li><code>transform</code> – 最关键的阶段，可以在瓦片输出的过程中对其做各种加工，比如旋转，染色</li><li><code>response hook</code> – 响应后处理，比如 headers、jsonp 插件</li></ul><h1 id="1-插件共有部分"><a href="#1-插件共有部分" class="headerlink" title="1. 插件共有部分"></a>1. 插件共有部分</h1><p>所有的插件都可以拥有<code>init</code>和<code>destroy</code>两个生命周期方法，分别在插件的启动和结束时触发。这个两个方法的第一个参数是当前<code>tilestrata</code>实例，第二个参数是<code>callback</code>回调函数。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">init: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">    callback(err);</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure><h1 id="2-request-请求插件"><a href="#2-request-请求插件" class="headerlink" title="2. request 请求插件"></a>2. request 请求插件</h1><p>request 类型的插件必须提供<code>reqhook</code>方法，参数如下。其中<code>req</code>参数代表<a href="http://nodejs.org/api/http.html#http_http_incomingmessage" target="_blank" rel="noopener">http.IncomingMessage</a> ，<code>res</code>参数代表<a href="http://nodejs.org/api/http.html#http_class_http_serverresponse" target="_blank" rel="noopener">http.ServerResponse</a>。 如果不执行里面的<code>callback</code>回调函数，甚至这个请求都不会进入后续<code>server</code>的处理逻辑中，所以千万别忘了它。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span>(<span class="params">options</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    name: <span class="string">'myplugin'</span>,</span><br><span class="line">    init: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    reqhook: <span class="function"><span class="keyword">function</span>(<span class="params">server, tile, req, res, callback</span>) </span>&#123;</span><br><span class="line">      callback()</span><br><span class="line">    &#125;,</span><br><span class="line">    destroy: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="3-Caches-缓存插件"><a href="#3-Caches-缓存插件" class="headerlink" title="3. Caches 缓存插件"></a>3. Caches 缓存插件</h1><p>Caches 类型的插件必须提供<code>get</code>和<code>set</code>方法，参数分别如下。当你的缓存过程失败出错的时候，tilestrata 会忽略错误，无视这段逻辑，尝试直接从数据源获取数据。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span>(<span class="params">options</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    name: <span class="string">'myplugin'</span>,</span><br><span class="line">    init: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    <span class="keyword">get</span>: function(server, tile, callback) &#123;</span><br><span class="line">      callback(err, buffer, headers <span class="comment">/* refresh */</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    <span class="keyword">set</span>: function(server, tile, buffer, headers, callback) &#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    destroy: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>有一种特殊的场景：虽然请求命中了缓存，仍然需要在后台生成一张新的瓦片。比方说某一张瓦片已经很久没更新了，需要重新生成，但又不是那么着急，一定要用户等着这个新瓦片渲染出来，所以其实我们需要的是返回缓存结果，同时更新瓦片缓存。这种情况下为<code>get()</code>方法的<code>callback</code>增加第四个参数<code>true</code>就好了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">callback(<span class="literal">null</span>, buffer, headers, <span class="literal">true</span>)</span><br></pre></td></tr></table></figure><h1 id="4-Providers-数据源插件"><a href="#4-Providers-数据源插件" class="headerlink" title="4. Providers 数据源插件"></a>4. Providers 数据源插件</h1><p>Providers 类型的插件必须提供<code>serve</code>方法，参数如下。主要作用就是提供<strong>数据源</strong>，数据可以来源于第本地的某个功能模块比方说 mapnik 服务，可以来源于在线的第三方服务，比如说对 bing 地图服务做了一个代理转发。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span>(<span class="params">options</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    name: <span class="string">'myplugin'</span>,</span><br><span class="line">    init: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    serve: <span class="function"><span class="keyword">function</span>(<span class="params">server, tile, callback</span>) </span>&#123;</span><br><span class="line">      callback(err, buffer, headers)</span><br><span class="line">    &#125;,</span><br><span class="line">    destroy: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="5-Transforms-数据处理插件"><a href="#5-Transforms-数据处理插件" class="headerlink" title="5. Transforms 数据处理插件"></a>5. Transforms 数据处理插件</h1><p>Transforms 类型的插件必须提供<code>transform</code>方法，参数如下。它的主要作用是在数据从 provider 取出来之后，做一些额外的处理，比方说加一个水印，调整一下透明度，再把处理后的结果输出出去。要注意下这个过程是发生在<strong>缓存之前</strong>的。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span>(<span class="params">options</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    name: <span class="string">'myplugin'</span>,</span><br><span class="line">    init: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    transform: <span class="function"><span class="keyword">function</span>(<span class="params">server, tile, buffer, headers, callback</span>) </span>&#123;</span><br><span class="line">      callback(err, buffer, headers)</span><br><span class="line">    &#125;,</span><br><span class="line">    destroy: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="6-Response-响应插件"><a href="#6-Response-响应插件" class="headerlink" title="6. Response 响应插件"></a>6. Response 响应插件</h1><p>Response 类型的插件必须提供<code>reshook</code>方法，参数如下，其中参数里面的<code>result</code>包含了三个属性：<code>headers</code>, <code>buffer</code>和 <code>status</code>，这三个属性在返回真正的结果之前都是可以被修改的。比方说在返回结果之前，给每一个<code>headers</code>都加上<code>&#39;Access-Control-Allow-Origin&#39;: &#39;*&#39;</code>来让请求支持跨域访问。此外<code>req</code>参数代表<a href="http://nodejs.org/api/http.html#http_http_incomingmessage" target="_blank" rel="noopener">http.IncomingMessage</a> ，<code>res</code>参数代表<a href="http://nodejs.org/api/http.html#http_class_http_serverresponse" target="_blank" rel="noopener">http.ServerResponse</a>。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span>(<span class="params">options</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    name: <span class="string">'myplugin'</span>,</span><br><span class="line">    init: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    reshook: <span class="function"><span class="keyword">function</span>(<span class="params">server, tile, req, res, result, callback</span>) </span>&#123;</span><br><span class="line">      callback()</span><br><span class="line">    &#125;,</span><br><span class="line">    destroy: <span class="function"><span class="keyword">function</span>(<span class="params">server, callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="7-同时提供多个功能的插件"><a href="#7-同时提供多个功能的插件" class="headerlink" title="7. 同时提供多个功能的插件"></a>7. 同时提供多个功能的插件</h1><p>有时候一个插件由多个部分组成，所以你可以把多个功能放进一个数组传递给 TileStrata。例如我想做一个插件记录响应时间，那既要在 request 时做出记录，也要在 response 时做出记录，把它俩放进数组就 ok 了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> [</span><br><span class="line">        <span class="comment">//  第一个插件</span></span><br><span class="line">        &#123;<span class="attr">name</span>: <span class="string">'myplugin'</span>, <span class="attr">reqhook</span>: <span class="function"><span class="keyword">function</span>(<span class="params">...</span>) </span>&#123; <span class="comment">/* ... */</span> &#125;&#125;,</span><br><span class="line">        <span class="comment">//  第一个插件</span></span><br><span class="line">        &#123;<span class="attr">name</span>: <span class="string">'myplugin'</span>, <span class="attr">reshook</span>: <span class="function"><span class="keyword">function</span>(<span class="params">...</span>) </span>&#123; <span class="comment">/* ... */</span> &#125;&#125;</span><br><span class="line">    ];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>通观下来，整个 Tilestrata 插件开发的流程其实是非常舒畅和自由的，可以充分的发挥创造力。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Tilestrata&lt;/a&gt;的插件开发基本围绕着生命周期各个阶段展开，如果要开发一个自己的插件，首先想清楚插件的功能属于生命周期的哪个阶段，依据官方规定好的该阶段代码结构，写好插件内容，在服务端利用&lt;code&gt;use&lt;/code&gt;进行注册就好。个人觉得&lt;code&gt;Tilestrata&lt;/code&gt;吸引人的一个地方就是在于插件机制非常的灵活和便捷，十分好上手。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata 入门指南</title>
    <link href="http://kael.top/2019/07/18/tilestrata-intro/"/>
    <id>http://kael.top/2019/07/18/tilestrata-intro/</id>
    <published>2019-07-18T10:13:53.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p>简单来说<a href="https://github.com/naturalatlas/tilestrata" target="_blank" rel="noopener"><strong>Tilestrata</strong></a>就是一个瓦片地图服务器。根据其自身的介绍，它有如下的特点：零配置，简洁，测试覆盖率高，性能高，拓展性强。可以通过<a href="https://github.com/naturalatlas/tilestrata-balancer" target="_blank" rel="noopener">TileStrata Balancer</a>实现负载均衡，无缝伸缩。可以通过内置<a href="https://github.com/naturalatlas/tilestrata#profiling--debugging-performance" target="_blank" rel="noopener">控制台</a>去调试查看渲染时间。</p><a id="more"></a><p>本文主要基于官方的文档介绍及部分使用心得，旨在帮助大家快速上手，进阶使用会后续说明。</p><p>安装：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata --save</span><br></pre></td></tr></table></figure><h2 id="1-主要结构"><a href="#1-主要结构" class="headerlink" title="1. 主要结构"></a>1. 主要结构</h2><p>TileStrata 的结构上有以下五个主要组成部分，通常用来实现各种插件，我个人习惯把它们看作是生命周期流程，正是由于这些 hook 的存在，我们就可以在 TileStrata 生命周期的各个阶段去实现各种巧妙的功能：</p><ul><li><code>request hook</code> – 请求预处理</li><li><code>cache</code> – 缓存控制</li><li><code>provider</code> – 数据源，比如 mapnik</li><li><code>transform</code> – 最关键的阶段，可以在瓦片输出的过程中对其做各种加工，比如旋转，染色</li><li><code>response hook</code> – 响应后处理，比如 headers、jsonp 插件</li></ul><p>官方提供了一张很直观的流程图：</p><p><img src="https://camo.githubusercontent.com/67abe0f563502d78ef12b4ce20cba57222dfcf6f/68747470733a2f2f63646e2e7261776769742e636f6d2f6e61747572616c61746c61732f74696c657374726174612f6d61737465722f6d6973632f67726170686963732f726571666c6f772e737667" alt></p><h2 id="2-常用插件"><a href="#2-常用插件" class="headerlink" title="2. 常用插件"></a>2. 常用插件</h2><ul><li><a href="https://github.com/naturalatlas/tilestrata-mapnik" target="_blank" rel="noopener">tilestrata-mapnik</a> – 使用 <a href="http://mapnik.org/" target="_blank" rel="noopener">mapnik</a>做数据源进行渲染</li><li><a href="https://github.com/naturalatlas/tilestrata-disk" target="_blank" rel="noopener">tilestrata-disk</a> – 服务端缓存插件</li><li><a href="https://github.com/naturalatlas/tilestrata-dependency" target="_blank" rel="noopener">tilestrata-dependency</a> – 直接使用其他定义好的图层来获取数据，而不是定义一个 provider</li><li><a href="https://github.com/naturalatlas/tilestrata-sharp" target="_blank" rel="noopener">tilestrata-sharp</a> – 用 <a href="https://www.npmjs.com/package/sharp" target="_blank" rel="noopener">libvips</a>来做一些图片的处理，比如压缩，旋转，转换类型</li><li><a href="https://github.com/naturalatlas/tilestrata-gm" target="_blank" rel="noopener">tilestrata-gm</a> – 使用 <a href="https://www.npmjs.com/package/gm" target="_blank" rel="noopener">GraphicsMagick</a>来对图片做处理，大体上和上面类似</li><li><a href="https://github.com/naturalatlas/tilestrata-headers" target="_blank" rel="noopener">tilestrata-headers</a> – 设置请求的响应头，可以解决跨域问题</li><li><a href="https://github.com/naturalatlas/tilestrata-blend" target="_blank" rel="noopener">tilestrata-blend</a> – 合成多个瓦片图层为一个图层，比方组合道路+建筑为一个图层</li><li><a href="https://github.com/naturalatlas/tilestrata-jsonp" target="_blank" rel="noopener">tilestrata-jsonp</a> – 使 utfgrids 类的 json 数据支持 jsonp 请求方式</li><li><a href="https://github.com/naturalatlas/tilestrata-datadog" target="_blank" rel="noopener">tilestrata-datadog</a> – 继承数据到<a href="https://www.datadoghq.com/" target="_blank" rel="noopener">Datadog</a>服务</li><li><a href="https://github.com/naturalatlas/tilestrata-utfmerge" target="_blank" rel="noopener">tilestrata-utfmerge</a> – 合并两个 utfgrids 类图层为一个图层</li><li><a href="https://github.com/naturalatlas/tilestrata-vtile" target="_blank" rel="noopener">tilestrata-vtile</a> – 输出 mapnik 数据为矢量切片 pbf 格式</li><li><a href="https://github.com/naturalatlas/tilestrata-vtile-raster" target="_blank" rel="noopener">tilestrata-vtile-raster</a> – 将上面的 pbf 的是瓦片转换成栅格瓦片</li><li><a href="https://github.com/naturalatlas/tilestrata-vtile-composite" target="_blank" rel="noopener">tilestrata-vtile-composite</a> – 合并多个矢量瓦片图层</li><li><a href="https://github.com/naturalatlas/tilestrata-proxy" target="_blank" rel="noopener">tilestrata-proxy</a> – 为其他瓦片服务器提供代理</li><li><a href="https://github.com/naturalatlas/tilestrata-lru" target="_blank" rel="noopener">tilestrata-lru</a> – 在内存中缓存瓦片数据</li><li><a href="https://github.com/naturalatlas/tilestrata-etag" target="_blank" rel="noopener">tilestrata-etag</a> – 让瓦片图层支持自定义 ETag 标签，一般用来处理 http 缓存</li><li><a href="https://github.com/naturalatlas/tilestrata-bing" target="_blank" rel="noopener">tilestrata-bing</a> – Bing 地图的 provider</li><li><a href="https://github.com/naturalatlas/tilestrata-underzoom" target="_blank" rel="noopener">tilestrata-underzoom</a> - Build mosaics of higher-zoom tiles</li><li><a href="https://github.com/Stezii/tilestrata-postgismvt" target="_blank" rel="noopener">tilestrata-postgismvt</a> – 使用 PostGIS 数据库输出 mvt 格式的瓦片</li><li><a href="https://github.com/naturalatlas/tilestrata-postgis-geojson-tiles" target="_blank" rel="noopener">tilestrata-postgis-geojson-tiles</a> – 使用 PostGIS 数据库输出 GeoJSON 格式的瓦片</li></ul><h2 id="3-基本使用"><a href="#3-基本使用" class="headerlink" title="3. 基本使用"></a>3. 基本使用</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">var</span> disk = <span class="built_in">require</span>(<span class="string">'tilestrata-disk'</span>)</span><br><span class="line"><span class="keyword">var</span> sharp = <span class="built_in">require</span>(<span class="string">'tilestrata-sharp'</span>)</span><br><span class="line"><span class="keyword">var</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"><span class="keyword">var</span> dependency = <span class="built_in">require</span>(<span class="string">'tilestrata-dependency'</span>)</span><br><span class="line"><span class="keyword">var</span> strata = tilestrata()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个图层</span></span><br><span class="line">strata</span><br><span class="line">  .layer(<span class="string">'layerName'</span>)</span><br><span class="line">  <span class="comment">// 设置第一个瓦片路径</span></span><br><span class="line">  .route(<span class="string">'tileName.png'</span>)</span><br><span class="line">  <span class="comment">// 设置缓存目录</span></span><br><span class="line">  .use(disk.cache(&#123; <span class="attr">dir</span>: <span class="string">'/var/lib/tiles/basemap'</span> &#125;))</span><br><span class="line">  <span class="comment">// 设置数据源mapnik</span></span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'/path/to/map.xml'</span>,</span><br><span class="line">      tileSize: <span class="number">512</span>,</span><br><span class="line">      scale: <span class="number">2</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  <span class="comment">// 设置第二个瓦片路径</span></span><br><span class="line">  .route(<span class="string">'tileSharp.png'</span>)</span><br><span class="line">  .use(disk.cache(&#123; <span class="attr">dir</span>: <span class="string">'/var/lib/tiles/basemap'</span> &#125;))</span><br><span class="line">  <span class="comment">// 用dependency插件直接使用layerName的源</span></span><br><span class="line">  .use(dependency(<span class="string">'layerName'</span>, <span class="string">'tileName.png'</span>))</span><br><span class="line">  <span class="comment">// 用sharp插件resize每个瓦片的大小</span></span><br><span class="line">  .use(</span><br><span class="line">    sharp(<span class="function"><span class="keyword">function</span>(<span class="params">image, sharp</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> image.resize(<span class="number">256</span>)</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">strata.listen(<span class="number">8080</span>)</span><br></pre></td></tr></table></figure><p>服务启动后，可以通过下面的地址来访问瓦片数据：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/layerName/:z/:x:/:y/tileName.png</span><br></pre></td></tr></table></figure><h3 id="3-1-不定义瓦片名称"><a href="#3-1-不定义瓦片名称" class="headerlink" title="3.1 不定义瓦片名称"></a>3.1 不定义瓦片名称</h3><p>从 <a href="https://github.com/naturalatlas/tilestrata/releases/tag/v2.1.0" target="_blank" rel="noopener">2.1.0</a>版本开始, 在设置<code>route</code>名称的时候，可以直接采用下面的形式，更加符合通用的瓦片格式：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">.route(<span class="string">'*.png'</span>) <span class="comment">// /layer/0/0/0.png</span></span><br><span class="line">.route(<span class="string">'*@2x.png'</span>) <span class="comment">// /layer/0/0/0@2x.png</span></span><br></pre></td></tr></table></figure><h3 id="3-2-整合-Express-js-Connect"><a href="#3-2-整合-Express-js-Connect" class="headerlink" title="3.2 整合 Express.js / Connect"></a>3.2 整合 <a href="http://expressjs.com/" target="_blank" rel="noopener">Express.js</a> / <a href="https://github.com/senchalabs/connect" target="_blank" rel="noopener">Connect</a></h3><p>TileStrata 可以定义成 Express 的一个中间件去使用，不必通过执行<code>strata.listen(8080)</code>来启动一个 server 服务，</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">var</span> strata = tilestrata()</span><br><span class="line">strata.layer(<span class="string">'basemap'</span>) <span class="comment">/* ... */</span></span><br><span class="line">strata.layer(<span class="string">'contours'</span>) <span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line">app.use(</span><br><span class="line">  tilestrata.middleware(&#123;</span><br><span class="line">    server: strata,</span><br><span class="line">    prefix: <span class="string">'/maps'</span></span><br><span class="line">  &#125;)</span><br><span class="line">)</span><br></pre></td></tr></table></figure><h2 id="4-使用说明"><a href="#4-使用说明" class="headerlink" title="4. 使用说明"></a>4. 使用说明</h2><h3 id="4-1-支持-Metatile-的负载均衡和图层共享"><a href="#4-1-支持-Metatile-的负载均衡和图层共享" class="headerlink" title="4.1 支持 Metatile 的负载均衡和图层共享"></a>4.1 支持 Metatile 的负载均衡和图层共享</h3><p>TileStrata 从 <a href="https://github.com/naturalatlas/tilestrata/releases/tag/v2.0.0" target="_blank" rel="noopener">2.0.0</a> 版本开始支持集成<a href="https://github.com/naturalatlas/tilestrata-balancer" target="_blank" rel="noopener">TileStrata Balancer</a>, 这是一个专门针对瓦片服务中的<a href="http://wiki.openstreetmap.org/wiki/Meta_tiles" target="_blank" rel="noopener">metatiles</a>设计的弹性负载均衡器，一般的负载均衡器并没有<code>metatiles</code>这个概念，所以会简单的把瓦片请求的分散到多个服务器，这样会导致冗余的渲染，不仅速度慢还会造成服务器资源浪费。</p><p>需要额外注意的是，均衡器并不会假设负载池中的所有服务器都提供了相同的图层， 它会持续追踪每个节点上提供的所有的图层，因此他知道某个图层明确的访问路径。</p><ul><li><strong>Fully elastic</strong> （最小化配置）</li><li><strong>Consistent routing</strong> (提高本地缓存命中率)</li><li><strong>Metatile-aware</strong> (防止冗余渲染)</li><li><strong>Layer-aware</strong> (支持图层的异构分布)</li></ul><p><a href="https://github.com/naturalatlas/tilestrata-balancer" target="_blank" rel="noopener"><strong>TileStrata Balancer 详细文档 →</strong></a></p><h3 id="4-2-重建瓦片缓存"><a href="#4-2-重建瓦片缓存" class="headerlink" title="4.2 重建瓦片缓存"></a>4.2 重建瓦片缓存</h3><p>在更新了图层的样式或者数据源后，你需要更新瓦片。这时候不必立刻删掉所有旧缓存，可以在请求头中设置<code>X-TileStrata-SkipCache</code>，逐步请求这些瓦片让服务器重新构建它们。<a href="https://github.com/naturalatlas/tilemantle" target="_blank" rel="noopener">TileMantle</a>工作可以让这个过程十分简单：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm install -g tilemantle</span><br><span class="line">tilemantle http://myhost.com/mylayer/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;/t.png \</span><br><span class="line">    -p 44.9457507,-109.5939822 -b 30mi -z 10-14 \</span><br><span class="line">    -H <span class="string">"X-TileStrata-SkipCache:mylayer/t.png"</span></span><br></pre></td></tr></table></figure><p>如果你使用了 <a href="https://github.com/naturalatlas/tilestrata-dependency" target="_blank" rel="noopener">tilestrata-dependency</a> 插件, 请求头则需要设置成如下形式：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">X-TileStrata-SkipCache:*</span><br><span class="line">X-TileStrata-SkipCache:[layer]/[file],[layer]/[file],...</span><br></pre></td></tr></table></figure><p>在一些高级的使用场景中，为了确保瓦片在缓存实际写入文件系统之前不要从服务器返回请求，需要额外做如下设置：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">X-TileStrata-CacheWait:1</span><br></pre></td></tr></table></figure><h3 id="4-1-健康检查"><a href="#4-1-健康检查" class="headerlink" title="4.1 健康检查"></a>4.1 健康检查</h3><p>TileStrata 包含一个名为<code>/health</code>内置路径，如果服务器连接正常，你会收到<code>200 OK</code>的返回值。这可以用来自定义一些错误和成功提醒，设置想要的返回值。只要在初始化 tilestrata 时，给选项中的 healthy 设置一个回调函数即可。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 不健康状态</span></span><br><span class="line"><span class="keyword">var</span> strata = tilestrata(&#123;</span><br><span class="line">  healthy: <span class="function"><span class="keyword">function</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">    callback(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'CPU is too high'</span>), &#123; <span class="attr">loadavg</span>: <span class="number">3</span> &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 错误信息'CPU is too high'会包装在message字段中返回</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 健康状态</span></span><br><span class="line"><span class="keyword">var</span> strata = tilestrata(&#123;</span><br><span class="line">  healthy: <span class="function"><span class="keyword">function</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">    callback(<span class="literal">null</span>, &#123; <span class="attr">loadavg</span>: <span class="number">1</span> &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确的返回值中将会额外的多一个loadavg字段，这将是你自定义的内容</span></span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;简单来说&lt;a href=&quot;https://github.com/naturalatlas/tilestrata&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;&lt;strong&gt;Tilestrata&lt;/strong&gt;&lt;/a&gt;就是一个瓦片地图服务器。根据其自身的介绍，它有如下的特点：零配置，简洁，测试覆盖率高，性能高，拓展性强。可以通过&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-balancer&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;TileStrata Balancer&lt;/a&gt;实现负载均衡，无缝伸缩。可以通过内置&lt;a href=&quot;https://github.com/naturalatlas/tilestrata#profiling--debugging-performance&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;控制台&lt;/a&gt;去调试查看渲染时间。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-disk 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/15/tilestrata-disk/"/>
    <id>http://kael.top/2019/07/15/tilestrata-disk/</id>
    <published>2019-07-15T13:39:59.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-disk" target="_blank" rel="noopener">tilestrata-disk</a>插件可以在磁盘中存储/检索瓦片。它可以兼顾瓦片缓存和瓦片提供两个功能，代码虽简单，但是功能十分全面，个人感觉非常的实用，很值得推荐。</p><p>用它来缓存瓦片时，一定要为每个图层使用不同的目录（例如：”tiles/layer_a”, “tiles/layer_b”）。如果设置了<code>maxage</code>参数，插件会检查瓦片的最近一次修改时间，如果间隔过长，将返回<code>null</code>。如果 设置<code>maxage=0</code>，就相当于没有开启缓存功能。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-disk --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"><span class="keyword">const</span> disk = <span class="built_in">require</span>(<span class="string">'tilestrata-disk'</span>)</span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'world_merc'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  <span class="comment">// 直接缓存</span></span><br><span class="line">  .use(disk.cache(&#123; <span class="attr">dir</span>: <span class="string">'tilecache/world'</span> &#125;))</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/world.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'province'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  <span class="comment">// 不同图层使用不同目录，同时设置最大缓存时间3600秒</span></span><br><span class="line">  .use(disk.cache(&#123; <span class="attr">maxage</span>: <span class="number">3600</span>, <span class="attr">dir</span>: <span class="string">'tilecache/province'</span> &#125;))</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/province.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><p>此外，disk 插件的功能远不止如此：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设置maxage为function，控制不同层级的过期时间</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'mylayer'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(<span class="comment">/* some provider */</span>)</span><br><span class="line">  .use(</span><br><span class="line">    disk.cache(&#123;</span><br><span class="line">      dir: <span class="string">'./tiles/mylayer'</span>,</span><br><span class="line">      maxage: <span class="function"><span class="keyword">function</span>(<span class="params">server, req</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (req.z &gt; <span class="number">15</span>) <span class="keyword">return</span> <span class="number">0</span> <span class="comment">// 不缓存</span></span><br><span class="line">        <span class="keyword">if</span> (req.z &gt; <span class="number">13</span>) <span class="keyword">return</span> <span class="number">3600</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">3600</span> * <span class="number">24</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以通过path自定义缓存的结构和文件名</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'mylayer'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(<span class="comment">/* some provider */</span>)</span><br><span class="line">  .use(disk.cache(&#123; <span class="attr">path</span>: <span class="string">'./tiles/&#123;layer&#125;/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;-&#123;filename&#125;'</span> &#125;))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过callback的方式来设置</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'mylayer'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(<span class="comment">/* some provider */</span>)</span><br><span class="line">  .use(</span><br><span class="line">    disk.cache(&#123;</span><br><span class="line">      path: <span class="function"><span class="keyword">function</span>(<span class="params">tile</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">'./tiles/'</span> + tile.layer + <span class="string">'/'</span> + tile.z + <span class="string">'/'</span> <span class="comment">/* ... */</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 直接使用缓存下来的文件当作provider，这样就不调用服务器生成图片了</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'mylayer'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(disk.provider(<span class="string">'/path/to/dir/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;/file.png'</span>))</span><br></pre></td></tr></table></figure><p>甚至在<code>0.6.0</code>及其以上的 tilestrata 版本中，你还可以通过设定<code>refreshage</code>来控制缓存数据的寿命，这个参数需要与<code>maxage</code>一起设设置：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">.use(disk.cache(&#123;</span><br><span class="line">    dir: <span class="string">'./tiles/mylayer'</span>,</span><br><span class="line">    refreshage: <span class="number">3600</span>, <span class="comment">// 1 hour</span></span><br><span class="line">    maxage: <span class="number">3600</span>*<span class="number">24</span>*<span class="number">7</span> <span class="comment">// 1 week</span></span><br><span class="line">&#125;));</span><br><span class="line"></span><br><span class="line">refreshage: <span class="literal">null</span>  <span class="comment">// 永远不刷新缓存</span></span><br><span class="line">refreshage: <span class="number">0</span>     <span class="comment">// 命中一次后就刷新</span></span><br><span class="line">refreshage: <span class="number">1800</span>  <span class="comment">// 半小时后刷新</span></span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>下图中设置了<code>tilecache</code>缓存目录，可以看到瓦片会按照层级建立对应的目录结构并保存图片：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-disk-20190705160620.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>因为插件即提供了<code>cache</code>功能，也提供了<code>Provider</code>功能，所以其实它是这两种类型插件的组合，作为<code>Provider</code>还是比较简单的，核心是 serve 方法:</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">serve: <span class="function"><span class="keyword">function</span>(<span class="params">server, tile, callback</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 解析路径</span></span><br><span class="line">  <span class="keyword">var</span> file = template</span><br><span class="line">    .replace(<span class="string">'&#123;layer&#125;'</span>, tile.layer)</span><br><span class="line">    .replace(<span class="string">'&#123;filename&#125;'</span>, tile.filename)</span><br><span class="line">    .replace(<span class="string">'&#123;x&#125;'</span>, tile.x)</span><br><span class="line">    .replace(<span class="string">'&#123;y&#125;'</span>, tile.y)</span><br><span class="line">    .replace(<span class="string">'&#123;z&#125;'</span>, tile.z);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 根绝路径读取文件为buffer</span></span><br><span class="line">  fs.readFile(file, <span class="function"><span class="keyword">function</span>(<span class="params">err, buffer</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (err) &#123;</span><br><span class="line">      <span class="keyword">if</span> (err.code === <span class="string">'ENOENT'</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> err = <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'File not found'</span>);</span><br><span class="line">        err.statusCode = <span class="number">404</span>;</span><br><span class="line">        <span class="keyword">return</span> callback(err);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> callback(err);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 返回buffer给tilestrata</span></span><br><span class="line">    callback(<span class="literal">null</span>, buffer, &#123;<span class="string">'Content-Type'</span>: mime(file)&#125;);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在<code>cache</code>功能中，需要提供 get 和 set 方法，主要就是在 set 方法中保存文件，在 get 方法中判断是否过期，是否文件名命中缓存，收否需要刷新，之后再返回对应结果的内容：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// set 方法</span></span><br><span class="line">FileSystemCache.prototype.set = <span class="function"><span class="keyword">function</span>(<span class="params"></span></span></span><br><span class="line"><span class="function"><span class="params">  server,</span></span></span><br><span class="line"><span class="function"><span class="params">  req,</span></span></span><br><span class="line"><span class="function"><span class="params">  buffer,</span></span></span><br><span class="line"><span class="function"><span class="params">  headers,</span></span></span><br><span class="line"><span class="function"><span class="params">  callback</span></span></span><br><span class="line"><span class="function"><span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> maxage = <span class="keyword">this</span>.ageTolerance(<span class="string">'maxage'</span>, req)</span><br><span class="line">  <span class="keyword">if</span> (maxage === <span class="number">0</span>) <span class="keyword">return</span> callback()</span><br><span class="line">  <span class="comment">// 只要maxage不为0，就在文件系统上存储图片，this._file是根据path或者dir设置计算出来的存储路径</span></span><br><span class="line">  fs.outputFile(<span class="keyword">this</span>._file(req), buffer, callback)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// get 方法（有所精简）</span></span><br><span class="line">FileSystemCache.prototype.get = <span class="function"><span class="keyword">function</span>(<span class="params">server, req, callback</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> maxage = <span class="keyword">this</span>.ageTolerance(<span class="string">'maxage'</span>, req)</span><br><span class="line">  <span class="comment">// maxage为0，直接结束</span></span><br><span class="line">  <span class="keyword">if</span> (maxage === <span class="number">0</span>) <span class="keyword">return</span> done()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> self = <span class="keyword">this</span></span><br><span class="line">  <span class="comment">// 获取缓存文件路径</span></span><br><span class="line">  <span class="keyword">var</span> file = <span class="keyword">this</span>._file(req)</span><br><span class="line">  fs.open(file, <span class="string">'r'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">err, fd</span>) </span>&#123;</span><br><span class="line">    fs.fstat(fd, <span class="function"><span class="keyword">function</span>(<span class="params">err, stats</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (err) <span class="keyword">return</span> done(err)</span><br><span class="line">      <span class="keyword">var</span> mtime = stats.mtime.getTime()</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 根据maxage是否过期判断是否直接结束</span></span><br><span class="line">      <span class="keyword">var</span> shouldServe = self.shouldServe(mtime, req)</span><br><span class="line">      <span class="keyword">if</span> (!shouldServe) <span class="keyword">return</span> done()</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 根据refreshage判断是否刷新</span></span><br><span class="line">      <span class="keyword">var</span> shouldRefresh = self.shouldRefresh(mtime, req)</span><br><span class="line">      <span class="keyword">var</span> headers = &#123; <span class="string">'Content-Type'</span>: mime(file) &#125;</span><br><span class="line">      <span class="keyword">var</span> buffer = <span class="keyword">new</span> Buffer(stats.size)</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 如果没有这个文件，直接结束</span></span><br><span class="line">      <span class="keyword">if</span> (!stats.size) &#123;</span><br><span class="line">        <span class="keyword">return</span> done(<span class="literal">null</span>, buffer, headers, shouldRefresh)</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 如果前面的都通过则读取文件为buffer</span></span><br><span class="line">      fs.read(fd, buffer, <span class="number">0</span>, stats.size, <span class="number">0</span>, <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (err) <span class="keyword">return</span> done(err)</span><br><span class="line">        <span class="comment">// 返回buffer</span></span><br><span class="line">        done(<span class="literal">null</span>, buffer, headers, shouldRefresh)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-disk&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-disk&lt;/a&gt;插件可以在磁盘中存储/检索瓦片。它可以兼顾瓦片缓存和瓦片提供两个功能，代码虽简单，但是功能十分全面，个人感觉非常的实用，很值得推荐。&lt;/p&gt;
&lt;p&gt;用它来缓存瓦片时，一定要为每个图层使用不同的目录（例如：”tiles/layer_a”, “tiles/layer_b”）。如果设置了&lt;code&gt;maxage&lt;/code&gt;参数，插件会检查瓦片的最近一次修改时间，如果间隔过长，将返回&lt;code&gt;null&lt;/code&gt;。如果 设置&lt;code&gt;maxage=0&lt;/code&gt;，就相当于没有开启缓存功能。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-etag 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/15/tilestrata-etag/"/>
    <id>http://kael.top/2019/07/15/tilestrata-etag/</id>
    <published>2019-07-15T13:39:54.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-etag" target="_blank" rel="noopener">tilestrata-etag</a>插件可以在 tilestrata 的请求中设置 ETag，可以让服务缓存变得更加高效可控，节省带宽。如果客户端的 ETag 的版本和服务器要发送的一致，则可以直接使用缓存中的数据，不再重新发送数据。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-etag --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><p>默认情况下，插件不会对所有大于 512kb 的数据启用，因为在 large buffers 上 ETag 的计算会很占用 CPU 的时间。但你也可以通过<code>limit</code>选项来更改这一默认设置，例如设置为<code>null</code>将会无视文件的大小上限，设置为一个文件大小的字符串或者数字，则会以此为上限。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> etag = <span class="built_in">require</span>(<span class="string">'tilestrata-etag'</span>)</span><br><span class="line"><span class="keyword">const</span> dependency = <span class="built_in">require</span>(<span class="string">'tilestrata-dependency'</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用mapnik数据源做一个普通的图层来对比</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'world_merc'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(disk.cache(&#123; <span class="attr">dir</span>: <span class="string">'tilecache'</span> &#125;))</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/world.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用相同的数据源设置etag后做一个新图层来对比</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'etag'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(dependency(<span class="string">'world_merc'</span>, <span class="string">'tile.png'</span>))</span><br><span class="line">  .use(etag(&#123; <span class="attr">limit</span>: <span class="string">'1mb'</span> &#125;))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>可以看到 etag 图层所有 response 中已经带上了<code>ETag</code></p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-etag-result-20190705101438.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>首先定义插件类型，这其实是一个<code>Response Hooks</code>类型的插件，劫持修改了正常的服务返回值。插件引入了<code>filesize-parser</code>用来计算文件大小，设置上限<code>max_length</code>，使用<a href="https://www.npmjs.com/package/etag" target="_blank" rel="noopener">etag</a>来生成 etag。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// max_length由limit设置得出</span></span><br><span class="line"><span class="keyword">if</span> (status_type === <span class="number">2</span> &amp;&amp; result.buffer &amp;&amp; result.buffer.length &lt; max_length) &#123;</span><br><span class="line">  <span class="keyword">var</span> resultEtag = result.headers[<span class="string">'etag'</span>] || result.headers[<span class="string">'ETag'</span>]</span><br><span class="line">  <span class="comment">// 如果没有etag，就使用etag库生成</span></span><br><span class="line">  <span class="keyword">if</span> (!resultEtag) &#123;</span><br><span class="line">    resultEtag = etag(result.buffer)</span><br><span class="line">    result.headers[<span class="string">'ETag'</span>] = resultEtag</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 插件还会判断是否有设置if-none-match</span></span><br><span class="line">  <span class="keyword">var</span> ifnonematch = req.headers[<span class="string">'if-none-match'</span>]</span><br><span class="line">  <span class="keyword">if</span> (ifnonematch &amp;&amp; ifnonematch === resultEtag) &#123;</span><br><span class="line">    result.status = <span class="number">304</span></span><br><span class="line">    result.buffer = <span class="keyword">new</span> Buffer([])</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-etag&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-etag&lt;/a&gt;插件可以在 tilestrata 的请求中设置 ETag，可以让服务缓存变得更加高效可控，节省带宽。如果客户端的 ETag 的版本和服务器要发送的一致，则可以直接使用缓存中的数据，不再重新发送数据。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-sharp 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/14/tilestrata-sharp/"/>
    <id>http://kael.top/2019/07/14/tilestrata-sharp/</id>
    <published>2019-07-14T12:54:55.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-sharp" target="_blank" rel="noopener">tilestrata-sharp</a>插件通过使用<a href="https://github.com/jcupitt/libvips" target="_blank" rel="noopener">libvips</a>的<a href="https://github.com/lovell/sharp" target="_blank" rel="noopener">sharp</a>库来转换处理图片。对于图片处理的一些方法参数都可以从 sharp 的<a href="https://sharp.pixelplumbing.com/en/stable/" target="_blank" rel="noopener">官方文档</a>中来找寻，这个插件相当于起到了一个搭桥铺路的作用，核心依然是 sharp。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install sharp --save</span><br><span class="line">$ npm install tilestrata-sharp --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><p>插件官网中对于使用的说明非常简单，因为插件本身不包含处理图片的逻辑，所以更多的使用方法得去<a href="https://sharp.pixelplumbing.com/en/stable/" target="_blank" rel="noopener">sharp</a>中找寻</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"><span class="keyword">const</span> sharp = <span class="built_in">require</span>(<span class="string">'tilestrata-sharp'</span>)</span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'world_merc_sharp'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    <span class="comment">// 定义数据源</span></span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/world.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(</span><br><span class="line">    sharp(<span class="function"><span class="keyword">function</span>(<span class="params">image, sharp</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> image</span><br><span class="line">        .resize(<span class="number">256</span>) <span class="comment">// 重新设置大小</span></span><br><span class="line">        .rotate(<span class="number">180</span>) <span class="comment">// 旋转</span></span><br><span class="line">        .greyscale() <span class="comment">// 灰度处理</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>下图是原始的瓦片样式：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-sharp-original-20190705114413.png" alt></p><p>在使用了 sharp 插件进行了旋转 180°，灰度处理之后，如下所示：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-sharp-result-20190705113728.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>这个插件比较简单，属于<code>transform</code>类型，返回 buffer 给 tilestrata 之前会根据用户设置的回调函数里面的处理规则，调用<code>sharp</code>处理成新的 buffer，然后再返回。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">transform: <span class="function"><span class="keyword">function</span>(<span class="params">server, req, buffer, headers, callback</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> image;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 用原buffer生成sharp对象</span></span><br><span class="line">        image = sharp(buffer);</span><br><span class="line">        <span class="comment">// 执行回调里面的处理规则</span></span><br><span class="line">        fn(image, sharp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">catch</span> (err) &#123; <span class="keyword">return</span> callback(err); &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 转化回buffer</span></span><br><span class="line">    image.toBuffer(<span class="function"><span class="keyword">function</span>(<span class="params">err, buffer, info</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (err) <span class="keyword">return</span> callback(err);</span><br><span class="line">        headers[<span class="string">'Content-Type'</span>] = <span class="string">'image/'</span> + info.format;</span><br><span class="line">        <span class="comment">// 还给tilestrata</span></span><br><span class="line">        callback(<span class="literal">null</span>, buffer, headers);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-sharp&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-sharp&lt;/a&gt;插件通过使用&lt;a href=&quot;https://github.com/jcupitt/libvips&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;libvips&lt;/a&gt;的&lt;a href=&quot;https://github.com/lovell/sharp&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;sharp&lt;/a&gt;库来转换处理图片。对于图片处理的一些方法参数都可以从 sharp 的&lt;a href=&quot;https://sharp.pixelplumbing.com/en/stable/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;官方文档&lt;/a&gt;中来找寻，这个插件相当于起到了一个搭桥铺路的作用，核心依然是 sharp。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-gm 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/14/tilestrata-gm/"/>
    <id>http://kael.top/2019/07/14/tilestrata-gm/</id>
    <published>2019-07-14T12:54:50.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-gm" target="_blank" rel="noopener">tilestrata-gm</a>插件和 tilestrata-sharp 功能上类似，背后使用的依赖<a href="http://www.graphicsmagick.org/" target="_blank" rel="noopener">graphicsmagick</a>的<a href="http://aheckmann.github.io/gm/docs.html" target="_blank" rel="noopener">gm</a>库来做图像处理，所以具体能够实现的哪些功可以去 gm 的文档中查询。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>由于插件依赖于<a href="http://www.graphicsmagick.org/" target="_blank" rel="noopener">graphicsmagick</a>，需要根据<a href="http://www.graphicsmagick.org/README.html" target="_blank" rel="noopener">安装指南</a>在各个平台下进行安装，MacOS 可以直接用 brew 安装</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ brew install graphicsmagick</span><br><span class="line"><span class="comment"># 再安装插件</span></span><br><span class="line">$ npm install tilestrata-gm --save</span><br></pre></td></tr></table></figure><h1 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"><span class="keyword">const</span> gm = <span class="built_in">require</span>(<span class="string">'tilestrata-gm'</span>)</span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'world_merc_gm'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    <span class="comment">// 定义数据源</span></span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/world.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(</span><br><span class="line">    gm(<span class="function"><span class="keyword">function</span>(<span class="params">image</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> image</span><br><span class="line">        .blur(<span class="number">7</span>, <span class="number">3</span>) <span class="comment">// 模糊处理</span></span><br><span class="line">        .rotate(<span class="string">'green'</span>, <span class="number">45</span>) <span class="comment">// 旋转，配色</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以同时能处理的任务的个数</span></span><br><span class="line">gm.setMaxConcurrency(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="效果"><a href="#效果" class="headerlink" title="效果"></a>效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>下图是原始的瓦片样式：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-sharp-original-20190705114413.png" alt></p><p>使用 gm 插件进行了模糊处理，旋转了 45°，背景填充为绿色后的结果：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-gm-result-20190705135250.png" alt></p><h1 id="代码浅析"><a href="#代码浅析" class="headerlink" title="代码浅析"></a>代码浅析</h1><p>插件的整体逻辑和 tilestrata-sharp 基本一致，在<code>toBuffer</code>阶段有所不同，gm 选择了在<code>async.queue</code>队列中逐步转化成 buffer 再返回给 tiletrata，这也是为什么会有<code>setMaxConcurrency</code>这个方法的原因。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码有所精简</span></span><br><span class="line"><span class="comment">// toBuffer队列</span></span><br><span class="line"><span class="keyword">var</span> gmQueue = <span class="keyword">async</span>.queue(<span class="function"><span class="keyword">function</span>(<span class="params">image, callback</span>) </span>&#123;</span><br><span class="line">image.toBuffer(<span class="function"><span class="keyword">function</span>(<span class="params">err, buffer</span>) </span>&#123;</span><br><span class="line">callback(err, buffer);</span><br><span class="line">&#125;);</span><br><span class="line">&#125;, concurrency);</span><br><span class="line"></span><br><span class="line">transform: <span class="function"><span class="keyword">function</span>(<span class="params">server, req, buffer, headers, callback</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> image;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 创建gm对象</span></span><br><span class="line">        image = gm(buffer);</span><br><span class="line">        <span class="comment">// 执行回调函数里面的处理规则</span></span><br><span class="line">        fn(image);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">catch</span> (err) &#123; <span class="keyword">return</span> callback(err); &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 放入async.queue中，逐步执行</span></span><br><span class="line">    gmQueue.push(image, <span class="function"><span class="keyword">function</span>(<span class="params">err, buffer</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (err) <span class="keyword">return</span> callback(err);</span><br><span class="line">        <span class="keyword">if</span> (image._outputFormat) &#123;</span><br><span class="line">            headers[<span class="string">'Content-Type'</span>] = <span class="string">'image/'</span> + image._outputFormat;</span><br><span class="line">        &#125;</span><br><span class="line">        callback(<span class="literal">null</span>, buffer, headers);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-gm&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-gm&lt;/a&gt;插件和 tilestrata-sharp 功能上类似，背后使用的依赖&lt;a href=&quot;http://www.graphicsmagick.org/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;graphicsmagick&lt;/a&gt;的&lt;a href=&quot;http://aheckmann.github.io/gm/docs.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;gm&lt;/a&gt;库来做图像处理，所以具体能够实现的哪些功可以去 gm 的文档中查询。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-lru  插件使用及浅析</title>
    <link href="http://kael.top/2019/07/11/tilestrata-lru/"/>
    <id>http://kael.top/2019/07/11/tilestrata-lru/</id>
    <published>2019-07-11T14:51:22.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-lru" target="_blank" rel="noopener">tilestrata-lru</a>插件使用内存来缓存瓦片。所以使用起来需要额外的小心，如果没有仔细的配置它有可能导致内存溢出。但若果你需要在不同的图层或者路径中共享某个资源，这个插件就是为此而生的。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-lru --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> lru = <span class="built_in">require</span>(<span class="string">'tilestrata-lru'</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">'tilestrata-headers'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> provider = mapnik(&#123;</span><br><span class="line">  pathname: <span class="string">'style/province.xml'</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置最大占用20mb的内存</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'lru_layer_size'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(provider)</span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">'Access-Control-Allow-Origin'</span>: <span class="string">'*'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(lru(&#123; <span class="attr">size</span>: <span class="string">'20mb'</span>, <span class="attr">ttl</span>: <span class="number">30</span> &#125;)) <span class="comment">// ttl in seconds</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置最多缓存20个数据</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'lru_layer_number'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(provider)</span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">'Access-Control-Allow-Origin'</span>: <span class="string">'*'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(lru(&#123; <span class="attr">size</span>: <span class="number">20</span>, <span class="attr">ttl</span>: <span class="number">30</span> &#125;))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 手动设置cache中数据对应key的结构</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'lru_layer_key'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(provider)</span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">'Access-Control-Allow-Origin'</span>: <span class="string">'*'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(</span><br><span class="line">    lru(&#123;</span><br><span class="line">      key: <span class="function"><span class="keyword">function</span>(<span class="params">req</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">          req.z +</span><br><span class="line">          <span class="string">','</span> +</span><br><span class="line">          req.x +</span><br><span class="line">          <span class="string">','</span> +</span><br><span class="line">          req.y +</span><br><span class="line">          <span class="string">','</span> +</span><br><span class="line">          req.layer +</span><br><span class="line">          <span class="string">','</span> +</span><br><span class="line">          req.filename +</span><br><span class="line">          <span class="string">','</span> +</span><br><span class="line">          <span class="string">'lru_layer_custom_key'</span></span><br><span class="line">        )</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>多次访问某个瓦片，如<code>http://127.0.0.1:9527/lru_layer_number/4/12/6/tile.png</code>，可以发现插件会在后几次从<code>cache</code>中直接 get 获取数据：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-lru-20190705162618.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>首先定义插件类型，<code>Caches</code>类型的插件需要给 tilestrata 提供<code>set</code>和<code>get</code>两个回调函数，核心是<a href="https://www.npmjs.com/package/lru-cache" target="_blank" rel="noopener">lru-cache</a>库</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码有所精简</span></span><br><span class="line"><span class="comment">// 设置缓存用到的key，如果没有自己设置key的结构，那默认将采用如下的结构</span></span><br><span class="line"><span class="keyword">var</span> key =</span><br><span class="line">  opts.key ||</span><br><span class="line">  <span class="function"><span class="keyword">function</span>(<span class="params">req</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      req.z + <span class="string">','</span> + req.x + <span class="string">','</span> + req.y + <span class="string">','</span> + req.layer + <span class="string">','</span> + req.filename</span><br><span class="line">    )</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断设置的是数量还是内存大小</span></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">typeof</span> opts.size === <span class="string">'string'</span>) &#123;</span><br><span class="line">  lruopts.max = filesizeParser(opts.size)</span><br><span class="line">  lruopts.length = <span class="function"><span class="keyword">function</span>(<span class="params">item</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> item.buffer.length</span><br><span class="line">  &#125;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">typeof</span> opts.size === <span class="string">'number'</span>) &#123;</span><br><span class="line">  lruopts.max = opts.size</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据options创建lru-cache对象</span></span><br><span class="line"><span class="keyword">var</span> cache = <span class="keyword">new</span> SyncCache(lruopts)</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> &#123;</span><br><span class="line">  <span class="keyword">get</span>: function(server, req, callback) &#123;</span><br><span class="line">    <span class="comment">// 获取数据</span></span><br><span class="line">    <span class="keyword">var</span> item = cache.get(key(req))</span><br><span class="line">    <span class="keyword">if</span> (item) <span class="keyword">return</span> callback(<span class="literal">null</span>, item.buffer, item.headers)</span><br><span class="line">    callback()</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="keyword">set</span>: function(server, req, buffer, headers, callback) &#123;</span><br><span class="line">    <span class="comment">// 缓存数据</span></span><br><span class="line">    cache.set(key(req), &#123; <span class="attr">buffer</span>: buffer, <span class="attr">headers</span>: headers &#125;)</span><br><span class="line">    callback()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-lru&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-lru&lt;/a&gt;插件使用内存来缓存瓦片。所以使用起来需要额外的小心，如果没有仔细的配置它有可能导致内存溢出。但若果你需要在不同的图层或者路径中共享某个资源，这个插件就是为此而生的。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-underzoom  插件使用及浅析</title>
    <link href="http://kael.top/2019/07/11/tilestrata-underzoom/"/>
    <id>http://kael.top/2019/07/11/tilestrata-underzoom/</id>
    <published>2019-07-11T14:44:51.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-underzoom" target="_blank" rel="noopener">tilestrata-underzoom</a>插件从介绍上看就是在请求高级别瓦片时，用低级别的瓦片进行拼接，而不是直接生产指定级别的瓦片。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik" target="_blank" rel="noopener">node-mapnik</a>。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="0-发生了什么"><a href="#0-发生了什么" class="headerlink" title="0. 发生了什么"></a>0. 发生了什么</h1><p>比方说我们请求了<code>4/13/6</code>瓦片，即<code>x=13,y=6,z=4</code>，插件会分别找到<code>[[26,12,5],[27,12,5],[27,13,5],[26,13,5]]</code>四个瓦片，获取数据，然后把四张图片进行合并，而不是直接渲染出<code>4/13/6</code>对应的图像。我个人的理解中，在向外缩放的过程中，人眼可视区域内的地图范围越来越大，对于 server 来说要渲染的内容也就越来越多，这个时候就要既保证渲染的展现结果，又要保证渲染过程对服务器资源的占用，<code>underzoom</code>对于瓦片地图来说就是针对这一问题的探索。</p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-underzoom --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> dependency = <span class="built_in">require</span>(<span class="string">'tilestrata-dependency'</span>)</span><br><span class="line"><span class="keyword">const</span> underzoom = <span class="built_in">require</span>(<span class="string">'tilestrata-underzoom'</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用mapnik定义一个世界范围地图</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'world_merc'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/world.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义underzoom图层</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'underzoom'</span>)</span><br><span class="line">  .route(<span class="string">'tile@2x.png'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    underzoom(&#123;</span><br><span class="line">      source: dependency(<span class="string">'world_merc'</span>, <span class="string">'tile.png'</span>),</span><br><span class="line">      inputSize: <span class="number">256</span>,</span><br><span class="line">      outputSize: <span class="number">512</span>,</span><br><span class="line">      zooms: <span class="number">1</span> <span class="comment">// 1以下层级全部采用underzoom的方式拼接</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>不启用 underzoom 的效果：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-underzoom-original-20190708145711.png" alt></p><p>underzoom 放大两倍的效果：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-underzoom-result-20190708145813.png" alt></p><p>可以看到，整个地图看上去<strong>清晰了很多，边缘更加明显</strong>，这是因为低级别瓦片本身的精细程度就高，合并成大图后的对于细节的效果也会更好。<br>但是地图左边区域原先就基本上是只由线勾勒出的<strong>白色区域</strong>，在低瓦片中可以看到，合并成大图后就基本上看不清了，这是因为本身线宽是很细的，比如说是 1px 宽度，合并大图的过程中这个宽度并不会增加，但图片整个的像素变大了，所以细线就基本看不清了，这也是插件不好的地方。</p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>说到合并图片，就能想到之前我们说过的<code>tilestrata-blend</code>插件，它是将两个不同数据源的结果图片进行了合并，而这个插件是将同一个数据源的四个低级别瓦片合并成了一个。</p><p>根据 underzoom 的<code>inputSize、zooms</code>的配置项，先准备好一个<code>Mapnik.Image</code>对象：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> canvas = <span class="keyword">new</span> Mapnik.Image(canvasSize, canvasSize)</span><br></pre></td></tr></table></figure><p>之后使用<code>@mapbox/tilebelt</code>库获取请求地址的下属四个低级别瓦片地址，分别请求数据，利用上面准备好的<code>Mapnik.Image</code>对象，使用<a href="http://mapnik.org/documentation/node-mapnik/3.6/#Image.composite" target="_blank" rel="noopener">Image.composite</a>方法来合并四张图片，最后返回结果就好。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码有所精简</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">startFetchAndComposite</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 例如请求 4/13/6</span></span><br><span class="line">  <span class="comment">// 此时coordList就是 [[26,12,5],[27,12,5],[27,13,5],[26,13,5]]</span></span><br><span class="line">  <span class="keyword">var</span> coordList = getTileCoords(req.x, req.y, req.z, underzoomLevels)</span><br><span class="line">  <span class="comment">// 遍历coordList</span></span><br><span class="line">  <span class="keyword">async</span>.each(</span><br><span class="line">    coordList,</span><br><span class="line">    <span class="function"><span class="keyword">function</span>(<span class="params">coords, callback</span>) </span>&#123;</span><br><span class="line">      <span class="comment">// clone一个新的请求</span></span><br><span class="line">      <span class="keyword">var</span> childReq = req.clone()</span><br><span class="line">      childReq.x = coords[<span class="number">0</span>]</span><br><span class="line">      childReq.y = coords[<span class="number">1</span>]</span><br><span class="line">      childReq.z = coords[<span class="number">2</span>]</span><br><span class="line">      options.source.serve(server, childReq, <span class="function"><span class="keyword">function</span>(<span class="params"></span></span></span><br><span class="line"><span class="function"><span class="params">        err,</span></span></span><br><span class="line"><span class="function"><span class="params">        childBuffer,</span></span></span><br><span class="line"><span class="function"><span class="params">        childHeaders</span></span></span><br><span class="line"><span class="function"><span class="params">      </span>) </span>&#123;</span><br><span class="line">        <span class="comment">// 获取数据</span></span><br><span class="line">        Mapnik.Image.fromBytes(childBuffer, <span class="function"><span class="keyword">function</span>(<span class="params">err, image</span>) </span>&#123;</span><br><span class="line">          <span class="comment">// 执行合并操作</span></span><br><span class="line">          canvas.composite(</span><br><span class="line">            image,</span><br><span class="line">            &#123;</span><br><span class="line">              comp_op: Mapnik.compositeOp.src_over,</span><br><span class="line">              dx: x,</span><br><span class="line">              dy: y,</span><br><span class="line">              opacity: <span class="number">1</span></span><br><span class="line">            &#125;,</span><br><span class="line">            callback</span><br><span class="line">          )</span><br><span class="line">        &#125;)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;,</span><br><span class="line">    callback</span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-underzoom&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-underzoom&lt;/a&gt;插件从介绍上看就是在请求高级别瓦片时，用低级别的瓦片进行拼接，而不是直接生产指定级别的瓦片。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;node-mapnik&lt;/a&gt;。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-blend 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/08/tilestrata-blend/"/>
    <id>http://kael.top/2019/07/08/tilestrata-blend/</id>
    <published>2019-07-08T07:19:26.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-blend" target="_blank" rel="noopener">tilestrata-blend</a>插件可以将多个栅格瓦片图层合并成一个 png 图层，并且支持透明度、混合模式、图层过滤等选项。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik" target="_blank" rel="noopener">node-mapnik</a>。这个插件依然是一个利用 mapnik 强大的功能的<code>Provider</code>类型的插件，甚至可以基于 mapnik 增强它已有的功能。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-blend --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"><span class="keyword">const</span> blend = <span class="built_in">require</span>(<span class="string">'tilestrata-blend'</span>)</span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义第一个世界边界图层</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'world_merc'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(disk.cache(&#123; <span class="attr">dir</span>: <span class="string">'tilecache'</span> &#125;))</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/world.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义第二个中国省界图层</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'province'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/province.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义混合图层</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'blend_layer'</span>)</span><br><span class="line">  .route(<span class="string">'combined.png'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    blend(</span><br><span class="line">      [</span><br><span class="line">        [<span class="string">'world_merc'</span>, <span class="string">'tile.png'</span>],</span><br><span class="line">        [</span><br><span class="line">          <span class="string">'province'</span>,</span><br><span class="line">          <span class="string">'tile.png'</span>,</span><br><span class="line">          <span class="comment">// 配置一些省界图层合并时候的参数</span></span><br><span class="line">          &#123;</span><br><span class="line">            opacity: <span class="number">0.5</span>,</span><br><span class="line">            comp_op: <span class="string">'multiply'</span>,</span><br><span class="line">            image_filters: <span class="string">'agg-stack-blur(10,10)'</span></span><br><span class="line">          &#125;</span><br><span class="line">        ]</span><br><span class="line">      ],</span><br><span class="line">      <span class="comment">// 给合并后的图层设置一个可选的填充色</span></span><br><span class="line">      &#123;</span><br><span class="line">        matte: <span class="string">'ffffff'</span></span><br><span class="line">      &#125;</span><br><span class="line">    )</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>可以在同一张瓦片上看到黄色的中国省界图层和世界边界图层，下图是没有配置仍和<code>options</code>：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-blend-no-option-20190705113008.png" alt></p><p>下图是像上面演示的那样，给省界图层加入了一些<code>options</code>，例如 image_filters 和 opacity：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-blend-20190705102810.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>这个插件在背后依赖了<a href="https://github.com/naturalatlas/tilestrata-dependency" target="_blank" rel="noopener">tilestrata-dependency</a>，所以配置里面的<code>[&#39;world_merc&#39;, &#39;tile.png&#39;]</code>实际上是通过执行了<code>.use(dependency(&#39;world_merc_png&#39;, &#39;tile.png&#39;))</code>操作来获取数据源：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 找到每个图层的数据源（代码有所精简）</span></span><br><span class="line"><span class="keyword">var</span> layers = layers.map(<span class="function"><span class="keyword">function</span>(<span class="params">pair</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> layer = pair[<span class="number">0</span>]</span><br><span class="line">  <span class="keyword">var</span> filename = pair[<span class="number">1</span>]</span><br><span class="line">  <span class="keyword">var</span> comp_options = pair[<span class="number">2</span>] || &#123;&#125;</span><br><span class="line">  <span class="keyword">return</span> [dependency(layer, filename), comp_options]</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>当请求某个瓦片时候，对每个<code>layers</code>依次执行<code>fetchTile</code>获取对应图片和<code>prepareImage</code>预处理图片</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 对每个数据源，获取数据，预处理图片（代码有所精简）</span></span><br><span class="line"><span class="keyword">async</span>.series(</span><br><span class="line">  [</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">fetchTile</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">      callback(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">prepareImage</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">      <span class="comment">// 这一步会创建new mapnik.Image</span></span><br><span class="line">      generateMatte(noop)</span><br><span class="line">    &#125;</span><br><span class="line">  ],</span><br><span class="line">  <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">    callback(err, image ? [image, options] : <span class="literal">null</span>)</span><br><span class="line">  &#125;</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>完成每个数据源的单独处理后就是合并操作了，利用 mapnik 的<a href="http://mapnik.org/documentation/node-mapnik/3.6/#Image.composite" target="_blank" rel="noopener">omposite</a>方法来依次合并创建的每个<code>mapnik.Image</code>对象，最后再利用<a href="http://mapnik.org/documentation/node-mapnik/3.6/#Image.demultiply" target="_blank" rel="noopener">demultiply</a>解构为 buffer 返回给 tilestrata 就好了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// （代码有所精简）</span></span><br><span class="line"><span class="keyword">async</span>.eachSeries(images, <span class="function"><span class="keyword">function</span>(<span class="params">res, callback</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 依次合并图片</span></span><br><span class="line">    intermediate.composite(image, res[<span class="number">1</span>], callback);</span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 结构返回buffer</span></span><br><span class="line">    intermediate.demultiply(<span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">            callback(<span class="literal">null</span>, buffer, &#123;<span class="string">'Content-Type'</span>: <span class="string">'image/png'</span>&#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-blend&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-blend&lt;/a&gt;插件可以将多个栅格瓦片图层合并成一个 png 图层，并且支持透明度、混合模式、图层过滤等选项。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;node-mapnik&lt;/a&gt;。这个插件依然是一个利用 mapnik 强大的功能的&lt;code&gt;Provider&lt;/code&gt;类型的插件，甚至可以基于 mapnik 增强它已有的功能。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-dependency 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/07/tilestrata-dependency/"/>
    <id>http://kael.top/2019/07/07/tilestrata-dependency/</id>
    <published>2019-07-07T09:02:17.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-dependency" target="_blank" rel="noopener">tilestrata-dependency</a>插件其实就是共享其他图层的数据源，如果你想定义两个不同格式的图层，例如分别是 png 和 jpg，这个插件就会非常的便利。你甚至可以传入一个 function 来精确控制不同层级使用不同图层的数据源。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-dependency --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> dependency = <span class="built_in">require</span>(<span class="string">'tilestrata-dependency'</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">'tilestrata-mapnik'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  <span class="comment">// 定义世界边界png图层</span></span><br><span class="line">  .layer(<span class="string">'world_merc_png'</span>)</span><br><span class="line">  .route(<span class="string">'tile.png'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">'style/world.xml'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  <span class="comment">// 定义世界边界jpg图层</span></span><br><span class="line">  .layer(<span class="string">'world_merc_jpg'</span>)</span><br><span class="line">  .route(<span class="string">'tile.jpg'</span>)</span><br><span class="line">  <span class="comment">// 直接用png的源</span></span><br><span class="line">  .use(dependency(<span class="string">'world_merc_png'</span>, <span class="string">'tile.png'</span>))</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'function_layer'</span>)</span><br><span class="line">  .route(<span class="string">'tile.jpg'</span>)</span><br><span class="line">  <span class="comment">// 不同层级用不同的源</span></span><br><span class="line">  .use(dependency(<span class="function"><span class="keyword">function</span>(<span class="params">req</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (req.z &lt; <span class="number">5</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> [<span class="string">'world_merc_png'</span>, <span class="string">'tile.png'</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> [<span class="string">'world_merc_jpg'</span>, <span class="string">'tile.jpg'</span>];</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p><code>world_merc_png</code>和<code>world_merc_jpg</code>使用了相同的源，展示的内容一模一样：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-dependency-20190705152851.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>要弄清楚<code>dependency</code>插件的实现，首先要弄清楚它的类型，dependency 源代码很简单，它属于<code>Providers</code>类型的插件:</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Providers 类型插件需要提供serve方法</span></span><br><span class="line">serve: <span class="function"><span class="keyword">function</span>(<span class="params">server, tile, callback</span>) </span>&#123;</span><br><span class="line">    callback(err, buffer, headers);</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure><p>接下来的代码主要逻辑就是如何利用指定的图层源创造一个<code>buffer</code>返回给新图层，以下代码就是 server 函数，有所精简：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// req 即上面的tile参数，包含请求的瓦片地址和类型，例如tile.jpg</span></span><br><span class="line"><span class="keyword">var</span> mock = req.clone()</span><br><span class="line"><span class="comment">// source 即指定的其他图层的数据源['world_merc_png', 'tile.png']</span></span><br><span class="line"><span class="keyword">var</span> source = getSource(req)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 分别把当前tile的layer和filename替换成指定源的对应值</span></span><br><span class="line">mock.layer = source[<span class="number">0</span>]</span><br><span class="line">mock.filename = source[<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">// server 即上面的server参数，代表整个tilestrata服务</span></span><br><span class="line">server.serve(mock, <span class="literal">false</span>, <span class="function"><span class="keyword">function</span>(<span class="params">status, buffer, headers</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (status === <span class="number">200</span>) &#123;</span><br><span class="line">    <span class="comment">// 利用伪造出来的指定源的请求内容去向服务器请求数据，获取buffer后返回给当前源</span></span><br><span class="line">    callback(<span class="literal">null</span>, buffer, headers)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    callback(err)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>当前图层拿到了 buffer 内容，只要再进入自己后续的加工逻辑，处理成指定格式返回就好了。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-dependency&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-dependency&lt;/a&gt;插件其实就是共享其他图层的数据源，如果你想定义两个不同格式的图层，例如分别是 png 和 jpg，这个插件就会非常的便利。你甚至可以传入一个 function 来精确控制不同层级使用不同图层的数据源。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-vtile-composite 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/07/tilestrata-vtile-composite/"/>
    <id>http://kael.top/2019/07/07/tilestrata-vtile-composite/</id>
    <published>2019-07-07T08:56:34.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-vtile-composite" target="_blank" rel="noopener">tilestrata-vtile-composite</a>插件用来合并多个矢量瓦片图层，这些矢量瓦片图层来自于<a href="https://github.com/naturalatlas/tilestrata-vtile" target="_blank" rel="noopener">tilestrata-vtile</a>插件。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik" target="_blank" rel="noopener">node-mapnik</a>。<br>这个插件的作用和<a href="https://github.com/naturalatlas/tilestrata-blend" target="_blank" rel="noopener">tilestrata-blend</a>类似，只是适用对象是 vtile 生产出来的矢量瓦片。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-vtile-composite --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> vtile = <span class="built_in">require</span>(<span class="string">'tilestrata-vtile'</span>)</span><br><span class="line"><span class="keyword">const</span> vtilecomposite = <span class="built_in">require</span>(<span class="string">'tilestrata-vtile-composite'</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">'tilestrata-headers'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 省界边界源</span></span><br><span class="line"><span class="keyword">const</span> provinceXml = &#123;</span><br><span class="line">  xml: <span class="string">'style/province.xml'</span>,</span><br><span class="line">  tileSize: <span class="number">256</span>,</span><br><span class="line">  metatile: <span class="number">1</span>,</span><br><span class="line">  bufferSize: <span class="number">128</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 世界边界源</span></span><br><span class="line"><span class="keyword">const</span> worldXml = &#123;</span><br><span class="line">  xml: <span class="string">'style/world.xml'</span>,</span><br><span class="line">  tileSize: <span class="number">256</span>,</span><br><span class="line">  metatile: <span class="number">1</span>,</span><br><span class="line">  bufferSize: <span class="number">128</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义第一个数据源</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'province_pbf'</span>)</span><br><span class="line">  .route(<span class="string">'t.pbf'</span>)</span><br><span class="line">  .use(vtile(provinceXml))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义第一个数据源</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'world_pbf'</span>)</span><br><span class="line">  .route(<span class="string">'t.pbf'</span>)</span><br><span class="line">  .use(vtile(worldXml))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 合并两个数据源，开启跨域访问</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'combined_pbf'</span>)</span><br><span class="line">  .route(<span class="string">'combined.pbf'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">'Access-Control-Allow-Origin'</span>: <span class="string">'*'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(vtilecomposite([[<span class="string">'world_pbf'</span>, <span class="string">'t.pbf'</span>], [<span class="string">'province_pbf'</span>, <span class="string">'t.pbf'</span>]]))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>这是单独的中国区域 pbf：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-composite-pro-20190704173456.png" alt></p><p>这是单独的世界区域 pbf：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-composite-world-20190704173556.png" alt></p><p>合并之后的图层：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-composite-20190704173718.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>其实原理也就解释了为什么官方说明中一定要在依赖中加入<code>node-mapnik</code>。核心代码如下：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">compositeTiles</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (vtiles.length === <span class="number">0</span>) <span class="keyword">return</span> callback()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> merged = <span class="keyword">new</span> mapnik.VectorTile(req.z, req.x, req.y)</span><br><span class="line">  <span class="comment">// 我是关键</span></span><br><span class="line">  merged.composite(vtiles, <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">    vtiles = <span class="literal">null</span></span><br><span class="line">    <span class="keyword">if</span> (err) <span class="keyword">return</span> callback(err)</span><br><span class="line"></span><br><span class="line">    result = merged.getData(dataopts)</span><br><span class="line">    result._vtile = merged</span><br><span class="line">    result._vx = req.x</span><br><span class="line">    result._vy = req.y</span><br><span class="line">    result._vz = req.z</span><br><span class="line">    callback()</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在连续请求完两个数据源的数据之后，新建 mapnik 的<code>VectorTile</code>对象，并调用其<a href="http://mapnik.org/documentation/node-mapnik/3.6/#VectorTile.composite" target="_blank" rel="noopener">VectorTile.composite</a>方法，对两个数据源请求来的数据进行合并，最后再<code>getData</code>输出结果就好。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile-composite&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-vtile-composite&lt;/a&gt;插件用来合并多个矢量瓦片图层，这些矢量瓦片图层来自于&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-vtile&lt;/a&gt;插件。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;node-mapnik&lt;/a&gt;。&lt;br&gt;这个插件的作用和&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-blend&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-blend&lt;/a&gt;类似，只是适用对象是 vtile 生产出来的矢量瓦片。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-vtile-raster 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/05/tilestrata-vtile-raster/"/>
    <id>http://kael.top/2019/07/05/tilestrata-vtile-raster/</id>
    <published>2019-07-05T06:05:38.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-vtile-raster" target="_blank" rel="noopener">tilestrata-vtile-raster</a>是配合<code>tilestrata-vtile</code>插件用来将 mapnik 的<code>pbf</code>格式的矢量瓦片转换成栅格图片。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik" target="_blank" rel="noopener">node-mapnik</a>。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-vtile-raster --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> vtile = <span class="built_in">require</span>(<span class="string">'tilestrata-vtile'</span>)</span><br><span class="line"><span class="keyword">const</span> vtileraster = <span class="built_in">require</span>(<span class="string">'tilestrata-vtile-raster'</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">'tilestrata-headers'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义省界边界数据源</span></span><br><span class="line"><span class="keyword">const</span> common = &#123;</span><br><span class="line">  xml: <span class="string">'style/province.xml'</span>,</span><br><span class="line">  tileSize: <span class="number">256</span>,</span><br><span class="line">  metatile: <span class="number">1</span>,</span><br><span class="line">  bufferSize: <span class="number">128</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'osm_pbf_raster'</span>)</span><br><span class="line">  .route(<span class="string">'tile.pbf'</span>) <span class="comment">// 定义vtile pbf图层做对比</span></span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">'Access-Control-Allow-Origin'</span>: <span class="string">'*'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(vtile(common))</span><br><span class="line">  .route(<span class="string">'tile.png'</span>) <span class="comment">// 定义vtile-raster png图层做对比</span></span><br><span class="line">  .use(</span><br><span class="line">    vtileraster(common, &#123;</span><br><span class="line">      tilesource: [<span class="string">'osm_pbf_raster'</span>, <span class="string">'tile.pbf'</span>]</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .route(<span class="string">'tile.json'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    vtileraster(common, &#123;</span><br><span class="line">      tilesource: [<span class="string">'osm_pbf_raster'</span>, <span class="string">'tile.pbf'</span>]</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>可以直接使用常规的栅格瓦片加载方式来加载<code>tile.png</code>图层，无须使用矢量瓦片的加载方式。可以和<a href="https://kael.top/2019/07/04/tilestrata-vtile/">tilestrata-vtile 插件浅析</a>对比观看。</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-raster-result-20190704172822.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>核心仍然是 mapnik 的功能，过程上大体是和 tilestrata-vtile 插件类似的，不同的地方在当请求图层的时候，不再单纯的使用<a href="http://mapnik.org/documentation/node-mapnik/3.6/#VectorTile" target="_blank" rel="noopener">VectorTile</a>的<code>getData</code>方法，首先代码会依据是否可交互属性<code>interactivity</code>来构造一个类型是<code>mapnik.Image</code>的<code>surface</code>参数</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (self.interactivity) &#123;</span><br><span class="line">  surface = <span class="keyword">new</span> mapnik.Grid(dim, dim)</span><br><span class="line">  <span class="comment">// ... 省略</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  surface = <span class="keyword">new</span> mapnik.Image(dim, dim)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后使用<a href="http://mapnik.org/documentation/node-mapnik/3.6/#VectorTile.render" target="_blank" rel="noopener">VectorTile.render</a>这个方法，通过 xml 构建出来的 map 和上面的 surface 参数，就可以将矢量数据转化为栅格图片：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">vectorTile.render(self.map, surface, options, callback)</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile-raster&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-vtile-raster&lt;/a&gt;是配合&lt;code&gt;tilestrata-vtile&lt;/code&gt;插件用来将 mapnik 的&lt;code&gt;pbf&lt;/code&gt;格式的矢量瓦片转换成栅格图片。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;node-mapnik&lt;/a&gt;。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-vtile 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/04/tilestrata-vtile/"/>
    <id>http://kael.top/2019/07/04/tilestrata-vtile/</id>
    <published>2019-07-04T09:39:41.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-vtile" target="_blank" rel="noopener">tilestrata-vtile</a>插件可以通过 mapnik 数据源生成<code>pbf</code>格式的矢量瓦片。如果需要对瓦片做放大倍数，交互这些操作，矢量瓦片不需要再额外的请求数据，使用起来非常方便。后续还可以通过配套的<a href="https://github.com/naturalatlas/tilestrata-vtile-raster" target="_blank" rel="noopener">tilestrata-vtile-raster</a>插件来将矢量数据渲染成普通的图片瓦片。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik" target="_blank" rel="noopener">node-mapnik</a>。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-vtile --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> vtile = <span class="built_in">require</span>(<span class="string">'tilestrata-vtile'</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">'tilestrata-headers'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义省界边界数据源</span></span><br><span class="line"><span class="keyword">const</span> common = &#123;</span><br><span class="line">  xml: <span class="string">'style/province.xml'</span>,</span><br><span class="line">  tileSize: <span class="number">256</span>,</span><br><span class="line">  metatile: <span class="number">1</span>,</span><br><span class="line">  bufferSize: <span class="number">128</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 开启跨域，定义图层</span></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'province_pbf'</span>)</span><br><span class="line">  .route(<span class="string">'tile.pbf'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">'Access-Control-Allow-Origin'</span>: <span class="string">'*'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(vtile(common))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><p>插件还提供了<code>overrideRenderOptions</code>配置项，用来做层级的精确控制，控制瓦片的返回内容</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">server</span><br><span class="line">  .layer(<span class="string">'mylayer'</span>)</span><br><span class="line">  .route(<span class="string">'t.pbf'</span>)</span><br><span class="line">  .use(</span><br><span class="line">    vtile(&#123;</span><br><span class="line">      xml: <span class="string">'/path/to/map.xml'</span>,</span><br><span class="line">      tileSize: <span class="number">256</span>,</span><br><span class="line">      metatile: <span class="number">1</span>,</span><br><span class="line">      bufferSize: <span class="number">128</span>,</span><br><span class="line">      overrideRenderOptions: <span class="function"><span class="keyword">function</span>(<span class="params">opts, z, maxz</span>) </span>&#123;</span><br><span class="line">        opts.simplify_distance = z &lt; maxz ? <span class="number">8</span> : <span class="number">1</span></span><br><span class="line">        <span class="keyword">return</span> opts</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br></pre></td></tr></table></figure><p>回调函数的第一个参数<code>opts</code>包含了以下内容，可以在<a href="#4-代码浅析">第四节</a>的代码分析中看到这些值是怎么来的：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  simplify_distance,</span><br><span class="line">  path_multiplier, </span><br><span class="line">  buffer_size, </span><br><span class="line">  scale_denominator</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-result-20190704171541.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>原理上很简单，从代码上就能看出来，传入了 mapnik 需要的<code>xml</code>配置文件，所以其实是利用了 mapnik 的<a href="http://mapnik.org/documentation/node-mapnik/3.6/#VectorTile" target="_blank" rel="noopener">VectorTile</a>功能，代码如下：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 新建VectorTile源，options也和传入的otps息息相关</span></span><br><span class="line">map.render(<span class="keyword">new</span> mapnik.VectorTile(z, x, y), options, <span class="function"><span class="keyword">function</span>(<span class="params">err, image</span>) </span>&#123;</span><br><span class="line">  self.pool.release(map)</span><br><span class="line">  <span class="keyword">if</span> (err) <span class="keyword">return</span> callback(err)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (image.empty()) &#123;</span><br><span class="line">    err = <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'No data'</span>)</span><br><span class="line">    err.statusCode = <span class="number">204</span></span><br><span class="line">    <span class="keyword">return</span> callback(err)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 通过getdata方法获取数据并返回给tilestrata</span></span><br><span class="line">  <span class="keyword">var</span> buffer = image.getData(self.dataopts)</span><br><span class="line">  buffer.metatile = self.metatile</span><br><span class="line">  buffer._vtile = image</span><br><span class="line">  buffer._vx = x</span><br><span class="line">  buffer._vy = y</span><br><span class="line">  buffer._vz = z</span><br><span class="line"></span><br><span class="line">  callback(<span class="literal">null</span>, buffer)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>第二节使用中提到的<code>opts</code>的参数实际上是 mapnik 中<a href="http://mapnik.org/documentation/node-mapnik/3.6/#Map.render" target="_blank" rel="noopener">Map.render</a>方法的几个可选参数，他们会在 tilestrata-vtile 中被合并使用：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> options = self.overrideRenderOptions(</span><br><span class="line">  &#123;</span><br><span class="line">    simplify_distance: real_z &lt; self.maxzoom ? <span class="number">8</span> : <span class="number">1</span>,</span><br><span class="line">    path_multiplier: <span class="number">16</span> * self.metatile,</span><br><span class="line">    buffer_size: self.bufferSize,</span><br><span class="line">    scale_denominator: <span class="number">559082264.028</span> / (<span class="number">1</span> &lt;&lt; real_z)</span><br><span class="line">  &#125;,</span><br><span class="line">  real_z,</span><br><span class="line">  self.maxzoom</span><br><span class="line">)</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-vtile&lt;/a&gt;插件可以通过 mapnik 数据源生成&lt;code&gt;pbf&lt;/code&gt;格式的矢量瓦片。如果需要对瓦片做放大倍数，交互这些操作，矢量瓦片不需要再额外的请求数据，使用起来非常方便。后续还可以通过配套的&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile-raster&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-vtile-raster&lt;/a&gt;插件来将矢量数据渲染成普通的图片瓦片。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;node-mapnik&lt;/a&gt;。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-postgis-geojson-tiles 插件使用及浅析</title>
    <link href="http://kael.top/2019/07/02/tilestrata-postgis-geojson-tiles/"/>
    <id>http://kael.top/2019/07/02/tilestrata-postgis-geojson-tiles/</id>
    <published>2019-07-02T13:42:51.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-postgis-geojson-tiles" target="_blank" rel="noopener">tilestrata-postgis-geojson-tiles</a>插件以 PostGIS 为数据源，生成 GeoJSON 格式的矢量瓦片。轻量但功能强大，配置项丰富，效果拔群。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-postgis-geojson-tiles --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><p>插件的配置比较复杂：</p><ul><li><code>geometryField</code> <em>( string, 必选)</em> : 数据表中存储地理数据的列名。 (默认 = <code>&quot;geom&quot;</code>)</li><li><code>sql</code> <em>( function, 必选)</em> : 一个返回 PostGIS 查询语句的函数。 <em>注意防治 sql 注入。</em> 查询语句包含以下内容:<ul><li><code>{bbox}</code> (box2d): 缓冲区的 bbox 范围</li><li><code>{geojson}</code> (text): 被裁剪出来的地理信息数据</li></ul></li><li><code>pgConfig</code> <em>( object, 必选)</em> : postgres 数据库配置：<ul><li><code>{host}</code> (string)</li><li><code>{password}</code> (string)</li><li><code>{user}</code> (string)</li><li><code>{port}</code> (string)</li><li><code>{database}</code> (string)</li></ul></li><li><code>simplifyFactor</code> <em>( number, 可选 )</em> : 地理数据简化程度的参数。 (默认 = <code>0.75</code>)</li><li><code>buffer</code> <em>( number, 可选)</em> : 围绕每个切片的缓冲大小，以像素为单位 (默认 = <code>16</code>)</li><li><code>collectGeometry</code> <em>( boolean, 可选 )</em> : 在转换成 GeoJson 之前是用 ST_Collect 把多个图形合并成一个简单图形。 (默认 = <code>false</code>)</li><li><code>mergeMultiLineStrings</code> <em>( boolean, 可选 )</em> : 在转换成 GeoJson 之前是和否用 ST_LineMerge 合并 MultiLineStrings 数据。 (默认 = <code>false</code>)</li><li><code>dumpGeometry</code> <em>( boolean, 可选 )</em> : 是否用 ST_Dump 来拆分图形 (默认 = <code>false</code>)</li></ul><p>这里我使用了 demo 中的 world_merc.shp 数据导入了 PostGIS 中作为测试数据源</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">'tilestrata'</span>)</span><br><span class="line"><span class="keyword">const</span> tilestrataPostGISGeoJSON = <span class="built_in">require</span>(<span class="string">'tilestrata-postgis-geojson-tiles'</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">'tilestrata-headers'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = tilestrata()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">'geojson-tiles'</span>)</span><br><span class="line">  .route(<span class="string">'tile.json'</span>)</span><br><span class="line">  <span class="comment">// 配置跨域</span></span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">'Access-Control-Allow-Origin'</span>: <span class="string">'*'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  <span class="comment">// 定义数据源</span></span><br><span class="line">  .use(</span><br><span class="line">    tilestrataPostGISGeoJSON(&#123;</span><br><span class="line">      geometryField: <span class="string">'geom'</span>,</span><br><span class="line">      sql: <span class="function"><span class="keyword">function</span>(<span class="params">server, req</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">'select name, &#123;geojson&#125; from world_merc WHERE ST_Intersects(geom, &#123;bbox&#125;)'</span></span><br><span class="line">      &#125;,</span><br><span class="line">      pgConfig: &#123;</span><br><span class="line">        username: <span class="string">'postgres'</span>,</span><br><span class="line">        password: <span class="string">'postgres'</span>,</span><br><span class="line">        host: <span class="string">'10.211.55.4'</span>,</span><br><span class="line">        port: <span class="string">'5432'</span>,</span><br><span class="line">        database: <span class="string">'shapefile'</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">server.listen(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p><code>http://127.0.0.1:9527/world_merc/6/55/25/tile.png</code>对应的栅格瓦片的结果如下图所示</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/geojson-tile-png-20190701212419.png" alt></p><p><code>http://127.0.0.1:9527/geojson-tiles/6/55/25/tile.json</code>请求对应的地址获取 GeoJSON 格式的矢量数据：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/geojson-tile-json-20190701212838.png" alt></p><p>在<a href="http://geojson.io" target="_blank" rel="noopener">geojson.io</a>查看效果如下图所示：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/geojson-tile-result-20190701212926.png" alt></p><p>GeoJSON 结果和渲染出来的 png 的图形基本是一致。</p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>从插件功能可以看出来属于<code>Providers</code>类型的插件，那么核心就是 serve 方法。首先插件会根据我们传入的数据库信息来建立 Postgres 对象，这个不需要展开讲。</p><p>当请求某个瓦片的时候，插件根据传入的 zxy 信息，调用了<code>@mapbox/sphericalmercator</code>库来把 zxy 坐标转换成 bbox 对象</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sm = <span class="keyword">new</span> SphericalMercator(&#123; <span class="attr">size</span>: <span class="number">256</span> &#125;)</span><br><span class="line"><span class="keyword">var</span> bbox = sm.bbox(tile.x, tile.y, tile.z)</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> w = bbox[<span class="number">2</span>] - bbox[<span class="number">0</span>]</span><br><span class="line"><span class="keyword">var</span> h = bbox[<span class="number">3</span>] - bbox[<span class="number">1</span>]</span><br><span class="line"><span class="keyword">var</span> bufferX = (buffer * w) / <span class="number">256</span></span><br><span class="line"><span class="keyword">var</span> bufferY = (buffer * h) / <span class="number">256</span></span><br><span class="line">bbox[<span class="number">0</span>] -= bufferX</span><br><span class="line">bbox[<span class="number">1</span>] -= bufferY</span><br><span class="line">bbox[<span class="number">2</span>] += bufferX</span><br><span class="line">bbox[<span class="number">3</span>] += bufferY</span><br></pre></td></tr></table></figure><p>在定义图层的时候会传入一个<code>sql</code>函数，里面包含了<code>{bbox}</code>和<code>{geojson}</code>两个预设的字段，在源代码中这两个字段将会分别被计算出来，合成一个 sql 在数据库中进行查询</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码为了便于理解有所精简改动</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 利用上面计算的bbox来组装bboxsql</span></span><br><span class="line"><span class="keyword">var</span> bboxSQL =</span><br><span class="line">  <span class="string">"ST_SetSRID('BOX("</span> +</span><br><span class="line">  bbox[<span class="number">0</span>] +</span><br><span class="line">  <span class="string">' '</span> +</span><br><span class="line">  bbox[<span class="number">1</span>] +</span><br><span class="line">  <span class="string">','</span> +</span><br><span class="line">  bbox[<span class="number">2</span>] +</span><br><span class="line">  <span class="string">' '</span> +</span><br><span class="line">  bbox[<span class="number">3</span>] +</span><br><span class="line">  <span class="string">" )'::box2d, 4326)"</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据传入的geomField等字段组装geojsonSQL</span></span><br><span class="line"><span class="keyword">var</span> geojsonSQL =</span><br><span class="line">  <span class="string">'ST_MakeValid(ST_SimplifyPreserveTopology('</span> +</span><br><span class="line">  geomField +</span><br><span class="line">  <span class="string">', '</span> +</span><br><span class="line">  simplifyTolerance +</span><br><span class="line">  <span class="string">')) ST_Intersection('</span> +</span><br><span class="line">  geojsonSQL +</span><br><span class="line">  <span class="string">', &#123;bbox&#125;) ST_AsGeoJSON('</span> +</span><br><span class="line">  geojsonSQL +</span><br><span class="line">  <span class="string">') AS geojson'</span></span><br><span class="line"></span><br><span class="line">sql = sql.replace(<span class="regexp">/&#123;geojson&#125;/g</span>, geojsonSQL).replace(<span class="regexp">/&#123;bbox&#125;/g</span>, bboxSQL)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查询</span></span><br><span class="line">pgPool.query(sql, callback)</span><br></pre></td></tr></table></figure><p>最后把查询出来的结果封装在<code>FeatureCollection</code>中形成一个完整的 GeoJSON 返回给 tilestrata 就可以了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 组装GeoJSON</span></span><br><span class="line"><span class="keyword">var</span> outputText =</span><br><span class="line">  <span class="string">'&#123;"type": "FeatureCollection", "features": ['</span> +</span><br><span class="line">  result.rows</span><br><span class="line">    .map(<span class="function"><span class="keyword">function</span>(<span class="params">row</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (row.geojson) &#123;</span><br><span class="line">        <span class="keyword">var</span> featureString = <span class="string">'&#123;"type": "Feature", "geometry": '</span> + row.geojson</span><br><span class="line">        <span class="keyword">delete</span> row.geojson</span><br><span class="line">        <span class="keyword">return</span> featureString + <span class="string">', "properties": '</span> + <span class="built_in">JSON</span>.stringify(row) + <span class="string">'&#125;'</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .join(<span class="string">','</span>) +</span><br><span class="line">  <span class="string">']&#125;'</span></span><br><span class="line"><span class="comment">// 返回结果</span></span><br><span class="line">callback(<span class="literal">null</span>, outputText, &#123; <span class="string">'Content-Type'</span>: <span class="string">'application/json'</span> &#125;)</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-postgis-geojson-tiles&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-postgis-geojson-tiles&lt;/a&gt;插件以 PostGIS 为数据源，生成 GeoJSON 格式的矢量瓦片。轻量但功能强大，配置项丰富，效果拔群。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-headers 插件使用及浅析</title>
    <link href="http://kael.top/2019/06/26/tilestrata-headers/"/>
    <id>http://kael.top/2019/06/26/tilestrata-headers/</id>
    <published>2019-06-26T03:25:05.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-headers" target="_blank" rel="noopener">tilestrata-headers</a>插件用来设置请求的响应头<code>response headers</code>，常用的可以解决缓存控制、跨域等问题，实用性很高。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install tilestrata-headers --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> headers = <span class="built_in">require</span>(<span class="string">"tilestrata-headers"</span>);</span><br><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">"tilestrata"</span>);</span><br><span class="line"></span><br><span class="line">server = tilestrata();</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">"osm_mvt_amenities"</span>)</span><br><span class="line">  .route(<span class="string">"tile.mvt"</span>)</span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">"Access-Control-Allow-Origin"</span>: <span class="string">"*"</span>,</span><br><span class="line">      <span class="string">"Cache-Control"</span>: <span class="string">"max-age=3600"</span></span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">server.listen(<span class="number">8080</span>);</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>未开启</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-headers-null-20190606114255.png" alt></p><p>开启</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-headers-result-20190606114432.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>原理比较简单，在<code>tilestrata</code>的设计中，会使用<code>use</code>去注册各种类型的插件，<code>tilestrata-headers</code>就是<code>reshook</code>类型的插件，在初始化的过程中会被注册</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">TileRequestHandler.prototype.use = <span class="function"><span class="keyword">function</span>(<span class="params">plugin</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!plugin) <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">  <span class="comment">// ...简化后的代码</span></span><br><span class="line">  <span class="keyword">if</span> (plugin.reshook) <span class="keyword">return</span> <span class="keyword">this</span>._registerResponseHook(plugin);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">TileRequestHandler.prototype._registerResponseHook = <span class="function"><span class="keyword">function</span>(<span class="params">plugin</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> id = <span class="string">"reshook#"</span> + <span class="keyword">this</span>.responseHooks.length;</span><br><span class="line">  <span class="keyword">if</span> (!plugin) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">"Falsy value passed to registerResponseHook()"</span>);</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> plugin.reshook !== <span class="string">"function"</span>)</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(</span><br><span class="line">      <span class="string">"Attempted to register a response hook with no reshook() method"</span></span><br><span class="line">    );</span><br><span class="line">  <span class="keyword">this</span>.responseHooks.push(&#123; <span class="attr">id</span>: id, <span class="attr">plugin</span>: plugin &#125;);</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>通过这种方式，插件劫持每个 response，给每个 response header 加上我们自定义的内容。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-headers&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-headers&lt;/a&gt;插件用来设置请求的响应头&lt;code&gt;response headers&lt;/code&gt;，常用的可以解决缓存控制、跨域等问题，实用性很高。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-jsonp 插件使用及浅析</title>
    <link href="http://kael.top/2019/06/23/tilestrata-jsonp/"/>
    <id>http://kael.top/2019/06/23/tilestrata-jsonp/</id>
    <published>2019-06-23T12:15:44.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-jsonp" target="_blank" rel="noopener">tilestrata-jsonp</a>插件可以将一些网格（utfgrids）的 json 请求包装成 jsonp，使你不必配置 CORS 也可以跨域访问这些资源。这是除了 tilestrata-headers 以外的另一个实用型插件。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install tilestrata-jsonp --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">"tilestrata"</span>);</span><br><span class="line"><span class="keyword">var</span> jsonp = <span class="built_in">require</span>(<span class="string">"tilestrata-jsonp"</span>);</span><br><span class="line"><span class="keyword">var</span> tilestrataPostGISGeoJSON = <span class="built_in">require</span>(<span class="string">"tilestrata-postgis-geojson-tiles"</span>);</span><br><span class="line"></span><br><span class="line">server = tilestrata();</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">"geojson_tiles_jsonp"</span>)</span><br><span class="line">  .route(<span class="string">"tile.json"</span>)</span><br><span class="line">  .use(jsonp(&#123; <span class="attr">variable</span>: <span class="string">"callback"</span> &#125;))</span><br><span class="line">  .use(</span><br><span class="line">    <span class="comment">// Provider 这里用tilestrataPostGISGeoJSON做示例</span></span><br><span class="line">    tilestrataPostGISGeoJSON(&#123;</span><br><span class="line">      geometryField: <span class="string">"geom"</span>,</span><br><span class="line">      sql: <span class="function"><span class="keyword">function</span>(<span class="params">server, req</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"select name, &#123;geojson&#125; from (select name, ST_Transform(way,4326) as geom from osm_polygon where name ~ '上海') as a1 WHERE ST_Intersects(geom, &#123;bbox&#125;)"</span>;</span><br><span class="line">      &#125;,</span><br><span class="line">      pgConfig: &#123;</span><br><span class="line">        username: <span class="string">"postgres"</span>,</span><br><span class="line">        password: <span class="string">"postgres"</span>,</span><br><span class="line">        host: <span class="string">"localhost"</span>,</span><br><span class="line">        port: <span class="string">"5432"</span>,</span><br><span class="line">        database: <span class="string">"osm"</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">server.listen(<span class="number">9527</span>);</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>在启动 server 后，我们可以直接用<code>jquery</code>请求某个固定层级的瓦片，来测试是否成功触发<code>callbak</code>函数</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$.ajax(&#123;</span><br><span class="line">  url: <span class="string">"http://127.0.0.1:9527/geojson_tiles_jsonp/4/15/8/tile.json"</span>,</span><br><span class="line">  type: <span class="string">"GET"</span>,</span><br><span class="line">  dataType: <span class="string">"jsonp"</span>,</span><br><span class="line">  success: <span class="function"><span class="keyword">function</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"jsonp使用成功"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>结果</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-jsonp-result-20190606145036.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>原理比较简单，和 headers 插件类似，在<code>tilestrata</code>的设计中，会使用<code>use</code>去注册各种类型的插件，<code>tilestrata-headers</code>就是<code>reshook</code>类型的插件，在初始化的过程中会被注册</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">TileRequestHandler.prototype.use = <span class="function"><span class="keyword">function</span>(<span class="params">plugin</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!plugin) <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">  <span class="comment">// ...简化后的代码</span></span><br><span class="line">  <span class="keyword">if</span> (plugin.reshook) <span class="keyword">return</span> <span class="keyword">this</span>._registerResponseHook(plugin);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">TileRequestHandler.prototype._registerResponseHook = <span class="function"><span class="keyword">function</span>(<span class="params">plugin</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> id = <span class="string">"reshook#"</span> + <span class="keyword">this</span>.responseHooks.length;</span><br><span class="line">  <span class="keyword">if</span> (!plugin) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">"Falsy value passed to registerResponseHook()"</span>);</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> plugin.reshook !== <span class="string">"function"</span>)</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(</span><br><span class="line">      <span class="string">"Attempted to register a response hook with no reshook() method"</span></span><br><span class="line">    );</span><br><span class="line">  <span class="keyword">this</span>.responseHooks.push(&#123; <span class="attr">id</span>: id, <span class="attr">plugin</span>: plugin &#125;);</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>通过这种方式，插件会设置响应内容为<code>Content-Type: text/javascript;</code>，给我们返回一段包含了回调函数的代码，来执行我们预先定义好的 callbak 函数。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-jsonp&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-jsonp&lt;/a&gt;插件可以将一些网格（utfgrids）的 json 请求包装成 jsonp，使你不必配置 CORS 也可以跨域访问这些资源。这是除了 tilestrata-headers 以外的另一个实用型插件。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-postgismvt 插件使用及浅析</title>
    <link href="http://kael.top/2019/06/13/tilestrata-postgismvt/"/>
    <id>http://kael.top/2019/06/13/tilestrata-postgismvt/</id>
    <published>2019-06-13T08:02:44.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/Stezii/tilestrata-postgismvt" target="_blank" rel="noopener">tilestrata-postgismvt</a>是一个用来从 PostGIS 数据库中检索出 mvt 格式矢量瓦片的插件。PostGIS 在 2.4.0 版本后支持了直接生成 mvt 数据，这让我们更方便的加工出矢量瓦片。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-基本要求"><a href="#1-基本要求" class="headerlink" title="1. 基本要求"></a>1. 基本要求</h1><p>PostGIS 需要在<a href="https://postgis.net/2017/09/30/postgis-2.4.0/" target="_blank" rel="noopener">2.4.0</a>版本及以上</p><h1 id="2-安装"><a href="#2-安装" class="headerlink" title="2. 安装"></a>2. 安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">## 不推荐</span></span><br><span class="line">npm install tilestrata-postgismvt --save</span><br></pre></td></tr></table></figure><p>在 ST_AsMVT 方法更新后 ，虽然作者更新了代码，但是 npm 包并不是最新的，建议用下面的方法安装最新版，或者直接去 Github 上下载</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">## 推荐</span></span><br><span class="line">npm install --save https://github.com/Stezii/tilestrata-postgismvt</span><br></pre></td></tr></table></figure><p>或者通过 package.json</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">"tilestrata-postgismvt": "github:Stezii/tilestrata-postgismvt"</span><br></pre></td></tr></table></figure><h1 id="3-使用"><a href="#3-使用" class="headerlink" title="3. 使用"></a>3. 使用</h1><p>前提：一个导入了 osm 数据的支持 PostGIS 2.4.0 的数据服务器</p><p>执行 mapbox 的脚本<a href="https://github.com/mapbox/postgis-vt-util/blob/master/src/TileBBox.sql" target="_blank" rel="noopener">TileBBox.sql</a>添加<code>TileBBox</code>函数</p><h2 id="3-1-普通模式"><a href="#3-1-普通模式" class="headerlink" title="3.1 普通模式"></a>3.1 普通模式</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">"tilestrata"</span>);</span><br><span class="line"><span class="keyword">var</span> postgismvt = <span class="built_in">require</span>(<span class="string">"tilestrata-postgismvt"</span>);</span><br><span class="line"><span class="keyword">var</span> headers = <span class="built_in">require</span>(<span class="string">"tilestrata-headers"</span>);</span><br><span class="line"></span><br><span class="line">server = tilestrata();</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">"osm_mvt"</span>)</span><br><span class="line">  .route(<span class="string">"tile.mvt"</span>)</span><br><span class="line">  .use(</span><br><span class="line">    headers(&#123;</span><br><span class="line">      <span class="string">"Access-Control-Allow-Origin"</span>: <span class="string">"*"</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .use(</span><br><span class="line">    postgismvt(&#123;</span><br><span class="line">      lyr: &#123;</span><br><span class="line">        table: <span class="string">"import.osm_buildings"</span>, <span class="comment">// 表名</span></span><br><span class="line">        geometry: <span class="string">"geometry"</span>, <span class="comment">// geometry在表中的字段名</span></span><br><span class="line">        srid: <span class="number">3857</span>, <span class="comment">// 坐标系</span></span><br><span class="line">        minZoom: <span class="number">3</span>,</span><br><span class="line">        maxZoom: <span class="number">19</span>,</span><br><span class="line">        buffer: <span class="number">10</span>, <span class="comment">// 裁剪geometry时的缓冲距离，PostGIS默认为256</span></span><br><span class="line">        fields: <span class="string">"name"</span>, <span class="comment">// 额外字段名</span></span><br><span class="line">        resolution: <span class="number">256</span> <span class="comment">// 分辨率，可以是function</span></span><br><span class="line">      &#125;,</span><br><span class="line">      pgConfig: &#123;</span><br><span class="line">        <span class="comment">// 通过`pgConfig`属性配置PostGIS相关属性</span></span><br><span class="line">        host: <span class="string">"localhost"</span>,</span><br><span class="line">        user: <span class="string">"gis"</span>,</span><br><span class="line">        password: <span class="string">"gis"</span>,</span><br><span class="line">        database: <span class="string">"gis"</span>,</span><br><span class="line">        port: <span class="string">"6543"</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">server.listen(<span class="number">8080</span>);</span><br></pre></td></tr></table></figure><h2 id="3-2-点云数据"><a href="#3-2-点云数据" class="headerlink" title="3.2 点云数据"></a>3.2 点云数据</h2><p>如果是点要素，可以使用 cluster 模式，只要定义<code>lry</code>中的<code>mode</code>属性为<code>cluster_fields、cluster或function</code>,注意的是需要同时定义<code>type</code>为<code>circle</code></p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">server.layer(<span class="string">'layer_name'</span>).route(<span class="string">'tile.mvt'</span>)</span><br><span class="line">  .use(postgismvt(&#123;</span><br><span class="line">    lyr: &#123;</span><br><span class="line">      ...</span><br><span class="line">      mode: <span class="string">'cluster'</span> <span class="comment">// or 'cluster_fields'</span></span><br><span class="line">    &#125;,</span><br><span class="line">    pgConfig: &#123;</span><br><span class="line">      ...</span><br><span class="line">    &#125;&#125;))</span><br><span class="line">  );</span><br></pre></td></tr></table></figure><p><code>mode</code>和<code>resolution</code>都支持是一个函数。通过这种方式可以手动指定一些缩放级别，用来控制精度</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">server.layer(<span class="string">'layer_name'</span>).route(<span class="string">'tile.mvt'</span>)</span><br><span class="line">  .use(postgismvt(&#123;</span><br><span class="line">    lyr: &#123;</span><br><span class="line">      resolution: <span class="function"><span class="keyword">function</span>(<span class="params">server, req</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (req.z &gt; <span class="number">12</span>) <span class="keyword">return</span> <span class="number">512</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">256</span>;</span><br><span class="line">      &#125;,</span><br><span class="line">      mode: <span class="function"><span class="keyword">function</span>(<span class="params">server, req</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (req.z &gt; <span class="number">15</span>) <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">if</span> (req.z &gt; <span class="number">13</span>) <span class="keyword">return</span> <span class="string">'cluster_fields'</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">'cluster'</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    pgConfig: &#123;</span><br><span class="line">      ...</span><br><span class="line">    &#125;&#125;))</span><br><span class="line">  );</span><br></pre></td></tr></table></figure><h1 id="4-效果"><a href="#4-效果" class="headerlink" title="4. 效果"></a>4. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-postgismvt20190606111135.png" alt></p><h1 id="5-代码浅析"><a href="#5-代码浅析" class="headerlink" title="5. 代码浅析"></a>5. 代码浅析</h1><p>其实核心的方法就是用到了 PostGIS 的<code>ST_AsMVT</code>和<code>ST_AsMVTGeom</code>函数。mapbox 提供<a href="https://github.com/mapbox/postgis-vt-util/blob/master/src/TileBBox.sql" target="_blank" rel="noopener">TileBBox.sql</a>用来计算 zxy 位置的瓦片对应的 box 边界，再查询出其中的 geom，转换成 mvt 格式。</p><p>比如说 <code>z=14,x=12917,y=6430</code>的瓦片对应的 sql 如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> ST_AsMVT(q, <span class="string">'osm_mvt'</span>, <span class="number">256</span>, <span class="string">'geom'</span>) <span class="keyword">AS</span> mvt</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">  <span class="keyword">SELECT</span> ST_AsMVTGeom(</span><br><span class="line">    ST_Transform(</span><br><span class="line">  import.osm_buildings.geometry, <span class="number">3857</span>),</span><br><span class="line">  TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>),</span><br><span class="line">  <span class="number">256</span>,</span><br><span class="line">  <span class="number">10</span>,</span><br><span class="line">      <span class="literal">true</span></span><br><span class="line">) geom ,<span class="keyword">name</span></span><br><span class="line">    <span class="keyword">FROM</span> import.osm_buildings</span><br><span class="line">    <span class="keyword">WHERE</span> ST_Intersects(TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>), import.osm_buildings.geometry)</span><br><span class="line">) <span class="keyword">AS</span> q</span><br></pre></td></tr></table></figure><p>此外，<code>fields</code>是可以用逗号分割传递多个想要的字段名的。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/Stezii/tilestrata-postgismvt&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-postgismvt&lt;/a&gt;是一个用来从 PostGIS 数据库中检索出 mvt 格式矢量瓦片的插件。PostGIS 在 2.4.0 版本后支持了直接生成 mvt 数据，这让我们更方便的加工出矢量瓦片。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-mapnik 插件使用及浅析</title>
    <link href="http://kael.top/2019/06/11/tilestrata-mapnik/"/>
    <id>http://kael.top/2019/06/11/tilestrata-mapnik/</id>
    <published>2019-06-11T03:26:12.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-mapnik" target="_blank" rel="noopener">tilestrata-mapnik</a>顾名思义，mapnik 作为地图数据源，tilestrata 负责瓦片化的一个插件。</p><a id="more"></a><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install mapnik --save</span><br><span class="line">npm install tilestrata-mapnik --save</span><br></pre></td></tr></table></figure><p>虽然最新的 mapnik4.0 已经支持了 nodejs 10 版本，这里仍然建议采用<strong>nodejs 8</strong> 环境，更为稳定，如果安装失败可以考虑切换到<strong>nodejs 8</strong></p><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><p>准备好 mapnik 的 xml 配置文件</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">"tilestrata"</span>);</span><br><span class="line"><span class="keyword">var</span> mapnik = <span class="built_in">require</span>(<span class="string">"tilestrata-mapnik"</span>);</span><br><span class="line"></span><br><span class="line">server = tilestrata();</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .layer(<span class="string">"layer_name"</span>)</span><br><span class="line">  .route(<span class="string">"tile.png"</span>)</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: <span class="string">"style/china.xml"</span></span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">server.listen(<span class="number">8080</span>);</span><br></pre></td></tr></table></figure><p>tilestrata-mapnik 支持的<code>options</code>有</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  interactivity: <span class="literal">false</span>,</span><br><span class="line">  xml: <span class="literal">null</span>,</span><br><span class="line">  pathname: <span class="literal">null</span>,</span><br><span class="line">  metatile: <span class="number">2</span>,</span><br><span class="line">  resolution: <span class="number">4</span>,</span><br><span class="line">  bufferSize: <span class="number">128</span>,</span><br><span class="line">  tileSize: <span class="number">256</span>,</span><br><span class="line">  scale: <span class="number">1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-mapnik-result-20190606105844.png" alt></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>插件会根据传入的<code>options</code>构建一个<a href="https://github.com/mapbox/tilelive-mapnik" target="_blank" rel="noopener">tilelive-mapnik</a>对象 source</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">source = <span class="keyword">new</span> MapnikBackend(options, <span class="function"><span class="keyword">function</span>(<span class="params">err, result</span>) </span>&#123;</span><br><span class="line">  source = result;</span><br><span class="line">  callback(err);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>根据<code>options</code>里面<code>interactivity</code>的值，分别调用了下面两个函数，对数据源会做个是否满足调用条件的判断，如果满足就设置一下<code>options</code>里面数据<code>format</code>的值</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// interactivity为 false，设置 format为 png</span></span><br><span class="line">source.getTile();</span><br><span class="line"></span><br><span class="line"><span class="comment">// interactivity为 true，设置 format为 utf</span></span><br><span class="line">source.getGrid();</span><br></pre></td></tr></table></figure><p>针对两种不同的<code>format</code>，分别创建 mapnik 实例</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// format为 png</span></span><br><span class="line">image = <span class="keyword">new</span> mapnik.Image();</span><br><span class="line"></span><br><span class="line"><span class="comment">// format为 utf</span></span><br><span class="line">image = <span class="keyword">new</span> mapnik.Grid();</span><br></pre></td></tr></table></figure><p>在请求具体的某个 zxy 瓦片的时候，就使用 mapnik 获取指定瓦片的方法<code>view</code>就好了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">image.view(x, y, width, height); <span class="comment">// 获取指定瓦片</span></span><br></pre></td></tr></table></figure><p>流程图</p><pre class="mermaid">graph TDA[tilestrata-mapnik] -->|options| B(tilelive-mapnik)B --> C{interactivity}C -->|true| D[getGrid]D --> F[mapnik.Grid]C -->|false| E[getTile]E --> G[mapnik.Image]F --> H[view]G --> H[view]</pre>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-mapnik&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;tilestrata-mapnik&lt;/a&gt;顾名思义，mapnik 作为地图数据源，tilestrata 负责瓦片化的一个插件。&lt;/p&gt;
    
    </summary>
    
      <category term="TileStrata" scheme="http://kael.top/categories/TileStrata/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>用PostGIS生成mvt格式的矢量瓦片</title>
    <link href="http://kael.top/2019/06/10/osm-postgis-mvt/"/>
    <id>http://kael.top/2019/06/10/osm-postgis-mvt/</id>
    <published>2019-06-10T07:05:16.000Z</published>
    <updated>2020-05-17T09:04:57.336Z</updated>
    
    <content type="html"><![CDATA[<p>PostGIS 从 2.4.0 版本开始支持通过 box 生成 mvt 数据，我们用尽可能简单的方法搭建一个生产 mvt 数据的测试数据库。</p><a id="more"></a><p>基础工作环境：</p><ol><li><a href="https://docs.docker.com/install/" target="_blank" rel="noopener">docker</a></li><li><a href="https://nodejs.org/en/" target="_blank" rel="noopener">nodejs 8</a></li></ol><h1 id="1-数据源"><a href="#1-数据源" class="headerlink" title="1. 数据源"></a>1. 数据源</h1><p>从<a href="http://download.geofabrik.de/" target="_blank" rel="noopener">geofabrik</a>上我们可以下载的分好地区的<a href="https://www.openstreetmap.org/" target="_blank" rel="noopener">OpenStreetMap</a>数据，这样可以得到一个比较小的测试数据集。可以通过下面的地址下载到最新的中国区域数据：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 直接下载国区的数据</span></span><br><span class="line">http://download.geofabrik.de/asia/china-latest.osm.pbf</span><br></pre></td></tr></table></figure><h1 id="2-准备-PostGIS-环境"><a href="#2-准备-PostGIS-环境" class="headerlink" title="2. 准备 PostGIS 环境"></a>2. 准备 PostGIS 环境</h1><p>我们直接用<a href="https://hub.docker.com/r/mdillon/postgis/" target="_blank" rel="noopener">mdillon/postgis</a>的 docker 镜像跑一个 PostGIS 数据库：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建一个名称为gis网络</span></span><br><span class="line">docker network create gis</span><br><span class="line">docker run -d --name postgis -e POSTGRES_USER=gis --network gis -p 6543:5432 mdillon/postgis:10</span><br></pre></td></tr></table></figure><p>这个镜像可以通过<code>POSTGRES_PASSWORD</code>指定数据库密码，如果没指定的话，密码会和<code>POSTGRES_USER</code>即用户名保持一致。</p><p>建议通过<code>-p 5432:5432</code>映射下端口，方便我们在宿主机上直接连接调试。</p><h1 id="3-导入-osm-数据到-PostGIS"><a href="#3-导入-osm-数据到-PostGIS" class="headerlink" title="3. 导入 osm 数据到 PostGIS"></a>3. 导入 osm 数据到 PostGIS</h1><p>我们用<a href="https://github.com/omniscale/imposm3" target="_blank" rel="noopener">imposm3</a>来导入 osm 数据到库中。</p><p>imposm3 主要需要配置 cache 目录（可选），connection 链接地址（必须）和 mapping 映射文件（必须）三个参数，官方仓库提供了一个<a href="https://github.com/omniscale/imposm3/blob/master/example-mapping.json" target="_blank" rel="noopener">mapping 样例</a>供参考。这里我们方便测试，就用这个<a href="https://blog-1255388623.cos.ap-shanghai.myqcloud.com/static/mapping.json" target="_blank" rel="noopener">简单版本</a>，定义了<code>admim、amenities、buildings</code>三张表。</p><p>准备一个文件夹，把下载的 pbf 和 mapping 文件丢进去，再创建 cache 目录</p><p>依然用上强大的 docker，<a href="https://hub.docker.com/r/jawg/imposm3/" target="_blank" rel="noopener">jawg/imposm3</a>可以帮助我们方便的执行 imposm3 操作。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run --network gis --rm \</span><br><span class="line">-v $(pwd)/cache:/tmp/imposm3 \</span><br><span class="line">-v $(pwd)/china-latest.osm.pbf:/opt/imposm3/china-latest.osm.pbf \</span><br><span class="line">-v $(pwd)/mapping.json:/opt/imposm3/mapping.json \</span><br><span class="line">jawg/imposm3 import \</span><br><span class="line">-mapping mapping.json \</span><br><span class="line">-read china-latest.osm.pbf \</span><br><span class="line">-overwritecache -write -connection &apos;postgis://gis:gis@postgis/gis&apos;</span><br></pre></td></tr></table></figure><p>耐心等待导入完成，可以直接进入 docker 查看数据情况：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker exec -it postgis psql gis gis</span><br></pre></td></tr></table></figure><p>或者通过第三方软件连接数据库，可以看到 import 导入的三张表：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/osm_mvt_postgis.png" alt></p><h1 id="4-执行-SQL-脚本"><a href="#4-执行-SQL-脚本" class="headerlink" title="4. 执行 SQL 脚本"></a>4. 执行 SQL 脚本</h1><p>在数据库中执行 mapbox 提供的<a href="https://github.com/mapbox/postgis-vt-util/blob/master/src/TileBBox.sql" target="_blank" rel="noopener">TileBBox</a>脚本，创建 TileBBox 函数</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https://github.com/mapbox/postgis-vt-util/blob/master/src/TileBBox.sql</span><br></pre></td></tr></table></figure><p>这个函数的作用就是传入指定的<code>x,y,z</code>瓦片序列，转换成 MVT 需要的<code>tile coordinate space</code>，通常是 4096x4096 的网格</p><h1 id="5-测试数据结果"><a href="#5-测试数据结果" class="headerlink" title="5. 测试数据结果"></a>5. 测试数据结果</h1><p>mvt 瓦片生成的关键就是<a href="https://postgis.net/docs/ST_AsMVT.html" target="_blank" rel="noopener">ST_AsMVT</a>和<a href="https://postgis.net/docs/ST_AsMVTGeom.html" target="_blank" rel="noopener">ST_AsMVTGeom</a>两个函数，所以可以直接调用来做个测试，这里查询<code>z=14,y=12917,6430</code>的瓦片对应的 mvt 数据</p><p>先查询出范围内的 geometry，并转化成 mvt 需要的格式</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> ST_AsMVTGeom(</span><br><span class="line">   ST_Transform(</span><br><span class="line">  import.osm_buildings.geometry, <span class="number">3857</span>),</span><br><span class="line">  TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>),</span><br><span class="line">  <span class="number">256</span>,</span><br><span class="line">  <span class="number">10</span>,</span><br><span class="line">      <span class="literal">true</span></span><br><span class="line">   ) geom, <span class="keyword">name</span></span><br><span class="line"><span class="keyword">FROM</span> import.osm_buildings</span><br><span class="line"><span class="keyword">WHERE</span> ST_Intersects(TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>), import.osm_buildings.geometry)</span><br></pre></td></tr></table></figure><p>得到结果应该如下：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/asmvtgeom_result.png" alt></p><p>再使用 ST_AsMVT 进行转换就行，完成的合并后的 sql 如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> ST_AsMVT(q, <span class="string">'osm_mvt'</span>, <span class="number">256</span>, <span class="string">'geom'</span>) <span class="keyword">AS</span> mvt</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">  <span class="keyword">SELECT</span> ST_AsMVTGeom(</span><br><span class="line">    ST_Transform(</span><br><span class="line">  import.osm_buildings.geometry, <span class="number">3857</span>),</span><br><span class="line">  TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>),</span><br><span class="line">  <span class="number">256</span>,</span><br><span class="line">  <span class="number">10</span>,</span><br><span class="line">      <span class="literal">true</span></span><br><span class="line">  ) geom , <span class="keyword">name</span></span><br><span class="line">  <span class="keyword">FROM</span> import.osm_buildings</span><br><span class="line">  <span class="keyword">WHERE</span> ST_Intersects(TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>), import.osm_buildings.geometry)</span><br><span class="line">) <span class="keyword">AS</span> q</span><br></pre></td></tr></table></figure><h1 id="6-数据展示"><a href="#6-数据展示" class="headerlink" title="6. 数据展示"></a>6. 数据展示</h1><p>完整的地图区域数据前端展示可以使用 tilestrata 这个服务端工具，配合 tilestrata-postgismvt 插件就可以生成供地图调用的瓦片。</p><p>具体的 demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>对应的文章可以看一下这篇<a href="http://kael.top/2019/06/13/tilestrata-postgismvt/">tilestrata-postgismvt 使用</a></p><p>clone 项目<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a>，将项目里面<code>src/mvt/</code>目录下<a href="https://github.com/zzcyrus/tilestrata-sample-code/blob/master/src/mvt/index.js" target="_blank" rel="noopener">index.js</a>的<code>pgConfig</code>改成你的设置</p><p>安装依赖，启动项目</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure><p>访问<code>example/mvt.html</code>就可以看到结果了。</p><p>我们导入了三张表，分别是边界数据（蓝色）、设施（红点）和建筑（紫色），全部叠加之后如下图所示：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/mvt_preview.png" alt></p><h1 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h1><p><a href="https://blog.jawg.io/how-to-make-mvt-with-postgis/" target="_blank" rel="noopener">https://blog.jawg.io/how-to-make-mvt-with-postgis/</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;PostGIS 从 2.4.0 版本开始支持通过 box 生成 mvt 数据，我们用尽可能简单的方法搭建一个生产 mvt 数据的测试数据库。&lt;/p&gt;
    
    </summary>
    
      <category term="PostGIS" scheme="http://kael.top/categories/PostGIS/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>制作openstreetmap-carto风格离线瓦片地图</title>
    <link href="http://kael.top/2019/06/06/openstreetmap-carto-map/"/>
    <id>http://kael.top/2019/06/06/openstreetmap-carto-map/</id>
    <published>2019-06-06T07:13:27.000Z</published>
    <updated>2020-05-17T09:04:57.332Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/gravitystorm/openstreetmap-carto" target="_blank" rel="noopener">openstreetmap-carto</a>是<a href="https://www.openstreetmap.org" target="_blank" rel="noopener">openstreetmap</a>的默认地图风格，广受好评，各方面均衡，是离线地图的常用选择。</p><a id="more"></a><p>搭建的主要步骤基本围绕<a href="https://kael.top/2019/03/12/osm-PostGIS-setup/">使用 osm 数据做一个自己的 PostGIS 数据库</a>这篇文章展开。</p><p>我们完成<strong>1、2、3</strong>步骤后，再导入 osm 数据前要做一些修改</p><h1 id="4-依据-openstreetmap-carto-样式导入数据"><a href="#4-依据-openstreetmap-carto-样式导入数据" class="headerlink" title="4. 依据 openstreetmap-carto 样式导入数据"></a>4. 依据 openstreetmap-carto 样式导入数据</h1><p>仍然是安装好 osm2pgsql 工具，下载好中国区 pbf 数据，之后我们要准备样式包</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 下载openstreetmap-carto相关数据文件</span></span><br><span class="line">git <span class="built_in">clone</span> https://github.com/gravitystorm/openstreetmap-carto.git</span><br><span class="line"></span><br><span class="line"><span class="comment"># 导入数据，手动指定carto样式文件</span></span><br><span class="line">osm2pgsql -c -d china -G --slim --hstore --style /home/openstreetmap-carto/openstreetmap-carto.style --tag-transform-script /home/openstreetmap-carto/openstreetmap-carto.lua -C 2000 -p china -r pbf /home/CN</span><br></pre></td></tr></table></figure><h1 id="5-执行各种脚本"><a href="#5-执行各种脚本" class="headerlink" title="5. 执行各种脚本"></a>5. 执行各种脚本</h1><p>进入 clone 下来的<code>openstreetmap-carto</code>文件夹</p><h2 id="5-1-索引"><a href="#5-1-索引" class="headerlink" title="5.1 索引"></a>5.1 索引</h2><p>首先是建立数据库索引，可以加快渲染访问的速度，这步是<strong>可选</strong>的</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">psql -d gis -f indexes.sql</span><br></pre></td></tr></table></figure><h2 id="5-2-下载-shapefile"><a href="#5-2-下载-shapefile" class="headerlink" title="5.2 下载 shapefile"></a>5.2 下载 shapefile</h2><p>样式中用到了海岸线、水域等 shp 文件，需要手动下载，文件较大，<code>openstreetmap-carto</code>已经提供了下载脚本</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scripts/get-shapefiles.py</span><br></pre></td></tr></table></figure><p>如果下载出错，加上<code>-s</code>关闭 shapeindex</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scripts/get-shapefiles.py -s</span><br></pre></td></tr></table></figure><p>如果实在网络不行，可以根据<a href="https://github.com/gravitystorm/openstreetmap-carto/blob/master/INSTALL.md#manual-download" target="_blank" rel="noopener">安装指南</a>中说明手动下载这些文件，放在<code>openstreetmap-carto/data</code>目录下就好</p><h2 id="5-3-下载字体"><a href="#5-3-下载字体" class="headerlink" title="5.3 下载字体"></a>5.3 下载字体</h2><p>如果是用了 Ubuntu/Debian 服务器，可以直接安装</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install fonts-noto-cjk fonts-noto-hinted fonts-noto-unhinted fonts-hanazono ttf-unifont</span><br></pre></td></tr></table></figure><p>其他系统也可以<a href="https://github.com/gravitystorm/openstreetmap-carto/blob/master/INSTALL.md#installation-on-other-operation-systems" target="_blank" rel="noopener">手动下载字体文件并安装</a></p><h1 id="6-渲染"><a href="#6-渲染" class="headerlink" title="6. 渲染"></a>6. 渲染</h1><p>准备好<code>nodejs</code>环境，渲染工具使用 tilestrata 这个服务端工具，配合 tilestrata-mapnik 插件就可以生成供地图调用的瓦片。</p><p>具体的 demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a></p><p>对应的文章可以看一下这篇<a href="https://kael.top/2019/06/11/tilestrata-mapnik/">tilestrata-mapnik 使用</a></p><h2 id="6-1-准备-mapnik-配置文件"><a href="#6-1-准备-mapnik-配置文件" class="headerlink" title="6.1 准备 mapnik 配置文件"></a>6.1 准备 mapnik 配置文件</h2><p>因为使用了 mapnik，要将<code>openstreetmap-carto</code>的<a href="https://github.com/gravitystorm/openstreetmap-carto/blob/master/project.mml" target="_blank" rel="noopener">project.mml</a>转换成 mapnik 可识别的 xml 文件</p><p>打开文件夹下面的<code>project.mml</code>文件，找到如下字段，修改成你数据库的相关配置信息</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/carto_project_mml.png" alt></p><p>安装<code>carto</code>工具包进行转换</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo npm install -g carto</span><br><span class="line">carto project.mml &gt; osm.xml</span><br></pre></td></tr></table></figure><h2 id="6-2-准备服务端"><a href="#6-2-准备服务端" class="headerlink" title="6.2 准备服务端"></a>6.2 准备服务端</h2><p>clone 项目<a href="https://github.com/zzcyrus/tilestrata-sample-code" target="_blank" rel="noopener">tilestrata-sample-code</a>，将项目里面<code>/src/mapnik/</code>目录下<a href="https://github.com/zzcyrus/tilestrata-sample-code/blob/master/src/mapnik/index.js" target="_blank" rel="noopener">index.js</a>增加以下内容</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">server</span><br><span class="line">  .layer(&apos;osm&apos;)</span><br><span class="line">  .route(&apos;tile.png&apos;)</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: &apos;style/osm.xml&apos;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br></pre></td></tr></table></figure><p>将<code>openstreetmap-carto</code>文件夹下面的<code>data，symbols，osm.xml（刚生成的）</code>三个文件（夹）拷贝到<code>tilestrata-sample-code</code>的<code>style</code>文件夹下，拷贝完成的目录结构如下所示：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/osm_mapnik_folder.png" alt></p><p>安装相关依赖并启动项目</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure><p>将<code>/example/mapnik.html</code>文件里的 tileLayer 地址换成<code>&quot;http://127.0.0.1:9527/osm/{z}/{x}/{y}/tile.png&quot;,</code>，打开文件即可看到效果</p><h1 id="7-对比结果"><a href="#7-对比结果" class="headerlink" title="7 对比结果"></a>7 对比结果</h1><p>官网<br><img src="http://blog-img-1255388623.cossh.myqcloud.com/osm.png" alt="官网效果"></p><p>我们自己生成的<br><img src="http://blog-img-1255388623.cossh.myqcloud.com/osm_mapnik.png" alt></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://github.com/gravitystorm/openstreetmap-carto&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;openstreetmap-carto&lt;/a&gt;是&lt;a href=&quot;https://www.openstreetmap.org&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;openstreetmap&lt;/a&gt;的默认地图风格，广受好评，各方面均衡，是离线地图的常用选择。&lt;/p&gt;
    
    </summary>
    
      <category term="PostGIS" scheme="http://kael.top/categories/PostGIS/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>使用osm数据做一个自己的PostGIS数据库</title>
    <link href="http://kael.top/2019/03/12/osm-PostGIS-setup/"/>
    <id>http://kael.top/2019/03/12/osm-PostGIS-setup/</id>
    <published>2019-03-12T11:54:08.000Z</published>
    <updated>2020-05-17T09:04:57.332Z</updated>
    
    <content type="html"><![CDATA[<p>PostGIS 是目前开源方案里面使用广泛的一种地理信息数据库，可玩性很强，实用性也很强，为了实现更多的地理信息操作，我们先来搭建一个简单的 PostGIS 数据库。</p><a id="more"></a><h1 id="1-安装步骤"><a href="#1-安装步骤" class="headerlink" title="1. 安装步骤"></a>1. 安装步骤</h1><ol><li>准备一个 CentOS 7.x x64 环境</li><li>安装数据库 PostgreSQL 9.6</li><li>安装对应版本的 PostGIS 2.2.2</li><li>导入 openstreetmap 的中国区域数据</li></ol><h1 id="2-PostgreSQL-9-6"><a href="#2-PostgreSQL-9-6" class="headerlink" title="2. PostgreSQL 9.6"></a>2. PostgreSQL 9.6</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装数据库</span></span><br><span class="line">yum install https://download.postgresql.org/pub/repos/yum/9.6/redhat/rhel-7-x86_64/pgdg-centos96-9.6-3.noarch.rpm</span><br><span class="line"></span><br><span class="line">yum install postgresql96</span><br><span class="line"></span><br><span class="line">yum install postgresql96-server</span><br><span class="line"></span><br><span class="line"><span class="comment"># 初始化数据库及服务</span></span><br><span class="line">/usr/pgsql-9.6/bin/postgresql96-setup initdb</span><br><span class="line">systemctl <span class="built_in">enable</span> postgresql-9.6</span><br><span class="line">systemctl start postgresql-9.6</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置环境变量</span></span><br><span class="line"><span class="built_in">export</span> PATH=/usr/pgsql-9.6/bin:<span class="variable">$PATH</span></span><br><span class="line"><span class="built_in">export</span> LD_LIBRARY_PATH=/usr/pgsql-9.6/lib:<span class="variable">$LD_LIBRARY_PATH</span></span><br><span class="line"><span class="built_in">export</span> PGDATA=/home/postgres/postgresql_data</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 以下可选</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 为数据库设置密码</span></span><br><span class="line">su - postgres</span><br><span class="line">psql</span><br><span class="line">postgres=<span class="comment"># ALTER USER postgres WITH PASSWORD 'postgres';</span></span><br><span class="line">postgres=<span class="comment"># \q</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 开放防火墙端口</span></span><br><span class="line">firewall-cmd --permanent --add-port=5432/tcp</span><br><span class="line">firewall-cmd --reload</span><br><span class="line"></span><br><span class="line"><span class="comment"># 修改配置文件允许外部使用密码访问</span></span><br><span class="line">vim /var/lib/pgsql/9.6/data/postgresql.conf</span><br><span class="line">listen_addresses = <span class="string">'localhost'</span>  为  listen_addresses=<span class="string">'*'</span></span><br><span class="line"></span><br><span class="line">vim /var/lib/pgsql/9.6/data/pg_hba.conf</span><br><span class="line">host    all            all      0.0.0.0/0      md5</span><br></pre></td></tr></table></figure><h1 id="3-PostGIS"><a href="#3-PostGIS" class="headerlink" title="3. PostGIS"></a>3. PostGIS</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装PostGIS</span></span><br><span class="line">sudo yum -y install epel-release</span><br><span class="line">sudo yum install postgis24_96</span><br><span class="line"></span><br><span class="line"><span class="comment"># 初始化数据库osm</span></span><br><span class="line">createdb osm</span><br><span class="line"></span><br><span class="line">psql -h 127.0.0.1 -d osm -U postgres -f /usr/pgsql-9.6/share/contrib/postgis-2.4/postgis.sql</span><br><span class="line">psql -h 127.0.0.1 -d osm -U postgres -f /usr/pgsql-9.6/share/contrib/postgis-2.4/spatial_ref_sys.sql</span><br><span class="line">psql -d osm -c <span class="string">'CREATE EXTENSION postgis; CREATE EXTENSION hstore;'</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 可以基于osm创建china数据库（把osm作为一个可重复备份的库）</span></span><br><span class="line">createdb -T osm china</span><br></pre></td></tr></table></figure><h1 id="4-osm-数据导入"><a href="#4-osm-数据导入" class="headerlink" title="4. osm 数据导入"></a>4. osm 数据导入</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装导入工具osm2pgsql</span></span><br><span class="line">yum install osm2pgsql</span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载中国区域的公开数据</span></span><br><span class="line">wget http://download.gisgraphy.com/openstreetmap/pbf/CN.tar.bz2</span><br><span class="line"></span><br><span class="line"><span class="comment"># 解压数据</span></span><br><span class="line">tar jxvf CN.tar.bz2</span><br><span class="line"></span><br><span class="line"><span class="comment"># 开始导入数据到china数据库</span></span><br><span class="line">osm2pgsql -c -d china --slim --hstore -C 2000 -p china -r pbf /home/parallels/Downloads/CN</span><br></pre></td></tr></table></figure><p>#5. 测试</p><p>至此，我们已经基于 osm 的数据有了一个地理信息库，我们可以通过以下 sql 做一些简单的测试</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">name</span> <span class="keyword">FROM</span> china_polygon <span class="keyword">WHERE</span> <span class="keyword">name</span> ~ <span class="string">'南京'</span>;</span><br></pre></td></tr></table></figure><p>结果：<br><img src="polygon.png" alt></p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> p1.name,p2.name,ST_Distance(p1.way,p2.way) <span class="keyword">FROM</span></span><br><span class="line">(<span class="keyword">SELECT</span> * <span class="keyword">FROM</span> china_point <span class="keyword">WHERE</span> place=<span class="string">'city'</span> <span class="keyword">AND</span> <span class="keyword">name</span> = <span class="string">'南京市'</span>) p1 ,</span><br><span class="line">(<span class="keyword">SELECT</span> * <span class="keyword">FROM</span> china_point <span class="keyword">WHERE</span> place=<span class="string">'city'</span> <span class="keyword">AND</span> <span class="keyword">name</span> = <span class="string">'北京市'</span>) p2</span><br></pre></td></tr></table></figure><p>结果：<br><img src="distance.png" alt></p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">name</span>, ST_AsText(ST_Transform(way,<span class="number">4326</span>)) <span class="keyword">FROM</span> china_point <span class="keyword">WHERE</span> place=<span class="string">'city'</span>;</span><br></pre></td></tr></table></figure><p>结果：<br><img src="point.png" alt></p><p>当然数据库的功能不局限于此，基于此数据库我们可以做些更有趣的事情，今后我们会慢慢探讨。</p><h1 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h1><p><a href="https://github.com/digoal/blog/blob/master/201609/20160906_01.md" target="_blank" rel="noopener">digoal 的博客</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;PostGIS 是目前开源方案里面使用广泛的一种地理信息数据库，可玩性很强，实用性也很强，为了实现更多的地理信息操作，我们先来搭建一个简单的 PostGIS 数据库。&lt;/p&gt;
    
    </summary>
    
      <category term="PostGIS" scheme="http://kael.top/categories/PostGIS/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>mongoDB中地理空间查询指北</title>
    <link href="http://kael.top/2019/01/20/mongoDB-geospatial/"/>
    <id>http://kael.top/2019/01/20/mongoDB-geospatial/</id>
    <published>2019-01-20T09:41:24.000Z</published>
    <updated>2020-05-17T09:04:57.332Z</updated>
    
    <content type="html"><![CDATA[<p>现在地理空间查询是越来越常见的一种需求，实现的方式也有很多种，相较于PostGIS，mongoDB既可以直接存储GeoJSON对象，又支持地理空间索引，可以满足高效的常见地理空间服务。</p><a id="more"></a><p><a href="https://github.com/zzcyrus/mongoDB-geospatial-demos" target="_blank" rel="noopener">一个可供参考的案例仓库</a></p><h2 id="索引方式介绍"><a href="#索引方式介绍" class="headerlink" title="索引方式介绍"></a>索引方式介绍</h2><h3 id="2d-索引"><a href="#2d-索引" class="headerlink" title="2d 索引"></a>2d 索引</h3><p><a href="https://docs.mongodb.com/manual/core/2d/" target="_blank" rel="noopener">官方介绍</a></p><p>2d index 通常用在2维平面上的点数据，如果你的MongoDB 版本小于2.2或者你不使用一些GeoJSON类型的数据，官方推荐你使用2d index。</p><h3 id="2dsphere-索引"><a href="#2dsphere-索引" class="headerlink" title="2dsphere 索引"></a>2dsphere 索引</h3><p><a href="https://docs.mongodb.com/manual/core/2dsphere/" target="_blank" rel="noopener">官方介绍</a></p><p>2dsphere index 支持所有的mongoDB空间查询，支持类地球的球面上几何要素的查询，支持数据存储为GeoJSON或者是传统坐标。</p><p><em>所以如果你正在做一个现代化的GIS应用，建议直接采取2dsphere索引，这将会非常方便，GeoJSON格式的直接导入，配合WebGIS的直接查询或插入</em></p><h2 id="查询方法介绍"><a href="#查询方法介绍" class="headerlink" title="查询方法介绍"></a>查询方法介绍</h2><p><a href="https://docs.mongodb.com/manual/reference/operator/query-geospatial/" target="_blank" rel="noopener">官方介绍</a></p><p>下面这个表格包含了一些个人的总结和官方的说明，具体查询的作用其实通过查询方法的名字就能猜测出来，所以不详细介绍了</p><table><thead><tr><th>查询方式</th><th style="text-align:center">2d索引</th><th style="text-align:center">sphere索引</th><th style="text-align:center">几何类型（2d/sphere）</th></tr></thead><tbody><tr><td>$near</td><td style="text-align:center">✅</td><td style="text-align:center">✅</td><td style="text-align:center">平面/球面</td></tr><tr><td>$nearSphere</td><td style="text-align:center">✅</td><td style="text-align:center">✅</td><td style="text-align:center">球面</td></tr><tr><td>$geoWithin:{$center:…}</td><td style="text-align:center">✅</td><td style="text-align:center">⚠️</td><td style="text-align:center">平面</td></tr><tr><td>$geoWithin:{$centerSphere:…}</td><td style="text-align:center">✅</td><td style="text-align:center">✅</td><td style="text-align:center">球面</td></tr><tr><td>$geoWithin:{$box:…}</td><td style="text-align:center">✅</td><td style="text-align:center">⚠️</td><td style="text-align:center">平面</td></tr><tr><td>$geoWithin:{$polygon:…}</td><td style="text-align:center">✅</td><td style="text-align:center">⚠️</td><td style="text-align:center">平面</td></tr><tr><td>$geoWithin:{$geometry:…}</td><td style="text-align:center">❎</td><td style="text-align:center">✅</td><td style="text-align:center">球面</td></tr><tr><td>$geoIntersects</td><td style="text-align:center">❎</td><td style="text-align:center">✅</td><td style="text-align:center">球面</td></tr></tbody></table><blockquote><p>说明： ✅ 支持 ❎ 不支持 ⚠️ 官方文档说不支持，但测试发现可以使用</p></blockquote><h2 id="使用注意事项"><a href="#使用注意事项" class="headerlink" title="使用注意事项"></a>使用注意事项</h2><ol><li>在2dsphere索引中使用$geometry去查询，如果自己构造$geometry，要注意geometry的coordinates必须是一个<code>闭合</code>的数组，首尾两项要一致。</li><li>与此同时，在使用$polygon去查询时，<code>不必</code>是一个闭合的坐标数组列，mongoDB会自动帮我们关闭这个图形</li><li>在使用$near或者$geoWithin之类的查询中会涉及到距离、半径定义，这里如果你使用经纬度作为坐标，使用m（米）作为距离、半径单位，那么要进行<code>转换</code>，具体的转换规则可以参考下面两段代码示例：</li></ol><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 1度大致对应111千米，所以需要/111000,具体可以细化</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;*&#125;</span> </span>meter 米</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">const</span> m2degree = <span class="function">(<span class="params">meter</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> meter / <span class="number">111000</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> query = &#123;</span><br><span class="line">  location: &#123;</span><br><span class="line">    $geoWithin: &#123;</span><br><span class="line">      $center: [[lon, lat], m2degree(radius)]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 米转换成弧度，需要除以地球半径，大约6378100米</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;*&#125;</span> <span class="variable">meter</span></span></span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">const</span> m2rad = <span class="function">(<span class="params">meter</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> meter / <span class="number">6378100</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> query = &#123;</span><br><span class="line">  location: &#123;</span><br><span class="line">    $nearSphere: [lon, lat],</span><br><span class="line">    $maxDistance: m2rad(maxDistance)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="demo"><a href="#demo" class="headerlink" title="demo"></a>demo</h2><p>这里提供<a href="https://github.com/zzcyrus/mongoDB-geospatial-demos" target="_blank" rel="noopener">一个可供参考的简单的案例仓库</a><br>案例中使用了中国天气网上的749个气象站点温度数据作为数据源，分别建立两种类型的索引，对每种地理查询都做了接口化，你可以<strong>自行运行探索</strong>，或者为其增加一个WebGIS的界面，直接进行操作，体验一下mongoDB地理查询的快捷。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;现在地理空间查询是越来越常见的一种需求，实现的方式也有很多种，相较于PostGIS，mongoDB既可以直接存储GeoJSON对象，又支持地理空间索引，可以满足高效的常见地理空间服务。&lt;/p&gt;
    
    </summary>
    
      <category term="Node" scheme="http://kael.top/categories/Node/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>【译】Vue中父子组件的生命周期函数</title>
    <link href="http://kael.top/2018/06/29/Vue-Parent-and-Child-lifecycle-hooks/"/>
    <id>http://kael.top/2018/06/29/Vue-Parent-and-Child-lifecycle-hooks/</id>
    <published>2018-06-29T07:42:41.000Z</published>
    <updated>2020-05-17T09:04:57.328Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;最近项目中遇到了地图模块挂载到DOM节点并进行初始化的问题，背后其实就是Vue中父子组件初始化的顺序问题，发现这篇文章的例子通俗易懂，遂翻译一下，如果错误还请指正。</p><a id="more"></a><p><a href="https://medium.com/@brockreece/vue-parent-and-child-lifecycle-hooks-5d6236bd561f" target="_blank" rel="noopener">原文链接</a></p><p>如果你在项目中使用Vue，那么我肯定你很熟悉组件生命周期内的钩子函数，但是你未必清楚父子组件内生命周期钩子触发的顺序和对属性（props）的影响</p><p style="text-align:center;"><img src="lifecircle.png" width="50%" height="50%"></p><h2 id="父-子组件内的生命周期钩子"><a href="#父-子组件内的生命周期钩子" class="headerlink" title="父/子组件内的生命周期钩子"></a>父/子组件内的生命周期钩子</h2><p>下面的例子在父子组件的<strong>Mounted</strong>和<strong>Created</strong>两个钩子触发时会给出相应的提示。如你所见，<strong>Created</strong>是正常的先父后子的顺序触发，但<strong>Mounted</strong>钩子则正好相反</p><iframe src="https://codesandbox.io/embed/j38wnqjy65" style="width:100%; height:500px; border:0; border-radius: 4px; overflow:hidden;" sandbox="allow-modals allow-forms allow-popups allow-scripts allow-same-origin"></iframe><p>一开始这可能很难很难理解，但如果我们通过逻辑的角度去遵循组件初始化工作流程，就能理解这种顺序。我们只要记住，父组件在其挂载template到DOM之前必须等待所有的子组件都完成挂载（mounted）操作。</p><p style="text-align:center;"><img src="parent-child-hooks.png" width="50%" height="50%"></p><p>若果你的组件通过属性（props）来通讯，以上钩子的执行顺序会很重要。</p><h2 id="属性（props）的响应式（reactivity）"><a href="#属性（props）的响应式（reactivity）" class="headerlink" title="属性（props）的响应式（reactivity）"></a>属性（props）的响应式（reactivity）</h2><p>一个组件在<strong>Created</strong>钩子触发之前就可以是响应式的，这意味在其父组件的<strong>Mounted</strong>钩子触发之前它就能够开始追踪属性（props）的变化。如果你在父组件的<strong>Mounted</strong>钩子中去设置一些属性的值，你一定要牢记这一点。</p><p>下面的例子展示了如果在父组件的<strong>Created</strong>和<strong>Mounted</strong>两个钩子中都去设置子组件的属性值会发生什么。如你所见，在子组件的<strong>Mounted</strong>钩子中我们监测到了两次对属性值的改变操作（个人补充：父组件<strong>Mounted</strong>之前的data里面对属性赋值触发了一次，父组件<strong>Mounted</strong>时候的改变属性值又让子组件触发了一次）<br></p><iframe src="https://codesandbox.io/embed/z2yonj9kq4" style="width:100%; height:500px; border:0; border-radius: 4px; overflow:hidden;" sandbox="allow-modals allow-forms allow-popups allow-scripts allow-same-origin"></iframe><p>假设你的子组件在属性值改变的时候会进行一次Ajax请求，这样就会触发两次请求，甚至造成请求的未响应。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>如果你想深入的了解Vue的生命周期钩子，我强烈建议你读一下<a href="https://alligator.io/vuejs/component-lifecycle/" target="_blank" rel="noopener">这篇文章</a>。</p><p>但通常来说，我建议如果你只在组件的<strong>Mounted</strong>钩子进行需要和DOM交互的操作，而其他的操作都放在<strong>Created</strong>钩子中</p><h3 id="个人总结"><a href="#个人总结" class="headerlink" title="个人总结"></a>个人总结</h3><p>从生命周期的角度来看： 父 Created - 子 Created - 子 Mounted - 父 Mounted<br>对应过程Props的改变： 子 prop 改变 ——— 子 prop 改变 ——— 子 prop 改变  </p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;nbsp;&amp;nbsp;最近项目中遇到了地图模块挂载到DOM节点并进行初始化的问题，背后其实就是Vue中父子组件初始化的顺序问题，发现这篇文章的例子通俗易懂，遂翻译一下，如果错误还请指正。&lt;/p&gt;
    
    </summary>
    
      <category term="前端" scheme="http://kael.top/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="FE" scheme="http://kael.top/tags/FE/"/>
    
      <category term="Vue" scheme="http://kael.top/tags/Vue/"/>
    
  </entry>
  
  <entry>
    <title>矢量切片的使用尝试1—openlayers应用</title>
    <link href="http://kael.top/2017/11/12/vector-tile-openlayers-try/"/>
    <id>http://kael.top/2017/11/12/vector-tile-openlayers-try/</id>
    <published>2017-11-12T02:54:57.000Z</published>
    <updated>2020-05-17T09:04:57.328Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;对于 GIS 行业来说，栅格切片已经处于垄断地位很长时间了，但随着 mapbox 的发展，矢量切片越来越勾引起大家探究的欲望，最近我也小小的观望了下，发现现在技术还是比较成熟了，使用上却不是很广泛，遂分享下自己瞎折腾的一些路，想着帮助大家少踩点坑，本文将介绍下服务端和网页端中 OpenLayers 对于矢量切片的一些应用。</p><a id="more"></a><p>&nbsp;&nbsp;在项目实际使用中还是建议大家首先了解下矢量和栅格切片的优缺点，网上能搜到的很多，水平有限，这里不献丑了。对于我个人来说吸引我的无非三点：1.支持大数据；2.自定义渲染；3.要素交互。</p><p>&nbsp;&nbsp;不多说了进入主题吧，OpenLayers 中支持<code>ol.source.VectorTile</code>,对于我们来说想使用它无非就是创造这一类型的<code>source</code>，目前来说想要自定义矢量切片源我探索的有以下几种方式：</p><h1 id="geoserver-中的-vectortiles-plugin-插件"><a href="#geoserver-中的-vectortiles-plugin-插件" class="headerlink" title="geoserver 中的 vectortiles-plugin 插件"></a>geoserver 中的 vectortiles-plugin 插件</h1><p>这种方式网上教程很多，是大家目前常用的，优点是支持的数据格式比较多，输出的数据格式也很多，geojson、topojson、mvt 都能做到，不做过多介绍</p><h1 id="mapbox-开发的-geojson-vt-库"><a href="#mapbox-开发的-geojson-vt-库" class="headerlink" title="mapbox 开发的 geojson-vt 库"></a>mapbox 开发的 geojson-vt 库</h1><p>这个库可能大家不去仔细关注都不会发现，mapbox 推出，<a href="https://github.com/mapbox/geojson-vt" target="_blank" rel="noopener">geojson-vt</a>，作用很简单，官方说明简单明了，把 geojson 转换成 mvt 格式的矢量数据源。</p><p>翻译一下使用：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 通过geojson数据源构建切片索引</span></span><br><span class="line"><span class="keyword">var</span> tileIndex = geojsonvt(geoJSON);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过z,x,y来在切片索引中请求某个具体瓦片</span></span><br><span class="line"><span class="keyword">var</span> features = tileIndex.getTile(z, x, y).features;</span><br></pre></td></tr></table></figure><p>拿到 features 后无非就是做样式调整之类的工作了。OpenLayers 官方也有一个 demo，叫做<a href="https://openlayers.org/en/latest/examples/geojson-vt.html" target="_blank" rel="noopener">geojson-vt integration</a>，详细介绍了怎么在 OpenLayers 中结合使用该库。</p><p>我在实际使用中发现，这个库可以说很 imba 了，有测试 200M 以上的 geojson 源文件，都能流畅的展示出来。mapbox 官方对于这个库的说明是，把 geojson 切割成矢量切片在<strong>浏览器端</strong>使用，所以我觉得因为<strong>网络传输</strong>的不确定性，对于<strong>小一点</strong>的数据量，可以考虑直接在浏览器端使用这个库。</p><h1 id="geojson-vt-的-nodejs-服务端实现"><a href="#geojson-vt-的-nodejs-服务端实现" class="headerlink" title="geojson-vt 的 nodejs 服务端实现"></a>geojson-vt 的 nodejs 服务端实现</h1><p>(｡･∀･)ﾉﾞ嗨，既然是 js，那就意味着，我们可以用 nodejs 搭建服务端呀，在浏览到某个层级时候，只请求当前需要的瓦片，不就能解决大数据的传输问题了，nodejs 大法好！</p><p>具体的代码可以移步<a href="https://github.com/zzcyrus/openlayers-demos/tree/master/1.vector_tile" target="_blank" rel="noopener">github</a></p><p>这里对一些主要部分做一些说明</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 读取数据源文件，构建切片索引</span></span><br><span class="line"><span class="keyword">const</span> dataFile = <span class="string">"./data/world.json"</span>;</span><br><span class="line"><span class="keyword">const</span> dataSource = <span class="built_in">JSON</span>.parse(fs.readFileSync(dataFile));</span><br><span class="line"><span class="keyword">const</span> tileIndex = geojsonvt(dataSource, &#123;</span><br><span class="line">  extent: <span class="number">4096</span>,</span><br><span class="line">  debug: <span class="number">1</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 从url中解析瓦片请求位置的x，y，z</span></span><br><span class="line">app.use(<span class="keyword">async</span> (ctx, next) =&gt; &#123;</span><br><span class="line">  <span class="keyword">let</span> path = ctx.request.path.toString();</span><br><span class="line">  ctx.response.type = <span class="string">"application/json"</span>;</span><br><span class="line">  <span class="keyword">if</span> (path.indexOf(<span class="string">".vector"</span>) !== <span class="number">-1</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> pathArr = path.substring(<span class="number">1</span>, path.indexOf(<span class="string">".vector"</span>)).split(<span class="string">"/"</span>);</span><br><span class="line">    <span class="keyword">const</span> z = pathArr[pathArr.length - <span class="number">3</span>];</span><br><span class="line">    <span class="keyword">const</span> x = pathArr[pathArr.length - <span class="number">2</span>];</span><br><span class="line">    <span class="keyword">const</span> y = pathArr[pathArr.length - <span class="number">1</span>];</span><br><span class="line">    <span class="keyword">const</span> data = tileIndex.getTile(<span class="built_in">Number</span>(z), <span class="built_in">Number</span>(x), <span class="built_in">Number</span>(y));</span><br><span class="line">    <span class="keyword">const</span> features = <span class="built_in">JSON</span>.stringify(</span><br><span class="line">      &#123;</span><br><span class="line">        type: <span class="string">"FeatureCollection"</span>,</span><br><span class="line">        features: data ? data.features : [],</span><br><span class="line">      &#125;,</span><br><span class="line">      replacer</span><br><span class="line">    ); <span class="comment">// replacer 是geojson格式转换函数</span></span><br><span class="line">    ctx.response.body = features;</span><br><span class="line">    <span class="keyword">await</span> next();</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    ctx.response.body = <span class="string">"Error"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>接下来是浏览器中的调用</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> vectorSource = <span class="keyword">new</span> ol.source.VectorTile(&#123;</span><br><span class="line">  <span class="comment">// 因为转换函数是geojson格式，所以这里format为geojson</span></span><br><span class="line">  format: <span class="keyword">new</span> ol.format.GeoJSON(&#123;</span><br><span class="line">    <span class="comment">// 要定义数据源的坐标系为瓦片像素</span></span><br><span class="line">    defaultDataProjection: <span class="keyword">new</span> ol.proj.Projection(&#123;</span><br><span class="line">      code: <span class="string">"TILE_PIXELS"</span>,</span><br><span class="line">      units: <span class="string">"tile-pixels"</span>,</span><br><span class="line">    &#125;),</span><br><span class="line">  &#125;),</span><br><span class="line">  <span class="comment">// 调用时候要注意z，x，y 的位置，实际上是与服务端相对应的</span></span><br><span class="line">  url: <span class="string">"http://localhost:3000/gettile/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;.vector"</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>你可以在 <a href="https://github.com/zzcyrus/openlayers-demos" target="_blank" rel="noopener">github</a> 上看到 demo，执行安装启动</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd ./1.vector_tile</span><br><span class="line">npm install</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure><p>打开 vectortile.hmtl 可以在 network 中看到瓦片请求了。</p><p><img src="network.png" alt></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;nbsp;&amp;nbsp;对于 GIS 行业来说，栅格切片已经处于垄断地位很长时间了，但随着 mapbox 的发展，矢量切片越来越勾引起大家探究的欲望，最近我也小小的观望了下，发现现在技术还是比较成熟了，使用上却不是很广泛，遂分享下自己瞎折腾的一些路，想着帮助大家少踩点坑，本文将介绍下服务端和网页端中 OpenLayers 对于矢量切片的一些应用。&lt;/p&gt;
    
    </summary>
    
      <category term="WebGIS" scheme="http://kael.top/categories/WebGIS/"/>
    
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
      <category term="OpenLayers" scheme="http://kael.top/tags/OpenLayers/"/>
    
  </entry>
  
  <entry>
    <title>Leaflet常用的一些插件</title>
    <link href="http://kael.top/2017/09/17/plugins/"/>
    <id>http://kael.top/2017/09/17/plugins/</id>
    <published>2017-09-17T05:48:15.000Z</published>
    <updated>2020-05-17T09:04:57.328Z</updated>
    
    <content type="html"><![CDATA[<p>Leaflet 中常用的 gis 功能可以满足一般使用，有些特殊需求，官方也有<a href="http://leafletjs.com/plugins.html" target="_blank" rel="noopener">插件系统</a>，本文会挑选一些常用的，经过测试可用的插件单独放出来使用方法和一些注意事项，希望能够帮助到同僚们。</p><a id="more"></a><p><a href="https://github.com/zzcyrus/Leaflet-demos" target="_blank" rel="noopener">Leaflet 系列地址</a><br><a href="https://github.com/zzcyrus/Leaflet-demos/blob/master/1.plugins/plugins.html" target="_blank" rel="noopener">本文 demo 地址</a></p><h1 id="ImageWMS"><a href="#ImageWMS" class="headerlink" title="ImageWMS"></a>ImageWMS</h1><p>&nbsp;&nbsp;在 openlayers 中，wms 图层的调用了提供了 IamgeWMS 和 tileWMS 两种方式，通常情况下，如果 wms 服务作为底图，由于数据量大，我们采用瓦片的方式加载十分有利，如果数据量小，我们采用单张 image 的方式无论从请求发送上和显示的效果上都更好。<br><img src="tileWMS.png" alt><br>&nbsp;&nbsp;<del>可以发现，瓦片会发出多次请求，每次请求都会包含该瓦片上涉及到的信息，数据量小十分没必要</del></p><p>&nbsp;&nbsp;Leaflet 中只提供了 tile 瓦片的方式加载 WMS 图层，在使用了很多第三方解决方案后，我发现了这款插件<a href="https://github.com/ptv-logistics/Leaflet.NonTiledLayer" target="_blank" rel="noopener">nonTiledLayer</a>，使用之后的效果基本上可以达到 openlaysers 中的要求，更多情况可以点击链接进去了解。</p><p>调用方式</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> layer = L.nonTiledLayer</span><br><span class="line">  .wms(url, &#123;</span><br><span class="line">    maxZoom: <span class="number">19</span>,</span><br><span class="line">    minZoom: <span class="number">0</span>,</span><br><span class="line">    opacity: <span class="number">1.0</span>,</span><br><span class="line">    layers: <span class="string">"xmap-gravelpit-fg"</span>,</span><br><span class="line">    format: <span class="string">"image/png"</span>,</span><br><span class="line">    transparent: <span class="literal">true</span>,</span><br><span class="line">    attribution: <span class="string">""</span>,</span><br><span class="line">    pane: <span class="string">"tilePane"</span>,</span><br><span class="line">    zIndex: <span class="number">3</span>,</span><br><span class="line">  &#125;)</span><br><span class="line">  .addTo(map);</span><br></pre></td></tr></table></figure><p><a href="https://github.com/ptv-logistics/Leaflet.NonTiledLayer" target="_blank" rel="noopener">nonTiledLayer</a>调用方式基本沿用 Leaflet 自身的 wms 调用，提供的属性也很全面</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">attribution - 图层数据来源.Default:&apos;</span><br><span class="line">opacity - 透明度.Default: 1</span><br><span class="line">minZoom - 最小缩放. Default: 0</span><br><span class="line">maxZoom - 最大缩放. Default: 18</span><br><span class="line">bounds - 边界条件. Default: L.latLngBounds([-180, -85.05], [180, 85.05])</span><br><span class="line">zIndex - 位置. Default: undefined</span><br><span class="line">pane - 插入的div的名称. Default: &apos;overlayPane&apos;</span><br><span class="line">pointerEvents - 鼠标事件的样式. Default: null</span><br><span class="line">errorImageUrl - 默认错误图层. Default: 1px transparent gif data:image/gif;base64,R0lGODlhAQABAHAAACH5BAUAAAAALAAAAAABAAEAAAICRAEAOw==</span><br><span class="line">useCanvas - 渲染方式. Default: undefined</span><br></pre></td></tr></table></figure><p>具体的使用效果可以移步 demo</p><h1 id="WKT-数据插件"><a href="#WKT-数据插件" class="headerlink" title="WKT 数据插件"></a>WKT 数据插件</h1><p>&nbsp;&nbsp;wkt 作为 GIS 常用的一种地理数据格式，因为通用性需求度也很高,Leaflet 官方插件中提供了许多支持 wkt 的第三方解决方案，使用下来，发现 mapbox 出品的<a href="https://github.com/mapbox/leaflet-omnivore" target="_blank" rel="noopener">leaflet-omnivore</a>效果可以说是目前最满足要求的了。</p><p><code>omnivore</code>支持的功能比较强大</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">omnivore.csv(<span class="string">"a.csv"</span>).addTo(map);</span><br><span class="line">omnivore.gpx(<span class="string">"a.gpx"</span>).addTo(map);</span><br><span class="line">omnivore.kml(<span class="string">"a.kml"</span>).addTo(map);</span><br><span class="line">omnivore.wkt(<span class="string">"a.wkt"</span>).addTo(map);</span><br><span class="line">omnivore.topojson(<span class="string">"a.topojson"</span>).addTo(map);</span><br><span class="line">omnivore.geojson(<span class="string">"a.geojson"</span>).addTo(map);</span><br><span class="line">omnivore.polyline(<span class="string">"a.txt"</span>).addTo(map);</span><br></pre></td></tr></table></figure><p>其中对于 wkt 的加载有两种方式。<br>其中，<code>customlayer</code>是通过<code>L.geojson</code>图层来为加载进来的 wkt 数据设置样式</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> customLayer = L.geoJson(<span class="literal">null</span>, &#123;</span><br><span class="line">  style: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> &#123; <span class="attr">color</span>: <span class="string">"#0ff"</span> &#125;;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><ul><li><p><code>omnivore.wkt(url, parser_options?, customLayer?)</code>: 通过 url 加载</p></li><li><p><code>omnivore.wkt.parse(wktString，parser_options?, customLayer?)</code>: 通过转换 wkt 字符串加载</p></li></ul><p>具体的使用效果可以移步<a href="https://github.com/zzcyrus/Leaflet-demos/blob/master/1.plugins/plugins.html" target="_blank" rel="noopener">demo</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Leaflet 中常用的 gis 功能可以满足一般使用，有些特殊需求，官方也有&lt;a href=&quot;http://leafletjs.com/plugins.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;插件系统&lt;/a&gt;，本文会挑选一些常用的，经过测试可用的插件单独放出来使用方法和一些注意事项，希望能够帮助到同僚们。&lt;/p&gt;
    
    </summary>
    
      <category term="WebGIS" scheme="http://kael.top/categories/WebGIS/"/>
    
    
      <category term="FE" scheme="http://kael.top/tags/FE/"/>
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
      <category term="Leaflet" scheme="http://kael.top/tags/Leaflet/"/>
    
  </entry>
  
  <entry>
    <title>Leaflet多种基础图层加载</title>
    <link href="http://kael.top/2017/09/17/basemap/"/>
    <id>http://kael.top/2017/09/17/basemap/</id>
    <published>2017-09-17T05:46:08.000Z</published>
    <updated>2020-05-17T09:04:57.328Z</updated>
    
    <content type="html"><![CDATA[<p>&nbsp;&nbsp;最近项目中有移动端的开发需求，自然就用到了最轻量级的 gis 组件——Leaflet，100 多 k 的体积，五脏俱全，丰富的第三方插件，虽然开发者数量上不及大哥级产品 Arcigs，但也基本够用，对于 esri 的自家系统支持好，svg 和 canvas 两种渲染方式，让数据量大的要素图层渲染也不是那么吃力。</p><p>&nbsp;&nbsp;这里把自己对于 Leaflet 的一些探索研究记录一下，一方面便于自己后期回顾，另一方面也希望和大家一起探讨学习。</p><a id="more"></a><p><a href="https://github.com/zzcyrus/Leaflet-demos" target="_blank" rel="noopener">Leaflet 系列地址</a><br><a href="https://github.com/zzcyrus/Leaflet-demos/blob/master/0.basemap/basemap.html" target="_blank" rel="noopener">本文 demo 地址</a></p><h1 id="基础图层的加载"><a href="#基础图层的加载" class="headerlink" title="基础图层的加载"></a>基础图层的加载</h1><p>基础图层的加载都是通过<code>L.tileLayer</code>的方式，所以加载的难点不在于 api 的使用，是一些常用的基础瓦片的收集，这里给出一些常用的地址</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">mapRadios: [</span><br><span class="line">  &#123;</span><br><span class="line">    label: <span class="string">"高德地图"</span>,</span><br><span class="line">    value: <span class="string">"1"</span>,</span><br><span class="line">    url:</span><br><span class="line">      <span class="string">"http://webrd01.is.autonavi.com/appmaptile?lang=zh_cn&amp;size=1&amp;scale=1&amp;style=8&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;"</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    label: <span class="string">"Esri地图"</span>,</span><br><span class="line">    value: <span class="string">"2"</span>,</span><br><span class="line">    url:</span><br><span class="line">      <span class="string">"http://map.geoq.cn/ArcGIS/rest/services/ChinaOnlineCommunity/MapServer/tile/&#123;z&#125;/&#123;y&#125;/&#123;x&#125;"</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    label: <span class="string">"天地图道路"</span>,</span><br><span class="line">    value: <span class="string">"3"</span>,</span><br><span class="line">    url: <span class="string">"http://t0.tianditu.cn/DataServer?T=vec_w&amp;X=&#123;x&#125;&amp;Y=&#123;y&#125;&amp;L=&#123;z&#125;"</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    label: <span class="string">"天地图标注"</span>,</span><br><span class="line">    value: <span class="string">"4"</span>,</span><br><span class="line">    url: <span class="string">"http://t5.tianditu.cn/DataServer?T=cva_w&amp;X=&#123;x&#125;&amp;Y=&#123;y&#125;&amp;L=&#123;z&#125;"</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    label: <span class="string">"天地图影像"</span>,</span><br><span class="line">    value: <span class="string">"5"</span>,</span><br><span class="line">    url:</span><br><span class="line">      <span class="string">"http://t1.tianditu.cn/img_w/wmts?service=wmts&amp;request=GetTile&amp;version=1.0.0&amp;LAYER=img&amp;tileMatrixSet=w&amp;TileMatrix=&#123;z&#125;&amp;TileRow=&#123;y&#125;&amp;TileCol=&#123;x&#125;&amp;style=default&amp;format=tiles"</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    label: <span class="string">"谷歌地形"</span>,</span><br><span class="line">    value: <span class="string">"6"</span>,</span><br><span class="line">    url:</span><br><span class="line">      <span class="string">"http://mt3.google.cn/vt/lyrs=t@131&amp;hl=zh-CN&amp;gl=cn&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;&amp;s=Ga"</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    label: <span class="string">"谷歌影像"</span>,</span><br><span class="line">    value: <span class="string">"7"</span>,</span><br><span class="line">    url: <span class="string">"http://mt3.google.cn/vt/lyrs=s&amp;hl=zh-CN&amp;gl=cn&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;"</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    label: <span class="string">"谷歌标注"</span>,</span><br><span class="line">    value: <span class="string">"8"</span>,</span><br><span class="line">    url:</span><br><span class="line">      <span class="string">"http://mt2.google.cn/vt/lyrs=m@167000000&amp;hl=zh-CN&amp;gl=cn&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;"</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">];</span><br></pre></td></tr></table></figure><p>更多的瓦片地址可以在 demo 中找到，具体的使用效果也可以移步<a href="https://github.com/zzcyrus/Leaflet-demos/blob/master/0.basemap/basemap.html" target="_blank" rel="noopener">demo</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;nbsp;&amp;nbsp;最近项目中有移动端的开发需求，自然就用到了最轻量级的 gis 组件——Leaflet，100 多 k 的体积，五脏俱全，丰富的第三方插件，虽然开发者数量上不及大哥级产品 Arcigs，但也基本够用，对于 esri 的自家系统支持好，svg 和 canvas 两种渲染方式，让数据量大的要素图层渲染也不是那么吃力。&lt;/p&gt;
&lt;p&gt;&amp;nbsp;&amp;nbsp;这里把自己对于 Leaflet 的一些探索研究记录一下，一方面便于自己后期回顾，另一方面也希望和大家一起探讨学习。&lt;/p&gt;
    
    </summary>
    
      <category term="WebGIS" scheme="http://kael.top/categories/WebGIS/"/>
    
    
      <category term="FE" scheme="http://kael.top/tags/FE/"/>
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
      <category term="Leaflet" scheme="http://kael.top/tags/Leaflet/"/>
    
  </entry>
  
  <entry>
    <title>openlayers 从台风风圈绘制到canvas样式和图层的应用</title>
    <link href="http://kael.top/2017/07/31/openlayers-canvas-typhoon/"/>
    <id>http://kael.top/2017/07/31/openlayers-canvas-typhoon/</id>
    <published>2017-07-31T14:31:02.000Z</published>
    <updated>2020-05-17T09:04:57.328Z</updated>
    
    <content type="html"><![CDATA[<p>本文中所使用的数据来源于<a href="http://www.wztf121.com/typhoon.html" target="_blank" rel="noopener">温州台风网</a>，通过 F12 抓取，你可以在我的<a href="https://github.com/zzcyrus/openlayers-demos" target="_blank" rel="noopener">GitHub</a>上查看数据和本文源代码</p><p>&nbsp;&nbsp;台风的风圈是一种不常见但算的上规则的图形，在上面的网站可以看到最终效果，简单的解剖下其实就是四个 1/4 圆</p><p><img src="goal.png" alt></p><p>从数据结构上也可以看出来：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> radius_quad = &#123;</span><br><span class="line">  ne: <span class="number">250</span>, <span class="comment">//单位为KM</span></span><br><span class="line">  se: <span class="number">250</span>,</span><br><span class="line">  sw: <span class="number">180</span>,</span><br><span class="line">  nw: <span class="number">150</span>,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>在某个固定经纬度点上，以此点为圆心组合成了一个所谓的风圈形状，四个方向分别代表每个 1/4 圆的半径。刚开始是为了实现这种效果进行了研究，后来发现 openlayers 对于这种效果的支持还挺有意思，记录下来已做分享。</p><a id="more"></a><h1 id="通过自定义-geometry-的实现"><a href="#通过自定义-geometry-的实现" class="headerlink" title="通过自定义 geometry 的实现"></a>通过自定义 geometry 的实现</h1><p>&nbsp;&nbsp;最原始的思路也是最容易切入的点，就是去看 openlayers 中怎么实现圆形的画法，在 ol 中，有<code>createRegularPolygon</code>这个类，原理就是给定一些参数，用点去填充我们想要的形状，当填进去点达到一定密集度，自然就能得到一个近似的圆。<br>&nbsp;&nbsp;通过这个思路我们可以计算台风风圈的每个 1/4 圆的坐标，用点去占位，最终实现绘制出想要的图形。</p><p>具体代码可以去看某大牛的博客，这里只给出一个传送门，写的很详细了：<br><a href="http://blog.csdn.net/gisshixisheng/article/details/76397068" target="_blank" rel="noopener">点我乘坐飞机</a> <strong>这个算法还有些不完善，就是在每个 1/4 圆结束时候少计算了一个点，导致看着有点不对劲</strong></p><p><strong>特点:</strong> 这种方式画出来的图形是一个图层中的一个 feature，好处自然不用多说，基本上 feature 支持的功能都能满足！</p><h1 id="通过-canvas-类型的-symbol-实现"><a href="#通过-canvas-类型的-symbol-实现" class="headerlink" title="通过 canvas 类型的 symbol 实现"></a>通过 canvas 类型的 symbol 实现</h1><p>&nbsp;&nbsp;在点密集的情况下，用上面的方式其实效果已经很不错了，但是有些细（tiao）心（ci）的使用者，非要放大到一定程度，然后说你这个不够圆，那。。。。。</p><p>&nbsp;&nbsp;那我们就用 canvas 画出来，openlayers 中，要素的样式是有这么一种方式<code>ol.style.Icon</code>来实现，我们可以把绘制好的元素作为<code>Icon</code>的参数</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> style = <span class="keyword">new</span> ol.style.Style(&#123;</span><br><span class="line">  image: <span class="keyword">new</span> ol.style.Icon(&#123;</span><br><span class="line">    opacity: <span class="number">0.3</span>,</span><br><span class="line">    img: canvas,</span><br><span class="line">    imgSize: [canvas.width, canvas.height],</span><br><span class="line">  &#125;),</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>canvans 绘制的方法：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">createTyphoon</span>(<span class="params">radius, radius_quad</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> canvas = <span class="built_in">document</span>.createElement(<span class="string">"canvas"</span>);</span><br><span class="line">  canvas.width = canvas.height = <span class="number">2</span> * radius;</span><br><span class="line">  <span class="keyword">var</span> context = canvas.getContext(<span class="string">"2d"</span>);</span><br><span class="line">  context.fillStyle = <span class="string">"#0000ff"</span>;</span><br><span class="line">  context.strokeStyle = <span class="string">"#ff0000"</span>;</span><br><span class="line">  context.lineWidth = <span class="number">3</span>;</span><br><span class="line">  context.beginPath();</span><br><span class="line">  context.arc(radius, radius, radius_quad.se, <span class="number">0</span>, <span class="number">0.5</span> * <span class="built_in">Math</span>.PI);</span><br><span class="line">  context.lineTo(radius, radius + radius_quad.sw);</span><br><span class="line">  context.arc(radius, radius, radius_quad.sw, <span class="number">0.5</span> * <span class="built_in">Math</span>.PI, <span class="built_in">Math</span>.PI);</span><br><span class="line">  context.lineTo(radius - radius_quad.nw, radius);</span><br><span class="line">  context.arc(radius, radius, radius_quad.nw, <span class="built_in">Math</span>.PI, <span class="number">1.5</span> * <span class="built_in">Math</span>.PI);</span><br><span class="line">  context.lineTo(radius, radius - radius_quad.ne);</span><br><span class="line">  context.arc(radius, radius, radius_quad.ne, <span class="number">1.5</span> * <span class="built_in">Math</span>.PI, <span class="number">0</span>);</span><br><span class="line">  context.lineTo(radius + radius_quad.se, radius);</span><br><span class="line">  context.fill();</span><br><span class="line">  context.stroke();</span><br><span class="line">  <span class="keyword">return</span> canvas;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>效果如下图：<br><img src="canvasSymbol.png" alt></p><p><a href="https://github.com/zzcyrus/openlayers-demos/blob/master/0.typhoon/canvasSymbol.html" target="_blank" rel="noopener">本方法完整代码</a></p><p><strong>特点：</strong> 这种方式，绘制出来的台风风圈其实只是一个 symbol 符号，需要把这个符号赋给一个具体的要素，比如一个点，一个圆之类的，而且根据分辨率还要去调整样式的缩放</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">map.getView().on(<span class="string">"change:resolution"</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> style = shape.getStyle();</span><br><span class="line">  <span class="comment">// 重新设置图标的缩放率</span></span><br><span class="line">  style.getImage().setScale(<span class="keyword">this</span>.getZoom() / <span class="number">8</span>);</span><br><span class="line">  shape.setStyle(style);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h1 id="通过-canvas-图层的方式实现"><a href="#通过-canvas-图层的方式实现" class="headerlink" title="通过 canvas 图层的方式实现"></a>通过 canvas 图层的方式实现</h1><p>&nbsp;&nbsp;再后来转念一想，既然支持 canvas 的 symbol，为何不直接使用 canvas 绘制固定元素呢，果然在 API 中找到了<code>ol.source.ImageCanvas</code>，直接把 canvas 要素当作图层来使用！</p><p><code>ol.source.ImageCanvas</code>的绘制有点需要特别注意的点，这里给出重要代码片段，完整 demo 可以去<a href="https://github.com/zzcyrus/openlayers-demos/blob/master/0.typhoon/canvasLayer.html" target="_blank" rel="noopener">GitHub</a>查看</p><h2 id="创建图层，在-canvasFunction-中写具体的绘图方法"><a href="#创建图层，在-canvasFunction-中写具体的绘图方法" class="headerlink" title="创建图层，在 canvasFunction 中写具体的绘图方法"></a>创建图层，在 canvasFunction 中写具体的绘图方法</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> canvasLayer = <span class="keyword">new</span> ol.layer.Image(&#123;</span><br><span class="line">  source: <span class="keyword">new</span> ol.source.ImageCanvas(&#123;</span><br><span class="line">    canvasFunction: canvasFunction,</span><br><span class="line">    projection: <span class="string">"EPSG:3857"</span>,</span><br><span class="line">  &#125;),</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h2 id="canvasFunction-中一定要注意画布和地图的偏移处理，还要通过分辨率计算实际风圈大小"><a href="#canvasFunction-中一定要注意画布和地图的偏移处理，还要通过分辨率计算实际风圈大小" class="headerlink" title="canvasFunction 中一定要注意画布和地图的偏移处理，还要通过分辨率计算实际风圈大小"></a>canvasFunction 中一定要注意画布和地图的偏移处理，还要通过分辨率计算实际风圈大小</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//计算画布和地图四至的偏移量</span></span><br><span class="line"><span class="keyword">var</span> mapExtent = map.getView().calculateExtent(map.getSize());</span><br><span class="line"><span class="keyword">var</span> canvasOrigin = map.getPixelFromCoordinate([extent[<span class="number">0</span>], extent[<span class="number">3</span>]]);</span><br><span class="line"><span class="keyword">var</span> mapOrigin = map.getPixelFromCoordinate([mapExtent[<span class="number">0</span>], mapExtent[<span class="number">3</span>]]);</span><br><span class="line"><span class="keyword">var</span> delta = [mapOrigin[<span class="number">0</span>] - canvasOrigin[<span class="number">0</span>], mapOrigin[<span class="number">1</span>] - canvasOrigin[<span class="number">1</span>]];</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//在计算台风风圈的中心点时要补充计算偏移量</span></span><br><span class="line"><span class="keyword">var</span> point = ol.proj.transform(coordinate, <span class="string">"EPSG:4326"</span>, <span class="string">"EPSG:3857"</span>);</span><br><span class="line"><span class="keyword">var</span> pixel = map.getPixelFromCoordinate(point);</span><br><span class="line"><span class="keyword">var</span> cX = pixel[<span class="number">0</span>] + delta[<span class="number">0</span>],</span><br><span class="line">  cY = pixel[<span class="number">1</span>] + delta[<span class="number">1</span>];</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//利用canvasFunction提供的默认参数分辨率，计算准确的坐标</span></span><br><span class="line"><span class="keyword">var</span> radius_quad = &#123;</span><br><span class="line">  ne: <span class="number">250000</span> / resolution,</span><br><span class="line">  se: <span class="number">250000</span> / resolution,</span><br><span class="line">  sw: <span class="number">180000</span> / resolution,</span><br><span class="line">  nw: <span class="number">150000</span> / resolution,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>最终效果如下，我在同一图层中绘制了多个：<br><img src="canvasLayer.png" alt></p><p>至于绘制 canvas 的方法和上面的 symbol 是一样的。具体代码还请移步<a href="https://github.com/zzcyrus/openlayers-demos/blob/master/0.typhoon/canvasLayer.html" target="_blank" rel="noopener">GitHub</a>(原谅我厚颜无耻的屡次打广告！)</p><p><strong>特点：</strong>这种方式可以在一个图层中添加多个风圈要素，同时图层支持的功能也比较多，基本满足需求，效果也还行</p><p>&nbsp;&nbsp;这篇应用主要是从一个基本的需求所拓展开来的，canvas 图层的应用我想应该很强大，刚开始研究 openlyaers，相比于 arcgis，ol 很多功能可能要自己实现，但似乎效果上还是能让人满意的，欢迎大家讨论。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文中所使用的数据来源于&lt;a href=&quot;http://www.wztf121.com/typhoon.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;温州台风网&lt;/a&gt;，通过 F12 抓取，你可以在我的&lt;a href=&quot;https://github.com/zzcyrus/openlayers-demos&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;GitHub&lt;/a&gt;上查看数据和本文源代码&lt;/p&gt;
&lt;p&gt;&amp;nbsp;&amp;nbsp;台风的风圈是一种不常见但算的上规则的图形，在上面的网站可以看到最终效果，简单的解剖下其实就是四个 1/4 圆&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;goal.png&quot; alt&gt;&lt;/p&gt;
&lt;p&gt;从数据结构上也可以看出来：&lt;/p&gt;
&lt;figure class=&quot;highlight js&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;var&lt;/span&gt; radius_quad = &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  ne: &lt;span class=&quot;number&quot;&gt;250&lt;/span&gt;, &lt;span class=&quot;comment&quot;&gt;//单位为KM&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  se: &lt;span class=&quot;number&quot;&gt;250&lt;/span&gt;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  sw: &lt;span class=&quot;number&quot;&gt;180&lt;/span&gt;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  nw: &lt;span class=&quot;number&quot;&gt;150&lt;/span&gt;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;在某个固定经纬度点上，以此点为圆心组合成了一个所谓的风圈形状，四个方向分别代表每个 1/4 圆的半径。刚开始是为了实现这种效果进行了研究，后来发现 openlayers 对于这种效果的支持还挺有意思，记录下来已做分享。&lt;/p&gt;
    
    </summary>
    
      <category term="WebGIS" scheme="http://kael.top/categories/WebGIS/"/>
    
    
      <category term="FE" scheme="http://kael.top/tags/FE/"/>
    
      <category term="GIS" scheme="http://kael.top/tags/GIS/"/>
    
      <category term="OpenLayers" scheme="http://kael.top/tags/OpenLayers/"/>
    
  </entry>
  
  <entry>
    <title>element dialog组件嵌套问题的临时方案</title>
    <link href="http://kael.top/2017/06/23/vue-dialog-nested/"/>
    <id>http://kael.top/2017/06/23/vue-dialog-nested/</id>
    <published>2017-06-23T14:09:55.000Z</published>
    <updated>2020-05-17T09:04:57.328Z</updated>
    
    <content type="html"><![CDATA[<p>&emsp;&emsp;最近用 Vue+element 开发项目时，在dialog组件上嵌套使用dialog组件或是MessageBox这类弹框组件，发现会出现遮罩层重合之类的问题，导致很多人只能选择关闭遮罩层。GitHub上就此问题也出现过类似讨论。有几种解决方案：</p><a id="more"></a> <ol><li><p>按照官方所说，将多个dialog类组件全部移动至<strong>&lt;body</strong>&gt;标签下</p></li><li><p>Github上某位大牛给出了不完全解决方案，基本思路仍然是将添加在<strong>&lt;el-dialog</strong>&gt;内的dom结构自动移动至<strong>&lt;body</strong>&gt;标签下。这个方式测试了下，似乎纯dialog嵌套问题不大，但是如果dialog内部包含了其他组件会有点问题，智者见智，自行探索了</p></li></ol><p>&emsp;&emsp;<a href="https://github.com/foolishchow/element-dialog2" target="_blank" rel="noopener">附上连接</a></p><ol start="3"><li>编写自己modal遮罩层，这里给出个不完全临时代码仅供参考</li></ol><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">Vue.prototype.$Modal = &#123;</span><br><span class="line">    open(element) &#123;</span><br><span class="line">        <span class="keyword">var</span> index = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">var</span> wrapper = element.querySelector(<span class="string">'.el-dialog__wrapper'</span>);</span><br><span class="line">        <span class="keyword">if</span> (wrapper) &#123;</span><br><span class="line">            index = wrapper.style.zIndex - <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">var</span> modalDom = <span class="built_in">document</span>.createElement(<span class="string">'div'</span>);</span><br><span class="line">        modalDom.className = <span class="string">'v-modal'</span>;</span><br><span class="line">        element.appendChild(modalDom);</span><br><span class="line">        modalDom.style.zIndex = index;</span><br><span class="line">    &#125;,</span><br><span class="line">    close() &#123;</span><br><span class="line">        <span class="keyword">var</span> modal = <span class="built_in">document</span>.getElementsByClassName(<span class="string">'v-modal'</span>)[<span class="number">0</span>;</span><br><span class="line">        modal.parentNode.removeChild(modal);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>&emsp;&emsp;一般调用方法<br><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">mounted: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> that = <span class="keyword">this</span>;</span><br><span class="line">    <span class="keyword">this</span>.$nextTick(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="comment">//遮罩层</span></span><br><span class="line">        that.$nextTick(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">            that.$Modal.open(that.$el);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure></p><p>&emsp;&emsp;主要想法就是在组件mounted完成的nextTick后，为页面中添加一个div，样式就直接采用element自己的了，因为遮罩层是添加在dialog组件下的，因此组件销毁后也会自己销毁，close方法也没用上，但也应该会遇到有用上的时候吧。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&amp;emsp;&amp;emsp;最近用 Vue+element 开发项目时，在dialog组件上嵌套使用dialog组件或是MessageBox这类弹框组件，发现会出现遮罩层重合之类的问题，导致很多人只能选择关闭遮罩层。GitHub上就此问题也出现过类似讨论。有几种解决方案：&lt;/p&gt;
    
    </summary>
    
      <category term="前端" scheme="http://kael.top/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="FE" scheme="http://kael.top/tags/FE/"/>
    
      <category term="Vue" scheme="http://kael.top/tags/Vue/"/>
    
  </entry>
  
</feed>
