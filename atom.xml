<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Kaely</title>
  
  <subtitle>迷陣</subtitle>
  <link href="https://kaely.net/atom.xml" rel="self"/>
  
  <link href="https://kaely.net/"/>
  <updated>2022-10-24T11:44:45.391Z</updated>
  <id>https://kaely.net/</id>
  
  <author>
    <name>Kaely</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>thematic工具的重构</title>
    <link href="https://kaely.net/2022/02/17/tool-thematic-refactor/"/>
    <id>https://kaely.net/2022/02/17/tool-thematic-refactor/</id>
    <published>2022-02-17T11:52:28.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p>前面说的thematic出图流程有一个明显<strong>诟病</strong>，从软件开发的角度上看相当的不成熟，就是一个纯业务流程的产物，前后端耦合极高，再加上很多很多业务化服务希望能自动集成出图的结果，所以一直有重构的想法，并让其更加易用、易扩展。之后幸运的看到了这么一篇文章<a href="https://github.com/ascoders/weekly/blob/v2/121.%E7%B2%BE%E8%AF%BB%E3%80%8A%E5%89%8D%E7%AB%AF%E4%B8%8E%20BI%E3%80%8B.md">精读《前端与 BI》</a>，让我对整个流程有了新的想法。</p><span id="more"></span><h1 id="模块重构"><a href="#模块重构" class="headerlink" title="模块重构"></a>模块重构</h1><blockquote><p>BI 1.0 阶段的核心概念包括 <strong>数据集、渲染引擎、数据模型、可视化</strong> 这四个技术模块。</p></blockquote><p>通过这四个模块来重新解构我的需求：</p><h2 id="1-数据集"><a href="#1-数据集" class="headerlink" title="1.数据集"></a>1.数据集</h2><p>BI的概念中为封装好的某一类数据，用于做后续的分析，出图整个场景就是在对数据不断的进行定制化结构改造，可以当作数据集来维护的有以下几个部分：</p><ol><li>观测类的站点数据，存放在数据库中，通过文本字符进行传输；</li><li>预报类的格点数据，存放在nc文件中，通过文本字符进行传输；</li><li>地理信息静态数据，可以是省市县的边界信息、特定的流域、区域，通常是geojson格式的文件。</li></ol><h3 id="数据集导入"><a href="#数据集导入" class="headerlink" title="数据集导入"></a>数据集导入</h3><p>对于上述1、2两类数据集，理论上由独立的数据服务提供交互，不涉及导入，对于第3类数据集，可以提供相应模板，支持用户根据模板自主上传地理信息数据。</p><h3 id="数据集建模"><a href="#数据集建模" class="headerlink" title="数据集建模"></a>数据集建模</h3><p>重点需要对上述各类型数据集的字段配置进行建模，确保在计算场景的交互上不会出现异常和报错。</p><h2 id="2-渲染引擎"><a href="#2-渲染引擎" class="headerlink" title="2.渲染引擎"></a>2.渲染引擎</h2><p>我将引擎理解为整个系统的驱动力和中枢神经。为了串联好各个环节的功能和数据，需要提前定义好以下几个重要的模块，来确保渲染的过程不会有任何的问题。</p><h3 id="数据层"><a href="#数据层" class="headerlink" title="数据层"></a>数据层</h3><p>主要用来对接所有的数据服务，或是对某个在线数据进行解析处理。整体而言是轻量的，有点adapter的意思，获取不同源头的数据并进行标准化的质控和预处理。</p><h3 id="计算层"><a href="#计算层" class="headerlink" title="计算层"></a>计算层</h3><p>主要用来对数据进行二次加工，或者对数据进行指定格式的转化。是原始数据到可绘制数据的中间桥梁，当然不是所有数据都要通过计算引擎。</p><h3 id="事件层"><a href="#事件层" class="headerlink" title="事件层"></a>事件层</h3><p>考虑业务流程中的各种操作频繁，也为了契合js事件驱动的想法，需要统一的事件中心来触发和相应各个流程节点和操作节点。</p><h3 id="编辑层"><a href="#编辑层" class="headerlink" title="编辑层"></a>编辑层</h3><p>该模块会依然沿用fabric.js的方案，来对所有绘制完的结果进行后期的组装、微调和输出。</p><h2 id="3-数据模型"><a href="#3-数据模型" class="headerlink" title="3.数据模型"></a>3.数据模型</h2><p>新thematic的数据模型应当包含业务逻辑和数据流程两条主线。业务逻辑即针对编辑引擎的位置坐标模型、针对图形最终结果的图层参数模型；数据流程即针对不同数据集的字段指标模型、针对不同计算结果的中间参数模型。这里没法穷举所有的结论，总体而言应该对不同标准的数据都需要对应的展现。</p><h2 id="4-可视化"><a href="#4-可视化" class="headerlink" title="4.可视化"></a>4.可视化</h2><p>正如文章中所说，<strong>大数据性能优化、边界数据展示优化、交互响应</strong>是BI可视化组件的与传统组件的几个显著区分点。这几个问题也是我们气象行业对数据进行展示遇到的老大难问题。<br>庆幸的是技术总是在日新月异的发展，我们所面临的挑战则是开发一套横向扩展性较高的系统，这样在对不同数据进行可视化呈现时，可以考量数据格式和开发语言不断从上述3个方向找寻最合适的方案。</p><h1 id="总体架构设计"><a href="#总体架构设计" class="headerlink" title="总体架构设计"></a>总体架构设计</h1><p>我希望重构完的thematic能实现如下几个重要的目标：</p><ul><li>区分admin和user的职责</li><li>admin负责配置出图的模板和任务，user通过接口来单向的获取图片结果</li><li>既然有了模板和任务，那意味着各种类型的图层参数都是完全可配的</li><li>有可能的话，为了追求性能，将渲染的过程以server端的形式来实现</li></ul><p>基于上面的目标，新的thematic会拆解成如下几个模块，具体的关系如下图所示意：</p><ul><li><strong>可视化平台</strong>：用于管理员配置各种类型图片的默认出图参数</li><li><strong>服务层</strong>：用于出图参数的增删改查</li><li><strong>渲染层</strong>：根据参数请求数据，并将数据渲染成图片</li></ul><p><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/chart-studio-design-20220209174839.jpg" alt="chart-studio-design"></p><p>PS：希望这是公司内第一个<code>no code</code>或者是<code>low code</code>平台。</p><p>参考文档：<br><a href="https://mp.weixin.qq.com/s?__biz=MzU4MzUzODc3Nw==&mid=2247484815&idx=1&sn=ccc3ea2e8d3799e9fd616e2a7a386d75&chksm=fda6c4b9cad14dafb26f02c10598dab11a6333f8a5890ae90f839a0e42cc9a9bc5a903af20e7&mpshare=1&scene=1&srcid=0109hTLZ16lbquz8Bd4ikilp&sharer_sharetime=1641738189459&sharer_shareid=4755c0bae34e62c2cd22339c28558953#rd">动态图片生成方案</a><br><a href="https://mp.weixin.qq.com/s?__biz=Mzg4MTYwMzY1Mw==&mid=2247498945&idx=1&sn=097b7c83c85f5f9fc5049ac7f009c108&chksm=cf61d9ebf81650fd27165b47a2b9b3bd863e91f75a6718fdbb2f6599855b6a89f544cb820b93&mpshare=1&scene=1&srcid=0115J8sWxXlw49K1YDVffSx3&sharer_sharetime=1642259798984&sharer_shareid=4755c0bae34e62c2cd22339c28558953#rd">如何设计实现H5营销页面搭建系统</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;前面说的thematic出图流程有一个明显&lt;strong&gt;诟病&lt;/strong&gt;，从软件开发的角度上看相当的不成熟，就是一个纯业务流程的产物，前后端耦合极高，再加上很多很多业务化服务希望能自动集成出图的结果，所以一直有重构的想法，并让其更加易用、易扩展。之后幸运的看到了这么一篇文章&lt;a href=&quot;https://github.com/ascoders/weekly/blob/v2/121.%E7%B2%BE%E8%AF%BB%E3%80%8A%E5%89%8D%E7%AB%AF%E4%B8%8E%20BI%E3%80%8B.md&quot;&gt;精读《前端与 BI》&lt;/a&gt;，让我对整个流程有了新的想法。&lt;/p&gt;</summary>
    
    
    
    <category term="可视化" scheme="https://kaely.net/categories/%E5%8F%AF%E8%A7%86%E5%8C%96/"/>
    
    
  </entry>
  
  <entry>
    <title>thematic工具的诞生</title>
    <link href="https://kaely.net/2022/02/12/tool-thematic-intro/"/>
    <id>https://kaely.net/2022/02/12/tool-thematic-intro/</id>
    <published>2022-02-12T11:40:09.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p>在气象行业，将各类型数据保存成图片，附着在各类文档材料中是个很常见的需求，其实行业内有些成熟的软件去处理这个事情，比方说常用的<a href="https://www.ncl.ucar.edu/">NCL</a>，python中也有matplotlib包来绘制各类型的图片。从系统的角度上来说，业务人员会希望有一个交互性更好的平台来实现出图。</p><span id="more"></span><h1 id="背景调研"><a href="#背景调研" class="headerlink" title="背景调研"></a>背景调研</h1><p>在行业内有一个<strong>weather-studio</strong>系统，底层依赖于Arcgis软件的<strong>GP服务</strong>，操作过程虽然繁琐，但是功能很强大。使用人员可以通过撰写脚本从库或者接口中获取原始数据，通过可视化、可配置的工作流来对数据进行二次处理，包括插值、抽稀等操作，最后利用Arcgis强大的展示功能来绘制出图片。</p><p>通过复刻这套流程，结合业务需求，我们也基本通过自行开发的代码来实现了查询&#x3D;&gt;处理&#x3D;&gt;绘制&#x3D;&gt;出图这一系列操作，功能性不如weather-studio，仅仅胜在简洁易用，这里对整体的流程进行描述，提供一个思路。</p><h1 id="流程图"><a href="#流程图" class="headerlink" title="流程图"></a>流程图</h1><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/thematic-flow-20220209174840.jpg" width="50%"><h1 id="功能实现"><a href="#功能实现" class="headerlink" title="功能实现"></a>功能实现</h1><p>具体说明一下各个功能点实现的大致方法：</p><h2 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h2><ul><li>对站点数据我们使用了ck来存储和查询，保证效率</li><li>对格点数据自然是读取原始的nc文件</li></ul><h2 id="插值"><a href="#插值" class="headerlink" title="插值"></a>插值</h2><ul><li>站点数据插值到格点我们提供了kriging和idw两种插值方式</li><li>插值完后的结果转化为等值面geojson</li></ul><h2 id="绘制"><a href="#绘制" class="headerlink" title="绘制"></a>绘制</h2><ul><li>等值面geojson我们直接采用d3来做自定义投影的绘制</li><li>格点数据我们基于maptalks开发了自定义图层来绘制，也是考虑了它对自定义投影的支持较好</li></ul><h2 id="裁剪"><a href="#裁剪" class="headerlink" title="裁剪"></a>裁剪</h2><ul><li>不管是哪种类型数据的绘制结果都是矩形的，跟业务需求不太一样，所以我们会利用geojson静态文件绘制出结果蒙版</li><li>通过canvas依照蒙版需求对指定区域进行裁剪</li></ul><h2 id="二次调整"><a href="#二次调整" class="headerlink" title="二次调整"></a>二次调整</h2><ul><li>将上述绘制的结果导出为图片加载进入fabric.js</li><li>将配置项里面的其他信息，比如标题、logo等分别转化为fabric.js的要素</li><li>用户在界面上进行位置、内容上的微调，或者图例的更换</li><li>保存图片，插入文字材料中</li></ul><h1 id="最终效果"><a href="#最终效果" class="headerlink" title="最终效果"></a>最终效果</h1><p><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/thematic-demo-20220209174839.png" alt="thematic-demo"></p><p>PS：正如同<code>任何可以用 JavaScript 来写的应用，最终都将用 JavaScript 来写</code>，当数据解析、传输、渲染都不再受网络带宽和浏览器性能影响后，许多曾经是server端的功能，也渐渐将搬到web端，只为了使用者可以更好的<strong>自定义</strong>。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;在气象行业，将各类型数据保存成图片，附着在各类文档材料中是个很常见的需求，其实行业内有些成熟的软件去处理这个事情，比方说常用的&lt;a href=&quot;https://www.ncl.ucar.edu/&quot;&gt;NCL&lt;/a&gt;，python中也有matplotlib包来绘制各类型的图片。从系统的角度上来说，业务人员会希望有一个交互性更好的平台来实现出图。&lt;/p&gt;</summary>
    
    
    
    <category term="可视化" scheme="https://kaely.net/categories/%E5%8F%AF%E8%A7%86%E5%8C%96/"/>
    
    
  </entry>
  
  <entry>
    <title>Cesium 开发系列8 贴地展示-自定义canvas</title>
    <link href="https://kaely.net/2022/02/10/cesium-dev-ground-primitive-custom-canvas/"/>
    <id>https://kaely.net/2022/02/10/cesium-dev-ground-primitive-custom-canvas/</id>
    <published>2022-02-10T15:30:22.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p>在Cesium中对点、线、面不同类型feature做贴地的具体操作，就不再赘述了，我们在二维的地图sdk使用过程中经常会开发一些自定义图层，大多基于canvas来实现，例如热力图、色板图、echarts图等，像leaflet中通常是一个新的dom来与主地图做同步，mapbox等新生代选手则可以直接添加到主地图的dom中。</p><p>对于这类自定义的canvas图层，在cesium中想要贴在地形之上进行展示，其实也很简单，<strong>GroundPrimitive</strong>配合上自定义<strong>material</strong>就好了，具体过程可以参考代码的注释。</p><span id="more"></span><h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建贴地的primitive</span></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">Cesium</span>.<span class="title class_">GroundPrimitive</span>(&#123;</span><br><span class="line">  <span class="attr">geometryInstances</span>: <span class="keyword">new</span> <span class="title class_">Cesium</span>.<span class="title class_">GeometryInstance</span>(&#123;</span><br><span class="line">    <span class="comment">// 和二维类似，本质上还是一个矩形</span></span><br><span class="line">    <span class="attr">geometry</span>: <span class="keyword">new</span> <span class="title class_">Cesium</span>.<span class="title class_">RectangleGeometry</span>(&#123;</span><br><span class="line">      <span class="attr">rectangle</span>: <span class="title class_">Cesium</span>.<span class="property">Rectangle</span>.<span class="title function_">fromDegrees</span>(</span><br><span class="line">        <span class="comment">// 提供矩形的四至经纬度</span></span><br><span class="line">        lonMin,</span><br><span class="line">        latMin,</span><br><span class="line">        lonMax,</span><br><span class="line">        latMax,</span><br><span class="line">      ),</span><br><span class="line">    &#125;),</span><br><span class="line">  &#125;),</span><br><span class="line">  <span class="attr">appearance</span>: <span class="keyword">new</span> <span class="title class_">Cesium</span>.<span class="title class_">Appearance</span>(&#123;</span><br><span class="line">    <span class="comment">// 创建一个完全自定义的material</span></span><br><span class="line">    <span class="attr">material</span>: <span class="keyword">new</span> <span class="title class_">Cesium</span>.<span class="title class_">Material</span>(&#123;</span><br><span class="line">      <span class="attr">fabric</span>: &#123;</span><br><span class="line">        <span class="attr">type</span>: <span class="string">&#x27;Custom-Canvas&#x27;</span>,</span><br><span class="line">        <span class="attr">uniforms</span>: &#123;</span><br><span class="line">  <span class="comment">// textureCanvas就是在二维sdk中绘制出来的自定义canvas图层</span></span><br><span class="line">          <span class="attr">image</span>: textureCanvas,</span><br><span class="line">        &#125;,</span><br><span class="line">    <span class="comment">// 这里是cesium原始的source内容，没有修改</span></span><br><span class="line">        <span class="attr">source</span>: <span class="string">`</span></span><br><span class="line"><span class="string">            uniform sampler2D image;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">            czm_material czm_getMaterial(czm_materialInput materialInput)&#123;</span></span><br><span class="line"><span class="string">              czm_material material = czm_getDefaultMaterial(materialInput);</span></span><br><span class="line"><span class="string">              vec2 st = materialInput.st;</span></span><br><span class="line"><span class="string">              vec4 colorImage = texture2D(image,st);</span></span><br><span class="line"><span class="string">              material.diffuse = colorImage.rgb;</span></span><br><span class="line"><span class="string">              material.alpha = colorImage.a;</span></span><br><span class="line"><span class="string">              return material;</span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string">           `</span>,</span><br><span class="line">      &#125;,</span><br><span class="line">    &#125;),</span><br><span class="line">    <span class="attr">flat</span>: <span class="literal">true</span>,</span><br><span class="line">  &#125;),</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>Cesium对Material是具备一定的开放性的，你可以在source中去对原始的canvas做二次加工，或者把一些消耗性能的绘制操作直接放在source中（需要一定的webgl基础）。</p><h1 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h1><p>在二维sdk中绘制自定义图层时，我们通常会使用类似<code>map.latLngToContainerPoint</code>的方法来将经纬度坐标转化为二维平面坐标，这是因为二维地图大多采用了墨卡托投影，而在上述贴地操作中，原始的canvas不需要做这一步转换，把它假设成一个等经纬度的网格来绘制就好了。Cesium会在加载的时候自动重投影，可以将这个过程理解成通过<code>SingleTileImageryProvider</code>来加载一张图片。<strong>（目前我的测试是这样，如果这个理解有误，后期会更新这个观点）</strong></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;在Cesium中对点、线、面不同类型feature做贴地的具体操作，就不再赘述了，我们在二维的地图sdk使用过程中经常会开发一些自定义图层，大多基于canvas来实现，例如热力图、色板图、echarts图等，像leaflet中通常是一个新的dom来与主地图做同步，mapbox等新生代选手则可以直接添加到主地图的dom中。&lt;/p&gt;
&lt;p&gt;对于这类自定义的canvas图层，在cesium中想要贴在地形之上进行展示，其实也很简单，&lt;strong&gt;GroundPrimitive&lt;/strong&gt;配合上自定义&lt;strong&gt;material&lt;/strong&gt;就好了，具体过程可以参考代码的注释。&lt;/p&gt;</summary>
    
    
    
    <category term="Cesium" scheme="https://kaely.net/categories/Cesium/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
    <category term="Cesium" scheme="https://kaely.net/tags/Cesium/"/>
    
  </entry>
  
  <entry>
    <title>试着还原下墨迹mapbox合作的雷达图展示</title>
    <link href="https://kaely.net/2020/06/23/radar-vector-tile-try/"/>
    <id>https://kaely.net/2020/06/23/radar-vector-tile-try/</id>
    <published>2020-06-23T06:58:12.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p>在<a href="https://juejin.im/post/5d4e8572e51d453c12504dd4">四步走，墨迹天气雷达数据可视化指南</a>这篇文章中，提到了用<strong>矢量瓦片</strong>代替传统传统栅格图像可视化方案，可以达到更流程的性能，更高清的数据展示，更灵活的样式表达。感觉挺有意思，同时也为了考察这么做的成本，就试着还原做了下。</p><p>从文章来看整个过程总结起来就是准备数据<code>geojson</code>，切成<code>pbf</code>或者<code>mbtiles</code>在 mapbox 中展示。</p><span id="more"></span><h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>按惯例先解释下雷达数据。我之前通俗的把气象数据划分为了站点（散点）和格点两类，暂且不细说雷达数据到底属于哪类，但最终都可以通过比方说插值来得到一份格点数据。传统的<code>栅格图像</code>可视化方案是基于格点数据利用<a href="https://matplotlib.org/3.2.1/api/_as_gen/matplotlib.pyplot.contourf.html">matplotlib.pyplot.contourf</a>这类工具，依据一定标准的图例，生成出来的一张等值面图，考虑到网络的传输，这种图片得体积小，分辨率就会下降，所以在缩放时候会失真，出现马赛克，矢量瓦片的出现就可以很自然的解决这个问题。</p><p>我会用降水格点数据来代替雷达数据进行了测试，二者数据格式内容上来说都相差不大，且都能用来反映未来降水的趋势。</p><h1 id="开发"><a href="#开发" class="headerlink" title="开发"></a>开发</h1><p>选用了 10 个时次的降水格点数据，格点大小为<code>511*581</code>，经度范围为<code>113.2, 119.4</code>，纬度范围为<code>37.4, 42.7</code>。</p><h2 id="生成-geojson"><a href="#生成-geojson" class="headerlink" title="生成 geojson"></a>生成 geojson</h2><p>这一步其实有很多选择，文章里面也提到了一些，我都做了一些尝试，列一下尝试结果：</p><ul><li><a href="https://github.com/d3/d3-contour">d3-contour</a></li></ul><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> contour = <span class="built_in">require</span>(<span class="string">&quot;d3-contour&quot;</span>).<span class="property">contours</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> levels = [</span><br><span class="line">  <span class="number">0.0</span>,</span><br><span class="line">  <span class="number">0.0001</span>,</span><br><span class="line">  <span class="number">0.5</span>,</span><br><span class="line">  <span class="number">1.0</span>,</span><br><span class="line">  <span class="number">2.0</span>,</span><br><span class="line">  <span class="number">3.0</span>,</span><br><span class="line">  <span class="number">5.0</span>,</span><br><span class="line">  <span class="number">7.0</span>,</span><br><span class="line">  <span class="number">10.0</span>,</span><br><span class="line">  <span class="number">13.0</span>,</span><br><span class="line">  <span class="number">16.0</span>,</span><br><span class="line">  <span class="number">20.0</span>,</span><br><span class="line">  <span class="number">25.0</span>,</span><br><span class="line">  <span class="number">40.0</span>,</span><br><span class="line">  <span class="number">9999.0</span>,</span><br><span class="line">];</span><br><span class="line"><span class="keyword">const</span> lonRange = [<span class="number">113.2</span>, <span class="number">119.4</span>];</span><br><span class="line"><span class="keyword">const</span> latRange = [<span class="number">37.4</span>, <span class="number">42.7</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 生成等值面，data为格点数据数组</span></span><br><span class="line"><span class="keyword">const</span> contours = <span class="title function_">contour</span>().<span class="title function_">size</span>([<span class="number">511</span>, <span class="number">581</span>]).<span class="title function_">thresholds</span>(levels)(data);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> geojson = [];</span><br><span class="line"><span class="comment">// d3用的是平面坐标，需要转换成经纬度</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; contours.<span class="property">length</span>; i++) &#123;</span><br><span class="line">  <span class="keyword">const</span> item = contours[i];</span><br><span class="line">  <span class="keyword">const</span> &#123; coordinates &#125; = item;</span><br><span class="line">  <span class="keyword">if</span> (coordinates.<span class="property">length</span> === <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">continue</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">const</span> coords = coordinates.<span class="title function_">map</span>(<span class="function">(<span class="params">item</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> item.<span class="title function_">map</span>(<span class="function">(<span class="params">ele</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> ele.<span class="title function_">map</span>(<span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">const</span> [x, y] = e;</span><br><span class="line">        <span class="keyword">return</span> [</span><br><span class="line">          lonRange[<span class="number">0</span>] + ((<span class="number">511</span> - x) * (lonRange[<span class="number">1</span>] - lonRange[<span class="number">0</span>])) / <span class="number">511</span>,</span><br><span class="line">          latRange[<span class="number">0</span>] + ((<span class="number">581</span> - y) * (latRange[<span class="number">1</span>] - latRange[<span class="number">0</span>])) / <span class="number">581</span>,</span><br><span class="line">        ];</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;);</span><br><span class="line">  geojson.<span class="title function_">push</span>(&#123;</span><br><span class="line">    <span class="attr">type</span>: <span class="string">&quot;Feature&quot;</span>,</span><br><span class="line">    <span class="attr">geometry</span>: &#123;</span><br><span class="line">      <span class="attr">type</span>: item.<span class="property">type</span>,</span><br><span class="line">      <span class="attr">coordinates</span>: coords,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">properties</span>: &#123;</span><br><span class="line">      <span class="attr">value</span>: item.<span class="property">value</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 最终结果</span></span><br><span class="line"><span class="keyword">const</span> isoFc = &#123;</span><br><span class="line">  <span class="attr">type</span>: <span class="string">&quot;FeatureCollection&quot;</span>,</span><br><span class="line">  <span class="attr">features</span>: geojson,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ul><li><a href="http://turfjs.org/docs/#isobands">turf-isobands</a></li></ul><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> point = <span class="built_in">require</span>(<span class="string">&quot;@turf/helpers&quot;</span>).<span class="property">point</span>;</span><br><span class="line"><span class="keyword">const</span> featureCollection = <span class="built_in">require</span>(<span class="string">&quot;@turf/helpers&quot;</span>).<span class="property">featureCollection</span>;</span><br><span class="line"><span class="keyword">const</span> isobands = <span class="built_in">require</span>(<span class="string">&quot;@turf/isobands&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> levels = [</span><br><span class="line">  <span class="number">0.0</span>,</span><br><span class="line">  <span class="number">0.0001</span>,</span><br><span class="line">  <span class="number">0.5</span>,</span><br><span class="line">  <span class="number">1.0</span>,</span><br><span class="line">  <span class="number">2.0</span>,</span><br><span class="line">  <span class="number">3.0</span>,</span><br><span class="line">  <span class="number">5.0</span>,</span><br><span class="line">  <span class="number">7.0</span>,</span><br><span class="line">  <span class="number">10.0</span>,</span><br><span class="line">  <span class="number">13.0</span>,</span><br><span class="line">  <span class="number">16.0</span>,</span><br><span class="line">  <span class="number">20.0</span>,</span><br><span class="line">  <span class="number">25.0</span>,</span><br><span class="line">  <span class="number">40.0</span>,</span><br><span class="line">  <span class="number">9999.0</span>,</span><br><span class="line">];</span><br><span class="line"><span class="keyword">const</span> lonRange = [<span class="number">113.2</span>, <span class="number">119.4</span>];</span><br><span class="line"><span class="keyword">const</span> latRange = [<span class="number">37.4</span>, <span class="number">42.7</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 把格点数据数组转换成feature类型为point的数组</span></span><br><span class="line"><span class="keyword">const</span> pointArray = [];</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; <span class="number">581</span>; j++) &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">511</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">const</span> lon = lonRange[<span class="number">0</span>] + ((<span class="number">511</span> - i) * (lonRange[<span class="number">1</span>] - lonRange[<span class="number">0</span>])) / <span class="number">511</span>;</span><br><span class="line">    <span class="keyword">const</span> lat = latRange[<span class="number">0</span>] + ((<span class="number">581</span> - j) * (latRange[<span class="number">1</span>] - latRange[<span class="number">0</span>])) / <span class="number">581</span>;</span><br><span class="line">    <span class="comment">// data 是格点数据数组</span></span><br><span class="line">    <span class="keyword">const</span> value = data[j * <span class="number">511</span> + i];</span><br><span class="line">    pointArray.<span class="title function_">push</span>(<span class="title function_">point</span>([lon, lat], &#123; value &#125;));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> gridFc = <span class="title function_">featureCollection</span>(pointArray);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 生成等值面</span></span><br><span class="line"><span class="keyword">const</span> isoFc = <span class="title function_">isobands</span>(gridFc, levels, &#123;</span><br><span class="line">  <span class="attr">zProperty</span>: <span class="string">&quot;value&quot;</span>,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// isoFc 就是生成的geojson内容，保存成文件即可</span></span><br></pre></td></tr></table></figure><p>上面两种方式生成的结果，偶尔会出现的不同级别等值面重叠的情况，这样在前端渲染的时候，由于颜色通道的叠加，会导致重叠部分数据渲染错误的情况，不知道是我使用时候生成方法的弄错还是渲染方法弄错了，效果不太满意。</p><ul><li><strong><a href="https://github.com/bartromgens/geojsoncontour">geojsoncounter（建议）</a></strong></li></ul><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> geojsoncontour</span><br><span class="line"></span><br><span class="line">levels = [<span class="number">0.0</span>, <span class="number">0.0001</span>, <span class="number">0.5</span>, <span class="number">1.0</span>, <span class="number">2.0</span>, <span class="number">3.0</span>, <span class="number">5.0</span>, <span class="number">7.0</span>, <span class="number">10.0</span>, <span class="number">13.0</span>, <span class="number">16.0</span>, <span class="number">20.0</span>, <span class="number">25.0</span>, <span class="number">40.0</span>, <span class="number">9999.0</span>]</span><br><span class="line">size = [<span class="number">511</span>, <span class="number">581</span>]</span><br><span class="line">lon_range = [<span class="number">113.2</span>, <span class="number">119.4</span>]</span><br><span class="line">lat_range = [<span class="number">37.4</span>, <span class="number">42.7</span>]</span><br><span class="line"></span><br><span class="line">lons = np.linspace(lon_range[<span class="number">0</span>], lon_range[<span class="number">1</span>], size[<span class="number">0</span>])</span><br><span class="line">lats = np.linspace(lat_range[<span class="number">0</span>], lat_range[<span class="number">1</span>], size[<span class="number">1</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># pre为格点数据数组</span></span><br><span class="line">data = np.array(pre)</span><br><span class="line"><span class="comment"># 生成等直面图</span></span><br><span class="line">contour = plt.contourf(lons, lats, data, levels=levels)</span><br><span class="line">geojson_filepath = <span class="string">&#x27;output/&#x27;</span></span><br><span class="line"><span class="comment"># 转换成geojson</span></span><br><span class="line">geojsoncontour.contourf_to_geojson(contourf=contour, ndigits=<span class="number">20</span>,geojson_filepath=geojson_filepath)</span><br></pre></td></tr></table></figure><p>实践下来感觉这个方法生成的 geojson 效果会更好，我个人比较推荐使用。原理上就是先生成了传统等值面图片，把像素结果转换成了 geojson 数据。</p><h2 id="合并-geojson（可选）"><a href="#合并-geojson（可选）" class="headerlink" title="合并 geojson（可选）"></a>合并 geojson（可选）</h2><p>这一步很有趣，如果把每个时刻的降水数据做成单独的 geojson，在根据时序进行播放的时候就需要每次移除、加载图层，会有卡顿的效果，而看文章里面墨迹使用了 mapbox 的<code>setFilter</code>功能，所以猜测是对 geojson 进行了合并。</p><p>在 QGIS 中打开了已经生成的 10 个 geojson 进行了合并，可以在属性表里面看到，每个图层的顺序是通过<code>layer</code>字段进行区分的，后面会用上。</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/merge-geojson-20200622155323.png"></p><h2 id="生成矢量瓦片"><a href="#生成矢量瓦片" class="headerlink" title="生成矢量瓦片"></a>生成矢量瓦片</h2><p>使用<a href="https://github.com/mapbox/tippecanoe">mapbox&#x2F;tippecanoe</a>这个官方工具就好了。如果在上面选择了合并 geojson，那么只需要对合并后的单个文件切瓦片就好了，不然就是对每个时刻的数据单独执行以下切瓦片的命令。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tippecanoe -e /Downloads/pbf --no-tile-compression -zg --drop-densest-as-needed -l pre merge.geojson</span><br></pre></td></tr></table></figure><blockquote><p>-e 是生成目录<br>–no-tile-compression 是不对生成的 pbf 进行压缩，否则加载时候会报错<br>-zg –drop-densest-as-needed 是自动去判断瓦片的最大最小级别<br>-l 是手动指定图层名字</p></blockquote><h2 id="展示"><a href="#展示" class="headerlink" title="展示"></a>展示</h2><ul><li>在 leaflet 加载使用插件<a href="https://github.com/Leaflet/Leaflet.VectorGrid">Leaflet.VectorGrid</a>就好</li></ul><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> map = L.<span class="title function_">map</span>(<span class="string">&quot;map&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">center</span>: [<span class="number">40.03193433735177</span>, <span class="number">118.03342979913123</span>],</span><br><span class="line">  <span class="attr">zoom</span>: <span class="number">6</span>,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> vectorTileOptions = &#123;</span><br><span class="line">  <span class="attr">rendererFactory</span>: L.<span class="property">canvas</span>.<span class="property">tile</span>,</span><br><span class="line">  <span class="comment">// 设置生成的pbf的最大zoom</span></span><br><span class="line">  <span class="attr">maxNativeZoom</span>: <span class="number">7</span>,</span><br><span class="line">  <span class="attr">vectorTileLayerStyles</span>: &#123;</span><br><span class="line">    <span class="comment">// pre 是 -l 参数的图层名称</span></span><br><span class="line">    <span class="attr">pre</span>: <span class="keyword">function</span> (<span class="params">properties, zoom</span>) &#123;</span><br><span class="line">      <span class="comment">// 找到属性中对应降水量的属性设置图例</span></span><br><span class="line">      <span class="keyword">const</span> level = <span class="built_in">parseFloat</span>(properties.<span class="property">title</span>).<span class="title function_">toString</span>();</span><br><span class="line">      <span class="keyword">let</span> color = <span class="string">&quot;rgba(0, 0, 0, 0)&quot;</span>;</span><br><span class="line">      <span class="keyword">switch</span> (level) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&quot;0.0001&quot;</span>:</span><br><span class="line">          color = <span class="string">&quot;rgba(153,255,153,255)&quot;</span>;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&quot;0.5&quot;</span>:</span><br><span class="line">          color = <span class="string">&quot;rgba(51,204,102,255)&quot;</span>;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&quot;1&quot;</span>:</span><br><span class="line">          color = <span class="string">&quot;rgba(51,153,0,255)&quot;</span>;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        <span class="comment">// ......</span></span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="attr">weight</span>: <span class="number">1</span>,</span><br><span class="line">        <span class="attr">color</span>: color,</span><br><span class="line">        <span class="attr">opacity</span>: <span class="number">1</span>,</span><br><span class="line">        <span class="attr">fillColor</span>: color,</span><br><span class="line">        <span class="attr">fill</span>: <span class="literal">true</span>,</span><br><span class="line">        <span class="attr">fillOpacity</span>: <span class="number">0.7</span>,</span><br><span class="line">      &#125;;</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> L.<span class="property">vectorGrid</span>.<span class="title function_">protobuf</span>(</span><br><span class="line">  <span class="string">`http://localhost:8080/assets/pbf/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;.pbf`</span>,</span><br><span class="line">  vectorTileOptions</span><br><span class="line">).<span class="title function_">addTo</span>(map);</span><br></pre></td></tr></table></figure><ul><li>在 mapbox 中加载直接定义一个自定义 source，并加载</li></ul><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> map = <span class="keyword">new</span> mapboxgl.<span class="title class_">Map</span>(&#123;</span><br><span class="line">  <span class="attr">container</span>: <span class="string">&quot;map&quot;</span>,</span><br><span class="line">  <span class="attr">style</span>: <span class="string">&quot;mapbox://styles/mapbox/light-v10&quot;</span>,</span><br><span class="line">  <span class="attr">zoom</span>: <span class="number">6</span>,</span><br><span class="line">  <span class="attr">center</span>: [<span class="number">118.03342979913123</span>, <span class="number">40.03193433735177</span>],</span><br><span class="line">  <span class="attr">antialias</span>: <span class="literal">true</span>,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">map.<span class="title function_">on</span>(<span class="string">&quot;load&quot;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  map.<span class="title function_">addSource</span>(<span class="string">&quot;radar-pbf&quot;</span>, &#123;</span><br><span class="line">    <span class="attr">type</span>: <span class="string">&quot;vector&quot;</span>,</span><br><span class="line">    <span class="attr">tiles</span>: [<span class="string">`http://localhost:8080/assets/collection/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;.pbf`</span>],</span><br><span class="line">    <span class="attr">maxzoom</span>: <span class="number">7</span>, <span class="comment">// 设置生成的pbf的最大zoom</span></span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  pbfMap.<span class="title function_">addLayer</span>(&#123;</span><br><span class="line">    <span class="attr">id</span>: <span class="string">&quot;radar-pbf&quot;</span>,</span><br><span class="line">    <span class="attr">type</span>: <span class="string">&quot;fill&quot;</span>,</span><br><span class="line">    <span class="attr">source</span>: <span class="string">&quot;radar-pbf&quot;</span>,</span><br><span class="line">    <span class="attr">filter</span>: [<span class="string">&quot;==&quot;</span>, <span class="string">&quot;layer&quot;</span>, <span class="string">&quot;1&quot;</span>], <span class="comment">// 合并的话可以使用过滤</span></span><br><span class="line">    <span class="string">&quot;source-layer&quot;</span>: <span class="string">&quot;pre&quot;</span>, <span class="comment">// pre 是 -l 参数的图层名称</span></span><br><span class="line">    <span class="attr">layout</span>: &#123;</span><br><span class="line">      <span class="attr">visibility</span>: <span class="string">&quot;visible&quot;</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">paint</span>: &#123;</span><br><span class="line">      <span class="string">&quot;fill-opacity&quot;</span>: <span class="number">0.7</span>,</span><br><span class="line">      <span class="string">&quot;fill-color&quot;</span>: [</span><br><span class="line">        <span class="string">&quot;match&quot;</span>,</span><br><span class="line">        [<span class="string">&quot;get&quot;</span>, <span class="string">&quot;title&quot;</span>],</span><br><span class="line">        [<span class="string">&quot;0.000100 &quot;</span>],</span><br><span class="line">        <span class="string">&quot;rgba(153,255,153,255)&quot;</span>,</span><br><span class="line">        [<span class="string">&quot;0.500000 &quot;</span>],</span><br><span class="line">        <span class="string">&quot;rgba(51,204,102,255)&quot;</span>,</span><br><span class="line">        [<span class="string">&quot;1.000000 &quot;</span>],</span><br><span class="line">        <span class="string">&quot;rgba(51,153,0,255)&quot;</span>,</span><br><span class="line">        <span class="comment">// ......</span></span><br><span class="line">        <span class="string">&quot;rgba(0, 0, 0, 0)&quot;</span>, <span class="comment">// 默认color</span></span><br><span class="line">      ],</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h1 id="待优化"><a href="#待优化" class="headerlink" title="待优化"></a>待优化</h1><p>其实还有个工作没有尝试就是原文里面说的，对两个相邻间隔时间的数据做插值处理，以追求更加平滑的播放效果。个人感觉这个处理对于面向大众的场景比较实用，但在专业方面，有待使用更科学的方法进行处理。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;在&lt;a href=&quot;https://juejin.im/post/5d4e8572e51d453c12504dd4&quot;&gt;四步走，墨迹天气雷达数据可视化指南&lt;/a&gt;这篇文章中，提到了用&lt;strong&gt;矢量瓦片&lt;/strong&gt;代替传统传统栅格图像可视化方案，可以达到更流程的性能，更高清的数据展示，更灵活的样式表达。感觉挺有意思，同时也为了考察这么做的成本，就试着还原做了下。&lt;/p&gt;
&lt;p&gt;从文章来看整个过程总结起来就是准备数据&lt;code&gt;geojson&lt;/code&gt;，切成&lt;code&gt;pbf&lt;/code&gt;或者&lt;code&gt;mbtiles&lt;/code&gt;在 mapbox 中展示。&lt;/p&gt;</summary>
    
    
    
    <category term="WebGIS" scheme="https://kaely.net/categories/WebGIS/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>在web中渲染格点数据探讨（1）</title>
    <link href="https://kaely.net/2020/05/17/web-grid-data-render-1/"/>
    <id>https://kaely.net/2020/05/17/web-grid-data-render-1/</id>
    <published>2020-05-17T08:18:51.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p>在地理信息行业中，我们常遇见的数据中有这么两类：</p><ul><li>一类是站点（散点）数据。地图上分布着位置不均匀的各种站点，比方说气象站，水文站，如果我们需要查看站点数值趋势，就可以通过等值线图等方式，比如<a href="https://github.com/d3/d3-contour">d3 的 contour</a>，<a href="https://turfjs.org/docs/#isolines">turf 的 isolines</a>，还可以通过插值方法将站点插值到网格上，转化成格点数据，后面我们也会讨论。</li><li>也有一类是格点数据。这个在气象上就非常常见，气象预报数据通常是模式跑出来的，如同渔网一样的格点，这种数据提供了查询任意位置数值的作用，它的渲染也同样常见，传统的方法是在服务器端生成好对应图片，在浏览器上贴图，这里追求实现的是<code>前端渲染</code>。</li></ul><span id="more"></span><p>太长不看可以直接<a href="https://github.com/zzcyrus/Leaflet.Tile.PixelLayer">项目仓库</a></p><h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>格点数据的特点往往是数据量大，蕴含的信息量大，不仅包含着某个点的数值，同时还有这个点的经纬度，时间维度信息，空间高度信息，所以我们通常会听见学气象的同学说某个数据有四个维度。随着计算机的发展，图形学的发展，想要全部展示这些时空数据变得越来越可能，我会在这个系列中逐步的提出一些我的探索，希望能给大家带来帮助。</p><p>格点数据有很多格式，我们可能常接触到的一些格点数据文件比方说<a href="https://en.wikipedia.org/wiki/GeoTIFF">GeoTIFF</a>，气象里面有<a href="https://en.wikipedia.org/wiki/GRIB">GRIB</a>，nc 等等，甚至一张简单的 png 也可以是由格点数据转换成的，它会包含基本的<strong>格点数量和值</strong>，比方说在经度方向有 300 个点，纬度方向有 200 个点，那么整个文件包含了 6w 个格点的数值，还通常包含一些<strong>经纬度信息</strong>，比方说每个点的经纬度值，也有可能只是四个顶点的经纬度信息，所以 raster（栅格）图层也在我的讨论范围之内。</p><h1 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h1><p>我会以 Leaflet 为主要的实现手段，个人觉得它的灵活性比较强，功能虽然基础，但是利于拓展，所以各类图层插件也层出不穷。<br>假设我想要在地图上渲染一组格点数据，数据长 300，宽 200，一共 60000 个点，从原理上分析无非就是把一个个的点钉在了地图上，我们可以清楚的<strong>读取或者推算</strong>出每个点的经纬度，要解决的就是点与点之间的空白怎么办，毕竟每个点从经纬度上来看即使只差了 1 度，算上比例尺，在地图上也是不等的像素点，所以我们需要通过<strong>插值计算</strong>来补齐点与点之间的空白。于是我就有了大致如下的想法：</p><blockquote><p>准备基础数据和图例等信息 &#x3D;&gt; 准备 canvas 图层 &#x3D;&gt; 找到初始点 &#x3D;&gt; 根据数值和图例找到对应颜色 &#x3D;&gt; 渲染颜色 &#x3D;&gt; 循环直至画完所有的点</p></blockquote><h1 id="开发"><a href="#开发" class="headerlink" title="开发"></a>开发</h1><p>按照上面的思路一步步开始，我已经准备好了一个<a href="https://github.com/zzcyrus/Leaflet.Tile.PixelLayer/blob/master/demo/data.json">温度相关的数据</a>和图例信息，后面也会再讨论数据的生成和优化，所以直接从第二步开始，这里只说大致的流程，具体的算法可以去看<a href="https://github.com/zzcyrus/Leaflet.Tile.PixelLayer/blob/master/src/index.js">源代码</a></p><blockquote><p>创建 TilePixelLayer 图层并继承基础的<code>TileLayer</code>，需要重写<code>createTile</code>方法，每一个 tile 最终都返回一个我们自己绘制出来的 canvas</p></blockquote><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">L.<span class="property">TilePixelLayer</span> = L.<span class="property">TileLayer</span>.<span class="title function_">extend</span>(&#123;</span><br><span class="line">    <span class="attr">createTile</span>: <span class="keyword">function</span>(<span class="params">coords</span>) &#123;</span><br><span class="line">      <span class="keyword">var</span> dom = L.<span class="property">DomUtil</span>.<span class="title function_">create</span>(<span class="string">&#x27;canvas&#x27;</span>, <span class="string">&#x27;leafvar-pixel-tile&#x27;</span>)</span><br><span class="line">      <span class="keyword">var</span> ctx = dom.<span class="title function_">getContext</span>(<span class="string">&#x27;2d&#x27;</span>)</span><br><span class="line">      <span class="comment">//  bounds为当前tile的一些边界信息，绘制时候要用到</span></span><br><span class="line">      <span class="variable language_">this</span>.<span class="title function_">interpolateTile</span>(ctx,  <span class="string">&#x27;bounds&#x27;</span>)</span><br><span class="line">      <span class="keyword">return</span> dom</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>我们在每一个 tile 对应的 canvas 中，从 0 点开始，分别循环 256 像素的行和列，然后通过插值得到像素对应的数值，再找到数值对应的颜色，直至完成整个 canvas 的上色过程。</p></blockquote><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="attr">interpolateTile</span>: <span class="keyword">function</span>(<span class="params">ctx, bounds</span>) &#123;  <span class="comment">// 逐像素绘制每一个tile</span></span><br><span class="line">  <span class="keyword">var</span> x = bounds.<span class="property">x</span>                 <span class="comment">// x坐标为当前瓦片在地图上的初始像素x</span></span><br><span class="line">  <span class="keyword">var</span> tileX = <span class="number">0</span>                       <span class="comment">// tileX为当前瓦片canvas的坐标，在0~256之间循环</span></span><br><span class="line">  <span class="keyword">var</span> gap = <span class="number">4</span>                        <span class="comment">// 每隔四个点计算一次加快渲染速度，这个数值可以自己设置</span></span><br><span class="line">  <span class="keyword">var</span> colorImageData = <span class="keyword">new</span> <span class="title class_">ImageData</span>(<span class="number">256</span>, <span class="number">256</span>)  <span class="comment">// 创建一个imageData接收颜色</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 循环行</span></span><br><span class="line">  <span class="keyword">var</span> <span class="title function_">batchInterpolate</span> = (<span class="params"></span>) =&gt; &#123;</span><br><span class="line">    <span class="keyword">while</span> (tileX &lt; bounds.<span class="property">w</span>) &#123;</span><br><span class="line">      <span class="title function_">interpolateColumn</span>(x, tileX)</span><br><span class="line">      x += gap</span><br><span class="line">      tileX += gap</span><br><span class="line">    &#125;</span><br><span class="line">    ctx.<span class="title function_">putImageData</span>(colorImageData, <span class="number">0</span>, <span class="number">0</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 循环列</span></span><br><span class="line">  <span class="keyword">var</span> <span class="title function_">interpolateColumn</span> = (<span class="params">x, tileX</span>) =&gt; &#123;</span><br><span class="line">   <span class="keyword">for</span> (                                   <span class="comment">// y坐标为当前瓦片在地图上的初始像素y</span></span><br><span class="line">      <span class="keyword">var</span> defaultColor = [<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>], y = bounds.<span class="property">y</span>, height = <span class="number">0</span>;</span><br><span class="line">      height &lt;= bounds.<span class="property">h</span>;</span><br><span class="line">      y += gap, height += gap</span><br><span class="line">    ) &#123;</span><br><span class="line">      <span class="comment">// 把循环到的某个点的像素坐标转化为经纬度</span></span><br><span class="line">      <span class="keyword">var</span> location = <span class="variable language_">this</span>.<span class="property">map</span>.<span class="title function_">unproject</span>(L.<span class="title function_">point</span>(x, y), zoom)</span><br><span class="line">      <span class="keyword">var</span> coord = [location.<span class="property">lng</span>, location.<span class="property">lat</span>]</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 找到经纬度对应的周围四个点的坐标，通过双线性插值算出一个近似值作为当前像素点的数值</span></span><br><span class="line">      <span class="keyword">var</span> gridValue = <span class="variable language_">this</span>.<span class="title function_">gridInterpolate</span>(coord)</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 用简单的线性插值通过图例得到数值对应的颜色</span></span><br><span class="line">      <span class="keyword">var</span> color = <span class="variable language_">this</span>.<span class="title function_">gradient</span>(gridValue)</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 因为设置了gap，gap范围内的每一个像素点都用这个颜色，如果gap设置为1，那么每个像素都独立参与了计算，性能有所慢，但是精度高</span></span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; gap; i++) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> j = <span class="number">0</span>; j &lt; gap; j++) &#123;</span><br><span class="line">          <span class="variable language_">this</span>.<span class="title function_">setColor</span>(colorImageData, i, j, color)  <span class="comment">// 上色</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 开始绘制</span></span><br><span class="line">  <span class="title function_">batchInterpolate</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>总结一下，每一个瓦片都对应了一个 canvas，每个 canvas 我们都逐像素处理，先把像素转化为经纬度，通过插值得到一个比较精确的对应格点数值，再通过图例进行了上色。最终就把所有的格点数据都渲染了出来。</p></blockquote><h1 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h1><p>完整的代码可以在<a href="https://github.com/zzcyrus/Leaflet.Tile.PixelLayer">github</a>上看到。<code>dist</code>目录下有编译好的 js 文件，在项目中引入就好。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilePixelLayer = L.<span class="title function_">tilePixelLayer</span>(&#123;</span><br><span class="line">  <span class="attr">data</span>: data,</span><br><span class="line">  <span class="attr">overlayAlpha</span>: <span class="number">230</span>, <span class="comment">// 透明度</span></span><br><span class="line">  <span class="attr">gradient</span>: [</span><br><span class="line">    [<span class="number">233.15</span>, [<span class="number">56</span>, <span class="number">4</span>, <span class="number">45</span>]],</span><br><span class="line">    [<span class="number">243.15</span>, [<span class="number">48</span>, <span class="number">0</span>, <span class="number">106</span>]],</span><br><span class="line">    [<span class="number">253.15</span>, [<span class="number">0</span>, <span class="number">14</span>, <span class="number">134</span>]],</span><br><span class="line">    [<span class="number">256.15</span>, [<span class="number">3</span>, <span class="number">44</span>, <span class="number">144</span>]],</span><br><span class="line">    <span class="comment">// ......</span></span><br><span class="line">  ], <span class="comment">// 准备一组图例</span></span><br><span class="line">  <span class="attr">clickEvt</span>: <span class="keyword">function</span> (<span class="params">e, gridValue</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">`该地点温度:<span class="subst">$&#123;<span class="built_in">Math</span>.round(gridValue - <span class="number">273.15</span>)&#125;</span>°`</span>);</span><br><span class="line">  &#125;, <span class="comment">// 事件</span></span><br><span class="line">&#125;);</span><br><span class="line">map.<span class="title function_">addLayer</span>(tilePixelLayer); <span class="comment">// 添加到地图上</span></span><br></pre></td></tr></table></figure><p>效果：</p> <iframe   height=400  width=100%  src="https://kaely.net/Leaflet.Tile.PixelLayer/demo/"   frameborder=0   allowfullscreen> </iframe><p><a href="https://kaely.net/Leaflet.Tile.PixelLayer/demo/">在线 demo</a></p><h1 id="后续"><a href="#后续" class="headerlink" title="后续"></a>后续</h1><p>这样我们已经初步在浏览器上完成了一个前端完全可控的格点数据渲染，当然对整个过程是否有更好的优化方法，我们后面会继续展开讨论。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;在地理信息行业中，我们常遇见的数据中有这么两类：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;一类是站点（散点）数据。地图上分布着位置不均匀的各种站点，比方说气象站，水文站，如果我们需要查看站点数值趋势，就可以通过等值线图等方式，比如&lt;a href=&quot;https://github.com/d3/d3-contour&quot;&gt;d3 的 contour&lt;/a&gt;，&lt;a href=&quot;https://turfjs.org/docs/#isolines&quot;&gt;turf 的 isolines&lt;/a&gt;，还可以通过插值方法将站点插值到网格上，转化成格点数据，后面我们也会讨论。&lt;/li&gt;
&lt;li&gt;也有一类是格点数据。这个在气象上就非常常见，气象预报数据通常是模式跑出来的，如同渔网一样的格点，这种数据提供了查询任意位置数值的作用，它的渲染也同样常见，传统的方法是在服务器端生成好对应图片，在浏览器上贴图，这里追求实现的是&lt;code&gt;前端渲染&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    <category term="WebGIS" scheme="https://kaely.net/categories/WebGIS/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
    <category term="Leaflet" scheme="https://kaely.net/tags/Leaflet/"/>
    
  </entry>
  
  <entry>
    <title>Leaflet.Canvas-Marker-Layer 图层的诞生</title>
    <link href="https://kaely.net/2019/11/18/leaflet-canvas-marker/"/>
    <id>https://kaely.net/2019/11/18/leaflet-canvas-marker/</id>
    <published>2019-11-18T08:49:18.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>之前写过一个基于 Leaflet 的标注点图层插件，<a href="https://github.com/zzcyrus/Leaflet.Canvas-Marker-Layer">Leaflet.Canvas-Marker-Layer</a>，今天简单的说一下为什么需要用到它。在 Leaflet 中如果你需要渲染 marker，有两种方式：</p><ul><li>一种是基于 dom 的<a href="https://leafletjs.com/reference-1.6.0.html#marker">UI-Marker</a></li><li>另一种是基于 canvas 的<a href="https://leafletjs.com/reference-1.6.0.html#circlemarker">CircleMarker</a>。</li></ul><p>可想而知：</p><ul><li>dom 提供了强大的自定义能力，我们可以通过 css 绘制出各种想要的效果，填充各种想要的数据内容，但问题在于 marker 过多就会导致页面特别的卡顿，当然官方也悉心的给出了许多<a href="https://leafletjs.com/plugins.html#clusteringdecluttering">cluster 解决方案</a>，通过聚类或者碰撞检测，只展示一部分内容，根据缩放等级来调整展示内容的数量。</li><li>canvas 则提供了非常高的渲染性能，但不足的是<code>CircleMarker</code>仅仅是在地图上绘制了一个矢量图形，比如圆，矩形等，灵活性比较差，通常是通过点击事件或 popup 来展示具体的内容。</li></ul><h1 id="起源"><a href="#起源" class="headerlink" title="起源"></a>起源</h1><p>那么如果我想两者兼得呢？实际上 canvas 本身的功能是很强大的，可以绘制各种图案，甚至各种颜色的文字，所以我们要做的其实就是拓展官方<code>CircleMarker</code>的功能，让它自定义程度更高，有点类似于<a href="https://leafletjs.com/reference-1.6.0.html#divicon">DivIcon</a>的概念。</p><p>在 github 上搜寻类似功能时候，我找到了<a href="https://github.com/eJuke/Leaflet.Canvas-Markers">eJuke 的 Leaflet.Canvas-Markers</a>这个库，和我想要的功能基本类似，采用 canvas 来绘制大量的 maker，但是他只提供了图片类型的 marker，所以我们稍加改动就好。</p><h1 id="源码说明"><a href="#源码说明" class="headerlink" title="源码说明"></a>源码说明</h1><p><code>ejuke</code>的库对于图层创建、事件处理已经比较完善，我们只要修改下<code>_drawMarker</code>方法就好：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="attr">_drawMarker</span>: <span class="keyword">function</span> (<span class="params">marker, pointPos</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> self = <span class="variable language_">this</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!<span class="variable language_">this</span>.<span class="property">_imageLookup</span>) <span class="variable language_">this</span>.<span class="property">_imageLookup</span> = &#123;&#125;</span><br><span class="line">    <span class="keyword">if</span> (!pointPos) &#123;</span><br><span class="line">      pointPos = self.<span class="property">_map</span>.<span class="title function_">latLngToContainerPoint</span>(marker.<span class="title function_">getLatLng</span>())</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 支持通过userDrawFunc返回layer（this）对象给用户</span></span><br><span class="line">    <span class="comment">// 通过layer._context就可以画出任何自定义的marker</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">userDrawFunc</span> &amp;&amp; <span class="keyword">typeof</span> (<span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">userDrawFunc</span>) === <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">      <span class="keyword">const</span> size = marker.<span class="property">options</span>.<span class="property">icon</span>.<span class="property">options</span>.<span class="property">iconSize</span></span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">options</span>.<span class="title function_">userDrawFunc</span>(<span class="variable language_">this</span>, marker, pointPos, size)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      self.<span class="title function_">_drawIcon</span>(marker, pointPos)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="使用说明"><a href="#使用说明" class="headerlink" title="使用说明"></a>使用说明</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** userDrawFunc提供的参数说明</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> layer      图层对象，layer._context即canvas的context</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> marker     当前marker上存储的信息</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> pointPos   当前marker的像素坐标</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> size       当前marker的大小</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 你可以简单的画一个圆</span></span><br><span class="line"><span class="keyword">var</span> layer = L.<span class="title function_">canvasMarkerLayer</span>(&#123;</span><br><span class="line">  <span class="attr">userDrawFunc</span>: <span class="keyword">function</span> (<span class="params">layer, marker, pointPos, size</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> ctx = layer.<span class="property">_context</span>;</span><br><span class="line">    ctx.<span class="title function_">beginPath</span>();</span><br><span class="line">    ctx.<span class="title function_">arc</span>(pointPos.<span class="property">x</span>, pointPos.<span class="property">y</span>, size[<span class="number">0</span>] / <span class="number">2</span>, <span class="number">0</span>, <span class="number">2</span> * <span class="title class_">Math</span>.<span class="property">PI</span>);</span><br><span class="line">    ctx.<span class="property">fillStyle</span> = <span class="string">&quot;rgba(255,12,0,0.4)&quot;</span>;</span><br><span class="line">    ctx.<span class="title function_">fill</span>();</span><br><span class="line">    ctx.<span class="title function_">closePath</span>();</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;).<span class="title function_">addTo</span>(map);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 你也可以画一个矩形里面再配上文字</span></span><br><span class="line"><span class="keyword">const</span> ciLayer = L.<span class="title function_">canvasMarkerLayer</span>(&#123;</span><br><span class="line">  <span class="attr">userDrawFunc</span>: <span class="keyword">function</span> (<span class="params">layer, marker, pointPos, size</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> ctx = layer.<span class="property">_context</span>;</span><br><span class="line">    <span class="keyword">const</span> number = marker.<span class="property">properties</span>.<span class="property">number</span>;</span><br><span class="line">    ctx.<span class="title function_">beginPath</span>();</span><br><span class="line">    ctx.<span class="property">fillStyle</span> = <span class="string">&quot;rgba(255,0,0,0.8)&quot;</span>;</span><br><span class="line">    ctx.<span class="title function_">fillRect</span>(</span><br><span class="line">      pointPos.<span class="property">x</span> - size[<span class="number">0</span>] / <span class="number">2</span>,</span><br><span class="line">      pointPos.<span class="property">y</span> - size[<span class="number">1</span>] / <span class="number">2</span>,</span><br><span class="line">      size[<span class="number">0</span>],</span><br><span class="line">      size[<span class="number">1</span>]</span><br><span class="line">    );</span><br><span class="line">    ctx.<span class="property">font</span> = <span class="string">&quot;12px Helvetica Neue&quot;</span>;</span><br><span class="line">    ctx.<span class="property">fillStyle</span> = <span class="string">&quot;#000&quot;</span>;</span><br><span class="line">    ctx.<span class="title function_">fillText</span>(number, pointPos.<span class="property">x</span>, pointPos.<span class="property">y</span> + size[<span class="number">1</span>] / <span class="number">4</span>);</span><br><span class="line">    ctx.<span class="property">textAlign</span> = <span class="string">&quot;center&quot;</span>;</span><br><span class="line">    ctx.<span class="title function_">closePath</span>();</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;).<span class="title function_">addTo</span>(map);</span><br></pre></td></tr></table></figure><p><a href="https://kaely.net/Leaflet.Canvas-Marker-Layer/example/text.html">demo</a></p><p>通过这样在 canvas 上绘制大量 marker 的效果就大大改善了。</p><p><a href="https://github.com/zzcyrus/Leaflet.Canvas-Marker-Layer">仓库</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;之前写过一个基于 Leaflet 的标注点图层插件，&lt;a href=&quot;https://github.com/zzcyrus/Leaflet.Canvas-Marker-Layer&quot;&gt;Leaflet.Canvas-Marker-Layer&lt;/a&gt;，今天简单的说一下为什么需要用</summary>
      
    
    
    
    <category term="WebGIS" scheme="https://kaely.net/categories/WebGIS/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
    <category term="Leaflet" scheme="https://kaely.net/tags/Leaflet/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata 插件发开指南</title>
    <link href="https://kaely.net/2019/07/22/tilestrata-plugin/"/>
    <id>https://kaely.net/2019/07/22/tilestrata-plugin/</id>
    <published>2019-07-22T10:16:33.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata">Tilestrata</a>的插件开发基本围绕着生命周期各个阶段展开，如果要开发一个自己的插件，首先想清楚插件的功能属于生命周期的哪个阶段，依据官方规定好的该阶段代码结构，写好插件内容，在服务端利用<code>use</code>进行注册就好。个人觉得<code>Tilestrata</code>吸引人的一个地方就是在于插件机制非常的灵活和便捷，十分好上手。</p><span id="more"></span><p>这篇文章主要基于官方文档说明。</p><p>生命周期的各个阶段如下：</p><ul><li><code>request hook</code> – 请求预处理</li><li><code>cache</code> – 缓存控制</li><li><code>provider</code> – 数据源，比如 mapnik</li><li><code>transform</code> – 最关键的阶段，可以在瓦片输出的过程中对其做各种加工，比如旋转，染色</li><li><code>response hook</code> – 响应后处理，比如 headers、jsonp 插件</li></ul><h1 id="1-插件共有部分"><a href="#1-插件共有部分" class="headerlink" title="1. 插件共有部分"></a>1. 插件共有部分</h1><p>所有的插件都可以拥有<code>init</code>和<code>destroy</code>两个生命周期方法，分别在插件的启动和结束时触发。这个两个方法的第一个参数是当前<code>tilestrata</code>实例，第二个参数是<code>callback</code>回调函数。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="attr">init</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">    <span class="title function_">callback</span>(err);</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure><h1 id="2-request-请求插件"><a href="#2-request-请求插件" class="headerlink" title="2. request 请求插件"></a>2. request 请求插件</h1><p>request 类型的插件必须提供<code>reqhook</code>方法，参数如下。其中<code>req</code>参数代表<a href="http://nodejs.org/api/http.html#http_http_incomingmessage">http.IncomingMessage</a> ，<code>res</code>参数代表<a href="http://nodejs.org/api/http.html#http_class_http_serverresponse">http.ServerResponse</a>。 如果不执行里面的<code>callback</code>回调函数，甚至这个请求都不会进入后续<code>server</code>的处理逻辑中，所以千万别忘了它。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = <span class="keyword">function</span>(<span class="params">options</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;myplugin&#x27;</span>,</span><br><span class="line">    <span class="attr">init</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    <span class="attr">reqhook</span>: <span class="keyword">function</span>(<span class="params">server, tile, req, res, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>()</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">destroy</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="3-Caches-缓存插件"><a href="#3-Caches-缓存插件" class="headerlink" title="3. Caches 缓存插件"></a>3. Caches 缓存插件</h1><p>Caches 类型的插件必须提供<code>get</code>和<code>set</code>方法，参数分别如下。当你的缓存过程失败出错的时候，tilestrata 会忽略错误，无视这段逻辑，尝试直接从数据源获取数据。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = <span class="keyword">function</span>(<span class="params">options</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;myplugin&#x27;</span>,</span><br><span class="line">    <span class="attr">init</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    <span class="attr">get</span>: <span class="keyword">function</span>(<span class="params">server, tile, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err, buffer, headers <span class="comment">/* refresh */</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    <span class="attr">set</span>: <span class="keyword">function</span>(<span class="params">server, tile, buffer, headers, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">destroy</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>有一种特殊的场景：虽然请求命中了缓存，仍然需要在后台生成一张新的瓦片。比方说某一张瓦片已经很久没更新了，需要重新生成，但又不是那么着急，一定要用户等着这个新瓦片渲染出来，所以其实我们需要的是返回缓存结果，同时更新瓦片缓存。这种情况下为<code>get()</code>方法的<code>callback</code>增加第四个参数<code>true</code>就好了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title function_">callback</span>(<span class="literal">null</span>, buffer, headers, <span class="literal">true</span>)</span><br></pre></td></tr></table></figure><h1 id="4-Providers-数据源插件"><a href="#4-Providers-数据源插件" class="headerlink" title="4. Providers 数据源插件"></a>4. Providers 数据源插件</h1><p>Providers 类型的插件必须提供<code>serve</code>方法，参数如下。主要作用就是提供<strong>数据源</strong>，数据可以来源于第本地的某个功能模块比方说 mapnik 服务，可以来源于在线的第三方服务，比如说对 bing 地图服务做了一个代理转发。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = <span class="keyword">function</span>(<span class="params">options</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;myplugin&#x27;</span>,</span><br><span class="line">    <span class="attr">init</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    <span class="attr">serve</span>: <span class="keyword">function</span>(<span class="params">server, tile, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err, buffer, headers)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">destroy</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="5-Transforms-数据处理插件"><a href="#5-Transforms-数据处理插件" class="headerlink" title="5. Transforms 数据处理插件"></a>5. Transforms 数据处理插件</h1><p>Transforms 类型的插件必须提供<code>transform</code>方法，参数如下。它的主要作用是在数据从 provider 取出来之后，做一些额外的处理，比方说加一个水印，调整一下透明度，再把处理后的结果输出出去。要注意下这个过程是发生在<strong>缓存之前</strong>的。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = <span class="keyword">function</span>(<span class="params">options</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;myplugin&#x27;</span>,</span><br><span class="line">    <span class="attr">init</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    <span class="attr">transform</span>: <span class="keyword">function</span>(<span class="params">server, tile, buffer, headers, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err, buffer, headers)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">destroy</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="6-Response-响应插件"><a href="#6-Response-响应插件" class="headerlink" title="6. Response 响应插件"></a>6. Response 响应插件</h1><p>Response 类型的插件必须提供<code>reshook</code>方法，参数如下，其中参数里面的<code>result</code>包含了三个属性：<code>headers</code>, <code>buffer</code>和 <code>status</code>，这三个属性在返回真正的结果之前都是可以被修改的。比方说在返回结果之前，给每一个<code>headers</code>都加上<code>&#39;Access-Control-Allow-Origin&#39;: &#39;*&#39;</code>来让请求支持跨域访问。此外<code>req</code>参数代表<a href="http://nodejs.org/api/http.html#http_http_incomingmessage">http.IncomingMessage</a> ，<code>res</code>参数代表<a href="http://nodejs.org/api/http.html#http_class_http_serverresponse">http.ServerResponse</a>。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = <span class="keyword">function</span>(<span class="params">options</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;myplugin&#x27;</span>,</span><br><span class="line">    <span class="attr">init</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 我是必须提供的核心方法</span></span><br><span class="line">    <span class="attr">reshook</span>: <span class="keyword">function</span>(<span class="params">server, tile, req, res, result, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>()</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">destroy</span>: <span class="keyword">function</span>(<span class="params">server, callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="7-同时提供多个功能的插件"><a href="#7-同时提供多个功能的插件" class="headerlink" title="7. 同时提供多个功能的插件"></a>7. 同时提供多个功能的插件</h1><p>有时候一个插件由多个部分组成，所以你可以把多个功能放进一个数组传递给 TileStrata。例如我想做一个插件记录响应时间，那既要在 request 时做出记录，也要在 response 时做出记录，把它俩放进数组就 ok 了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> [</span><br><span class="line">        <span class="comment">//  第一个插件</span></span><br><span class="line">        &#123;<span class="attr">name</span>: <span class="string">&#x27;myplugin&#x27;</span>, <span class="attr">reqhook</span>: <span class="keyword">function</span>(<span class="params">...</span>) &#123; <span class="comment">/* ... */</span> &#125;&#125;,</span><br><span class="line">        <span class="comment">//  第一个插件</span></span><br><span class="line">        &#123;<span class="attr">name</span>: <span class="string">&#x27;myplugin&#x27;</span>, <span class="attr">reshook</span>: <span class="keyword">function</span>(<span class="params">...</span>) &#123; <span class="comment">/* ... */</span> &#125;&#125;</span><br><span class="line">    ];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>通观下来，整个 Tilestrata 插件开发的流程其实是非常舒畅和自由的，可以充分的发挥创造力。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata&quot;&gt;Tilestrata&lt;/a&gt;的插件开发基本围绕着生命周期各个阶段展开，如果要开发一个自己的插件，首先想清楚插件的功能属于生命周期的哪个阶段，依据官方规定好的该阶段代码结构，写好插件内容，在服务端利用&lt;code&gt;use&lt;/code&gt;进行注册就好。个人觉得&lt;code&gt;Tilestrata&lt;/code&gt;吸引人的一个地方就是在于插件机制非常的灵活和便捷，十分好上手。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata 入门指南</title>
    <link href="https://kaely.net/2019/07/18/tilestrata-intro/"/>
    <id>https://kaely.net/2019/07/18/tilestrata-intro/</id>
    <published>2019-07-18T10:13:53.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p>简单来说<a href="https://github.com/naturalatlas/tilestrata"><strong>Tilestrata</strong></a>就是一个瓦片地图服务器。根据其自身的介绍，它有如下的特点：零配置，简洁，测试覆盖率高，性能高，拓展性强。可以通过<a href="https://github.com/naturalatlas/tilestrata-balancer">TileStrata Balancer</a>实现负载均衡，无缝伸缩。可以通过内置<a href="https://github.com/naturalatlas/tilestrata#profiling--debugging-performance">控制台</a>去调试查看渲染时间。</p><span id="more"></span><p>本文主要基于官方的文档介绍及部分使用心得，旨在帮助大家快速上手，进阶使用会后续说明。</p><p>安装：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata --save</span><br></pre></td></tr></table></figure><h2 id="1-主要结构"><a href="#1-主要结构" class="headerlink" title="1. 主要结构"></a>1. 主要结构</h2><p>TileStrata 的结构上有以下五个主要组成部分，通常用来实现各种插件，我个人习惯把它们看作是生命周期流程，正是由于这些 hook 的存在，我们就可以在 TileStrata 生命周期的各个阶段去实现各种巧妙的功能：</p><ul><li><code>request hook</code> – 请求预处理</li><li><code>cache</code> – 缓存控制</li><li><code>provider</code> – 数据源，比如 mapnik</li><li><code>transform</code> – 最关键的阶段，可以在瓦片输出的过程中对其做各种加工，比如旋转，染色</li><li><code>response hook</code> – 响应后处理，比如 headers、jsonp 插件</li></ul><p>官方提供了一张很直观的流程图：</p><p><img src="https://camo.githubusercontent.com/67abe0f563502d78ef12b4ce20cba57222dfcf6f/68747470733a2f2f63646e2e7261776769742e636f6d2f6e61747572616c61746c61732f74696c657374726174612f6d61737465722f6d6973632f67726170686963732f726571666c6f772e737667"></p><h2 id="2-常用插件"><a href="#2-常用插件" class="headerlink" title="2. 常用插件"></a>2. 常用插件</h2><ul><li><a href="https://github.com/naturalatlas/tilestrata-mapnik">tilestrata-mapnik</a> – 使用 <a href="http://mapnik.org/">mapnik</a>做数据源进行渲染</li><li><a href="https://github.com/naturalatlas/tilestrata-disk">tilestrata-disk</a> – 服务端缓存插件</li><li><a href="https://github.com/naturalatlas/tilestrata-dependency">tilestrata-dependency</a> – 直接使用其他定义好的图层来获取数据，而不是定义一个 provider</li><li><a href="https://github.com/naturalatlas/tilestrata-sharp">tilestrata-sharp</a> – 用 <a href="https://www.npmjs.com/package/sharp">libvips</a>来做一些图片的处理，比如压缩，旋转，转换类型</li><li><a href="https://github.com/naturalatlas/tilestrata-gm">tilestrata-gm</a> – 使用 <a href="https://www.npmjs.com/package/gm">GraphicsMagick</a>来对图片做处理，大体上和上面类似</li><li><a href="https://github.com/naturalatlas/tilestrata-headers">tilestrata-headers</a> – 设置请求的响应头，可以解决跨域问题</li><li><a href="https://github.com/naturalatlas/tilestrata-blend">tilestrata-blend</a> – 合成多个瓦片图层为一个图层，比方组合道路+建筑为一个图层</li><li><a href="https://github.com/naturalatlas/tilestrata-jsonp">tilestrata-jsonp</a> – 使 utfgrids 类的 json 数据支持 jsonp 请求方式</li><li><a href="https://github.com/naturalatlas/tilestrata-datadog">tilestrata-datadog</a> – 继承数据到<a href="https://www.datadoghq.com/">Datadog</a>服务</li><li><a href="https://github.com/naturalatlas/tilestrata-utfmerge">tilestrata-utfmerge</a> – 合并两个 utfgrids 类图层为一个图层</li><li><a href="https://github.com/naturalatlas/tilestrata-vtile">tilestrata-vtile</a> – 输出 mapnik 数据为矢量切片 pbf 格式</li><li><a href="https://github.com/naturalatlas/tilestrata-vtile-raster">tilestrata-vtile-raster</a> – 将上面的 pbf 的是瓦片转换成栅格瓦片</li><li><a href="https://github.com/naturalatlas/tilestrata-vtile-composite">tilestrata-vtile-composite</a> – 合并多个矢量瓦片图层</li><li><a href="https://github.com/naturalatlas/tilestrata-proxy">tilestrata-proxy</a> – 为其他瓦片服务器提供代理</li><li><a href="https://github.com/naturalatlas/tilestrata-lru">tilestrata-lru</a> – 在内存中缓存瓦片数据</li><li><a href="https://github.com/naturalatlas/tilestrata-etag">tilestrata-etag</a> – 让瓦片图层支持自定义 ETag 标签，一般用来处理 http 缓存</li><li><a href="https://github.com/naturalatlas/tilestrata-bing">tilestrata-bing</a> – Bing 地图的 provider</li><li><a href="https://github.com/naturalatlas/tilestrata-underzoom">tilestrata-underzoom</a> - Build mosaics of higher-zoom tiles</li><li><a href="https://github.com/Stezii/tilestrata-postgismvt">tilestrata-postgismvt</a> – 使用 PostGIS 数据库输出 mvt 格式的瓦片</li><li><a href="https://github.com/naturalatlas/tilestrata-postgis-geojson-tiles">tilestrata-postgis-geojson-tiles</a> – 使用 PostGIS 数据库输出 GeoJSON 格式的瓦片</li></ul><h2 id="3-基本使用"><a href="#3-基本使用" class="headerlink" title="3. 基本使用"></a>3. 基本使用</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">var</span> disk = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-disk&#x27;</span>)</span><br><span class="line"><span class="keyword">var</span> sharp = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-sharp&#x27;</span>)</span><br><span class="line"><span class="keyword">var</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"><span class="keyword">var</span> dependency = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-dependency&#x27;</span>)</span><br><span class="line"><span class="keyword">var</span> strata = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个图层</span></span><br><span class="line">strata</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;layerName&#x27;</span>)</span><br><span class="line">  <span class="comment">// 设置第一个瓦片路径</span></span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tileName.png&#x27;</span>)</span><br><span class="line">  <span class="comment">// 设置缓存目录</span></span><br><span class="line">  .<span class="title function_">use</span>(disk.<span class="title function_">cache</span>(&#123; <span class="attr">dir</span>: <span class="string">&#x27;/var/lib/tiles/basemap&#x27;</span> &#125;))</span><br><span class="line">  <span class="comment">// 设置数据源mapnik</span></span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;/path/to/map.xml&#x27;</span>,</span><br><span class="line">      <span class="attr">tileSize</span>: <span class="number">512</span>,</span><br><span class="line">      <span class="attr">scale</span>: <span class="number">2</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  <span class="comment">// 设置第二个瓦片路径</span></span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tileSharp.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(disk.<span class="title function_">cache</span>(&#123; <span class="attr">dir</span>: <span class="string">&#x27;/var/lib/tiles/basemap&#x27;</span> &#125;))</span><br><span class="line">  <span class="comment">// 用dependency插件直接使用layerName的源</span></span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">dependency</span>(<span class="string">&#x27;layerName&#x27;</span>, <span class="string">&#x27;tileName.png&#x27;</span>))</span><br><span class="line">  <span class="comment">// 用sharp插件resize每个瓦片的大小</span></span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">sharp</span>(<span class="keyword">function</span>(<span class="params">image, sharp</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> image.<span class="title function_">resize</span>(<span class="number">256</span>)</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">strata.<span class="title function_">listen</span>(<span class="number">8080</span>)</span><br></pre></td></tr></table></figure><p>服务启动后，可以通过下面的地址来访问瓦片数据：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/layerName/:z/:x:/:y/tileName.png</span><br></pre></td></tr></table></figure><h3 id="3-1-不定义瓦片名称"><a href="#3-1-不定义瓦片名称" class="headerlink" title="3.1 不定义瓦片名称"></a>3.1 不定义瓦片名称</h3><p>从 <a href="https://github.com/naturalatlas/tilestrata/releases/tag/v2.1.0">2.1.0</a>版本开始, 在设置<code>route</code>名称的时候，可以直接采用下面的形式，更加符合通用的瓦片格式：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">.<span class="title function_">route</span>(<span class="string">&#x27;*.png&#x27;</span>) <span class="comment">// /layer/0/0/0.png</span></span><br><span class="line">.<span class="title function_">route</span>(<span class="string">&#x27;*@2x.png&#x27;</span>) <span class="comment">// /layer/0/0/0@2x.png</span></span><br></pre></td></tr></table></figure><h3 id="3-2-整合-Express-js-x2F-Connect"><a href="#3-2-整合-Express-js-x2F-Connect" class="headerlink" title="3.2 整合 Express.js &#x2F; Connect"></a>3.2 整合 <a href="http://expressjs.com/">Express.js</a> &#x2F; <a href="https://github.com/senchalabs/connect">Connect</a></h3><p>TileStrata 可以定义成 Express 的一个中间件去使用，不必通过执行<code>strata.listen(8080)</code>来启动一个 server 服务，</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">var</span> strata = <span class="title function_">tilestrata</span>()</span><br><span class="line">strata.<span class="title function_">layer</span>(<span class="string">&#x27;basemap&#x27;</span>) <span class="comment">/* ... */</span></span><br><span class="line">strata.<span class="title function_">layer</span>(<span class="string">&#x27;contours&#x27;</span>) <span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line">app.<span class="title function_">use</span>(</span><br><span class="line">  tilestrata.<span class="title function_">middleware</span>(&#123;</span><br><span class="line">    <span class="attr">server</span>: strata,</span><br><span class="line">    <span class="attr">prefix</span>: <span class="string">&#x27;/maps&#x27;</span></span><br><span class="line">  &#125;)</span><br><span class="line">)</span><br></pre></td></tr></table></figure><h2 id="4-使用说明"><a href="#4-使用说明" class="headerlink" title="4. 使用说明"></a>4. 使用说明</h2><h3 id="4-1-支持-Metatile-的负载均衡和图层共享"><a href="#4-1-支持-Metatile-的负载均衡和图层共享" class="headerlink" title="4.1 支持 Metatile 的负载均衡和图层共享"></a>4.1 支持 Metatile 的负载均衡和图层共享</h3><p>TileStrata 从 <a href="https://github.com/naturalatlas/tilestrata/releases/tag/v2.0.0">2.0.0</a> 版本开始支持集成<a href="https://github.com/naturalatlas/tilestrata-balancer">TileStrata Balancer</a>, 这是一个专门针对瓦片服务中的<a href="http://wiki.openstreetmap.org/wiki/Meta_tiles">metatiles</a>设计的弹性负载均衡器，一般的负载均衡器并没有<code>metatiles</code>这个概念，所以会简单的把瓦片请求的分散到多个服务器，这样会导致冗余的渲染，不仅速度慢还会造成服务器资源浪费。</p><p>需要额外注意的是，均衡器并不会假设负载池中的所有服务器都提供了相同的图层， 它会持续追踪每个节点上提供的所有的图层，因此他知道某个图层明确的访问路径。</p><ul><li><strong>Fully elastic</strong> （最小化配置）</li><li><strong>Consistent routing</strong> (提高本地缓存命中率)</li><li><strong>Metatile-aware</strong> (防止冗余渲染)</li><li><strong>Layer-aware</strong> (支持图层的异构分布)</li></ul><p><a href="https://github.com/naturalatlas/tilestrata-balancer"><strong>TileStrata Balancer 详细文档 →</strong></a></p><h3 id="4-2-重建瓦片缓存"><a href="#4-2-重建瓦片缓存" class="headerlink" title="4.2 重建瓦片缓存"></a>4.2 重建瓦片缓存</h3><p>在更新了图层的样式或者数据源后，你需要更新瓦片。这时候不必立刻删掉所有旧缓存，可以在请求头中设置<code>X-TileStrata-SkipCache</code>，逐步请求这些瓦片让服务器重新构建它们。<a href="https://github.com/naturalatlas/tilemantle">TileMantle</a>工作可以让这个过程十分简单：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">npm install -g tilemantle</span><br><span class="line">tilemantle http://myhost.com/mylayer/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;/t.png \</span><br><span class="line">    -p 44.9457507,-109.5939822 -b 30mi -z 10-14 \</span><br><span class="line">    -H <span class="string">&quot;X-TileStrata-SkipCache:mylayer/t.png&quot;</span></span><br></pre></td></tr></table></figure><p>如果你使用了 <a href="https://github.com/naturalatlas/tilestrata-dependency">tilestrata-dependency</a> 插件, 请求头则需要设置成如下形式：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">X-TileStrata-SkipCache:*</span><br><span class="line">X-TileStrata-SkipCache:[layer]/[file],[layer]/[file],...</span><br></pre></td></tr></table></figure><p>在一些高级的使用场景中，为了确保瓦片在缓存实际写入文件系统之前不要从服务器返回请求，需要额外做如下设置：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">X-TileStrata-CacheWait:1</span><br></pre></td></tr></table></figure><h3 id="4-1-健康检查"><a href="#4-1-健康检查" class="headerlink" title="4.1 健康检查"></a>4.1 健康检查</h3><p>TileStrata 包含一个名为<code>/health</code>内置路径，如果服务器连接正常，你会收到<code>200 OK</code>的返回值。这可以用来自定义一些错误和成功提醒，设置想要的返回值。只要在初始化 tilestrata 时，给选项中的 healthy 设置一个回调函数即可。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 不健康状态</span></span><br><span class="line"><span class="keyword">var</span> strata = <span class="title function_">tilestrata</span>(&#123;</span><br><span class="line">  <span class="attr">healthy</span>: <span class="keyword">function</span>(<span class="params">callback</span>) &#123;</span><br><span class="line">    <span class="title function_">callback</span>(<span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;CPU is too high&#x27;</span>), &#123; <span class="attr">loadavg</span>: <span class="number">3</span> &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 错误信息&#x27;CPU is too high&#x27;会包装在message字段中返回</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 健康状态</span></span><br><span class="line"><span class="keyword">var</span> strata = <span class="title function_">tilestrata</span>(&#123;</span><br><span class="line">  <span class="attr">healthy</span>: <span class="keyword">function</span>(<span class="params">callback</span>) &#123;</span><br><span class="line">    <span class="title function_">callback</span>(<span class="literal">null</span>, &#123; <span class="attr">loadavg</span>: <span class="number">1</span> &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确的返回值中将会额外的多一个loadavg字段，这将是你自定义的内容</span></span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;简单来说&lt;a href=&quot;https://github.com/naturalatlas/tilestrata&quot;&gt;&lt;strong&gt;Tilestrata&lt;/strong&gt;&lt;/a&gt;就是一个瓦片地图服务器。根据其自身的介绍，它有如下的特点：零配置，简洁，测试覆盖率高，性能高，拓展性强。可以通过&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-balancer&quot;&gt;TileStrata Balancer&lt;/a&gt;实现负载均衡，无缝伸缩。可以通过内置&lt;a href=&quot;https://github.com/naturalatlas/tilestrata#profiling--debugging-performance&quot;&gt;控制台&lt;/a&gt;去调试查看渲染时间。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-disk 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/15/tilestrata-disk/"/>
    <id>https://kaely.net/2019/07/15/tilestrata-disk/</id>
    <published>2019-07-15T13:39:59.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-disk">tilestrata-disk</a>插件可以在磁盘中存储&#x2F;检索瓦片。它可以兼顾瓦片缓存和瓦片提供两个功能，代码虽简单，但是功能十分全面，个人感觉非常的实用，很值得推荐。</p><p>用它来缓存瓦片时，一定要为每个图层使用不同的目录（例如：”tiles&#x2F;layer_a”, “tiles&#x2F;layer_b”）。如果设置了<code>maxage</code>参数，插件会检查瓦片的最近一次修改时间，如果间隔过长，将返回<code>null</code>。如果 设置<code>maxage=0</code>，就相当于没有开启缓存功能。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-disk --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> disk = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-disk&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_merc&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  <span class="comment">// 直接缓存</span></span><br><span class="line">  .<span class="title function_">use</span>(disk.<span class="title function_">cache</span>(&#123; <span class="attr">dir</span>: <span class="string">&#x27;tilecache/world&#x27;</span> &#125;))</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/world.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;province&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  <span class="comment">// 不同图层使用不同目录，同时设置最大缓存时间3600秒</span></span><br><span class="line">  .<span class="title function_">use</span>(disk.<span class="title function_">cache</span>(&#123; <span class="attr">maxage</span>: <span class="number">3600</span>, <span class="attr">dir</span>: <span class="string">&#x27;tilecache/province&#x27;</span> &#125;))</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/province.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><p>此外，disk 插件的功能远不止如此：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设置maxage为function，控制不同层级的过期时间</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;mylayer&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="comment">/* some provider */</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    disk.<span class="title function_">cache</span>(&#123;</span><br><span class="line">      <span class="attr">dir</span>: <span class="string">&#x27;./tiles/mylayer&#x27;</span>,</span><br><span class="line">      <span class="attr">maxage</span>: <span class="keyword">function</span>(<span class="params">server, req</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (req.<span class="property">z</span> &gt; <span class="number">15</span>) <span class="keyword">return</span> <span class="number">0</span> <span class="comment">// 不缓存</span></span><br><span class="line">        <span class="keyword">if</span> (req.<span class="property">z</span> &gt; <span class="number">13</span>) <span class="keyword">return</span> <span class="number">3600</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">3600</span> * <span class="number">24</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以通过path自定义缓存的结构和文件名</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;mylayer&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="comment">/* some provider */</span>)</span><br><span class="line">  .<span class="title function_">use</span>(disk.<span class="title function_">cache</span>(&#123; <span class="attr">path</span>: <span class="string">&#x27;./tiles/&#123;layer&#125;/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;-&#123;filename&#125;&#x27;</span> &#125;))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过callback的方式来设置</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;mylayer&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="comment">/* some provider */</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    disk.<span class="title function_">cache</span>(&#123;</span><br><span class="line">      <span class="attr">path</span>: <span class="keyword">function</span>(<span class="params">tile</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&#x27;./tiles/&#x27;</span> + tile.<span class="property">layer</span> + <span class="string">&#x27;/&#x27;</span> + tile.<span class="property">z</span> + <span class="string">&#x27;/&#x27;</span> <span class="comment">/* ... */</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 直接使用缓存下来的文件当作provider，这样就不调用服务器生成图片了</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;mylayer&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(disk.<span class="title function_">provider</span>(<span class="string">&#x27;/path/to/dir/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;/file.png&#x27;</span>))</span><br></pre></td></tr></table></figure><p>甚至在<code>0.6.0</code>及其以上的 tilestrata 版本中，你还可以通过设定<code>refreshage</code>来控制缓存数据的寿命，这个参数需要与<code>maxage</code>一起设设置：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">.<span class="title function_">use</span>(disk.<span class="title function_">cache</span>(&#123;</span><br><span class="line">    <span class="attr">dir</span>: <span class="string">&#x27;./tiles/mylayer&#x27;</span>,</span><br><span class="line">    <span class="attr">refreshage</span>: <span class="number">3600</span>, <span class="comment">// 1 hour</span></span><br><span class="line">    <span class="attr">maxage</span>: <span class="number">3600</span>*<span class="number">24</span>*<span class="number">7</span> <span class="comment">// 1 week</span></span><br><span class="line">&#125;));</span><br><span class="line"></span><br><span class="line"><span class="attr">refreshage</span>: <span class="literal">null</span>  <span class="comment">// 永远不刷新缓存</span></span><br><span class="line"><span class="attr">refreshage</span>: <span class="number">0</span>     <span class="comment">// 命中一次后就刷新</span></span><br><span class="line"><span class="attr">refreshage</span>: <span class="number">1800</span>  <span class="comment">// 半小时后刷新</span></span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>下图中设置了<code>tilecache</code>缓存目录，可以看到瓦片会按照层级建立对应的目录结构并保存图片：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-disk-20190705160620.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>因为插件即提供了<code>cache</code>功能，也提供了<code>Provider</code>功能，所以其实它是这两种类型插件的组合，作为<code>Provider</code>还是比较简单的，核心是 serve 方法:</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="attr">serve</span>: <span class="keyword">function</span>(<span class="params">server, tile, callback</span>) &#123;</span><br><span class="line">  <span class="comment">// 解析路径</span></span><br><span class="line">  <span class="keyword">var</span> file = template</span><br><span class="line">    .<span class="title function_">replace</span>(<span class="string">&#x27;&#123;layer&#125;&#x27;</span>, tile.<span class="property">layer</span>)</span><br><span class="line">    .<span class="title function_">replace</span>(<span class="string">&#x27;&#123;filename&#125;&#x27;</span>, tile.<span class="property">filename</span>)</span><br><span class="line">    .<span class="title function_">replace</span>(<span class="string">&#x27;&#123;x&#125;&#x27;</span>, tile.<span class="property">x</span>)</span><br><span class="line">    .<span class="title function_">replace</span>(<span class="string">&#x27;&#123;y&#125;&#x27;</span>, tile.<span class="property">y</span>)</span><br><span class="line">    .<span class="title function_">replace</span>(<span class="string">&#x27;&#123;z&#125;&#x27;</span>, tile.<span class="property">z</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 根绝路径读取文件为buffer</span></span><br><span class="line">  fs.<span class="title function_">readFile</span>(file, <span class="keyword">function</span>(<span class="params">err, buffer</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (err) &#123;</span><br><span class="line">      <span class="keyword">if</span> (err.<span class="property">code</span> === <span class="string">&#x27;ENOENT&#x27;</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> err = <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;File not found&#x27;</span>);</span><br><span class="line">        err.<span class="property">statusCode</span> = <span class="number">404</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="title function_">callback</span>(err);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> <span class="title function_">callback</span>(err);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 返回buffer给tilestrata</span></span><br><span class="line">    <span class="title function_">callback</span>(<span class="literal">null</span>, buffer, &#123;<span class="string">&#x27;Content-Type&#x27;</span>: <span class="title function_">mime</span>(file)&#125;);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在<code>cache</code>功能中，需要提供 get 和 set 方法，主要就是在 set 方法中保存文件，在 get 方法中判断是否过期，是否文件名命中缓存，收否需要刷新，之后再返回对应结果的内容：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// set 方法</span></span><br><span class="line"><span class="title class_">FileSystemCache</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">set</span> = <span class="keyword">function</span>(<span class="params"></span></span><br><span class="line"><span class="params">  server,</span></span><br><span class="line"><span class="params">  req,</span></span><br><span class="line"><span class="params">  buffer,</span></span><br><span class="line"><span class="params">  headers,</span></span><br><span class="line"><span class="params">  callback</span></span><br><span class="line"><span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> maxage = <span class="variable language_">this</span>.<span class="title function_">ageTolerance</span>(<span class="string">&#x27;maxage&#x27;</span>, req)</span><br><span class="line">  <span class="keyword">if</span> (maxage === <span class="number">0</span>) <span class="keyword">return</span> <span class="title function_">callback</span>()</span><br><span class="line">  <span class="comment">// 只要maxage不为0，就在文件系统上存储图片，this._file是根据path或者dir设置计算出来的存储路径</span></span><br><span class="line">  fs.<span class="title function_">outputFile</span>(<span class="variable language_">this</span>.<span class="title function_">_file</span>(req), buffer, callback)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// get 方法（有所精简）</span></span><br><span class="line"><span class="title class_">FileSystemCache</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">get</span> = <span class="keyword">function</span>(<span class="params">server, req, callback</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> maxage = <span class="variable language_">this</span>.<span class="title function_">ageTolerance</span>(<span class="string">&#x27;maxage&#x27;</span>, req)</span><br><span class="line">  <span class="comment">// maxage为0，直接结束</span></span><br><span class="line">  <span class="keyword">if</span> (maxage === <span class="number">0</span>) <span class="keyword">return</span> <span class="title function_">done</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> self = <span class="variable language_">this</span></span><br><span class="line">  <span class="comment">// 获取缓存文件路径</span></span><br><span class="line">  <span class="keyword">var</span> file = <span class="variable language_">this</span>.<span class="title function_">_file</span>(req)</span><br><span class="line">  fs.<span class="title function_">open</span>(file, <span class="string">&#x27;r&#x27;</span>, <span class="keyword">function</span>(<span class="params">err, fd</span>) &#123;</span><br><span class="line">    fs.<span class="title function_">fstat</span>(fd, <span class="keyword">function</span>(<span class="params">err, stats</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="title function_">done</span>(err)</span><br><span class="line">      <span class="keyword">var</span> mtime = stats.<span class="property">mtime</span>.<span class="title function_">getTime</span>()</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 根据maxage是否过期判断是否直接结束</span></span><br><span class="line">      <span class="keyword">var</span> shouldServe = self.<span class="title function_">shouldServe</span>(mtime, req)</span><br><span class="line">      <span class="keyword">if</span> (!shouldServe) <span class="keyword">return</span> <span class="title function_">done</span>()</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 根据refreshage判断是否刷新</span></span><br><span class="line">      <span class="keyword">var</span> shouldRefresh = self.<span class="title function_">shouldRefresh</span>(mtime, req)</span><br><span class="line">      <span class="keyword">var</span> headers = &#123; <span class="string">&#x27;Content-Type&#x27;</span>: <span class="title function_">mime</span>(file) &#125;</span><br><span class="line">      <span class="keyword">var</span> buffer = <span class="keyword">new</span> <span class="title class_">Buffer</span>(stats.<span class="property">size</span>)</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 如果没有这个文件，直接结束</span></span><br><span class="line">      <span class="keyword">if</span> (!stats.<span class="property">size</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="title function_">done</span>(<span class="literal">null</span>, buffer, headers, shouldRefresh)</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 如果前面的都通过则读取文件为buffer</span></span><br><span class="line">      fs.<span class="title function_">read</span>(fd, buffer, <span class="number">0</span>, stats.<span class="property">size</span>, <span class="number">0</span>, <span class="keyword">function</span>(<span class="params">err</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="title function_">done</span>(err)</span><br><span class="line">        <span class="comment">// 返回buffer</span></span><br><span class="line">        <span class="title function_">done</span>(<span class="literal">null</span>, buffer, headers, shouldRefresh)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-disk&quot;&gt;tilestrata-disk&lt;/a&gt;插件可以在磁盘中存储&amp;#x2F;检索瓦片。它可以兼顾瓦片缓存和瓦片提供两个功能，代码虽简单，但是功能十分全面，个人感觉非常的实用，很值得推荐。&lt;/p&gt;
&lt;p&gt;用它来缓存瓦片时，一定要为每个图层使用不同的目录（例如：”tiles&amp;#x2F;layer_a”, “tiles&amp;#x2F;layer_b”）。如果设置了&lt;code&gt;maxage&lt;/code&gt;参数，插件会检查瓦片的最近一次修改时间，如果间隔过长，将返回&lt;code&gt;null&lt;/code&gt;。如果 设置&lt;code&gt;maxage=0&lt;/code&gt;，就相当于没有开启缓存功能。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-etag 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/15/tilestrata-etag/"/>
    <id>https://kaely.net/2019/07/15/tilestrata-etag/</id>
    <published>2019-07-15T13:39:54.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-etag">tilestrata-etag</a>插件可以在 tilestrata 的请求中设置 ETag，可以让服务缓存变得更加高效可控，节省带宽。如果客户端的 ETag 的版本和服务器要发送的一致，则可以直接使用缓存中的数据，不再重新发送数据。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-etag --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><p>默认情况下，插件不会对所有大于 512kb 的数据启用，因为在 large buffers 上 ETag 的计算会很占用 CPU 的时间。但你也可以通过<code>limit</code>选项来更改这一默认设置，例如设置为<code>null</code>将会无视文件的大小上限，设置为一个文件大小的字符串或者数字，则会以此为上限。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> etag = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-etag&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> dependency = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-dependency&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用mapnik数据源做一个普通的图层来对比</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_merc&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(disk.<span class="title function_">cache</span>(&#123; <span class="attr">dir</span>: <span class="string">&#x27;tilecache&#x27;</span> &#125;))</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/world.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用相同的数据源设置etag后做一个新图层来对比</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;etag&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">dependency</span>(<span class="string">&#x27;world_merc&#x27;</span>, <span class="string">&#x27;tile.png&#x27;</span>))</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">etag</span>(&#123; <span class="attr">limit</span>: <span class="string">&#x27;1mb&#x27;</span> &#125;))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>可以看到 etag 图层所有 response 中已经带上了<code>ETag</code></p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-etag-result-20190705101438.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>首先定义插件类型，这其实是一个<code>Response Hooks</code>类型的插件，劫持修改了正常的服务返回值。插件引入了<code>filesize-parser</code>用来计算文件大小，设置上限<code>max_length</code>，使用<a href="https://www.npmjs.com/package/etag">etag</a>来生成 etag。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// max_length由limit设置得出</span></span><br><span class="line"><span class="keyword">if</span> (status_type === <span class="number">2</span> &amp;&amp; result.<span class="property">buffer</span> &amp;&amp; result.<span class="property">buffer</span>.<span class="property">length</span> &lt; max_length) &#123;</span><br><span class="line">  <span class="keyword">var</span> resultEtag = result.<span class="property">headers</span>[<span class="string">&#x27;etag&#x27;</span>] || result.<span class="property">headers</span>[<span class="string">&#x27;ETag&#x27;</span>]</span><br><span class="line">  <span class="comment">// 如果没有etag，就使用etag库生成</span></span><br><span class="line">  <span class="keyword">if</span> (!resultEtag) &#123;</span><br><span class="line">    resultEtag = <span class="title function_">etag</span>(result.<span class="property">buffer</span>)</span><br><span class="line">    result.<span class="property">headers</span>[<span class="string">&#x27;ETag&#x27;</span>] = resultEtag</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 插件还会判断是否有设置if-none-match</span></span><br><span class="line">  <span class="keyword">var</span> ifnonematch = req.<span class="property">headers</span>[<span class="string">&#x27;if-none-match&#x27;</span>]</span><br><span class="line">  <span class="keyword">if</span> (ifnonematch &amp;&amp; ifnonematch === resultEtag) &#123;</span><br><span class="line">    result.<span class="property">status</span> = <span class="number">304</span></span><br><span class="line">    result.<span class="property">buffer</span> = <span class="keyword">new</span> <span class="title class_">Buffer</span>([])</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-etag&quot;&gt;tilestrata-etag&lt;/a&gt;插件可以在 tilestrata 的请求中设置 ETag，可以让服务缓存变得更加高效可控，节省带宽。如果客户端的 ETag 的版本和服务器要发送的一致，则可以直接使用缓存中的数据，不再重新发送数据。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-sharp 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/14/tilestrata-sharp/"/>
    <id>https://kaely.net/2019/07/14/tilestrata-sharp/</id>
    <published>2019-07-14T12:54:55.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-sharp">tilestrata-sharp</a>插件通过使用<a href="https://github.com/jcupitt/libvips">libvips</a>的<a href="https://github.com/lovell/sharp">sharp</a>库来转换处理图片。对于图片处理的一些方法参数都可以从 sharp 的<a href="https://sharp.pixelplumbing.com/en/stable/">官方文档</a>中来找寻，这个插件相当于起到了一个搭桥铺路的作用，核心依然是 sharp。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install sharp --save</span><br><span class="line">$ npm install tilestrata-sharp --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><p>插件官网中对于使用的说明非常简单，因为插件本身不包含处理图片的逻辑，所以更多的使用方法得去<a href="https://sharp.pixelplumbing.com/en/stable/">sharp</a>中找寻</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> sharp = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-sharp&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_merc_sharp&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="comment">// 定义数据源</span></span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/world.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">sharp</span>(<span class="keyword">function</span>(<span class="params">image, sharp</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> image</span><br><span class="line">        .<span class="title function_">resize</span>(<span class="number">256</span>) <span class="comment">// 重新设置大小</span></span><br><span class="line">        .<span class="title function_">rotate</span>(<span class="number">180</span>) <span class="comment">// 旋转</span></span><br><span class="line">        .<span class="title function_">greyscale</span>() <span class="comment">// 灰度处理</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>下图是原始的瓦片样式：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-sharp-original-20190705114413.png"></p><p>在使用了 sharp 插件进行了旋转 180°，灰度处理之后，如下所示：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-sharp-result-20190705113728.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>这个插件比较简单，属于<code>transform</code>类型，返回 buffer 给 tilestrata 之前会根据用户设置的回调函数里面的处理规则，调用<code>sharp</code>处理成新的 buffer，然后再返回。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="attr">transform</span>: <span class="keyword">function</span>(<span class="params">server, req, buffer, headers, callback</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> image;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 用原buffer生成sharp对象</span></span><br><span class="line">        image = <span class="title function_">sharp</span>(buffer);</span><br><span class="line">        <span class="comment">// 执行回调里面的处理规则</span></span><br><span class="line">        <span class="title function_">fn</span>(image, sharp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">catch</span> (err) &#123; <span class="keyword">return</span> <span class="title function_">callback</span>(err); &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 转化回buffer</span></span><br><span class="line">    image.<span class="title function_">toBuffer</span>(<span class="keyword">function</span>(<span class="params">err, buffer, info</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="title function_">callback</span>(err);</span><br><span class="line">        headers[<span class="string">&#x27;Content-Type&#x27;</span>] = <span class="string">&#x27;image/&#x27;</span> + info.<span class="property">format</span>;</span><br><span class="line">        <span class="comment">// 还给tilestrata</span></span><br><span class="line">        <span class="title function_">callback</span>(<span class="literal">null</span>, buffer, headers);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-sharp&quot;&gt;tilestrata-sharp&lt;/a&gt;插件通过使用&lt;a href=&quot;https://github.com/jcupitt/libvips&quot;&gt;libvips&lt;/a&gt;的&lt;a href=&quot;https://github.com/lovell/sharp&quot;&gt;sharp&lt;/a&gt;库来转换处理图片。对于图片处理的一些方法参数都可以从 sharp 的&lt;a href=&quot;https://sharp.pixelplumbing.com/en/stable/&quot;&gt;官方文档&lt;/a&gt;中来找寻，这个插件相当于起到了一个搭桥铺路的作用，核心依然是 sharp。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-gm 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/14/tilestrata-gm/"/>
    <id>https://kaely.net/2019/07/14/tilestrata-gm/</id>
    <published>2019-07-14T12:54:50.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-gm">tilestrata-gm</a>插件和 tilestrata-sharp 功能上类似，背后使用的依赖<a href="http://www.graphicsmagick.org/">graphicsmagick</a>的<a href="http://aheckmann.github.io/gm/docs.html">gm</a>库来做图像处理，所以具体能够实现的哪些功可以去 gm 的文档中查询。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>由于插件依赖于<a href="http://www.graphicsmagick.org/">graphicsmagick</a>，需要根据<a href="http://www.graphicsmagick.org/README.html">安装指南</a>在各个平台下进行安装，MacOS 可以直接用 brew 安装</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ brew install graphicsmagick</span><br><span class="line"><span class="comment"># 再安装插件</span></span><br><span class="line">$ npm install tilestrata-gm --save</span><br></pre></td></tr></table></figure><h1 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> gm = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-gm&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_merc_gm&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="comment">// 定义数据源</span></span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/world.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">gm</span>(<span class="keyword">function</span>(<span class="params">image</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> image</span><br><span class="line">        .<span class="title function_">blur</span>(<span class="number">7</span>, <span class="number">3</span>) <span class="comment">// 模糊处理</span></span><br><span class="line">        .<span class="title function_">rotate</span>(<span class="string">&#x27;green&#x27;</span>, <span class="number">45</span>) <span class="comment">// 旋转，配色</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以同时能处理的任务的个数</span></span><br><span class="line">gm.<span class="title function_">setMaxConcurrency</span>(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="效果"><a href="#效果" class="headerlink" title="效果"></a>效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>下图是原始的瓦片样式：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-sharp-original-20190705114413.png"></p><p>使用 gm 插件进行了模糊处理，旋转了 45°，背景填充为绿色后的结果：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-gm-result-20190705135250.png"></p><h1 id="代码浅析"><a href="#代码浅析" class="headerlink" title="代码浅析"></a>代码浅析</h1><p>插件的整体逻辑和 tilestrata-sharp 基本一致，在<code>toBuffer</code>阶段有所不同，gm 选择了在<code>async.queue</code>队列中逐步转化成 buffer 再返回给 tiletrata，这也是为什么会有<code>setMaxConcurrency</code>这个方法的原因。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码有所精简</span></span><br><span class="line"><span class="comment">// toBuffer队列</span></span><br><span class="line"><span class="keyword">var</span> gmQueue = <span class="keyword">async</span>.<span class="title function_">queue</span>(<span class="keyword">function</span>(<span class="params">image, callback</span>) &#123;</span><br><span class="line">image.<span class="title function_">toBuffer</span>(<span class="keyword">function</span>(<span class="params">err, buffer</span>) &#123;</span><br><span class="line"><span class="title function_">callback</span>(err, buffer);</span><br><span class="line">&#125;);</span><br><span class="line">&#125;, concurrency);</span><br><span class="line"></span><br><span class="line"><span class="attr">transform</span>: <span class="keyword">function</span>(<span class="params">server, req, buffer, headers, callback</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> image;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 创建gm对象</span></span><br><span class="line">        image = <span class="title function_">gm</span>(buffer);</span><br><span class="line">        <span class="comment">// 执行回调函数里面的处理规则</span></span><br><span class="line">        <span class="title function_">fn</span>(image);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">catch</span> (err) &#123; <span class="keyword">return</span> <span class="title function_">callback</span>(err); &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 放入async.queue中，逐步执行</span></span><br><span class="line">    gmQueue.<span class="title function_">push</span>(image, <span class="keyword">function</span>(<span class="params">err, buffer</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="title function_">callback</span>(err);</span><br><span class="line">        <span class="keyword">if</span> (image.<span class="property">_outputFormat</span>) &#123;</span><br><span class="line">            headers[<span class="string">&#x27;Content-Type&#x27;</span>] = <span class="string">&#x27;image/&#x27;</span> + image.<span class="property">_outputFormat</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="title function_">callback</span>(<span class="literal">null</span>, buffer, headers);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-gm&quot;&gt;tilestrata-gm&lt;/a&gt;插件和 tilestrata-sharp 功能上类似，背后使用的依赖&lt;a href=&quot;http://www.graphicsmagick.org/&quot;&gt;graphicsmagick&lt;/a&gt;的&lt;a href=&quot;http://aheckmann.github.io/gm/docs.html&quot;&gt;gm&lt;/a&gt;库来做图像处理，所以具体能够实现的哪些功可以去 gm 的文档中查询。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-lru  插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/11/tilestrata-lru/"/>
    <id>https://kaely.net/2019/07/11/tilestrata-lru/</id>
    <published>2019-07-11T14:51:22.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-lru">tilestrata-lru</a>插件使用内存来缓存瓦片。所以使用起来需要额外的小心，如果没有仔细的配置它有可能导致内存溢出。但若果你需要在不同的图层或者路径中共享某个资源，这个插件就是为此而生的。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-lru --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> lru = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-lru&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-headers&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> provider = <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">  <span class="attr">pathname</span>: <span class="string">&#x27;style/province.xml&#x27;</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置最大占用20mb的内存</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;lru_layer_size&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(provider)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>: <span class="string">&#x27;*&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">lru</span>(&#123; <span class="attr">size</span>: <span class="string">&#x27;20mb&#x27;</span>, <span class="attr">ttl</span>: <span class="number">30</span> &#125;)) <span class="comment">// ttl in seconds</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置最多缓存20个数据</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;lru_layer_number&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(provider)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>: <span class="string">&#x27;*&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">lru</span>(&#123; <span class="attr">size</span>: <span class="number">20</span>, <span class="attr">ttl</span>: <span class="number">30</span> &#125;))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 手动设置cache中数据对应key的结构</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;lru_layer_key&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(provider)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>: <span class="string">&#x27;*&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">lru</span>(&#123;</span><br><span class="line">      <span class="attr">key</span>: <span class="keyword">function</span>(<span class="params">req</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">          req.<span class="property">z</span> +</span><br><span class="line">          <span class="string">&#x27;,&#x27;</span> +</span><br><span class="line">          req.<span class="property">x</span> +</span><br><span class="line">          <span class="string">&#x27;,&#x27;</span> +</span><br><span class="line">          req.<span class="property">y</span> +</span><br><span class="line">          <span class="string">&#x27;,&#x27;</span> +</span><br><span class="line">          req.<span class="property">layer</span> +</span><br><span class="line">          <span class="string">&#x27;,&#x27;</span> +</span><br><span class="line">          req.<span class="property">filename</span> +</span><br><span class="line">          <span class="string">&#x27;,&#x27;</span> +</span><br><span class="line">          <span class="string">&#x27;lru_layer_custom_key&#x27;</span></span><br><span class="line">        )</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>多次访问某个瓦片，如<code>http://127.0.0.1:9527/lru_layer_number/4/12/6/tile.png</code>，可以发现插件会在后几次从<code>cache</code>中直接 get 获取数据：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-lru-20190705162618.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>首先定义插件类型，<code>Caches</code>类型的插件需要给 tilestrata 提供<code>set</code>和<code>get</code>两个回调函数，核心是<a href="https://www.npmjs.com/package/lru-cache">lru-cache</a>库</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码有所精简</span></span><br><span class="line"><span class="comment">// 设置缓存用到的key，如果没有自己设置key的结构，那默认将采用如下的结构</span></span><br><span class="line"><span class="keyword">var</span> key =</span><br><span class="line">  opts.<span class="property">key</span> ||</span><br><span class="line">  <span class="keyword">function</span>(<span class="params">req</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      req.<span class="property">z</span> + <span class="string">&#x27;,&#x27;</span> + req.<span class="property">x</span> + <span class="string">&#x27;,&#x27;</span> + req.<span class="property">y</span> + <span class="string">&#x27;,&#x27;</span> + req.<span class="property">layer</span> + <span class="string">&#x27;,&#x27;</span> + req.<span class="property">filename</span></span><br><span class="line">    )</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断设置的是数量还是内存大小</span></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">typeof</span> opts.<span class="property">size</span> === <span class="string">&#x27;string&#x27;</span>) &#123;</span><br><span class="line">  lruopts.<span class="property">max</span> = <span class="title function_">filesizeParser</span>(opts.<span class="property">size</span>)</span><br><span class="line">  lruopts.<span class="property">length</span> = <span class="keyword">function</span>(<span class="params">item</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> item.<span class="property">buffer</span>.<span class="property">length</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">typeof</span> opts.<span class="property">size</span> === <span class="string">&#x27;number&#x27;</span>) &#123;</span><br><span class="line">  lruopts.<span class="property">max</span> = opts.<span class="property">size</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据options创建lru-cache对象</span></span><br><span class="line"><span class="keyword">var</span> cache = <span class="keyword">new</span> <span class="title class_">SyncCache</span>(lruopts)</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> &#123;</span><br><span class="line">  <span class="attr">get</span>: <span class="keyword">function</span>(<span class="params">server, req, callback</span>) &#123;</span><br><span class="line">    <span class="comment">// 获取数据</span></span><br><span class="line">    <span class="keyword">var</span> item = cache.<span class="title function_">get</span>(<span class="title function_">key</span>(req))</span><br><span class="line">    <span class="keyword">if</span> (item) <span class="keyword">return</span> <span class="title function_">callback</span>(<span class="literal">null</span>, item.<span class="property">buffer</span>, item.<span class="property">headers</span>)</span><br><span class="line">    <span class="title function_">callback</span>()</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">set</span>: <span class="keyword">function</span>(<span class="params">server, req, buffer, headers, callback</span>) &#123;</span><br><span class="line">    <span class="comment">// 缓存数据</span></span><br><span class="line">    cache.<span class="title function_">set</span>(<span class="title function_">key</span>(req), &#123; <span class="attr">buffer</span>: buffer, <span class="attr">headers</span>: headers &#125;)</span><br><span class="line">    <span class="title function_">callback</span>()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-lru&quot;&gt;tilestrata-lru&lt;/a&gt;插件使用内存来缓存瓦片。所以使用起来需要额外的小心，如果没有仔细的配置它有可能导致内存溢出。但若果你需要在不同的图层或者路径中共享某个资源，这个插件就是为此而生的。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-underzoom  插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/11/tilestrata-underzoom/"/>
    <id>https://kaely.net/2019/07/11/tilestrata-underzoom/</id>
    <published>2019-07-11T14:44:51.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-underzoom">tilestrata-underzoom</a>插件从介绍上看就是在请求高级别瓦片时，用低级别的瓦片进行拼接，而不是直接生产指定级别的瓦片。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik">node-mapnik</a>。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="0-发生了什么"><a href="#0-发生了什么" class="headerlink" title="0. 发生了什么"></a>0. 发生了什么</h1><p>比方说我们请求了<code>4/13/6</code>瓦片，即<code>x=13,y=6,z=4</code>，插件会分别找到<code>[[26,12,5],[27,12,5],[27,13,5],[26,13,5]]</code>四个瓦片，获取数据，然后把四张图片进行合并，而不是直接渲染出<code>4/13/6</code>对应的图像。我个人的理解中，在向外缩放的过程中，人眼可视区域内的地图范围越来越大，对于 server 来说要渲染的内容也就越来越多，这个时候就要既保证渲染的展现结果，又要保证渲染过程对服务器资源的占用，<code>underzoom</code>对于瓦片地图来说就是针对这一问题的探索。</p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-underzoom --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> dependency = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-dependency&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> underzoom = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-underzoom&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用mapnik定义一个世界范围地图</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_merc&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/world.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义underzoom图层</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;underzoom&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile@2x.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">underzoom</span>(&#123;</span><br><span class="line">      <span class="attr">source</span>: <span class="title function_">dependency</span>(<span class="string">&#x27;world_merc&#x27;</span>, <span class="string">&#x27;tile.png&#x27;</span>),</span><br><span class="line">      <span class="attr">inputSize</span>: <span class="number">256</span>,</span><br><span class="line">      <span class="attr">outputSize</span>: <span class="number">512</span>,</span><br><span class="line">      <span class="attr">zooms</span>: <span class="number">1</span> <span class="comment">// 1以下层级全部采用underzoom的方式拼接</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>不启用 underzoom 的效果：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-underzoom-original-20190708145711.png"></p><p>underzoom 放大两倍的效果：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-underzoom-result-20190708145813.png"></p><p>可以看到，整个地图看上去<strong>清晰了很多，边缘更加明显</strong>，这是因为低级别瓦片本身的精细程度就高，合并成大图后的对于细节的效果也会更好。<br>但是地图左边区域原先就基本上是只由线勾勒出的<strong>白色区域</strong>，在低瓦片中可以看到，合并成大图后就基本上看不清了，这是因为本身线宽是很细的，比如说是 1px 宽度，合并大图的过程中这个宽度并不会增加，但图片整个的像素变大了，所以细线就基本看不清了，这也是插件不好的地方。</p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>说到合并图片，就能想到之前我们说过的<code>tilestrata-blend</code>插件，它是将两个不同数据源的结果图片进行了合并，而这个插件是将同一个数据源的四个低级别瓦片合并成了一个。</p><p>根据 underzoom 的<code>inputSize、zooms</code>的配置项，先准备好一个<code>Mapnik.Image</code>对象：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> canvas = <span class="keyword">new</span> <span class="title class_">Mapnik</span>.<span class="title class_">Image</span>(canvasSize, canvasSize)</span><br></pre></td></tr></table></figure><p>之后使用<code>@mapbox/tilebelt</code>库获取请求地址的下属四个低级别瓦片地址，分别请求数据，利用上面准备好的<code>Mapnik.Image</code>对象，使用<a href="http://mapnik.org/documentation/node-mapnik/3.6/#Image.composite">Image.composite</a>方法来合并四张图片，最后返回结果就好。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码有所精简</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">startFetchAndComposite</span>(<span class="params">callback</span>) &#123;</span><br><span class="line">  <span class="comment">// 例如请求 4/13/6</span></span><br><span class="line">  <span class="comment">// 此时coordList就是 [[26,12,5],[27,12,5],[27,13,5],[26,13,5]]</span></span><br><span class="line">  <span class="keyword">var</span> coordList = <span class="title function_">getTileCoords</span>(req.<span class="property">x</span>, req.<span class="property">y</span>, req.<span class="property">z</span>, underzoomLevels)</span><br><span class="line">  <span class="comment">// 遍历coordList</span></span><br><span class="line">  <span class="keyword">async</span>.<span class="title function_">each</span>(</span><br><span class="line">    coordList,</span><br><span class="line">    <span class="keyword">function</span>(<span class="params">coords, callback</span>) &#123;</span><br><span class="line">      <span class="comment">// clone一个新的请求</span></span><br><span class="line">      <span class="keyword">var</span> childReq = req.<span class="title function_">clone</span>()</span><br><span class="line">      childReq.<span class="property">x</span> = coords[<span class="number">0</span>]</span><br><span class="line">      childReq.<span class="property">y</span> = coords[<span class="number">1</span>]</span><br><span class="line">      childReq.<span class="property">z</span> = coords[<span class="number">2</span>]</span><br><span class="line">      options.<span class="property">source</span>.<span class="title function_">serve</span>(server, childReq, <span class="keyword">function</span>(<span class="params"></span></span><br><span class="line"><span class="params">        err,</span></span><br><span class="line"><span class="params">        childBuffer,</span></span><br><span class="line"><span class="params">        childHeaders</span></span><br><span class="line"><span class="params">      </span>) &#123;</span><br><span class="line">        <span class="comment">// 获取数据</span></span><br><span class="line">        <span class="title class_">Mapnik</span>.<span class="property">Image</span>.<span class="title function_">fromBytes</span>(childBuffer, <span class="keyword">function</span>(<span class="params">err, image</span>) &#123;</span><br><span class="line">          <span class="comment">// 执行合并操作</span></span><br><span class="line">          canvas.<span class="title function_">composite</span>(</span><br><span class="line">            image,</span><br><span class="line">            &#123;</span><br><span class="line">              <span class="attr">comp_op</span>: <span class="title class_">Mapnik</span>.<span class="property">compositeOp</span>.<span class="property">src_over</span>,</span><br><span class="line">              <span class="attr">dx</span>: x,</span><br><span class="line">              <span class="attr">dy</span>: y,</span><br><span class="line">              <span class="attr">opacity</span>: <span class="number">1</span></span><br><span class="line">            &#125;,</span><br><span class="line">            callback</span><br><span class="line">          )</span><br><span class="line">        &#125;)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;,</span><br><span class="line">    callback</span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-underzoom&quot;&gt;tilestrata-underzoom&lt;/a&gt;插件从介绍上看就是在请求高级别瓦片时，用低级别的瓦片进行拼接，而不是直接生产指定级别的瓦片。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot;&gt;node-mapnik&lt;/a&gt;。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-blend 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/08/tilestrata-blend/"/>
    <id>https://kaely.net/2019/07/08/tilestrata-blend/</id>
    <published>2019-07-08T07:19:26.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-blend">tilestrata-blend</a>插件可以将多个栅格瓦片图层合并成一个 png 图层，并且支持透明度、混合模式、图层过滤等选项。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik">node-mapnik</a>。这个插件依然是一个利用 mapnik 强大的功能的<code>Provider</code>类型的插件，甚至可以基于 mapnik 增强它已有的功能。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-blend --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> blend = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-blend&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义第一个世界边界图层</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_merc&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(disk.<span class="title function_">cache</span>(&#123; <span class="attr">dir</span>: <span class="string">&#x27;tilecache&#x27;</span> &#125;))</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/world.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义第二个中国省界图层</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;province&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/province.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义混合图层</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;blend_layer&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;combined.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">blend</span>(</span><br><span class="line">      [</span><br><span class="line">        [<span class="string">&#x27;world_merc&#x27;</span>, <span class="string">&#x27;tile.png&#x27;</span>],</span><br><span class="line">        [</span><br><span class="line">          <span class="string">&#x27;province&#x27;</span>,</span><br><span class="line">          <span class="string">&#x27;tile.png&#x27;</span>,</span><br><span class="line">          <span class="comment">// 配置一些省界图层合并时候的参数</span></span><br><span class="line">          &#123;</span><br><span class="line">            <span class="attr">opacity</span>: <span class="number">0.5</span>,</span><br><span class="line">            <span class="attr">comp_op</span>: <span class="string">&#x27;multiply&#x27;</span>,</span><br><span class="line">            <span class="attr">image_filters</span>: <span class="string">&#x27;agg-stack-blur(10,10)&#x27;</span></span><br><span class="line">          &#125;</span><br><span class="line">        ]</span><br><span class="line">      ],</span><br><span class="line">      <span class="comment">// 给合并后的图层设置一个可选的填充色</span></span><br><span class="line">      &#123;</span><br><span class="line">        <span class="attr">matte</span>: <span class="string">&#x27;ffffff&#x27;</span></span><br><span class="line">      &#125;</span><br><span class="line">    )</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>可以在同一张瓦片上看到黄色的中国省界图层和世界边界图层，下图是没有配置仍和<code>options</code>：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-blend-no-option-20190705113008.png"></p><p>下图是像上面演示的那样，给省界图层加入了一些<code>options</code>，例如 image_filters 和 opacity：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-blend-20190705102810.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>这个插件在背后依赖了<a href="https://github.com/naturalatlas/tilestrata-dependency">tilestrata-dependency</a>，所以配置里面的<code>[&#39;world_merc&#39;, &#39;tile.png&#39;]</code>实际上是通过执行了<code>.use(dependency(&#39;world_merc_png&#39;, &#39;tile.png&#39;))</code>操作来获取数据源：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 找到每个图层的数据源（代码有所精简）</span></span><br><span class="line"><span class="keyword">var</span> layers = layers.<span class="title function_">map</span>(<span class="keyword">function</span>(<span class="params">pair</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> layer = pair[<span class="number">0</span>]</span><br><span class="line">  <span class="keyword">var</span> filename = pair[<span class="number">1</span>]</span><br><span class="line">  <span class="keyword">var</span> comp_options = pair[<span class="number">2</span>] || &#123;&#125;</span><br><span class="line">  <span class="keyword">return</span> [<span class="title function_">dependency</span>(layer, filename), comp_options]</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>当请求某个瓦片时候，对每个<code>layers</code>依次执行<code>fetchTile</code>获取对应图片和<code>prepareImage</code>预处理图片</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 对每个数据源，获取数据，预处理图片（代码有所精简）</span></span><br><span class="line"><span class="keyword">async</span>.<span class="title function_">series</span>(</span><br><span class="line">  [</span><br><span class="line">    <span class="keyword">function</span> <span class="title function_">fetchTile</span>(<span class="params">callback</span>) &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err)</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="keyword">function</span> <span class="title function_">prepareImage</span>(<span class="params">callback</span>) &#123;</span><br><span class="line">      <span class="comment">// 这一步会创建new mapnik.Image</span></span><br><span class="line">      <span class="title function_">generateMatte</span>(noop)</span><br><span class="line">    &#125;</span><br><span class="line">  ],</span><br><span class="line">  <span class="keyword">function</span>(<span class="params">err</span>) &#123;</span><br><span class="line">    <span class="title function_">callback</span>(err, image ? [image, options] : <span class="literal">null</span>)</span><br><span class="line">  &#125;</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>完成每个数据源的单独处理后就是合并操作了，利用 mapnik 的<a href="http://mapnik.org/documentation/node-mapnik/3.6/#Image.composite">omposite</a>方法来依次合并创建的每个<code>mapnik.Image</code>对象，最后再利用<a href="http://mapnik.org/documentation/node-mapnik/3.6/#Image.demultiply">demultiply</a>解构为 buffer 返回给 tilestrata 就好了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// （代码有所精简）</span></span><br><span class="line"><span class="keyword">async</span>.<span class="title function_">eachSeries</span>(images, <span class="keyword">function</span>(<span class="params">res, callback</span>) &#123;</span><br><span class="line">    <span class="comment">// 依次合并图片</span></span><br><span class="line">    intermediate.<span class="title function_">composite</span>(image, res[<span class="number">1</span>], callback);</span><br><span class="line">&#125;, <span class="keyword">function</span>(<span class="params">err</span>) &#123;</span><br><span class="line">    <span class="comment">// 结构返回buffer</span></span><br><span class="line">    intermediate.<span class="title function_">demultiply</span>(<span class="keyword">function</span>(<span class="params">err</span>) &#123;</span><br><span class="line">            <span class="title function_">callback</span>(<span class="literal">null</span>, buffer, &#123;<span class="string">&#x27;Content-Type&#x27;</span>: <span class="string">&#x27;image/png&#x27;</span>&#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-blend&quot;&gt;tilestrata-blend&lt;/a&gt;插件可以将多个栅格瓦片图层合并成一个 png 图层，并且支持透明度、混合模式、图层过滤等选项。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot;&gt;node-mapnik&lt;/a&gt;。这个插件依然是一个利用 mapnik 强大的功能的&lt;code&gt;Provider&lt;/code&gt;类型的插件，甚至可以基于 mapnik 增强它已有的功能。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-dependency 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/07/tilestrata-dependency/"/>
    <id>https://kaely.net/2019/07/07/tilestrata-dependency/</id>
    <published>2019-07-07T09:02:17.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-dependency">tilestrata-dependency</a>插件其实就是共享其他图层的数据源，如果你想定义两个不同格式的图层，例如分别是 png 和 jpg，这个插件就会非常的便利。你甚至可以传入一个 function 来精确控制不同层级使用不同图层的数据源。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-dependency --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> dependency = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-dependency&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> mapnik = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-mapnik&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  <span class="comment">// 定义世界边界png图层</span></span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_merc_png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&#x27;style/world.xml&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  <span class="comment">// 定义世界边界jpg图层</span></span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_merc_jpg&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.jpg&#x27;</span>)</span><br><span class="line">  <span class="comment">// 直接用png的源</span></span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">dependency</span>(<span class="string">&#x27;world_merc_png&#x27;</span>, <span class="string">&#x27;tile.png&#x27;</span>))</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;function_layer&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.jpg&#x27;</span>)</span><br><span class="line">  <span class="comment">// 不同层级用不同的源</span></span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">dependency</span>(<span class="keyword">function</span>(<span class="params">req</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (req.<span class="property">z</span> &lt; <span class="number">5</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> [<span class="string">&#x27;world_merc_png&#x27;</span>, <span class="string">&#x27;tile.png&#x27;</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> [<span class="string">&#x27;world_merc_jpg&#x27;</span>, <span class="string">&#x27;tile.jpg&#x27;</span>];</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p><code>world_merc_png</code>和<code>world_merc_jpg</code>使用了相同的源，展示的内容一模一样：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-dependency-20190705152851.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>要弄清楚<code>dependency</code>插件的实现，首先要弄清楚它的类型，dependency 源代码很简单，它属于<code>Providers</code>类型的插件:</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Providers 类型插件需要提供serve方法</span></span><br><span class="line"><span class="attr">serve</span>: <span class="keyword">function</span>(<span class="params">server, tile, callback</span>) &#123;</span><br><span class="line">    <span class="title function_">callback</span>(err, buffer, headers);</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure><p>接下来的代码主要逻辑就是如何利用指定的图层源创造一个<code>buffer</code>返回给新图层，以下代码就是 server 函数，有所精简：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// req 即上面的tile参数，包含请求的瓦片地址和类型，例如tile.jpg</span></span><br><span class="line"><span class="keyword">var</span> mock = req.<span class="title function_">clone</span>()</span><br><span class="line"><span class="comment">// source 即指定的其他图层的数据源[&#x27;world_merc_png&#x27;, &#x27;tile.png&#x27;]</span></span><br><span class="line"><span class="keyword">var</span> source = <span class="title function_">getSource</span>(req)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 分别把当前tile的layer和filename替换成指定源的对应值</span></span><br><span class="line">mock.<span class="property">layer</span> = source[<span class="number">0</span>]</span><br><span class="line">mock.<span class="property">filename</span> = source[<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">// server 即上面的server参数，代表整个tilestrata服务</span></span><br><span class="line">server.<span class="title function_">serve</span>(mock, <span class="literal">false</span>, <span class="keyword">function</span>(<span class="params">status, buffer, headers</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (status === <span class="number">200</span>) &#123;</span><br><span class="line">    <span class="comment">// 利用伪造出来的指定源的请求内容去向服务器请求数据，获取buffer后返回给当前源</span></span><br><span class="line">    <span class="title function_">callback</span>(<span class="literal">null</span>, buffer, headers)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="title function_">callback</span>(err)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>当前图层拿到了 buffer 内容，只要再进入自己后续的加工逻辑，处理成指定格式返回就好了。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-dependency&quot;&gt;tilestrata-dependency&lt;/a&gt;插件其实就是共享其他图层的数据源，如果你想定义两个不同格式的图层，例如分别是 png 和 jpg，这个插件就会非常的便利。你甚至可以传入一个 function 来精确控制不同层级使用不同图层的数据源。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-vtile-composite 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/07/tilestrata-vtile-composite/"/>
    <id>https://kaely.net/2019/07/07/tilestrata-vtile-composite/</id>
    <published>2019-07-07T08:56:34.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-vtile-composite">tilestrata-vtile-composite</a>插件用来合并多个矢量瓦片图层，这些矢量瓦片图层来自于<a href="https://github.com/naturalatlas/tilestrata-vtile">tilestrata-vtile</a>插件。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik">node-mapnik</a>。<br>这个插件的作用和<a href="https://github.com/naturalatlas/tilestrata-blend">tilestrata-blend</a>类似，只是适用对象是 vtile 生产出来的矢量瓦片。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-vtile-composite --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> vtile = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-vtile&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> vtilecomposite = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-vtile-composite&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-headers&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 省界边界源</span></span><br><span class="line"><span class="keyword">const</span> provinceXml = &#123;</span><br><span class="line">  <span class="attr">xml</span>: <span class="string">&#x27;style/province.xml&#x27;</span>,</span><br><span class="line">  <span class="attr">tileSize</span>: <span class="number">256</span>,</span><br><span class="line">  <span class="attr">metatile</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="attr">bufferSize</span>: <span class="number">128</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 世界边界源</span></span><br><span class="line"><span class="keyword">const</span> worldXml = &#123;</span><br><span class="line">  <span class="attr">xml</span>: <span class="string">&#x27;style/world.xml&#x27;</span>,</span><br><span class="line">  <span class="attr">tileSize</span>: <span class="number">256</span>,</span><br><span class="line">  <span class="attr">metatile</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="attr">bufferSize</span>: <span class="number">128</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义第一个数据源</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;province_pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;t.pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">vtile</span>(provinceXml))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义第一个数据源</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;world_pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;t.pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">vtile</span>(worldXml))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 合并两个数据源，开启跨域访问</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;combined_pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;combined.pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>: <span class="string">&#x27;*&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">vtilecomposite</span>([[<span class="string">&#x27;world_pbf&#x27;</span>, <span class="string">&#x27;t.pbf&#x27;</span>], [<span class="string">&#x27;province_pbf&#x27;</span>, <span class="string">&#x27;t.pbf&#x27;</span>]]))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>这是单独的中国区域 pbf：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-composite-pro-20190704173456.png"></p><p>这是单独的世界区域 pbf：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-composite-world-20190704173556.png"></p><p>合并之后的图层：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-composite-20190704173718.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>其实原理也就解释了为什么官方说明中一定要在依赖中加入<code>node-mapnik</code>。核心代码如下：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">compositeTiles</span>(<span class="params">callback</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (vtiles.<span class="property">length</span> === <span class="number">0</span>) <span class="keyword">return</span> <span class="title function_">callback</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> merged = <span class="keyword">new</span> mapnik.<span class="title class_">VectorTile</span>(req.<span class="property">z</span>, req.<span class="property">x</span>, req.<span class="property">y</span>)</span><br><span class="line">  <span class="comment">// 我是关键</span></span><br><span class="line">  merged.<span class="title function_">composite</span>(vtiles, <span class="keyword">function</span>(<span class="params">err</span>) &#123;</span><br><span class="line">    vtiles = <span class="literal">null</span></span><br><span class="line">    <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="title function_">callback</span>(err)</span><br><span class="line"></span><br><span class="line">    result = merged.<span class="title function_">getData</span>(dataopts)</span><br><span class="line">    result.<span class="property">_vtile</span> = merged</span><br><span class="line">    result.<span class="property">_vx</span> = req.<span class="property">x</span></span><br><span class="line">    result.<span class="property">_vy</span> = req.<span class="property">y</span></span><br><span class="line">    result.<span class="property">_vz</span> = req.<span class="property">z</span></span><br><span class="line">    <span class="title function_">callback</span>()</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在连续请求完两个数据源的数据之后，新建 mapnik 的<code>VectorTile</code>对象，并调用其<a href="http://mapnik.org/documentation/node-mapnik/3.6/#VectorTile.composite">VectorTile.composite</a>方法，对两个数据源请求来的数据进行合并，最后再<code>getData</code>输出结果就好。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile-composite&quot;&gt;tilestrata-vtile-composite&lt;/a&gt;插件用来合并多个矢量瓦片图层，这些矢量瓦片图层来自于&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile&quot;&gt;tilestrata-vtile&lt;/a&gt;插件。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot;&gt;node-mapnik&lt;/a&gt;。&lt;br&gt;这个插件的作用和&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-blend&quot;&gt;tilestrata-blend&lt;/a&gt;类似，只是适用对象是 vtile 生产出来的矢量瓦片。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-vtile-raster 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/05/tilestrata-vtile-raster/"/>
    <id>https://kaely.net/2019/07/05/tilestrata-vtile-raster/</id>
    <published>2019-07-05T06:05:38.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-vtile-raster">tilestrata-vtile-raster</a>是配合<code>tilestrata-vtile</code>插件用来将 mapnik 的<code>pbf</code>格式的矢量瓦片转换成栅格图片。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik">node-mapnik</a>。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-vtile-raster --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> vtile = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-vtile&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> vtileraster = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-vtile-raster&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-headers&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义省界边界数据源</span></span><br><span class="line"><span class="keyword">const</span> common = &#123;</span><br><span class="line">  <span class="attr">xml</span>: <span class="string">&#x27;style/province.xml&#x27;</span>,</span><br><span class="line">  <span class="attr">tileSize</span>: <span class="number">256</span>,</span><br><span class="line">  <span class="attr">metatile</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="attr">bufferSize</span>: <span class="number">128</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;osm_pbf_raster&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.pbf&#x27;</span>) <span class="comment">// 定义vtile pbf图层做对比</span></span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>: <span class="string">&#x27;*&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">vtile</span>(common))</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.png&#x27;</span>) <span class="comment">// 定义vtile-raster png图层做对比</span></span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">vtileraster</span>(common, &#123;</span><br><span class="line">      <span class="attr">tilesource</span>: [<span class="string">&#x27;osm_pbf_raster&#x27;</span>, <span class="string">&#x27;tile.pbf&#x27;</span>]</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.json&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">vtileraster</span>(common, &#123;</span><br><span class="line">      <span class="attr">tilesource</span>: [<span class="string">&#x27;osm_pbf_raster&#x27;</span>, <span class="string">&#x27;tile.pbf&#x27;</span>]</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>可以直接使用常规的栅格瓦片加载方式来加载<code>tile.png</code>图层，无须使用矢量瓦片的加载方式。可以和<a href="https://kaely.net/2019/07/04/tilestrata-vtile/">tilestrata-vtile 插件浅析</a>对比观看。</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-raster-result-20190704172822.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>核心仍然是 mapnik 的功能，过程上大体是和 tilestrata-vtile 插件类似的，不同的地方在当请求图层的时候，不再单纯的使用<a href="http://mapnik.org/documentation/node-mapnik/3.6/#VectorTile">VectorTile</a>的<code>getData</code>方法，首先代码会依据是否可交互属性<code>interactivity</code>来构造一个类型是<code>mapnik.Image</code>的<code>surface</code>参数</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (self.<span class="property">interactivity</span>) &#123;</span><br><span class="line">  surface = <span class="keyword">new</span> mapnik.<span class="title class_">Grid</span>(dim, dim)</span><br><span class="line">  <span class="comment">// ... 省略</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  surface = <span class="keyword">new</span> mapnik.<span class="title class_">Image</span>(dim, dim)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后使用<a href="http://mapnik.org/documentation/node-mapnik/3.6/#VectorTile.render">VectorTile.render</a>这个方法，通过 xml 构建出来的 map 和上面的 surface 参数，就可以将矢量数据转化为栅格图片：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">vectorTile.<span class="title function_">render</span>(self.<span class="property">map</span>, surface, options, callback)</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile-raster&quot;&gt;tilestrata-vtile-raster&lt;/a&gt;是配合&lt;code&gt;tilestrata-vtile&lt;/code&gt;插件用来将 mapnik 的&lt;code&gt;pbf&lt;/code&gt;格式的矢量瓦片转换成栅格图片。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot;&gt;node-mapnik&lt;/a&gt;。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-vtile 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/04/tilestrata-vtile/"/>
    <id>https://kaely.net/2019/07/04/tilestrata-vtile/</id>
    <published>2019-07-04T09:39:41.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-vtile">tilestrata-vtile</a>插件可以通过 mapnik 数据源生成<code>pbf</code>格式的矢量瓦片。如果需要对瓦片做放大倍数，交互这些操作，矢量瓦片不需要再额外的请求数据，使用起来非常方便。后续还可以通过配套的<a href="https://github.com/naturalatlas/tilestrata-vtile-raster">tilestrata-vtile-raster</a>插件来将矢量数据渲染成普通的图片瓦片。使用这个插件必须安装依赖<a href="https://github.com/mapnik/node-mapnik">node-mapnik</a>。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-vtile --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> vtile = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-vtile&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-headers&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义省界边界数据源</span></span><br><span class="line"><span class="keyword">const</span> common = &#123;</span><br><span class="line">  <span class="attr">xml</span>: <span class="string">&#x27;style/province.xml&#x27;</span>,</span><br><span class="line">  <span class="attr">tileSize</span>: <span class="number">256</span>,</span><br><span class="line">  <span class="attr">metatile</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="attr">bufferSize</span>: <span class="number">128</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 开启跨域，定义图层</span></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;province_pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>: <span class="string">&#x27;*&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">vtile</span>(common))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动服务</span></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><p>插件还提供了<code>overrideRenderOptions</code>配置项，用来做层级的精确控制，控制瓦片的返回内容</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;mylayer&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;t.pbf&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">vtile</span>(&#123;</span><br><span class="line">      <span class="attr">xml</span>: <span class="string">&#x27;/path/to/map.xml&#x27;</span>,</span><br><span class="line">      <span class="attr">tileSize</span>: <span class="number">256</span>,</span><br><span class="line">      <span class="attr">metatile</span>: <span class="number">1</span>,</span><br><span class="line">      <span class="attr">bufferSize</span>: <span class="number">128</span>,</span><br><span class="line">      <span class="attr">overrideRenderOptions</span>: <span class="keyword">function</span>(<span class="params">opts, z, maxz</span>) &#123;</span><br><span class="line">        opts.<span class="property">simplify_distance</span> = z &lt; maxz ? <span class="number">8</span> : <span class="number">1</span></span><br><span class="line">        <span class="keyword">return</span> opts</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br></pre></td></tr></table></figure><p>回调函数的第一个参数<code>opts</code>包含了以下内容，可以在<a href="#4-%E4%BB%A3%E7%A0%81%E6%B5%85%E6%9E%90">第四节</a>的代码分析中看到这些值是怎么来的：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  simplify_distance,</span><br><span class="line">  path_multiplier, </span><br><span class="line">  buffer_size, </span><br><span class="line">  scale_denominator</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/v-tile-result-20190704171541.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>原理上很简单，从代码上就能看出来，传入了 mapnik 需要的<code>xml</code>配置文件，所以其实是利用了 mapnik 的<a href="http://mapnik.org/documentation/node-mapnik/3.6/#VectorTile">VectorTile</a>功能，代码如下：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 新建VectorTile源，options也和传入的otps息息相关</span></span><br><span class="line">map.<span class="title function_">render</span>(<span class="keyword">new</span> mapnik.<span class="title class_">VectorTile</span>(z, x, y), options, <span class="keyword">function</span>(<span class="params">err, image</span>) &#123;</span><br><span class="line">  self.<span class="property">pool</span>.<span class="title function_">release</span>(map)</span><br><span class="line">  <span class="keyword">if</span> (err) <span class="keyword">return</span> <span class="title function_">callback</span>(err)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (image.<span class="title function_">empty</span>()) &#123;</span><br><span class="line">    err = <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;No data&#x27;</span>)</span><br><span class="line">    err.<span class="property">statusCode</span> = <span class="number">204</span></span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">callback</span>(err)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 通过getdata方法获取数据并返回给tilestrata</span></span><br><span class="line">  <span class="keyword">var</span> buffer = image.<span class="title function_">getData</span>(self.<span class="property">dataopts</span>)</span><br><span class="line">  buffer.<span class="property">metatile</span> = self.<span class="property">metatile</span></span><br><span class="line">  buffer.<span class="property">_vtile</span> = image</span><br><span class="line">  buffer.<span class="property">_vx</span> = x</span><br><span class="line">  buffer.<span class="property">_vy</span> = y</span><br><span class="line">  buffer.<span class="property">_vz</span> = z</span><br><span class="line"></span><br><span class="line">  <span class="title function_">callback</span>(<span class="literal">null</span>, buffer)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>第二节使用中提到的<code>opts</code>的参数实际上是 mapnik 中<a href="http://mapnik.org/documentation/node-mapnik/3.6/#Map.render">Map.render</a>方法的几个可选参数，他们会在 tilestrata-vtile 中被合并使用：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> options = self.<span class="title function_">overrideRenderOptions</span>(</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">simplify_distance</span>: real_z &lt; self.<span class="property">maxzoom</span> ? <span class="number">8</span> : <span class="number">1</span>,</span><br><span class="line">    <span class="attr">path_multiplier</span>: <span class="number">16</span> * self.<span class="property">metatile</span>,</span><br><span class="line">    <span class="attr">buffer_size</span>: self.<span class="property">bufferSize</span>,</span><br><span class="line">    <span class="attr">scale_denominator</span>: <span class="number">559082264.028</span> / (<span class="number">1</span> &lt;&lt; real_z)</span><br><span class="line">  &#125;,</span><br><span class="line">  real_z,</span><br><span class="line">  self.<span class="property">maxzoom</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile&quot;&gt;tilestrata-vtile&lt;/a&gt;插件可以通过 mapnik 数据源生成&lt;code&gt;pbf&lt;/code&gt;格式的矢量瓦片。如果需要对瓦片做放大倍数，交互这些操作，矢量瓦片不需要再额外的请求数据，使用起来非常方便。后续还可以通过配套的&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-vtile-raster&quot;&gt;tilestrata-vtile-raster&lt;/a&gt;插件来将矢量数据渲染成普通的图片瓦片。使用这个插件必须安装依赖&lt;a href=&quot;https://github.com/mapnik/node-mapnik&quot;&gt;node-mapnik&lt;/a&gt;。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-postgis-geojson-tiles 插件使用及浅析</title>
    <link href="https://kaely.net/2019/07/02/tilestrata-postgis-geojson-tiles/"/>
    <id>https://kaely.net/2019/07/02/tilestrata-postgis-geojson-tiles/</id>
    <published>2019-07-02T13:42:51.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-postgis-geojson-tiles">tilestrata-postgis-geojson-tiles</a>插件以 PostGIS 为数据源，生成 GeoJSON 格式的矢量瓦片。轻量但功能强大，配置项丰富，效果拔群。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ npm install tilestrata-postgis-geojson-tiles --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><p>插件的配置比较复杂：</p><ul><li><code>geometryField</code> <em>( string, 必选)</em> : 数据表中存储地理数据的列名。 (默认 &#x3D; <code>&quot;geom&quot;</code>)</li><li><code>sql</code> <em>( function, 必选)</em> : 一个返回 PostGIS 查询语句的函数。 <em>注意防治 sql 注入。</em> 查询语句包含以下内容:<ul><li><code>&#123;bbox&#125;</code> (box2d): 缓冲区的 bbox 范围</li><li><code>&#123;geojson&#125;</code> (text): 被裁剪出来的地理信息数据</li></ul></li><li><code>pgConfig</code> <em>( object, 必选)</em> : postgres 数据库配置：<ul><li><code>&#123;host&#125;</code> (string)</li><li><code>&#123;password&#125;</code> (string)</li><li><code>&#123;user&#125;</code> (string)</li><li><code>&#123;port&#125;</code> (string)</li><li><code>&#123;database&#125;</code> (string)</li></ul></li><li><code>simplifyFactor</code> <em>( number, 可选 )</em> : 地理数据简化程度的参数。 (默认 &#x3D; <code>0.75</code>)</li><li><code>buffer</code> <em>( number, 可选)</em> : 围绕每个切片的缓冲大小，以像素为单位 (默认 &#x3D; <code>16</code>)</li><li><code>collectGeometry</code> <em>( boolean, 可选 )</em> : 在转换成 GeoJson 之前是用 ST_Collect 把多个图形合并成一个简单图形。 (默认 &#x3D; <code>false</code>)</li><li><code>mergeMultiLineStrings</code> <em>( boolean, 可选 )</em> : 在转换成 GeoJson 之前是和否用 ST_LineMerge 合并 MultiLineStrings 数据。 (默认 &#x3D; <code>false</code>)</li><li><code>dumpGeometry</code> <em>( boolean, 可选 )</em> : 是否用 ST_Dump 来拆分图形 (默认 &#x3D; <code>false</code>)</li></ul><p>这里我使用了 demo 中的 world_merc.shp 数据导入了 PostGIS 中作为测试数据源</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> tilestrata = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> tilestrataPostGISGeoJSON = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-postgis-geojson-tiles&#x27;</span>)</span><br><span class="line"><span class="keyword">const</span> headers = <span class="built_in">require</span>(<span class="string">&#x27;tilestrata-headers&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> server = <span class="title function_">tilestrata</span>()</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&#x27;geojson-tiles&#x27;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&#x27;tile.json&#x27;</span>)</span><br><span class="line">  <span class="comment">// 配置跨域</span></span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&#x27;Access-Control-Allow-Origin&#x27;</span>: <span class="string">&#x27;*&#x27;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  <span class="comment">// 定义数据源</span></span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">tilestrataPostGISGeoJSON</span>(&#123;</span><br><span class="line">      <span class="attr">geometryField</span>: <span class="string">&#x27;geom&#x27;</span>,</span><br><span class="line">      <span class="attr">sql</span>: <span class="keyword">function</span>(<span class="params">server, req</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&#x27;select name, &#123;geojson&#125; from world_merc WHERE ST_Intersects(geom, &#123;bbox&#125;)&#x27;</span></span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">pgConfig</span>: &#123;</span><br><span class="line">        <span class="attr">username</span>: <span class="string">&#x27;postgres&#x27;</span>,</span><br><span class="line">        <span class="attr">password</span>: <span class="string">&#x27;postgres&#x27;</span>,</span><br><span class="line">        <span class="attr">host</span>: <span class="string">&#x27;10.211.55.4&#x27;</span>,</span><br><span class="line">        <span class="attr">port</span>: <span class="string">&#x27;5432&#x27;</span>,</span><br><span class="line">        <span class="attr">database</span>: <span class="string">&#x27;shapefile&#x27;</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>)</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p><code>http://127.0.0.1:9527/world_merc/6/55/25/tile.png</code>对应的栅格瓦片的结果如下图所示</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/geojson-tile-png-20190701212419.png"></p><p><code>http://127.0.0.1:9527/geojson-tiles/6/55/25/tile.json</code>请求对应的地址获取 GeoJSON 格式的矢量数据：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/geojson-tile-json-20190701212838.png"></p><p>在<a href="http://geojson.io/">geojson.io</a>查看效果如下图所示：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/geojson-tile-result-20190701212926.png"></p><p>GeoJSON 结果和渲染出来的 png 的图形基本是一致。</p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>从插件功能可以看出来属于<code>Providers</code>类型的插件，那么核心就是 serve 方法。首先插件会根据我们传入的数据库信息来建立 Postgres 对象，这个不需要展开讲。</p><p>当请求某个瓦片的时候，插件根据传入的 zxy 信息，调用了<code>@mapbox/sphericalmercator</code>库来把 zxy 坐标转换成 bbox 对象</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sm = <span class="keyword">new</span> <span class="title class_">SphericalMercator</span>(&#123; <span class="attr">size</span>: <span class="number">256</span> &#125;)</span><br><span class="line"><span class="keyword">var</span> bbox = sm.<span class="title function_">bbox</span>(tile.<span class="property">x</span>, tile.<span class="property">y</span>, tile.<span class="property">z</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> w = bbox[<span class="number">2</span>] - bbox[<span class="number">0</span>]</span><br><span class="line"><span class="keyword">var</span> h = bbox[<span class="number">3</span>] - bbox[<span class="number">1</span>]</span><br><span class="line"><span class="keyword">var</span> bufferX = (buffer * w) / <span class="number">256</span></span><br><span class="line"><span class="keyword">var</span> bufferY = (buffer * h) / <span class="number">256</span></span><br><span class="line">bbox[<span class="number">0</span>] -= bufferX</span><br><span class="line">bbox[<span class="number">1</span>] -= bufferY</span><br><span class="line">bbox[<span class="number">2</span>] += bufferX</span><br><span class="line">bbox[<span class="number">3</span>] += bufferY</span><br></pre></td></tr></table></figure><p>在定义图层的时候会传入一个<code>sql</code>函数，里面包含了<code>&#123;bbox&#125;</code>和<code>&#123;geojson&#125;</code>两个预设的字段，在源代码中这两个字段将会分别被计算出来，合成一个 sql 在数据库中进行查询</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 代码为了便于理解有所精简改动</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 利用上面计算的bbox来组装bboxsql</span></span><br><span class="line"><span class="keyword">var</span> bboxSQL =</span><br><span class="line">  <span class="string">&quot;ST_SetSRID(&#x27;BOX(&quot;</span> +</span><br><span class="line">  bbox[<span class="number">0</span>] +</span><br><span class="line">  <span class="string">&#x27; &#x27;</span> +</span><br><span class="line">  bbox[<span class="number">1</span>] +</span><br><span class="line">  <span class="string">&#x27;,&#x27;</span> +</span><br><span class="line">  bbox[<span class="number">2</span>] +</span><br><span class="line">  <span class="string">&#x27; &#x27;</span> +</span><br><span class="line">  bbox[<span class="number">3</span>] +</span><br><span class="line">  <span class="string">&quot; )&#x27;::box2d, 4326)&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据传入的geomField等字段组装geojsonSQL</span></span><br><span class="line"><span class="keyword">var</span> geojsonSQL =</span><br><span class="line">  <span class="string">&#x27;ST_MakeValid(ST_SimplifyPreserveTopology(&#x27;</span> +</span><br><span class="line">  geomField +</span><br><span class="line">  <span class="string">&#x27;, &#x27;</span> +</span><br><span class="line">  simplifyTolerance +</span><br><span class="line">  <span class="string">&#x27;)) ST_Intersection(&#x27;</span> +</span><br><span class="line">  geojsonSQL +</span><br><span class="line">  <span class="string">&#x27;, &#123;bbox&#125;) ST_AsGeoJSON(&#x27;</span> +</span><br><span class="line">  geojsonSQL +</span><br><span class="line">  <span class="string">&#x27;) AS geojson&#x27;</span></span><br><span class="line"></span><br><span class="line">sql = sql.<span class="title function_">replace</span>(<span class="regexp">/&#123;geojson&#125;/g</span>, geojsonSQL).<span class="title function_">replace</span>(<span class="regexp">/&#123;bbox&#125;/g</span>, bboxSQL)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查询</span></span><br><span class="line">pgPool.<span class="title function_">query</span>(sql, callback)</span><br></pre></td></tr></table></figure><p>最后把查询出来的结果封装在<code>FeatureCollection</code>中形成一个完整的 GeoJSON 返回给 tilestrata 就可以了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 组装GeoJSON</span></span><br><span class="line"><span class="keyword">var</span> outputText =</span><br><span class="line">  <span class="string">&#x27;&#123;&quot;type&quot;: &quot;FeatureCollection&quot;, &quot;features&quot;: [&#x27;</span> +</span><br><span class="line">  result.<span class="property">rows</span></span><br><span class="line">    .<span class="title function_">map</span>(<span class="keyword">function</span>(<span class="params">row</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (row.<span class="property">geojson</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> featureString = <span class="string">&#x27;&#123;&quot;type&quot;: &quot;Feature&quot;, &quot;geometry&quot;: &#x27;</span> + row.<span class="property">geojson</span></span><br><span class="line">        <span class="keyword">delete</span> row.<span class="property">geojson</span></span><br><span class="line">        <span class="keyword">return</span> featureString + <span class="string">&#x27;, &quot;properties&quot;: &#x27;</span> + <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(row) + <span class="string">&#x27;&#125;&#x27;</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .<span class="title function_">join</span>(<span class="string">&#x27;,&#x27;</span>) +</span><br><span class="line">  <span class="string">&#x27;]&#125;&#x27;</span></span><br><span class="line"><span class="comment">// 返回结果</span></span><br><span class="line"><span class="title function_">callback</span>(<span class="literal">null</span>, outputText, &#123; <span class="string">&#x27;Content-Type&#x27;</span>: <span class="string">&#x27;application/json&#x27;</span> &#125;)</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-postgis-geojson-tiles&quot;&gt;tilestrata-postgis-geojson-tiles&lt;/a&gt;插件以 PostGIS 为数据源，生成 GeoJSON 格式的矢量瓦片。轻量但功能强大，配置项丰富，效果拔群。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-headers 插件使用及浅析</title>
    <link href="https://kaely.net/2019/06/26/tilestrata-headers/"/>
    <id>https://kaely.net/2019/06/26/tilestrata-headers/</id>
    <published>2019-06-26T03:25:05.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-headers">tilestrata-headers</a>插件用来设置请求的响应头<code>response headers</code>，常用的可以解决缓存控制、跨域等问题，实用性很高。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install tilestrata-headers --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> headers = <span class="built_in">require</span>(<span class="string">&quot;tilestrata-headers&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">&quot;tilestrata&quot;</span>);</span><br><span class="line"></span><br><span class="line">server = <span class="title function_">tilestrata</span>();</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&quot;osm_mvt_amenities&quot;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&quot;tile.mvt&quot;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&quot;Access-Control-Allow-Origin&quot;</span>: <span class="string">&quot;*&quot;</span>,</span><br><span class="line">      <span class="string">&quot;Cache-Control&quot;</span>: <span class="string">&quot;max-age=3600&quot;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">8080</span>);</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>未开启</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-headers-null-20190606114255.png"></p><p>开启</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-headers-result-20190606114432.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>原理比较简单，在<code>tilestrata</code>的设计中，会使用<code>use</code>去注册各种类型的插件，<code>tilestrata-headers</code>就是<code>reshook</code>类型的插件，在初始化的过程中会被注册</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title class_">TileRequestHandler</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">use</span> = <span class="keyword">function</span>(<span class="params">plugin</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!plugin) <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">  <span class="comment">// ...简化后的代码</span></span><br><span class="line">  <span class="keyword">if</span> (plugin.<span class="property">reshook</span>) <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="title function_">_registerResponseHook</span>(plugin);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="title class_">TileRequestHandler</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">_registerResponseHook</span> = <span class="keyword">function</span>(<span class="params">plugin</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> id = <span class="string">&quot;reshook#&quot;</span> + <span class="variable language_">this</span>.<span class="property">responseHooks</span>.<span class="property">length</span>;</span><br><span class="line">  <span class="keyword">if</span> (!plugin) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;Falsy value passed to registerResponseHook()&quot;</span>);</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> plugin.<span class="property">reshook</span> !== <span class="string">&quot;function&quot;</span>)</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(</span><br><span class="line">      <span class="string">&quot;Attempted to register a response hook with no reshook() method&quot;</span></span><br><span class="line">    );</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">responseHooks</span>.<span class="title function_">push</span>(&#123; <span class="attr">id</span>: id, <span class="attr">plugin</span>: plugin &#125;);</span><br><span class="line">  <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>通过这种方式，插件劫持每个 response，给每个 response header 加上我们自定义的内容。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-headers&quot;&gt;tilestrata-headers&lt;/a&gt;插件用来设置请求的响应头&lt;code&gt;response headers&lt;/code&gt;，常用的可以解决缓存控制、跨域等问题，实用性很高。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-jsonp 插件使用及浅析</title>
    <link href="https://kaely.net/2019/06/23/tilestrata-jsonp/"/>
    <id>https://kaely.net/2019/06/23/tilestrata-jsonp/</id>
    <published>2019-06-23T12:15:44.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-jsonp">tilestrata-jsonp</a>插件可以将一些网格（utfgrids）的 json 请求包装成 jsonp，使你不必配置 CORS 也可以跨域访问这些资源。这是除了 tilestrata-headers 以外的另一个实用型插件。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install tilestrata-jsonp --save</span><br></pre></td></tr></table></figure><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">&quot;tilestrata&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> jsonp = <span class="built_in">require</span>(<span class="string">&quot;tilestrata-jsonp&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> tilestrataPostGISGeoJSON = <span class="built_in">require</span>(<span class="string">&quot;tilestrata-postgis-geojson-tiles&quot;</span>);</span><br><span class="line"></span><br><span class="line">server = <span class="title function_">tilestrata</span>();</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&quot;geojson_tiles_jsonp&quot;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&quot;tile.json&quot;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">jsonp</span>(&#123; <span class="attr">variable</span>: <span class="string">&quot;callback&quot;</span> &#125;))</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="comment">// Provider 这里用tilestrataPostGISGeoJSON做示例</span></span><br><span class="line">    <span class="title function_">tilestrataPostGISGeoJSON</span>(&#123;</span><br><span class="line">      <span class="attr">geometryField</span>: <span class="string">&quot;geom&quot;</span>,</span><br><span class="line">      <span class="attr">sql</span>: <span class="keyword">function</span>(<span class="params">server, req</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;select name, &#123;geojson&#125; from (select name, ST_Transform(way,4326) as geom from osm_polygon where name ~ &#x27;上海&#x27;) as a1 WHERE ST_Intersects(geom, &#123;bbox&#125;)&quot;</span>;</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">pgConfig</span>: &#123;</span><br><span class="line">        <span class="attr">username</span>: <span class="string">&quot;postgres&quot;</span>,</span><br><span class="line">        <span class="attr">password</span>: <span class="string">&quot;postgres&quot;</span>,</span><br><span class="line">        <span class="attr">host</span>: <span class="string">&quot;localhost&quot;</span>,</span><br><span class="line">        <span class="attr">port</span>: <span class="string">&quot;5432&quot;</span>,</span><br><span class="line">        <span class="attr">database</span>: <span class="string">&quot;osm&quot;</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">9527</span>);</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>在启动 server 后，我们可以直接用<code>jquery</code>请求某个固定层级的瓦片，来测试是否成功触发<code>callbak</code>函数</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$.<span class="title function_">ajax</span>(&#123;</span><br><span class="line">  <span class="attr">url</span>: <span class="string">&quot;http://127.0.0.1:9527/geojson_tiles_jsonp/4/15/8/tile.json&quot;</span>,</span><br><span class="line">  <span class="attr">type</span>: <span class="string">&quot;GET&quot;</span>,</span><br><span class="line">  <span class="attr">dataType</span>: <span class="string">&quot;jsonp&quot;</span>,</span><br><span class="line">  <span class="attr">success</span>: <span class="keyword">function</span>(<span class="params">data</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;jsonp使用成功&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>结果</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-jsonp-result-20190606145036.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>原理比较简单，和 headers 插件类似，在<code>tilestrata</code>的设计中，会使用<code>use</code>去注册各种类型的插件，<code>tilestrata-headers</code>就是<code>reshook</code>类型的插件，在初始化的过程中会被注册</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title class_">TileRequestHandler</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">use</span> = <span class="keyword">function</span>(<span class="params">plugin</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!plugin) <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">  <span class="comment">// ...简化后的代码</span></span><br><span class="line">  <span class="keyword">if</span> (plugin.<span class="property">reshook</span>) <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="title function_">_registerResponseHook</span>(plugin);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="title class_">TileRequestHandler</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">_registerResponseHook</span> = <span class="keyword">function</span>(<span class="params">plugin</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> id = <span class="string">&quot;reshook#&quot;</span> + <span class="variable language_">this</span>.<span class="property">responseHooks</span>.<span class="property">length</span>;</span><br><span class="line">  <span class="keyword">if</span> (!plugin) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;Falsy value passed to registerResponseHook()&quot;</span>);</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> plugin.<span class="property">reshook</span> !== <span class="string">&quot;function&quot;</span>)</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(</span><br><span class="line">      <span class="string">&quot;Attempted to register a response hook with no reshook() method&quot;</span></span><br><span class="line">    );</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">responseHooks</span>.<span class="title function_">push</span>(&#123; <span class="attr">id</span>: id, <span class="attr">plugin</span>: plugin &#125;);</span><br><span class="line">  <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>通过这种方式，插件会设置响应内容为<code>Content-Type: text/javascript;</code>，给我们返回一段包含了回调函数的代码，来执行我们预先定义好的 callbak 函数。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-jsonp&quot;&gt;tilestrata-jsonp&lt;/a&gt;插件可以将一些网格（utfgrids）的 json 请求包装成 jsonp，使你不必配置 CORS 也可以跨域访问这些资源。这是除了 tilestrata-headers 以外的另一个实用型插件。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-postgismvt 插件使用及浅析</title>
    <link href="https://kaely.net/2019/06/13/tilestrata-postgismvt/"/>
    <id>https://kaely.net/2019/06/13/tilestrata-postgismvt/</id>
    <published>2019-06-13T08:02:44.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/Stezii/tilestrata-postgismvt">tilestrata-postgismvt</a>是一个用来从 PostGIS 数据库中检索出 mvt 格式矢量瓦片的插件。PostGIS 在 2.4.0 版本后支持了直接生成 mvt 数据，这让我们更方便的加工出矢量瓦片。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-基本要求"><a href="#1-基本要求" class="headerlink" title="1. 基本要求"></a>1. 基本要求</h1><p>PostGIS 需要在<a href="https://postgis.net/2017/09/30/postgis-2.4.0/">2.4.0</a>版本及以上</p><h1 id="2-安装"><a href="#2-安装" class="headerlink" title="2. 安装"></a>2. 安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">## 不推荐</span></span><br><span class="line">npm install tilestrata-postgismvt --save</span><br></pre></td></tr></table></figure><p>在 ST_AsMVT 方法更新后 ，虽然作者更新了代码，但是 npm 包并不是最新的，建议用下面的方法安装最新版，或者直接去 Github 上下载</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">## 推荐</span></span><br><span class="line">npm install --save https://github.com/Stezii/tilestrata-postgismvt</span><br></pre></td></tr></table></figure><p>或者通过 package.json</p><figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="attr">&quot;tilestrata-postgismvt&quot;</span><span class="punctuation">:</span> <span class="string">&quot;github:Stezii/tilestrata-postgismvt&quot;</span></span><br></pre></td></tr></table></figure><h1 id="3-使用"><a href="#3-使用" class="headerlink" title="3. 使用"></a>3. 使用</h1><p>前提：一个导入了 osm 数据的支持 PostGIS 2.4.0 的数据服务器</p><p>执行 mapbox 的脚本<a href="https://github.com/mapbox/postgis-vt-util/blob/master/src/TileBBox.sql">TileBBox.sql</a>添加<code>TileBBox</code>函数</p><h2 id="3-1-普通模式"><a href="#3-1-普通模式" class="headerlink" title="3.1 普通模式"></a>3.1 普通模式</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">&quot;tilestrata&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> postgismvt = <span class="built_in">require</span>(<span class="string">&quot;tilestrata-postgismvt&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> headers = <span class="built_in">require</span>(<span class="string">&quot;tilestrata-headers&quot;</span>);</span><br><span class="line"></span><br><span class="line">server = <span class="title function_">tilestrata</span>();</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&quot;osm_mvt&quot;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&quot;tile.mvt&quot;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">headers</span>(&#123;</span><br><span class="line">      <span class="string">&quot;Access-Control-Allow-Origin&quot;</span>: <span class="string">&quot;*&quot;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">postgismvt</span>(&#123;</span><br><span class="line">      <span class="attr">lyr</span>: &#123;</span><br><span class="line">        <span class="attr">table</span>: <span class="string">&quot;import.osm_buildings&quot;</span>, <span class="comment">// 表名</span></span><br><span class="line">        <span class="attr">geometry</span>: <span class="string">&quot;geometry&quot;</span>, <span class="comment">// geometry在表中的字段名</span></span><br><span class="line">        <span class="attr">srid</span>: <span class="number">3857</span>, <span class="comment">// 坐标系</span></span><br><span class="line">        <span class="attr">minZoom</span>: <span class="number">3</span>,</span><br><span class="line">        <span class="attr">maxZoom</span>: <span class="number">19</span>,</span><br><span class="line">        <span class="attr">buffer</span>: <span class="number">10</span>, <span class="comment">// 裁剪geometry时的缓冲距离，PostGIS默认为256</span></span><br><span class="line">        <span class="attr">fields</span>: <span class="string">&quot;name&quot;</span>, <span class="comment">// 额外字段名</span></span><br><span class="line">        <span class="attr">resolution</span>: <span class="number">256</span> <span class="comment">// 分辨率，可以是function</span></span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">pgConfig</span>: &#123;</span><br><span class="line">        <span class="comment">// 通过`pgConfig`属性配置PostGIS相关属性</span></span><br><span class="line">        <span class="attr">host</span>: <span class="string">&quot;localhost&quot;</span>,</span><br><span class="line">        <span class="attr">user</span>: <span class="string">&quot;gis&quot;</span>,</span><br><span class="line">        <span class="attr">password</span>: <span class="string">&quot;gis&quot;</span>,</span><br><span class="line">        <span class="attr">database</span>: <span class="string">&quot;gis&quot;</span>,</span><br><span class="line">        <span class="attr">port</span>: <span class="string">&quot;6543&quot;</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">8080</span>);</span><br></pre></td></tr></table></figure><h2 id="3-2-点云数据"><a href="#3-2-点云数据" class="headerlink" title="3.2 点云数据"></a>3.2 点云数据</h2><p>如果是点要素，可以使用 cluster 模式，只要定义<code>lry</code>中的<code>mode</code>属性为<code>cluster_fields、cluster或function</code>,注意的是需要同时定义<code>type</code>为<code>circle</code></p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">server.<span class="title function_">layer</span>(<span class="string">&#x27;layer_name&#x27;</span>).<span class="title function_">route</span>(<span class="string">&#x27;tile.mvt&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">postgismvt</span>(&#123;</span><br><span class="line">    <span class="attr">lyr</span>: &#123;</span><br><span class="line">      ...</span><br><span class="line">      <span class="attr">mode</span>: <span class="string">&#x27;cluster&#x27;</span> <span class="comment">// or &#x27;cluster_fields&#x27;</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">pgConfig</span>: &#123;</span><br><span class="line">      ...</span><br><span class="line">    &#125;&#125;))</span><br><span class="line">  );</span><br></pre></td></tr></table></figure><p><code>mode</code>和<code>resolution</code>都支持是一个函数。通过这种方式可以手动指定一些缩放级别，用来控制精度</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">server.<span class="title function_">layer</span>(<span class="string">&#x27;layer_name&#x27;</span>).<span class="title function_">route</span>(<span class="string">&#x27;tile.mvt&#x27;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(<span class="title function_">postgismvt</span>(&#123;</span><br><span class="line">    <span class="attr">lyr</span>: &#123;</span><br><span class="line">      <span class="attr">resolution</span>: <span class="keyword">function</span>(<span class="params">server, req</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (req.<span class="property">z</span> &gt; <span class="number">12</span>) <span class="keyword">return</span> <span class="number">512</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">256</span>;</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="attr">mode</span>: <span class="keyword">function</span>(<span class="params">server, req</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (req.<span class="property">z</span> &gt; <span class="number">15</span>) <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">if</span> (req.<span class="property">z</span> &gt; <span class="number">13</span>) <span class="keyword">return</span> <span class="string">&#x27;cluster_fields&#x27;</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&#x27;cluster&#x27;</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">pgConfig</span>: &#123;</span><br><span class="line">      ...</span><br><span class="line">    &#125;&#125;))</span><br><span class="line">  );</span><br></pre></td></tr></table></figure><h1 id="4-效果"><a href="#4-效果" class="headerlink" title="4. 效果"></a>4. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-postgismvt20190606111135.png"></p><h1 id="5-代码浅析"><a href="#5-代码浅析" class="headerlink" title="5. 代码浅析"></a>5. 代码浅析</h1><p>其实核心的方法就是用到了 PostGIS 的<code>ST_AsMVT</code>和<code>ST_AsMVTGeom</code>函数。mapbox 提供<a href="https://github.com/mapbox/postgis-vt-util/blob/master/src/TileBBox.sql">TileBBox.sql</a>用来计算 zxy 位置的瓦片对应的 box 边界，再查询出其中的 geom，转换成 mvt 格式。</p><p>比如说 <code>z=14,x=12917,y=6430</code>的瓦片对应的 sql 如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> ST_AsMVT(q, <span class="string">&#x27;osm_mvt&#x27;</span>, <span class="number">256</span>, <span class="string">&#x27;geom&#x27;</span>) <span class="keyword">AS</span> mvt</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">  <span class="keyword">SELECT</span> ST_AsMVTGeom(</span><br><span class="line">    ST_Transform(</span><br><span class="line">  import.osm_buildings.geometry, <span class="number">3857</span>),</span><br><span class="line">  TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>),</span><br><span class="line">  <span class="number">256</span>,</span><br><span class="line">  <span class="number">10</span>,</span><br><span class="line">      <span class="literal">true</span></span><br><span class="line">) geom ,name</span><br><span class="line">    <span class="keyword">FROM</span> import.osm_buildings</span><br><span class="line">    <span class="keyword">WHERE</span> ST_Intersects(TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>), import.osm_buildings.geometry)</span><br><span class="line">) <span class="keyword">AS</span> q</span><br></pre></td></tr></table></figure><p>此外，<code>fields</code>是可以用逗号分割传递多个想要的字段名的。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/Stezii/tilestrata-postgismvt&quot;&gt;tilestrata-postgismvt&lt;/a&gt;是一个用来从 PostGIS 数据库中检索出 mvt 格式矢量瓦片的插件。PostGIS 在 2.4.0 版本后支持了直接生成 mvt 数据，这让我们更方便的加工出矢量瓦片。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>tilestrata-mapnik 插件使用及浅析</title>
    <link href="https://kaely.net/2019/06/11/tilestrata-mapnik/"/>
    <id>https://kaely.net/2019/06/11/tilestrata-mapnik/</id>
    <published>2019-06-11T03:26:12.000Z</published>
    <updated>2022-10-24T11:44:45.391Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/naturalatlas/tilestrata-mapnik">tilestrata-mapnik</a>顾名思义，mapnik 作为地图数据源，tilestrata 负责瓦片化的一个插件。</p><span id="more"></span><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><h1 id="1-安装"><a href="#1-安装" class="headerlink" title="1. 安装"></a>1. 安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install mapnik --save</span><br><span class="line">npm install tilestrata-mapnik --save</span><br></pre></td></tr></table></figure><p>虽然最新的 mapnik4.0 已经支持了 nodejs 10 版本，这里仍然建议采用<strong>nodejs 8</strong> 环境，更为稳定，如果安装失败可以考虑切换到<strong>nodejs 8</strong></p><h1 id="2-使用"><a href="#2-使用" class="headerlink" title="2. 使用"></a>2. 使用</h1><p>准备好 mapnik 的 xml 配置文件</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> tilestrata = <span class="built_in">require</span>(<span class="string">&quot;tilestrata&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> mapnik = <span class="built_in">require</span>(<span class="string">&quot;tilestrata-mapnik&quot;</span>);</span><br><span class="line"></span><br><span class="line">server = <span class="title function_">tilestrata</span>();</span><br><span class="line"></span><br><span class="line">server</span><br><span class="line">  .<span class="title function_">layer</span>(<span class="string">&quot;layer_name&quot;</span>)</span><br><span class="line">  .<span class="title function_">route</span>(<span class="string">&quot;tile.png&quot;</span>)</span><br><span class="line">  .<span class="title function_">use</span>(</span><br><span class="line">    <span class="title function_">mapnik</span>(&#123;</span><br><span class="line">      <span class="attr">pathname</span>: <span class="string">&quot;style/china.xml&quot;</span></span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line"></span><br><span class="line">server.<span class="title function_">listen</span>(<span class="number">8080</span>);</span><br></pre></td></tr></table></figure><p>tilestrata-mapnik 支持的<code>options</code>有</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">interactivity</span>: <span class="literal">false</span>,</span><br><span class="line">  <span class="attr">xml</span>: <span class="literal">null</span>,</span><br><span class="line">  <span class="attr">pathname</span>: <span class="literal">null</span>,</span><br><span class="line">  <span class="attr">metatile</span>: <span class="number">2</span>,</span><br><span class="line">  <span class="attr">resolution</span>: <span class="number">4</span>,</span><br><span class="line">  <span class="attr">bufferSize</span>: <span class="number">128</span>,</span><br><span class="line">  <span class="attr">tileSize</span>: <span class="number">256</span>,</span><br><span class="line">  <span class="attr">scale</span>: <span class="number">1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="3-效果"><a href="#3-效果" class="headerlink" title="3. 效果"></a>3. 效果</h1><p>demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/tilestrata-mapnik-result-20190606105844.png"></p><h1 id="4-代码浅析"><a href="#4-代码浅析" class="headerlink" title="4. 代码浅析"></a>4. 代码浅析</h1><p>插件会根据传入的<code>options</code>构建一个<a href="https://github.com/mapbox/tilelive-mapnik">tilelive-mapnik</a>对象 source</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">source = <span class="keyword">new</span> <span class="title class_">MapnikBackend</span>(options, <span class="keyword">function</span>(<span class="params">err, result</span>) &#123;</span><br><span class="line">  source = result;</span><br><span class="line">  <span class="title function_">callback</span>(err);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>根据<code>options</code>里面<code>interactivity</code>的值，分别调用了下面两个函数，对数据源会做个是否满足调用条件的判断，如果满足就设置一下<code>options</code>里面数据<code>format</code>的值</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// interactivity为 false，设置 format为 png</span></span><br><span class="line">source.<span class="title function_">getTile</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// interactivity为 true，设置 format为 utf</span></span><br><span class="line">source.<span class="title function_">getGrid</span>();</span><br></pre></td></tr></table></figure><p>针对两种不同的<code>format</code>，分别创建 mapnik 实例</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// format为 png</span></span><br><span class="line">image = <span class="keyword">new</span> mapnik.<span class="title class_">Image</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// format为 utf</span></span><br><span class="line">image = <span class="keyword">new</span> mapnik.<span class="title class_">Grid</span>();</span><br></pre></td></tr></table></figure><p>在请求具体的某个 zxy 瓦片的时候，就使用 mapnik 获取指定瓦片的方法<code>view</code>就好了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">image.<span class="title function_">view</span>(x, y, width, height); <span class="comment">// 获取指定瓦片</span></span><br></pre></td></tr></table></figure><p>流程图</p><pre class="mermaid">graph TDA[tilestrata-mapnik] -->|options| B(tilelive-mapnik)B --> C{interactivity}C -->|true| D[getGrid]D --> F[mapnik.Grid]C -->|false| E[getTile]E --> G[mapnik.Image]F --> H[view]G --> H[view]</pre>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/naturalatlas/tilestrata-mapnik&quot;&gt;tilestrata-mapnik&lt;/a&gt;顾名思义，mapnik 作为地图数据源，tilestrata 负责瓦片化的一个插件。&lt;/p&gt;</summary>
    
    
    
    <category term="TileStrata" scheme="https://kaely.net/categories/TileStrata/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>用PostGIS生成mvt格式的矢量瓦片</title>
    <link href="https://kaely.net/2019/06/10/osm-postgis-mvt/"/>
    <id>https://kaely.net/2019/06/10/osm-postgis-mvt/</id>
    <published>2019-06-10T07:05:16.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>PostGIS 从 2.4.0 版本开始支持通过 box 生成 mvt 数据，我们用尽可能简单的方法搭建一个生产 mvt 数据的测试数据库。</p><span id="more"></span><p>基础工作环境：</p><ol><li><a href="https://docs.docker.com/install/">docker</a></li><li><a href="https://nodejs.org/en/">nodejs 8</a></li></ol><h1 id="1-数据源"><a href="#1-数据源" class="headerlink" title="1. 数据源"></a>1. 数据源</h1><p>从<a href="http://download.geofabrik.de/">geofabrik</a>上我们可以下载的分好地区的<a href="https://www.openstreetmap.org/">OpenStreetMap</a>数据，这样可以得到一个比较小的测试数据集。可以通过下面的地址下载到最新的中国区域数据：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 直接下载国区的数据</span></span><br><span class="line">http://download.geofabrik.de/asia/china-latest.osm.pbf</span><br></pre></td></tr></table></figure><h1 id="2-准备-PostGIS-环境"><a href="#2-准备-PostGIS-环境" class="headerlink" title="2. 准备 PostGIS 环境"></a>2. 准备 PostGIS 环境</h1><p>我们直接用<a href="https://hub.docker.com/r/mdillon/postgis/">mdillon&#x2F;postgis</a>的 docker 镜像跑一个 PostGIS 数据库：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建一个名称为gis网络</span></span><br><span class="line">docker network create gis</span><br><span class="line">docker run -d --name postgis -e POSTGRES_USER=gis --network gis -p 6543:5432 mdillon/postgis:10</span><br></pre></td></tr></table></figure><p>这个镜像可以通过<code>POSTGRES_PASSWORD</code>指定数据库密码，如果没指定的话，密码会和<code>POSTGRES_USER</code>即用户名保持一致。</p><p>建议通过<code>-p 5432:5432</code>映射下端口，方便我们在宿主机上直接连接调试。</p><h1 id="3-导入-osm-数据到-PostGIS"><a href="#3-导入-osm-数据到-PostGIS" class="headerlink" title="3. 导入 osm 数据到 PostGIS"></a>3. 导入 osm 数据到 PostGIS</h1><p>我们用<a href="https://github.com/omniscale/imposm3">imposm3</a>来导入 osm 数据到库中。</p><p>imposm3 主要需要配置 cache 目录（可选），connection 链接地址（必须）和 mapping 映射文件（必须）三个参数，官方仓库提供了一个<a href="https://github.com/omniscale/imposm3/blob/master/example-mapping.json">mapping 样例</a>供参考。这里我们方便测试，就用这个<a href="https://blog-1255388623.cos.ap-shanghai.myqcloud.com/static/mapping.json">简单版本</a>，定义了<code>admim、amenities、buildings</code>三张表。</p><p>准备一个文件夹，把下载的 pbf 和 mapping 文件丢进去，再创建 cache 目录</p><p>依然用上强大的 docker，<a href="https://hub.docker.com/r/jawg/imposm3/">jawg&#x2F;imposm3</a>可以帮助我们方便的执行 imposm3 操作。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker run --network gis --rm \</span><br><span class="line">-v $(pwd)/cache:/tmp/imposm3 \</span><br><span class="line">-v $(pwd)/china-latest.osm.pbf:/opt/imposm3/china-latest.osm.pbf \</span><br><span class="line">-v $(pwd)/mapping.json:/opt/imposm3/mapping.json \</span><br><span class="line">jawg/imposm3 import \</span><br><span class="line">-mapping mapping.json \</span><br><span class="line">-read china-latest.osm.pbf \</span><br><span class="line">-overwritecache -write -connection &#x27;postgis://gis:gis@postgis/gis&#x27;</span><br></pre></td></tr></table></figure><p>耐心等待导入完成，可以直接进入 docker 查看数据情况：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker exec -it postgis psql gis gis</span><br></pre></td></tr></table></figure><p>或者通过第三方软件连接数据库，可以看到 import 导入的三张表：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/osm_mvt_postgis.png"></p><h1 id="4-执行-SQL-脚本"><a href="#4-执行-SQL-脚本" class="headerlink" title="4. 执行 SQL 脚本"></a>4. 执行 SQL 脚本</h1><p>在数据库中执行 mapbox 提供的<a href="https://github.com/mapbox/postgis-vt-util/blob/master/src/TileBBox.sql">TileBBox</a>脚本，创建 TileBBox 函数</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">https://github.com/mapbox/postgis-vt-util/blob/master/src/TileBBox.sql</span><br></pre></td></tr></table></figure><p>这个函数的作用就是传入指定的<code>x,y,z</code>瓦片序列，转换成 MVT 需要的<code>tile coordinate space</code>，通常是 4096x4096 的网格</p><h1 id="5-测试数据结果"><a href="#5-测试数据结果" class="headerlink" title="5. 测试数据结果"></a>5. 测试数据结果</h1><p>mvt 瓦片生成的关键就是<a href="https://postgis.net/docs/ST_AsMVT.html">ST_AsMVT</a>和<a href="https://postgis.net/docs/ST_AsMVTGeom.html">ST_AsMVTGeom</a>两个函数，所以可以直接调用来做个测试，这里查询<code>z=14,y=12917,6430</code>的瓦片对应的 mvt 数据</p><p>先查询出范围内的 geometry，并转化成 mvt 需要的格式</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> ST_AsMVTGeom(</span><br><span class="line">   ST_Transform(</span><br><span class="line">  import.osm_buildings.geometry, <span class="number">3857</span>),</span><br><span class="line">  TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>),</span><br><span class="line">  <span class="number">256</span>,</span><br><span class="line">  <span class="number">10</span>,</span><br><span class="line">      <span class="literal">true</span></span><br><span class="line">   ) geom, name</span><br><span class="line"><span class="keyword">FROM</span> import.osm_buildings</span><br><span class="line"><span class="keyword">WHERE</span> ST_Intersects(TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>), import.osm_buildings.geometry)</span><br></pre></td></tr></table></figure><p>得到结果应该如下：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/asmvtgeom_result.png"></p><p>再使用 ST_AsMVT 进行转换就行，完成的合并后的 sql 如下：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> ST_AsMVT(q, <span class="string">&#x27;osm_mvt&#x27;</span>, <span class="number">256</span>, <span class="string">&#x27;geom&#x27;</span>) <span class="keyword">AS</span> mvt</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">  <span class="keyword">SELECT</span> ST_AsMVTGeom(</span><br><span class="line">    ST_Transform(</span><br><span class="line">  import.osm_buildings.geometry, <span class="number">3857</span>),</span><br><span class="line">  TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>),</span><br><span class="line">  <span class="number">256</span>,</span><br><span class="line">  <span class="number">10</span>,</span><br><span class="line">      <span class="literal">true</span></span><br><span class="line">  ) geom , name</span><br><span class="line">  <span class="keyword">FROM</span> import.osm_buildings</span><br><span class="line">  <span class="keyword">WHERE</span> ST_Intersects(TileBBox(<span class="number">14</span>, <span class="number">12917</span>, <span class="number">6430</span>, <span class="number">3857</span>), import.osm_buildings.geometry)</span><br><span class="line">) <span class="keyword">AS</span> q</span><br></pre></td></tr></table></figure><h1 id="6-数据展示"><a href="#6-数据展示" class="headerlink" title="6. 数据展示"></a>6. 数据展示</h1><p>完整的地图区域数据前端展示可以使用 tilestrata 这个服务端工具，配合 tilestrata-postgismvt 插件就可以生成供地图调用的瓦片。</p><p>具体的 demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>对应的文章可以看一下这篇<a href="http://kaely.net/2019/06/13/tilestrata-postgismvt/">tilestrata-postgismvt 使用</a></p><p>clone 项目<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a>，将项目里面<code>src/mvt/</code>目录下<a href="https://github.com/zzcyrus/tilestrata-sample-code/blob/master/src/mvt/index.js">index.js</a>的<code>pgConfig</code>改成你的设置</p><p>安装依赖，启动项目</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure><p>访问<code>example/mvt.html</code>就可以看到结果了。</p><p>我们导入了三张表，分别是边界数据（蓝色）、设施（红点）和建筑（紫色），全部叠加之后如下图所示：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/mvt_preview.png"></p><h1 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h1><p><a href="https://blog.jawg.io/how-to-make-mvt-with-postgis/">https://blog.jawg.io/how-to-make-mvt-with-postgis/</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;PostGIS 从 2.4.0 版本开始支持通过 box 生成 mvt 数据，我们用尽可能简单的方法搭建一个生产 mvt 数据的测试数据库。&lt;/p&gt;</summary>
    
    
    
    <category term="PostGIS" scheme="https://kaely.net/categories/PostGIS/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>制作openstreetmap-carto风格离线瓦片地图</title>
    <link href="https://kaely.net/2019/06/06/openstreetmap-carto-map/"/>
    <id>https://kaely.net/2019/06/06/openstreetmap-carto-map/</id>
    <published>2019-06-06T07:13:27.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/gravitystorm/openstreetmap-carto">openstreetmap-carto</a>是<a href="https://www.openstreetmap.org/">openstreetmap</a>的默认地图风格，广受好评，各方面均衡，是离线地图的常用选择。</p><span id="more"></span><p>搭建的主要步骤基本围绕<a href="https://kaely.net/2019/03/12/osm-PostGIS-setup/">使用 osm 数据做一个自己的 PostGIS 数据库</a>这篇文章展开。</p><p>我们完成<strong>1、2、3</strong>步骤后，再导入 osm 数据前要做一些修改</p><h1 id="4-依据-openstreetmap-carto-样式导入数据"><a href="#4-依据-openstreetmap-carto-样式导入数据" class="headerlink" title="4. 依据 openstreetmap-carto 样式导入数据"></a>4. 依据 openstreetmap-carto 样式导入数据</h1><p>仍然是安装好 osm2pgsql 工具，下载好中国区 pbf 数据，之后我们要准备样式包</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 下载openstreetmap-carto相关数据文件</span></span><br><span class="line">git <span class="built_in">clone</span> https://github.com/gravitystorm/openstreetmap-carto.git</span><br><span class="line"></span><br><span class="line"><span class="comment"># 导入数据，手动指定carto样式文件</span></span><br><span class="line">osm2pgsql -c -d china -G --slim --hstore --style /home/openstreetmap-carto/openstreetmap-carto.style --tag-transform-script /home/openstreetmap-carto/openstreetmap-carto.lua -C 2000 -p china -r pbf /home/CN</span><br></pre></td></tr></table></figure><h1 id="5-执行各种脚本"><a href="#5-执行各种脚本" class="headerlink" title="5. 执行各种脚本"></a>5. 执行各种脚本</h1><p>进入 clone 下来的<code>openstreetmap-carto</code>文件夹</p><h2 id="5-1-索引"><a href="#5-1-索引" class="headerlink" title="5.1 索引"></a>5.1 索引</h2><p>首先是建立数据库索引，可以加快渲染访问的速度，这步是<strong>可选</strong>的</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">psql -d gis -f indexes.sql</span><br></pre></td></tr></table></figure><h2 id="5-2-下载-shapefile"><a href="#5-2-下载-shapefile" class="headerlink" title="5.2 下载 shapefile"></a>5.2 下载 shapefile</h2><p>样式中用到了海岸线、水域等 shp 文件，需要手动下载，文件较大，<code>openstreetmap-carto</code>已经提供了下载脚本</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scripts/get-shapefiles.py</span><br></pre></td></tr></table></figure><p>如果下载出错，加上<code>-s</code>关闭 shapeindex</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scripts/get-shapefiles.py -s</span><br></pre></td></tr></table></figure><p>如果实在网络不行，可以根据<a href="https://github.com/gravitystorm/openstreetmap-carto/blob/master/INSTALL.md#manual-download">安装指南</a>中说明手动下载这些文件，放在<code>openstreetmap-carto/data</code>目录下就好</p><h2 id="5-3-下载字体"><a href="#5-3-下载字体" class="headerlink" title="5.3 下载字体"></a>5.3 下载字体</h2><p>如果是用了 Ubuntu&#x2F;Debian 服务器，可以直接安装</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install fonts-noto-cjk fonts-noto-hinted fonts-noto-unhinted fonts-hanazono ttf-unifont</span><br></pre></td></tr></table></figure><p>其他系统也可以<a href="https://github.com/gravitystorm/openstreetmap-carto/blob/master/INSTALL.md#installation-on-other-operation-systems">手动下载字体文件并安装</a></p><h1 id="6-渲染"><a href="#6-渲染" class="headerlink" title="6. 渲染"></a>6. 渲染</h1><p>准备好<code>nodejs</code>环境，渲染工具使用 tilestrata 这个服务端工具，配合 tilestrata-mapnik 插件就可以生成供地图调用的瓦片。</p><p>具体的 demo 项目可以参考<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a></p><p>对应的文章可以看一下这篇<a href="https://kaely.net/2019/06/11/tilestrata-mapnik/">tilestrata-mapnik 使用</a></p><h2 id="6-1-准备-mapnik-配置文件"><a href="#6-1-准备-mapnik-配置文件" class="headerlink" title="6.1 准备 mapnik 配置文件"></a>6.1 准备 mapnik 配置文件</h2><p>因为使用了 mapnik，要将<code>openstreetmap-carto</code>的<a href="https://github.com/gravitystorm/openstreetmap-carto/blob/master/project.mml">project.mml</a>转换成 mapnik 可识别的 xml 文件</p><p>打开文件夹下面的<code>project.mml</code>文件，找到如下字段，修改成你数据库的相关配置信息</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/carto_project_mml.png"></p><p>安装<code>carto</code>工具包进行转换</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo npm install -g carto</span><br><span class="line">carto project.mml &gt; osm.xml</span><br></pre></td></tr></table></figure><h2 id="6-2-准备服务端"><a href="#6-2-准备服务端" class="headerlink" title="6.2 准备服务端"></a>6.2 准备服务端</h2><p>clone 项目<a href="https://github.com/zzcyrus/tilestrata-sample-code">tilestrata-sample-code</a>，将项目里面<code>/src/mapnik/</code>目录下<a href="https://github.com/zzcyrus/tilestrata-sample-code/blob/master/src/mapnik/index.js">index.js</a>增加以下内容</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">server</span><br><span class="line">  .layer(&#x27;osm&#x27;)</span><br><span class="line">  .route(&#x27;tile.png&#x27;)</span><br><span class="line">  .use(</span><br><span class="line">    mapnik(&#123;</span><br><span class="line">      pathname: &#x27;style/osm.xml&#x27;</span><br><span class="line">    &#125;)</span><br><span class="line">  )</span><br></pre></td></tr></table></figure><p>将<code>openstreetmap-carto</code>文件夹下面的<code>data，symbols，osm.xml（刚生成的）</code>三个文件（夹）拷贝到<code>tilestrata-sample-code</code>的<code>style</code>文件夹下，拷贝完成的目录结构如下所示：</p><p><img src="http://blog-img-1255388623.cossh.myqcloud.com/osm_mapnik_folder.png"></p><p>安装相关依赖并启动项目</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure><p>将<code>/example/mapnik.html</code>文件里的 tileLayer 地址换成<code>&quot;http://127.0.0.1:9527/osm/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;/tile.png&quot;,</code>，打开文件即可看到效果</p><h1 id="7-对比结果"><a href="#7-对比结果" class="headerlink" title="7 对比结果"></a>7 对比结果</h1><p>官网<br><img src="http://blog-img-1255388623.cossh.myqcloud.com/osm.png" alt="官网效果"></p><p>我们自己生成的<br><img src="http://blog-img-1255388623.cossh.myqcloud.com/osm_mapnik.png"></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;a href=&quot;https://github.com/gravitystorm/openstreetmap-carto&quot;&gt;openstreetmap-carto&lt;/a&gt;是&lt;a href=&quot;https://www.openstreetmap.org/&quot;&gt;openstreetmap&lt;/a&gt;的默认地图风格，广受好评，各方面均衡，是离线地图的常用选择。&lt;/p&gt;</summary>
    
    
    
    <category term="PostGIS" scheme="https://kaely.net/categories/PostGIS/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>使用osm数据做一个自己的PostGIS数据库</title>
    <link href="https://kaely.net/2019/03/12/osm-PostGIS-setup/"/>
    <id>https://kaely.net/2019/03/12/osm-PostGIS-setup/</id>
    <published>2019-03-12T11:54:08.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>PostGIS 是目前开源方案里面使用广泛的一种地理信息数据库，可玩性很强，实用性也很强，为了实现更多的地理信息操作，我们先来搭建一个简单的 PostGIS 数据库。</p><span id="more"></span><h1 id="1-安装步骤"><a href="#1-安装步骤" class="headerlink" title="1. 安装步骤"></a>1. 安装步骤</h1><ol><li>准备一个 CentOS 7.x x64 环境</li><li>安装数据库 PostgreSQL 9.6</li><li>安装对应版本的 PostGIS 2.2.2</li><li>导入 openstreetmap 的中国区域数据</li></ol><h1 id="2-PostgreSQL-9-6"><a href="#2-PostgreSQL-9-6" class="headerlink" title="2. PostgreSQL 9.6"></a>2. PostgreSQL 9.6</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装数据库</span></span><br><span class="line">yum install https://download.postgresql.org/pub/repos/yum/9.6/redhat/rhel-7-x86_64/pgdg-centos96-9.6-3.noarch.rpm</span><br><span class="line"></span><br><span class="line">yum install postgresql96</span><br><span class="line"></span><br><span class="line">yum install postgresql96-server</span><br><span class="line"></span><br><span class="line"><span class="comment"># 初始化数据库及服务</span></span><br><span class="line">/usr/pgsql-9.6/bin/postgresql96-setup initdb</span><br><span class="line">systemctl <span class="built_in">enable</span> postgresql-9.6</span><br><span class="line">systemctl start postgresql-9.6</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置环境变量</span></span><br><span class="line"><span class="built_in">export</span> PATH=/usr/pgsql-9.6/bin:<span class="variable">$PATH</span></span><br><span class="line"><span class="built_in">export</span> LD_LIBRARY_PATH=/usr/pgsql-9.6/lib:<span class="variable">$LD_LIBRARY_PATH</span></span><br><span class="line"><span class="built_in">export</span> PGDATA=/home/postgres/postgresql_data</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 以下可选</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 为数据库设置密码</span></span><br><span class="line">su - postgres</span><br><span class="line">psql</span><br><span class="line">postgres=<span class="comment"># ALTER USER postgres WITH PASSWORD &#x27;postgres&#x27;;</span></span><br><span class="line">postgres=<span class="comment"># \q</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 开放防火墙端口</span></span><br><span class="line">firewall-cmd --permanent --add-port=5432/tcp</span><br><span class="line">firewall-cmd --reload</span><br><span class="line"></span><br><span class="line"><span class="comment"># 修改配置文件允许外部使用密码访问</span></span><br><span class="line">vim /var/lib/pgsql/9.6/data/postgresql.conf</span><br><span class="line">listen_addresses = <span class="string">&#x27;localhost&#x27;</span>  为  listen_addresses=<span class="string">&#x27;*&#x27;</span></span><br><span class="line"></span><br><span class="line">vim /var/lib/pgsql/9.6/data/pg_hba.conf</span><br><span class="line">host    all            all      0.0.0.0/0      md5</span><br><span class="line"></span><br></pre></td></tr></table></figure><h1 id="3-PostGIS"><a href="#3-PostGIS" class="headerlink" title="3. PostGIS"></a>3. PostGIS</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装PostGIS</span></span><br><span class="line">sudo yum -y install epel-release</span><br><span class="line">sudo yum install postgis24_96</span><br><span class="line"></span><br><span class="line"><span class="comment"># 初始化数据库osm</span></span><br><span class="line">createdb osm</span><br><span class="line"></span><br><span class="line">psql -h 127.0.0.1 -d osm -U postgres -f /usr/pgsql-9.6/share/contrib/postgis-2.4/postgis.sql</span><br><span class="line">psql -h 127.0.0.1 -d osm -U postgres -f /usr/pgsql-9.6/share/contrib/postgis-2.4/spatial_ref_sys.sql</span><br><span class="line">psql -d osm -c <span class="string">&#x27;CREATE EXTENSION postgis; CREATE EXTENSION hstore;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 可以基于osm创建china数据库（把osm作为一个可重复备份的库）</span></span><br><span class="line">createdb -T osm china</span><br><span class="line"></span><br></pre></td></tr></table></figure><h1 id="4-osm-数据导入"><a href="#4-osm-数据导入" class="headerlink" title="4. osm 数据导入"></a>4. osm 数据导入</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装导入工具osm2pgsql</span></span><br><span class="line">yum install osm2pgsql</span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载中国区域的公开数据</span></span><br><span class="line">wget http://download.gisgraphy.com/openstreetmap/pbf/CN.tar.bz2</span><br><span class="line"></span><br><span class="line"><span class="comment"># 解压数据</span></span><br><span class="line">tar jxvf CN.tar.bz2</span><br><span class="line"></span><br><span class="line"><span class="comment"># 开始导入数据到china数据库</span></span><br><span class="line">osm2pgsql -c -d china --slim --hstore -C 2000 -p china -r pbf /home/parallels/Downloads/CN</span><br></pre></td></tr></table></figure><p>#5. 测试</p><p>至此，我们已经基于 osm 的数据有了一个地理信息库，我们可以通过以下 sql 做一些简单的测试</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name <span class="keyword">FROM</span> china_polygon <span class="keyword">WHERE</span> name <span class="operator">~</span> <span class="string">&#x27;南京&#x27;</span>;</span><br></pre></td></tr></table></figure><p>结果：<br><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/osm-postgis-polygon-202202201215836.png"></p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> p1.name,p2.name,ST_Distance(p1.way,p2.way) <span class="keyword">FROM</span></span><br><span class="line">(<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> china_point <span class="keyword">WHERE</span> place<span class="operator">=</span><span class="string">&#x27;city&#x27;</span> <span class="keyword">AND</span> name <span class="operator">=</span> <span class="string">&#x27;南京市&#x27;</span>) p1 ,</span><br><span class="line">(<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> china_point <span class="keyword">WHERE</span> place<span class="operator">=</span><span class="string">&#x27;city&#x27;</span> <span class="keyword">AND</span> name <span class="operator">=</span> <span class="string">&#x27;北京市&#x27;</span>) p2</span><br></pre></td></tr></table></figure><p>结果：<br><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/osm-postgis-distance-202202201214562.png"></p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name, ST_AsText(ST_Transform(way,<span class="number">4326</span>)) <span class="keyword">FROM</span> china_point <span class="keyword">WHERE</span> place<span class="operator">=</span><span class="string">&#x27;city&#x27;</span>;</span><br></pre></td></tr></table></figure><p>结果：<br><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/osm-postgis-point-202202201215470.png"></p><p>当然数据库的功能不局限于此，基于此数据库我们可以做些更有趣的事情，今后我们会慢慢探讨。</p><h1 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h1><p><a href="https://github.com/digoal/blog/blob/master/201609/20160906_01.md">digoal 的博客</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;PostGIS 是目前开源方案里面使用广泛的一种地理信息数据库，可玩性很强，实用性也很强，为了实现更多的地理信息操作，我们先来搭建一个简单的 PostGIS 数据库。&lt;/p&gt;</summary>
    
    
    
    <category term="PostGIS" scheme="https://kaely.net/categories/PostGIS/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>mongoDB中地理空间查询指北</title>
    <link href="https://kaely.net/2019/01/20/mongoDB-geospatial/"/>
    <id>https://kaely.net/2019/01/20/mongoDB-geospatial/</id>
    <published>2019-01-20T09:41:24.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>现在地理空间查询是越来越常见的一种需求，实现的方式也有很多种，相较于PostGIS，mongoDB既可以直接存储GeoJSON对象，又支持地理空间索引，可以满足高效的常见地理空间服务。</p><span id="more"></span><p><a href="https://github.com/zzcyrus/mongoDB-geospatial-demos">一个可供参考的案例仓库</a></p><h2 id="索引方式介绍"><a href="#索引方式介绍" class="headerlink" title="索引方式介绍"></a>索引方式介绍</h2><h3 id="2d-索引"><a href="#2d-索引" class="headerlink" title="2d 索引"></a>2d 索引</h3><p><a href="https://docs.mongodb.com/manual/core/2d/">官方介绍</a></p><p>2d index 通常用在2维平面上的点数据，如果你的MongoDB 版本小于2.2或者你不使用一些GeoJSON类型的数据，官方推荐你使用2d index。</p><h3 id="2dsphere-索引"><a href="#2dsphere-索引" class="headerlink" title="2dsphere 索引"></a>2dsphere 索引</h3><p><a href="https://docs.mongodb.com/manual/core/2dsphere/">官方介绍</a></p><p>2dsphere index 支持所有的mongoDB空间查询，支持类地球的球面上几何要素的查询，支持数据存储为GeoJSON或者是传统坐标。</p><p><em>所以如果你正在做一个现代化的GIS应用，建议直接采取2dsphere索引，这将会非常方便，GeoJSON格式的直接导入，配合WebGIS的直接查询或插入</em></p><h2 id="查询方法介绍"><a href="#查询方法介绍" class="headerlink" title="查询方法介绍"></a>查询方法介绍</h2><p><a href="https://docs.mongodb.com/manual/reference/operator/query-geospatial/">官方介绍</a></p><p>下面这个表格包含了一些个人的总结和官方的说明，具体查询的作用其实通过查询方法的名字就能猜测出来，所以不详细介绍了</p><p>|查询方式|2d索引|sphere索引|几何类型（2d&#x2F;sphere）|<br>| — | :-: | :-: | :-: | — |<br>|$near| ✅ | ✅ | 平面&#x2F;球面 |<br>|$nearSphere | ✅ | ✅ | 球面 |<br>|$geoWithin:{$center:…}| ✅ | ⚠️ | 平面 |<br>|$geoWithin:{$centerSphere:…} | ✅ | ✅ | 球面 |<br>|$geoWithin:{$box:…}| ✅ | ⚠️ | 平面 |<br>|$geoWithin:{$polygon:…}| ✅ | ⚠️ | 平面 |<br>|$geoWithin:{$geometry:…}| ❎ | ✅ | 球面 |<br>|$geoIntersects | ❎ | ✅ | 球面 |</p><blockquote><p>说明： ✅ 支持 ❎ 不支持 ⚠️ 官方文档说不支持，但测试发现可以使用</p></blockquote><h2 id="使用注意事项"><a href="#使用注意事项" class="headerlink" title="使用注意事项"></a>使用注意事项</h2><ol><li>在2dsphere索引中使用$geometry去查询，如果自己构造$geometry，要注意geometry的coordinates必须是一个<code>闭合</code>的数组，首尾两项要一致。</li><li>与此同时，在使用$polygon去查询时，<code>不必</code>是一个闭合的坐标数组列，mongoDB会自动帮我们关闭这个图形</li><li>在使用$near或者$geoWithin之类的查询中会涉及到距离、半径定义，这里如果你使用经纬度作为坐标，使用m（米）作为距离、半径单位，那么要进行<code>转换</code>，具体的转换规则可以参考下面两段代码示例：</li></ol><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 1度大致对应111千米，所以需要/111000,具体可以细化</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">*</span>&#125; meter 米</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">m2degree</span> = (<span class="params">meter</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">return</span> meter / <span class="number">111000</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> query = &#123;</span><br><span class="line">  <span class="attr">location</span>: &#123;</span><br><span class="line">    <span class="attr">$geoWithin</span>: &#123;</span><br><span class="line">      <span class="attr">$center</span>: [[lon, lat], <span class="title function_">m2degree</span>(radius)]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 米转换成弧度，需要除以地球半径，大约6378100米</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &#123;<span class="type">*</span>&#125; <span class="variable">meter</span></span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">m2rad</span> = (<span class="params">meter</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">return</span> meter / <span class="number">6378100</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> query = &#123;</span><br><span class="line">  <span class="attr">location</span>: &#123;</span><br><span class="line">    <span class="attr">$nearSphere</span>: [lon, lat],</span><br><span class="line">    <span class="attr">$maxDistance</span>: <span class="title function_">m2rad</span>(maxDistance)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="demo"><a href="#demo" class="headerlink" title="demo"></a>demo</h2><p>这里提供<a href="https://github.com/zzcyrus/mongoDB-geospatial-demos">一个可供参考的简单的案例仓库</a><br>案例中使用了中国天气网上的749个气象站点温度数据作为数据源，分别建立两种类型的索引，对每种地理查询都做了接口化，你可以<strong>自行运行探索</strong>，或者为其增加一个WebGIS的界面，直接进行操作，体验一下mongoDB地理查询的快捷。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;现在地理空间查询是越来越常见的一种需求，实现的方式也有很多种，相较于PostGIS，mongoDB既可以直接存储GeoJSON对象，又支持地理空间索引，可以满足高效的常见地理空间服务。&lt;/p&gt;</summary>
    
    
    
    <category term="Node" scheme="https://kaely.net/categories/Node/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
  </entry>
  
  <entry>
    <title>【译】Vue中父子组件的生命周期函数</title>
    <link href="https://kaely.net/2018/06/29/Vue-Parent-and-Child-lifecycle-hooks/"/>
    <id>https://kaely.net/2018/06/29/Vue-Parent-and-Child-lifecycle-hooks/</id>
    <published>2018-06-29T07:42:41.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>最近项目中遇到了地图模块挂载到DOM节点并进行初始化的问题，背后其实就是Vue中父子组件初始化的顺序问题，发现这篇文章的例子通俗易懂，遂翻译一下，如果错误还请指正。</p><span id="more"></span><p><a href="https://medium.com/@brockreece/vue-parent-and-child-lifecycle-hooks-5d6236bd561f">原文链接</a></p><p>如果你在项目中使用Vue，那么我肯定你很熟悉组件生命周期内的钩子函数，但是你未必清楚父子组件内生命周期钩子触发的顺序和对属性（props）的影响</p><p style="text-align:center;"><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/vue-lifecircle-202202201213018.png" width="50%" height="50%"></p><h2 id="父-x2F-子组件内的生命周期钩子"><a href="#父-x2F-子组件内的生命周期钩子" class="headerlink" title="父&#x2F;子组件内的生命周期钩子"></a>父&#x2F;子组件内的生命周期钩子</h2><p>下面的例子在父子组件的<strong>Mounted</strong>和<strong>Created</strong>两个钩子触发时会给出相应的提示。如你所见，<strong>Created</strong>是正常的先父后子的顺序触发，但<strong>Mounted</strong>钩子则正好相反</p><iframe src="https://codesandbox.io/embed/j38wnqjy65" style="width:100%; height:500px; border:0; border-radius: 4px; overflow:hidden;" sandbox="allow-modals allow-forms allow-popups allow-scripts allow-same-origin"></iframe><p>一开始这可能很难很难理解，但如果我们通过逻辑的角度去遵循组件初始化工作流程，就能理解这种顺序。我们只要记住，父组件在其挂载template到DOM之前必须等待所有的子组件都完成挂载（mounted）操作。</p><p style="text-align:center;"><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/vue-parent-child-hooks-202202201213815.png" width="50%" height="50%"></p><p>若果你的组件通过属性（props）来通讯，以上钩子的执行顺序会很重要。</p><h2 id="属性（props）的响应式（reactivity）"><a href="#属性（props）的响应式（reactivity）" class="headerlink" title="属性（props）的响应式（reactivity）"></a>属性（props）的响应式（reactivity）</h2><p>一个组件在<strong>Created</strong>钩子触发之前就可以是响应式的，这意味在其父组件的<strong>Mounted</strong>钩子触发之前它就能够开始追踪属性（props）的变化。如果你在父组件的<strong>Mounted</strong>钩子中去设置一些属性的值，你一定要牢记这一点。</p><p>下面的例子展示了如果在父组件的<strong>Created</strong>和<strong>Mounted</strong>两个钩子中都去设置子组件的属性值会发生什么。如你所见，在子组件的<strong>Mounted</strong>钩子中我们监测到了两次对属性值的改变操作（个人补充：父组件<strong>Mounted</strong>之前的data里面对属性赋值触发了一次，父组件<strong>Mounted</strong>时候的改变属性值又让子组件触发了一次）<br></p><iframe src="https://codesandbox.io/embed/z2yonj9kq4" style="width:100%; height:500px; border:0; border-radius: 4px; overflow:hidden;" sandbox="allow-modals allow-forms allow-popups allow-scripts allow-same-origin"></iframe><p>假设你的子组件在属性值改变的时候会进行一次Ajax请求，这样就会触发两次请求，甚至造成请求的未响应。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>如果你想深入的了解Vue的生命周期钩子，我强烈建议你读一下<a href="https://alligator.io/vuejs/component-lifecycle/">这篇文章</a>。</p><p>但通常来说，我建议如果你只在组件的<strong>Mounted</strong>钩子进行需要和DOM交互的操作，而其他的操作都放在<strong>Created</strong>钩子中</p><h3 id="个人总结"><a href="#个人总结" class="headerlink" title="个人总结"></a>个人总结</h3><p>从生命周期的角度来看： 父 Created - 子 Created - 子 Mounted - 父 Mounted<br>对应过程Props的改变： 子 prop 改变 ——— 子 prop 改变 ——— 子 prop 改变  </p>]]></content>
    
    
    <summary type="html">&lt;p&gt;最近项目中遇到了地图模块挂载到DOM节点并进行初始化的问题，背后其实就是Vue中父子组件初始化的顺序问题，发现这篇文章的例子通俗易懂，遂翻译一下，如果错误还请指正。&lt;/p&gt;</summary>
    
    
    
    <category term="前端" scheme="https://kaely.net/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
    <category term="FE" scheme="https://kaely.net/tags/FE/"/>
    
    <category term="Vue" scheme="https://kaely.net/tags/Vue/"/>
    
  </entry>
  
  <entry>
    <title>矢量切片的使用尝试1—openlayers应用</title>
    <link href="https://kaely.net/2017/11/12/vector-tile-openlayers-try/"/>
    <id>https://kaely.net/2017/11/12/vector-tile-openlayers-try/</id>
    <published>2017-11-12T02:54:57.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>对于 GIS 行业来说，栅格切片已经处于垄断地位很长时间了，但随着 mapbox 的发展，矢量切片越来越勾引起大家探究的欲望，最近我也小小的观望了下，发现现在技术还是比较成熟了，使用上却不是很广泛，遂分享下自己瞎折腾的一些路，想着帮助大家少踩点坑，本文将介绍下服务端和网页端中 OpenLayers 对于矢量切片的一些应用。</p><span id="more"></span><p>在项目实际使用中还是建议大家首先了解下矢量和栅格切片的优缺点，网上能搜到的很多，水平有限，这里不献丑了。对于我个人来说吸引我的无非三点：1.支持大数据；2.自定义渲染；3.要素交互。</p><p>不多说了进入主题吧，OpenLayers 中支持<code>ol.source.VectorTile</code>,对于我们来说想使用它无非就是创造这一类型的<code>source</code>，目前来说想要自定义矢量切片源我探索的有以下几种方式：</p><h1 id="geoserver-中的-vectortiles-plugin-插件"><a href="#geoserver-中的-vectortiles-plugin-插件" class="headerlink" title="geoserver 中的 vectortiles-plugin 插件"></a>geoserver 中的 vectortiles-plugin 插件</h1><p>这种方式网上教程很多，是大家目前常用的，优点是支持的数据格式比较多，输出的数据格式也很多，geojson、topojson、mvt 都能做到，不做过多介绍</p><h1 id="mapbox-开发的-geojson-vt-库"><a href="#mapbox-开发的-geojson-vt-库" class="headerlink" title="mapbox 开发的 geojson-vt 库"></a>mapbox 开发的 geojson-vt 库</h1><p>这个库可能大家不去仔细关注都不会发现，mapbox 推出，<a href="https://github.com/mapbox/geojson-vt">geojson-vt</a>，作用很简单，官方说明简单明了，把 geojson 转换成 mvt 格式的矢量数据源。</p><p>翻译一下使用：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 通过geojson数据源构建切片索引</span></span><br><span class="line"><span class="keyword">var</span> tileIndex = <span class="title function_">geojsonvt</span>(geoJSON);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过z,x,y来在切片索引中请求某个具体瓦片</span></span><br><span class="line"><span class="keyword">var</span> features = tileIndex.<span class="title function_">getTile</span>(z, x, y).<span class="property">features</span>;</span><br></pre></td></tr></table></figure><p>拿到 features 后无非就是做样式调整之类的工作了。OpenLayers 官方也有一个 demo，叫做<a href="https://openlayers.org/en/latest/examples/geojson-vt.html">geojson-vt integration</a>，详细介绍了怎么在 OpenLayers 中结合使用该库。</p><p>我在实际使用中发现，这个库可以说很 imba 了，有测试 200M 以上的 geojson 源文件，都能流畅的展示出来。mapbox 官方对于这个库的说明是，把 geojson 切割成矢量切片在<strong>浏览器端</strong>使用，所以我觉得因为<strong>网络传输</strong>的不确定性，对于<strong>小一点</strong>的数据量，可以考虑直接在浏览器端使用这个库。</p><h1 id="geojson-vt-的-nodejs-服务端实现"><a href="#geojson-vt-的-nodejs-服务端实现" class="headerlink" title="geojson-vt 的 nodejs 服务端实现"></a>geojson-vt 的 nodejs 服务端实现</h1><p>(｡･∀･)ﾉﾞ嗨，既然是 js，那就意味着，我们可以用 nodejs 搭建服务端呀，在浏览到某个层级时候，只请求当前需要的瓦片，不就能解决大数据的传输问题了，nodejs 大法好！</p><p><a href="https://github.com/zzcyrus/WebGIS-demos/blob/main/openlayers/1.vector_tile">本文具体代码</a></p><p>这里对一些主要部分做一些说明</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 读取数据源文件，构建切片索引</span></span><br><span class="line"><span class="keyword">const</span> dataFile = <span class="string">&quot;./data/world.json&quot;</span>;</span><br><span class="line"><span class="keyword">const</span> dataSource = <span class="title class_">JSON</span>.<span class="title function_">parse</span>(fs.<span class="title function_">readFileSync</span>(dataFile));</span><br><span class="line"><span class="keyword">const</span> tileIndex = <span class="title function_">geojsonvt</span>(dataSource, &#123;</span><br><span class="line">  <span class="attr">extent</span>: <span class="number">4096</span>,</span><br><span class="line">  <span class="attr">debug</span>: <span class="number">1</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 从url中解析瓦片请求位置的x，y，z</span></span><br><span class="line">app.<span class="title function_">use</span>(<span class="keyword">async</span> (ctx, next) =&gt; &#123;</span><br><span class="line">  <span class="keyword">let</span> path = ctx.<span class="property">request</span>.<span class="property">path</span>.<span class="title function_">toString</span>();</span><br><span class="line">  ctx.<span class="property">response</span>.<span class="property">type</span> = <span class="string">&quot;application/json&quot;</span>;</span><br><span class="line">  <span class="keyword">if</span> (path.<span class="title function_">indexOf</span>(<span class="string">&quot;.vector&quot;</span>) !== -<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> pathArr = path.<span class="title function_">substring</span>(<span class="number">1</span>, path.<span class="title function_">indexOf</span>(<span class="string">&quot;.vector&quot;</span>)).<span class="title function_">split</span>(<span class="string">&quot;/&quot;</span>);</span><br><span class="line">    <span class="keyword">const</span> z = pathArr[pathArr.<span class="property">length</span> - <span class="number">3</span>];</span><br><span class="line">    <span class="keyword">const</span> x = pathArr[pathArr.<span class="property">length</span> - <span class="number">2</span>];</span><br><span class="line">    <span class="keyword">const</span> y = pathArr[pathArr.<span class="property">length</span> - <span class="number">1</span>];</span><br><span class="line">    <span class="keyword">const</span> data = tileIndex.<span class="title function_">getTile</span>(<span class="title class_">Number</span>(z), <span class="title class_">Number</span>(x), <span class="title class_">Number</span>(y));</span><br><span class="line">    <span class="keyword">const</span> features = <span class="title class_">JSON</span>.<span class="title function_">stringify</span>(</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="attr">type</span>: <span class="string">&quot;FeatureCollection&quot;</span>,</span><br><span class="line">        <span class="attr">features</span>: data ? data.<span class="property">features</span> : [],</span><br><span class="line">      &#125;,</span><br><span class="line">      replacer</span><br><span class="line">    ); <span class="comment">// replacer 是geojson格式转换函数</span></span><br><span class="line">    ctx.<span class="property">response</span>.<span class="property">body</span> = features;</span><br><span class="line">    <span class="keyword">await</span> <span class="title function_">next</span>();</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    ctx.<span class="property">response</span>.<span class="property">body</span> = <span class="string">&quot;Error&quot;</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>接下来是浏览器中的调用</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> vectorSource = <span class="keyword">new</span> ol.<span class="property">source</span>.<span class="title class_">VectorTile</span>(&#123;</span><br><span class="line">  <span class="comment">// 因为转换函数是geojson格式，所以这里format为geojson</span></span><br><span class="line">  <span class="attr">format</span>: <span class="keyword">new</span> ol.<span class="property">format</span>.<span class="title class_">GeoJSON</span>(&#123;</span><br><span class="line">    <span class="comment">// 要定义数据源的坐标系为瓦片像素</span></span><br><span class="line">    <span class="attr">defaultDataProjection</span>: <span class="keyword">new</span> ol.<span class="property">proj</span>.<span class="title class_">Projection</span>(&#123;</span><br><span class="line">      <span class="attr">code</span>: <span class="string">&quot;TILE_PIXELS&quot;</span>,</span><br><span class="line">      <span class="attr">units</span>: <span class="string">&quot;tile-pixels&quot;</span>,</span><br><span class="line">    &#125;),</span><br><span class="line">  &#125;),</span><br><span class="line">  <span class="comment">// 调用时候要注意z，x，y 的位置，实际上是与服务端相对应的</span></span><br><span class="line">  <span class="attr">url</span>: <span class="string">&quot;http://localhost:3000/gettile/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;.vector&quot;</span>,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>你可以在 <a href="https://github.com/zzcyrus/WebGIS-demos/tree/main/openlayers/1.vector_tile">github</a> 上看到 demo，执行安装启动</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd ./1.vector_tile</span><br><span class="line">npm install</span><br><span class="line">npm start</span><br></pre></td></tr></table></figure><p>打开 vectortile.hmtl 可以在 network 中看到瓦片请求了。</p><p><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/vector-tile-network-202202201207900.png"></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;对于 GIS 行业来说，栅格切片已经处于垄断地位很长时间了，但随着 mapbox 的发展，矢量切片越来越勾引起大家探究的欲望，最近我也小小的观望了下，发现现在技术还是比较成熟了，使用上却不是很广泛，遂分享下自己瞎折腾的一些路，想着帮助大家少踩点坑，本文将介绍下服务端和网页端中 OpenLayers 对于矢量切片的一些应用。&lt;/p&gt;</summary>
    
    
    
    <category term="WebGIS" scheme="https://kaely.net/categories/WebGIS/"/>
    
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
    <category term="OpenLayers" scheme="https://kaely.net/tags/OpenLayers/"/>
    
  </entry>
  
  <entry>
    <title>Leaflet常用的一些插件</title>
    <link href="https://kaely.net/2017/09/17/plugins/"/>
    <id>https://kaely.net/2017/09/17/plugins/</id>
    <published>2017-09-17T05:48:15.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>Leaflet 中常用的 gis 功能可以满足一般使用，有些特殊需求，官方也有<a href="http://leafletjs.com/plugins.html">插件系统</a>，本文会挑选一些常用的，经过测试可用的插件单独放出来使用方法和一些注意事项，希望能够帮助到同僚们。</p><span id="more"></span><p><a href="https://github.com/zzcyrus/WebGIS-demos/tree/main/leaflet">Leaflet 系列地址</a><br><a href="https://kaely.net/WebGIS-demos/leaflet/1.plugins/plugins.html">demo 地址</a></p><h1 id="ImageWMS"><a href="#ImageWMS" class="headerlink" title="ImageWMS"></a>ImageWMS</h1><p>在 openlayers 中，wms 图层的调用了提供了 IamgeWMS 和 tileWMS 两种方式，通常情况下，如果 wms 服务作为底图，由于数据量大，我们采用瓦片的方式加载十分有利，如果数据量小，我们采用单张 image 的方式无论从请求发送上和显示的效果上都更好。<br><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/none-tile-wms-202202201209390.png"><br><del>可以发现，瓦片会发出多次请求，每次请求都会包含该瓦片上涉及到的信息，数据量小十分没必要</del></p><p>Leaflet 中只提供了 tile 瓦片的方式加载 WMS 图层，在使用了很多第三方解决方案后，我发现了这款插件<a href="https://github.com/ptv-logistics/Leaflet.NonTiledLayer">nonTiledLayer</a>，使用之后的效果基本上可以达到 openlaysers 中的要求，更多情况可以点击链接进去了解。</p><p>调用方式</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> layer = L.<span class="property">nonTiledLayer</span></span><br><span class="line">  .<span class="title function_">wms</span>(url, &#123;</span><br><span class="line">    <span class="attr">maxZoom</span>: <span class="number">19</span>,</span><br><span class="line">    <span class="attr">minZoom</span>: <span class="number">0</span>,</span><br><span class="line">    <span class="attr">opacity</span>: <span class="number">1.0</span>,</span><br><span class="line">    <span class="attr">layers</span>: <span class="string">&quot;xmap-gravelpit-fg&quot;</span>,</span><br><span class="line">    <span class="attr">format</span>: <span class="string">&quot;image/png&quot;</span>,</span><br><span class="line">    <span class="attr">transparent</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">attribution</span>: <span class="string">&quot;&quot;</span>,</span><br><span class="line">    <span class="attr">pane</span>: <span class="string">&quot;tilePane&quot;</span>,</span><br><span class="line">    <span class="attr">zIndex</span>: <span class="number">3</span>,</span><br><span class="line">  &#125;)</span><br><span class="line">  .<span class="title function_">addTo</span>(map);</span><br></pre></td></tr></table></figure><p><a href="https://github.com/ptv-logistics/Leaflet.NonTiledLayer">nonTiledLayer</a>调用方式基本沿用 Leaflet 自身的 wms 调用，提供的属性也很全面</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">attribution - 图层数据来源.Default:&#x27;</span><br><span class="line">opacity - 透明度.Default: 1</span><br><span class="line">minZoom - 最小缩放. Default: 0</span><br><span class="line">maxZoom - 最大缩放. Default: 18</span><br><span class="line">bounds - 边界条件. Default: L.latLngBounds([-180, -85.05], [180, 85.05])</span><br><span class="line">zIndex - 位置. Default: undefined</span><br><span class="line">pane - 插入的div的名称. Default: &#x27;overlayPane&#x27;</span><br><span class="line">pointerEvents - 鼠标事件的样式. Default: null</span><br><span class="line">errorImageUrl - 默认错误图层. Default: 1px transparent gif data:image/gif;base64,R0lGODlhAQABAHAAACH5BAUAAAAALAAAAAABAAEAAAICRAEAOw==</span><br><span class="line">useCanvas - 渲染方式. Default: undefined</span><br></pre></td></tr></table></figure><p>具体的使用效果可以移步 demo</p><h1 id="WKT-数据插件"><a href="#WKT-数据插件" class="headerlink" title="WKT 数据插件"></a>WKT 数据插件</h1><p>wkt 作为 GIS 常用的一种地理数据格式，因为通用性需求度也很高,Leaflet 官方插件中提供了许多支持 wkt 的第三方解决方案，使用下来，发现 mapbox 出品的<a href="https://github.com/mapbox/leaflet-omnivore">leaflet-omnivore</a>效果可以说是目前最满足要求的了。</p><p><code>omnivore</code>支持的功能比较强大</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">omnivore.<span class="title function_">csv</span>(<span class="string">&quot;a.csv&quot;</span>).<span class="title function_">addTo</span>(map);</span><br><span class="line">omnivore.<span class="title function_">gpx</span>(<span class="string">&quot;a.gpx&quot;</span>).<span class="title function_">addTo</span>(map);</span><br><span class="line">omnivore.<span class="title function_">kml</span>(<span class="string">&quot;a.kml&quot;</span>).<span class="title function_">addTo</span>(map);</span><br><span class="line">omnivore.<span class="title function_">wkt</span>(<span class="string">&quot;a.wkt&quot;</span>).<span class="title function_">addTo</span>(map);</span><br><span class="line">omnivore.<span class="title function_">topojson</span>(<span class="string">&quot;a.topojson&quot;</span>).<span class="title function_">addTo</span>(map);</span><br><span class="line">omnivore.<span class="title function_">geojson</span>(<span class="string">&quot;a.geojson&quot;</span>).<span class="title function_">addTo</span>(map);</span><br><span class="line">omnivore.<span class="title function_">polyline</span>(<span class="string">&quot;a.txt&quot;</span>).<span class="title function_">addTo</span>(map);</span><br></pre></td></tr></table></figure><p>其中对于 wkt 的加载有两种方式。<br>其中，<code>customlayer</code>是通过<code>L.geojson</code>图层来为加载进来的 wkt 数据设置样式</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> customLayer = L.<span class="title function_">geoJson</span>(<span class="literal">null</span>, &#123;</span><br><span class="line">  <span class="attr">style</span>: <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123; <span class="attr">color</span>: <span class="string">&quot;#0ff&quot;</span> &#125;;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><ul><li><p><code>omnivore.wkt(url, parser_options?, customLayer?)</code>: 通过 url 加载</p></li><li><p><code>omnivore.wkt.parse(wktString，parser_options?, customLayer?)</code>: 通过转换 wkt 字符串加载</p></li></ul><p>具体的使用可以移步<a href="https://github.com/zzcyrus/WebGIS-demos/blob/main/leaflet/1.plugins/plugins.html">demo源代码</a>。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;Leaflet 中常用的 gis 功能可以满足一般使用，有些特殊需求，官方也有&lt;a href=&quot;http://leafletjs.com/plugins.html&quot;&gt;插件系统&lt;/a&gt;，本文会挑选一些常用的，经过测试可用的插件单独放出来使用方法和一些注意事项，希望能够帮助到同僚们。&lt;/p&gt;</summary>
    
    
    
    <category term="WebGIS" scheme="https://kaely.net/categories/WebGIS/"/>
    
    
    <category term="FE" scheme="https://kaely.net/tags/FE/"/>
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
    <category term="Leaflet" scheme="https://kaely.net/tags/Leaflet/"/>
    
  </entry>
  
  <entry>
    <title>Leaflet多种基础图层加载</title>
    <link href="https://kaely.net/2017/09/17/basemap/"/>
    <id>https://kaely.net/2017/09/17/basemap/</id>
    <published>2017-09-17T05:46:08.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>最近项目中有移动端的开发需求，自然就用到了最轻量级的 gis 组件——Leaflet，100 多 k 的体积，五脏俱全，丰富的第三方插件，虽然开发者数量上不及大哥级产品 Arcigs，但也基本够用，对于 esri 的自家系统支持好，svg 和 canvas 两种渲染方式，让数据量大的要素图层渲染也不是那么吃力。</p><p>这里把自己对于 Leaflet 的一些探索研究记录一下，一方面便于自己后期回顾，另一方面也希望和大家一起探讨学习。</p><span id="more"></span><p><a href="https://github.com/zzcyrus/WebGIS-demos/tree/main/leaflet">Leaflet 系列地址</a><br><a href="https://kaely.net/WebGIS-demos/leaflet/0.basemap/basemap.html">demo 地址</a></p><h1 id="基础图层的加载"><a href="#基础图层的加载" class="headerlink" title="基础图层的加载"></a>基础图层的加载</h1><p>基础图层的加载都是通过<code>L.tileLayer</code>的方式，所以加载的难点不在于 api 的使用，是一些常用的基础瓦片的收集，这里给出一些常用的地址</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="attr">mapRadios</span>: [</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="string">&quot;高德地图&quot;</span>,</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&quot;1&quot;</span>,</span><br><span class="line">    <span class="attr">url</span>:</span><br><span class="line">      <span class="string">&quot;http://webrd01.is.autonavi.com/appmaptile?lang=zh_cn&amp;size=1&amp;scale=1&amp;style=8&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="string">&quot;Esri地图&quot;</span>,</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&quot;2&quot;</span>,</span><br><span class="line">    <span class="attr">url</span>:</span><br><span class="line">      <span class="string">&quot;http://map.geoq.cn/ArcGIS/rest/services/ChinaOnlineCommunity/MapServer/tile/&#123;z&#125;/&#123;y&#125;/&#123;x&#125;&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="string">&quot;天地图道路&quot;</span>,</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&quot;3&quot;</span>,</span><br><span class="line">    <span class="attr">url</span>: <span class="string">&quot;http://t0.tianditu.cn/DataServer?T=vec_w&amp;X=&#123;x&#125;&amp;Y=&#123;y&#125;&amp;L=&#123;z&#125;&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="string">&quot;天地图标注&quot;</span>,</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&quot;4&quot;</span>,</span><br><span class="line">    <span class="attr">url</span>: <span class="string">&quot;http://t5.tianditu.cn/DataServer?T=cva_w&amp;X=&#123;x&#125;&amp;Y=&#123;y&#125;&amp;L=&#123;z&#125;&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="string">&quot;天地图影像&quot;</span>,</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&quot;5&quot;</span>,</span><br><span class="line">    <span class="attr">url</span>:</span><br><span class="line">      <span class="string">&quot;http://t1.tianditu.cn/img_w/wmts?service=wmts&amp;request=GetTile&amp;version=1.0.0&amp;LAYER=img&amp;tileMatrixSet=w&amp;TileMatrix=&#123;z&#125;&amp;TileRow=&#123;y&#125;&amp;TileCol=&#123;x&#125;&amp;style=default&amp;format=tiles&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="string">&quot;谷歌地形&quot;</span>,</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&quot;6&quot;</span>,</span><br><span class="line">    <span class="attr">url</span>:</span><br><span class="line">      <span class="string">&quot;http://mt3.google.cn/vt/lyrs=t@131&amp;hl=zh-CN&amp;gl=cn&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;&amp;s=Ga&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="string">&quot;谷歌影像&quot;</span>,</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&quot;7&quot;</span>,</span><br><span class="line">    <span class="attr">url</span>: <span class="string">&quot;http://mt3.google.cn/vt/lyrs=s&amp;hl=zh-CN&amp;gl=cn&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="string">&quot;谷歌标注&quot;</span>,</span><br><span class="line">    <span class="attr">value</span>: <span class="string">&quot;8&quot;</span>,</span><br><span class="line">    <span class="attr">url</span>:</span><br><span class="line">      <span class="string">&quot;http://mt2.google.cn/vt/lyrs=m@167000000&amp;hl=zh-CN&amp;gl=cn&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">];</span><br></pre></td></tr></table></figure><p>更多的瓦片地址可以在<a href="https://github.com/zzcyrus/WebGIS-demos/blob/main/leaflet/0.basemap/basemap.html">demo代码</a>中找到。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;最近项目中有移动端的开发需求，自然就用到了最轻量级的 gis 组件——Leaflet，100 多 k 的体积，五脏俱全，丰富的第三方插件，虽然开发者数量上不及大哥级产品 Arcigs，但也基本够用，对于 esri 的自家系统支持好，svg 和 canvas 两种渲染方式，让数据量大的要素图层渲染也不是那么吃力。&lt;/p&gt;
&lt;p&gt;这里把自己对于 Leaflet 的一些探索研究记录一下，一方面便于自己后期回顾，另一方面也希望和大家一起探讨学习。&lt;/p&gt;</summary>
    
    
    
    <category term="WebGIS" scheme="https://kaely.net/categories/WebGIS/"/>
    
    
    <category term="FE" scheme="https://kaely.net/tags/FE/"/>
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
    <category term="Leaflet" scheme="https://kaely.net/tags/Leaflet/"/>
    
  </entry>
  
  <entry>
    <title>openlayers 从台风风圈绘制到canvas样式和图层的应用</title>
    <link href="https://kaely.net/2017/07/31/openlayers-canvas-typhoon/"/>
    <id>https://kaely.net/2017/07/31/openlayers-canvas-typhoon/</id>
    <published>2017-07-31T14:31:02.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>本文中所使用的数据来源于<a href="http://www.wztf121.com/">温州台风网</a>，通过 F12 抓取，你可以在我的<a href="https://github.com/zzcyrus/WebGIS-demos/tree/main/openlayers/0.typhoon">GitHub</a>上查看数据和本文源代码</p><p>台风的风圈是一种不常见但算的上规则的图形，在上面的网站可以看到最终效果，简单的解剖下其实就是四个 1&#x2F;4 圆</p><p><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/typhoon-goal-202202201211623.png"></p><p>从数据结构上也可以看出来：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> radius_quad = &#123;</span><br><span class="line">  <span class="attr">ne</span>: <span class="number">250</span>, <span class="comment">//单位为KM</span></span><br><span class="line">  <span class="attr">se</span>: <span class="number">250</span>,</span><br><span class="line">  <span class="attr">sw</span>: <span class="number">180</span>,</span><br><span class="line">  <span class="attr">nw</span>: <span class="number">150</span>,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>在某个固定经纬度点上，以此点为圆心组合成了一个所谓的风圈形状，四个方向分别代表每个 1&#x2F;4 圆的半径。刚开始是为了实现这种效果进行了研究，后来发现 openlayers 对于这种效果的支持还挺有意思，记录下来已做分享。</p><span id="more"></span><h1 id="通过自定义-geometry-的实现"><a href="#通过自定义-geometry-的实现" class="headerlink" title="通过自定义 geometry 的实现"></a>通过自定义 geometry 的实现</h1><p>最原始的思路也是最容易切入的点，就是去看 openlayers 中怎么实现圆形的画法，在 ol 中，有<code>createRegularPolygon</code>这个类，原理就是给定一些参数，用点去填充我们想要的形状，当填进去点达到一定密集度，自然就能得到一个近似的圆。<br>通过这个思路我们可以计算台风风圈的每个 1&#x2F;4 圆的坐标，用点去占位，最终实现绘制出想要的图形。</p><p>具体代码可以去看某大牛的博客，这里只给出一个传送门，写的很详细了：<br><a href="http://blog.csdn.net/gisshixisheng/article/details/76397068">点我乘坐飞机</a> <strong>这个算法还有些不完善，就是在每个 1&#x2F;4 圆结束时候少计算了一个点，导致看着有点不对劲</strong></p><p><strong>特点:</strong> 这种方式画出来的图形是一个图层中的一个 feature，好处自然不用多说，基本上 feature 支持的功能都能满足！</p><h1 id="通过-canvas-类型的-symbol-实现"><a href="#通过-canvas-类型的-symbol-实现" class="headerlink" title="通过 canvas 类型的 symbol 实现"></a>通过 canvas 类型的 symbol 实现</h1><p>在点密集的情况下，用上面的方式其实效果已经很不错了，但是有些细（tiao）心（ci）的使用者，非要放大到一定程度，然后说你这个不够圆，那。。。。。</p><p>那我们就用 canvas 画出来，openlayers 中，要素的样式是有这么一种方式<code>ol.style.Icon</code>来实现，我们可以把绘制好的元素作为<code>Icon</code>的参数</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> style = <span class="keyword">new</span> ol.<span class="property">style</span>.<span class="title class_">Style</span>(&#123;</span><br><span class="line">  <span class="attr">image</span>: <span class="keyword">new</span> ol.<span class="property">style</span>.<span class="title class_">Icon</span>(&#123;</span><br><span class="line">    <span class="attr">opacity</span>: <span class="number">0.3</span>,</span><br><span class="line">    <span class="attr">img</span>: canvas,</span><br><span class="line">    <span class="attr">imgSize</span>: [canvas.<span class="property">width</span>, canvas.<span class="property">height</span>],</span><br><span class="line">  &#125;),</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>canvans 绘制的方法：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createTyphoon</span>(<span class="params">radius, radius_quad</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> canvas = <span class="variable language_">document</span>.<span class="title function_">createElement</span>(<span class="string">&quot;canvas&quot;</span>);</span><br><span class="line">  canvas.<span class="property">width</span> = canvas.<span class="property">height</span> = <span class="number">2</span> * radius;</span><br><span class="line">  <span class="keyword">var</span> context = canvas.<span class="title function_">getContext</span>(<span class="string">&quot;2d&quot;</span>);</span><br><span class="line">  context.<span class="property">fillStyle</span> = <span class="string">&quot;#0000ff&quot;</span>;</span><br><span class="line">  context.<span class="property">strokeStyle</span> = <span class="string">&quot;#ff0000&quot;</span>;</span><br><span class="line">  context.<span class="property">lineWidth</span> = <span class="number">3</span>;</span><br><span class="line">  context.<span class="title function_">beginPath</span>();</span><br><span class="line">  context.<span class="title function_">arc</span>(radius, radius, radius_quad.<span class="property">se</span>, <span class="number">0</span>, <span class="number">0.5</span> * <span class="title class_">Math</span>.<span class="property">PI</span>);</span><br><span class="line">  context.<span class="title function_">lineTo</span>(radius, radius + radius_quad.<span class="property">sw</span>);</span><br><span class="line">  context.<span class="title function_">arc</span>(radius, radius, radius_quad.<span class="property">sw</span>, <span class="number">0.5</span> * <span class="title class_">Math</span>.<span class="property">PI</span>, <span class="title class_">Math</span>.<span class="property">PI</span>);</span><br><span class="line">  context.<span class="title function_">lineTo</span>(radius - radius_quad.<span class="property">nw</span>, radius);</span><br><span class="line">  context.<span class="title function_">arc</span>(radius, radius, radius_quad.<span class="property">nw</span>, <span class="title class_">Math</span>.<span class="property">PI</span>, <span class="number">1.5</span> * <span class="title class_">Math</span>.<span class="property">PI</span>);</span><br><span class="line">  context.<span class="title function_">lineTo</span>(radius, radius - radius_quad.<span class="property">ne</span>);</span><br><span class="line">  context.<span class="title function_">arc</span>(radius, radius, radius_quad.<span class="property">ne</span>, <span class="number">1.5</span> * <span class="title class_">Math</span>.<span class="property">PI</span>, <span class="number">0</span>);</span><br><span class="line">  context.<span class="title function_">lineTo</span>(radius + radius_quad.<span class="property">se</span>, radius);</span><br><span class="line">  context.<span class="title function_">fill</span>();</span><br><span class="line">  context.<span class="title function_">stroke</span>();</span><br><span class="line">  <span class="keyword">return</span> canvas;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>效果如下图：<br><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/typhoon-canvas-symbol-202202201211656.png"></p><p><a href="https://kaely.net/WebGIS-demos/openlayers/0.typhoon/canvasSymbol.html">demo</a><br><a href="https://github.com/zzcyrus/WebGIS-demos/blob/main/openlayers/0.typhoon/canvasSymbol.html">完整代码</a></p><p><strong>特点：</strong> 这种方式，绘制出来的台风风圈其实只是一个 symbol 符号，需要把这个符号赋给一个具体的要素，比如一个点，一个圆之类的，而且根据分辨率还要去调整样式的缩放</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">map.<span class="title function_">getView</span>().<span class="title function_">on</span>(<span class="string">&quot;change:resolution&quot;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> style = shape.<span class="title function_">getStyle</span>();</span><br><span class="line">  <span class="comment">// 重新设置图标的缩放率</span></span><br><span class="line">  style.<span class="title function_">getImage</span>().<span class="title function_">setScale</span>(<span class="variable language_">this</span>.<span class="title function_">getZoom</span>() / <span class="number">8</span>);</span><br><span class="line">  shape.<span class="title function_">setStyle</span>(style);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h1 id="通过-canvas-图层的方式实现"><a href="#通过-canvas-图层的方式实现" class="headerlink" title="通过 canvas 图层的方式实现"></a>通过 canvas 图层的方式实现</h1><p>再后来转念一想，既然支持 canvas 的 symbol，为何不直接使用 canvas 绘制固定元素呢，果然在 API 中找到了<code>ol.source.ImageCanvas</code>，直接把 canvas 要素当作图层来使用！</p><p><code>ol.source.ImageCanvas</code>的绘制有点需要特别注意的点，这里给出重要代码片段</p><h2 id="创建图层，在-canvasFunction-中写具体的绘图方法"><a href="#创建图层，在-canvasFunction-中写具体的绘图方法" class="headerlink" title="创建图层，在 canvasFunction 中写具体的绘图方法"></a>创建图层，在 canvasFunction 中写具体的绘图方法</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> canvasLayer = <span class="keyword">new</span> ol.<span class="property">layer</span>.<span class="title class_">Image</span>(&#123;</span><br><span class="line">  <span class="attr">source</span>: <span class="keyword">new</span> ol.<span class="property">source</span>.<span class="title class_">ImageCanvas</span>(&#123;</span><br><span class="line">    <span class="attr">canvasFunction</span>: canvasFunction,</span><br><span class="line">    <span class="attr">projection</span>: <span class="string">&quot;EPSG:3857&quot;</span>,</span><br><span class="line">  &#125;),</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h2 id="canvasFunction-中一定要注意画布和地图的偏移处理，还要通过分辨率计算实际风圈大小"><a href="#canvasFunction-中一定要注意画布和地图的偏移处理，还要通过分辨率计算实际风圈大小" class="headerlink" title="canvasFunction 中一定要注意画布和地图的偏移处理，还要通过分辨率计算实际风圈大小"></a>canvasFunction 中一定要注意画布和地图的偏移处理，还要通过分辨率计算实际风圈大小</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//计算画布和地图四至的偏移量</span></span><br><span class="line"><span class="keyword">var</span> mapExtent = map.<span class="title function_">getView</span>().<span class="title function_">calculateExtent</span>(map.<span class="title function_">getSize</span>());</span><br><span class="line"><span class="keyword">var</span> canvasOrigin = map.<span class="title function_">getPixelFromCoordinate</span>([extent[<span class="number">0</span>], extent[<span class="number">3</span>]]);</span><br><span class="line"><span class="keyword">var</span> mapOrigin = map.<span class="title function_">getPixelFromCoordinate</span>([mapExtent[<span class="number">0</span>], mapExtent[<span class="number">3</span>]]);</span><br><span class="line"><span class="keyword">var</span> delta = [mapOrigin[<span class="number">0</span>] - canvasOrigin[<span class="number">0</span>], mapOrigin[<span class="number">1</span>] - canvasOrigin[<span class="number">1</span>]];</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//在计算台风风圈的中心点时要补充计算偏移量</span></span><br><span class="line"><span class="keyword">var</span> point = ol.<span class="property">proj</span>.<span class="title function_">transform</span>(coordinate, <span class="string">&quot;EPSG:4326&quot;</span>, <span class="string">&quot;EPSG:3857&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> pixel = map.<span class="title function_">getPixelFromCoordinate</span>(point);</span><br><span class="line"><span class="keyword">var</span> cX = pixel[<span class="number">0</span>] + delta[<span class="number">0</span>],</span><br><span class="line">  cY = pixel[<span class="number">1</span>] + delta[<span class="number">1</span>];</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//利用canvasFunction提供的默认参数分辨率，计算准确的坐标</span></span><br><span class="line"><span class="keyword">var</span> radius_quad = &#123;</span><br><span class="line">  <span class="attr">ne</span>: <span class="number">250000</span> / resolution,</span><br><span class="line">  <span class="attr">se</span>: <span class="number">250000</span> / resolution,</span><br><span class="line">  <span class="attr">sw</span>: <span class="number">180000</span> / resolution,</span><br><span class="line">  <span class="attr">nw</span>: <span class="number">150000</span> / resolution,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>最终效果如下，我在同一图层中绘制了多个：<br><img src="https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/typhoon-canvas-layer-202202201212305.png"></p><p>至于绘制 canvas 的方法和上面的 symbol 是一样的。<br><a href="https://kaely.net/WebGIS-demos/openlayers/0.typhoon/canvasLayer.html">demo</a><br><a href="https://github.com/zzcyrus/WebGIS-demos/blob/main/openlayers/0.typhoon/canvasLayer.html">完整代码</a></p><p><strong>特点：</strong>这种方式可以在一个图层中添加多个风圈要素，同时图层支持的功能也比较多，基本满足需求，效果也还行</p><p>这篇应用主要是从一个基本的需求所拓展开来的，canvas 图层的应用我想应该很强大，刚开始研究 openlyaers，相比于 arcgis，ol 很多功能可能要自己实现，但似乎效果上还是能让人满意的，欢迎大家讨论。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文中所使用的数据来源于&lt;a href=&quot;http://www.wztf121.com/&quot;&gt;温州台风网&lt;/a&gt;，通过 F12 抓取，你可以在我的&lt;a href=&quot;https://github.com/zzcyrus/WebGIS-demos/tree/main/openlayers/0.typhoon&quot;&gt;GitHub&lt;/a&gt;上查看数据和本文源代码&lt;/p&gt;
&lt;p&gt;台风的风圈是一种不常见但算的上规则的图形，在上面的网站可以看到最终效果，简单的解剖下其实就是四个 1&amp;#x2F;4 圆&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://blog-img-1255388623.cos.ap-shanghai.myqcloud.com/typhoon-goal-202202201211623.png&quot;&gt;&lt;/p&gt;
&lt;p&gt;从数据结构上也可以看出来：&lt;/p&gt;
&lt;figure class=&quot;highlight js&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;var&lt;/span&gt; radius_quad = &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &lt;span class=&quot;attr&quot;&gt;ne&lt;/span&gt;: &lt;span class=&quot;number&quot;&gt;250&lt;/span&gt;, &lt;span class=&quot;comment&quot;&gt;//单位为KM&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &lt;span class=&quot;attr&quot;&gt;se&lt;/span&gt;: &lt;span class=&quot;number&quot;&gt;250&lt;/span&gt;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &lt;span class=&quot;attr&quot;&gt;sw&lt;/span&gt;: &lt;span class=&quot;number&quot;&gt;180&lt;/span&gt;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &lt;span class=&quot;attr&quot;&gt;nw&lt;/span&gt;: &lt;span class=&quot;number&quot;&gt;150&lt;/span&gt;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;p&gt;在某个固定经纬度点上，以此点为圆心组合成了一个所谓的风圈形状，四个方向分别代表每个 1&amp;#x2F;4 圆的半径。刚开始是为了实现这种效果进行了研究，后来发现 openlayers 对于这种效果的支持还挺有意思，记录下来已做分享。&lt;/p&gt;</summary>
    
    
    
    <category term="WebGIS" scheme="https://kaely.net/categories/WebGIS/"/>
    
    
    <category term="FE" scheme="https://kaely.net/tags/FE/"/>
    
    <category term="GIS" scheme="https://kaely.net/tags/GIS/"/>
    
    <category term="OpenLayers" scheme="https://kaely.net/tags/OpenLayers/"/>
    
  </entry>
  
  <entry>
    <title>element dialog组件嵌套问题的临时方案</title>
    <link href="https://kaely.net/2017/06/23/vue-dialog-nested/"/>
    <id>https://kaely.net/2017/06/23/vue-dialog-nested/</id>
    <published>2017-06-23T14:09:55.000Z</published>
    <updated>2022-10-24T11:44:45.387Z</updated>
    
    <content type="html"><![CDATA[<p>&amp;emsp;&amp;emsp;最近用 Vue+element 开发项目时，在dialog组件上嵌套使用dialog组件或是MessageBox这类弹框组件，发现会出现遮罩层重合之类的问题，导致很多人只能选择关闭遮罩层。GitHub上就此问题也出现过类似讨论。有几种解决方案：</p><span id="more"></span> <ol><li><p>按照官方所说，将多个dialog类组件全部移动至**&lt;body**&gt;标签下</p></li><li><p>Github上某位大牛给出了不完全解决方案，基本思路仍然是将添加在**&lt;el-dialog**&gt;内的dom结构自动移动至**&lt;body**&gt;标签下。这个方式测试了下，似乎纯dialog嵌套问题不大，但是如果dialog内部包含了其他组件会有点问题，智者见智，自行探索了</p></li></ol><p>&amp;emsp;&amp;emsp;<a href="https://github.com/foolishchow/element-dialog2">附上连接</a></p><ol start="3"><li>编写自己modal遮罩层，这里给出个不完全临时代码仅供参考</li></ol><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Vue</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">$Modal</span> = &#123;</span><br><span class="line">    <span class="title function_">open</span>(<span class="params">element</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> index = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">var</span> wrapper = element.<span class="title function_">querySelector</span>(<span class="string">&#x27;.el-dialog__wrapper&#x27;</span>);</span><br><span class="line">        <span class="keyword">if</span> (wrapper) &#123;</span><br><span class="line">            index = wrapper.<span class="property">style</span>.<span class="property">zIndex</span> - <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">var</span> modalDom = <span class="variable language_">document</span>.<span class="title function_">createElement</span>(<span class="string">&#x27;div&#x27;</span>);</span><br><span class="line">        modalDom.<span class="property">className</span> = <span class="string">&#x27;v-modal&#x27;</span>;</span><br><span class="line">        element.<span class="title function_">appendChild</span>(modalDom);</span><br><span class="line">        modalDom.<span class="property">style</span>.<span class="property">zIndex</span> = index;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="title function_">close</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> modal = <span class="variable language_">document</span>.<span class="title function_">getElementsByClassName</span>(<span class="string">&#x27;v-modal&#x27;</span>)[<span class="number">0</span>;</span><br><span class="line">        modal.<span class="property">parentNode</span>.<span class="title function_">removeChild</span>(modal);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>&amp;emsp;&amp;emsp;一般调用方法</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="attr">mounted</span>: <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> that = <span class="variable language_">this</span>;</span><br><span class="line">    <span class="variable language_">this</span>.$nextTick(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">        <span class="comment">//遮罩层</span></span><br><span class="line">        that.$nextTick(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">            that.<span class="property">$Modal</span>.<span class="title function_">open</span>(that.<span class="property">$el</span>);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure><p>&amp;emsp;&amp;emsp;主要想法就是在组件mounted完成的nextTick后，为页面中添加一个div，样式就直接采用element自己的了，因为遮罩层是添加在dialog组件下的，因此组件销毁后也会自己销毁，close方法也没用上，但也应该会遇到有用上的时候吧。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&amp;amp;emsp;&amp;amp;emsp;最近用 Vue+element 开发项目时，在dialog组件上嵌套使用dialog组件或是MessageBox这类弹框组件，发现会出现遮罩层重合之类的问题，导致很多人只能选择关闭遮罩层。GitHub上就此问题也出现过类似讨论。有几种解决方案：&lt;/p&gt;</summary>
    
    
    
    <category term="前端" scheme="https://kaely.net/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
    <category term="FE" scheme="https://kaely.net/tags/FE/"/>
    
    <category term="Vue" scheme="https://kaely.net/tags/Vue/"/>
    
  </entry>
  
</feed>
